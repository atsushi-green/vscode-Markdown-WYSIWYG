# AGENT.md - Markdown WYSIWYG Editor

## プロジェクト概要

このプロジェクトは、VS Code上でMarkdownファイルをWYSIWYG（What You See Is What You Get）形式で編集できる拡張機能です。ユーザーはMarkdownの構文を意識せずに、見たままの状態で文書を編集できます。

## 主要機能

- **WYSIWYG編集**: Markdownの構文を意識せずに直感的に編集
- **Mermaid図のサポート**: Mermaid記法によるダイアグラムのリアルタイムプレビューと編集
  - 分割表示モード（ソースコードとプレビューの同時表示）
  - 高解像度PNG画像としてエクスポート（4x スケール + デバイスピクセル比考慮）
  - クリップボードへの画像コピー
- **テーブル編集機能**: Excel風のインタラクティブなテーブル編集
  - 方向キーでのセル移動（↑↓←→、Tab、Enter）
  - セルの直接編集
  - 行・列の追加/削除
  - Excelからのデータ貼り付け（タブ区切りデータ対応）
  - テーブル全体のコピー（Excel等へ貼り付け可能）
  - Markdown形式との自動相互変換
  - 空テーブルの挿入（中核）: `table.js` の純粋関数 `buildEmptyTableMarkdown(rows, cols)`（ヘッダ＋区切り＋本文rows行の空テーブルMarkdown・0以下は最低1行1列）と `insertTable(rows, cols)`（`markdownToHtml` に通し、キャレットのあるトップレベルブロック直後＝無ければ末尾へ挿入して `render()` で即インタラクティブ化。挿入方式は `commands.insertToc` と同じ）。
  - 空テーブルの挿入（呼び出しUI・**実機確認前提**）: `table.js` の `setupContextMenu(editor)`（`editor.js` init で数式メニューの後に配線）が `contextmenu` を監視し、**空行・エディタ余白**でのみ自前メニュー「➕ 表を挿入…」を出す。対象判定は純粋関数 `shouldShowInsertMenu(target, editor)`＝`findExcludedAncestor`（数式 `.math-block`/`.math-inline`・Mermaid・既存 `table`/`.table-container` 上は false）と `isBlockEmpty(block)`（テキスト無し＝空白/ゼロ幅のみ・`<br>` 以外の要素を含まない）を組み合わせ、**本文テキスト上ではブラウザ既定メニュー（貼り付け・スペル修正等）を尊重して `preventDefault` しない**（数式メニューの後に配線するため `e.defaultPrevented` も見て二重処理を避ける）。メニュー項目→行数・列数ダイアログ（`prompt()` 不可のため `.link-dialog` 系スタイルで動的生成・`markdownEditor.ts` 非変更）→OK で `clampTableDimensions`（空/非数値/0以下→1・上限 行50/列20）を通し `insertTable(rows, cols)` を呼ぶ。挿入位置は右クリック時の `document.caretRangeFromPoint` を `pendingInsertRange` に退避し確定直前に選択へ復元（ダイアログ入力へフォーカスが移ると選択が失われるため）。メニュー/ダイアログCSSは `.table-context-menu`/`.table-menu-item` が `.mermaid-context-menu`/`.math-context-menu` 系と同ルールを共有。**ダイアログの表示位置**: 従来`.link-dialog`の固定CSS（`position:absolute; top:48px; right:20px`）で常に画面右上に出ていたのを、`showInsertDialog(onInserted, anchor)`が`computeDialogPosition(anchor, dialogWidth, dialogHeight, viewportWidth, viewportHeight)`（`anchor`があれば`computeMenuPosition`と同じ規則でその近くへ、無ければビューポート中央へ）で算出した`left`/`top`を都度インライン設定するよう変更（`.table-insert-dialog`にCSSで`position:fixed`を追加し`.link-dialog`の絶対配置を上書き。`#linkDialog`自体は非変更で従来どおり右上固定のまま）。右クリックメニュー経由は`contextmenu`時に記録した`lastContextMenuPoint`（右クリック座標）を、「/」コマンドメニュー経由（`editor.js`）はキャレット位置（`range.getBoundingClientRect()`。全て0の矩形が返った場合はレイアウト未確定とみなし`anchor`を渡さずビューポート中央へフォールバック）をそれぞれ`anchor`として渡す。純粋関数（`shouldShowInsertMenu`/`isBlockEmpty`/`findExcludedAncestor`/`computeMenuPosition`/`computeDialogPosition`/`clampTableDimensions`）＋メニュー表示・抑止・ダイアログ挿入フロー・右クリック位置の反映をユニットテスト済み。`media/` のみ＝統合テスト非対象。**既知の制約（ROADMAPへ）**: `computeMenuPosition`/`computeDialogPosition`は右端・下端のはみ出し補正のみで左端・上端（anchor座標が負の場合）は補正しない
  - 矩形範囲選択(1/3・マウスドラッグ): 「表の矩形選択とコピー＆ペースト」Lを分割した基盤部分（(2/3)コピー・(3/3)貼り付けはROADMAPに残置。まだクリップボード連携は無くハイライト表示のみ）。`table.js`の`setupCellRangeDrag(cell, table)`を各セルへ配線し、`mousedown`（主ボタンのみ）で開始セルの行・列（`cellPosition`が`table.rows`/`row.cells`のindexOfで算出＝既存の矢印キーナビゲーションと同じ考え方）を記録、ドラッグ中（`e.buttons===1`）に別セルへ`mouseenter`すると`computeCellRange`（開始・終了の前後を問わず正規化）で矩形範囲を求め`applyRangeHighlight`が範囲内の全セルへ`table-cell-range-selected`を付与（既存の単一選択`table-cell-selected`は解除）。`document`の`mouseup`（`setupRangeSelectionMouseUp`・`editor.js`のinitEditorで一度だけ配線）でドラッグ追跡のみ終了し、確定した範囲（`currentCellRange`）とそのハイライトは次のクリック・別範囲開始まで保持（将来の(2/3)コピーが参照する想定で`getCurrentCellRange()`を公開）。単なるクリック（mousedown/mouseup が同一セル）は別セルへの`mouseenter`が発生しないためこの配線は無反応＝既存の単一セル選択と競合しない。**`/local-review`指摘A-1（medium-high）を同サイクルで修正**: 複数テーブルを持つ文書で、表1に範囲確定→表2で新たに範囲確定すると表1側のハイライトが誰にも解除されず残留し、さらに表1のセルを単純クリックすると本来解除すべき表1ではなく`currentCellRange.table`（表2）を誤って解除する不具合があった→`mousedown`時に`currentCellRange.table !== table`なら即座にその別テーブルのハイライトを解除するよう修正（clickは必ずmousedownの後に発火するため、単純クリック・ドラッグ開始のどちらの経路でも機能する）。A-2（medium）: `.table-cell-selected`/`.table-cell-range-selected`が単体（1クラス）だと詳細度で`.table-cell:hover`（クラス+疑似クラス）に負け、ドラッグ中まさにマウスが乗っているセルだけホバー色に上書きされる問題→両クラスを`.table-cell`と複合させ詳細度を`:hover`と同等以上に揃えて修正。A-3（low-medium）: `cleanup()`が新設の`table-cell-range-selected`を除去せず・モジュール状態もリセットしていなかった→除去対象とrange状態リセットを追加。B（severity low、いずれもROADMAPへ）: `makeEditable`が行・列追加のたびに全セルへリスナーを多重登録する既存問題が今回の2種類のリスナー追加で悪化／`applyRangeHighlight`が大きな表でO(セル数×行数)。C（確信度中・対応不要と判断）: セル間のドラッグは通常のブラウザ文字列選択より矩形選択を優先する挙動になるが、Excel的なセル選択優先UXとして意図的な仕様と判断。ユニットテスト18件（純粋関数`cellPosition`/`computeCellRange`/`isCellInRange`の全分岐＋DOM配線のドラッグ・mouseup後の追跡終了・単純クリックとの非競合・複数テーブル間の非干渉2件＋`cleanup`でのクラス除去）。`media/` のみ＝統合テスト非対象。**実際のドラッグ操作感・ハイライト色の見た目（`:hover`とのCSS優先順位を含む）は実機（拡張機能開発ホスト）確認前提**
  - 矩形範囲選択(2/3・選択範囲のコピー): (1/3)の`currentCellRange`に対応する範囲だけをTSV＋text/htmlでクリップボードへ書き込む。`copy(table)`は`currentCellRange.table === table`なら`currentCellRange.range`を、無ければ従来どおりテーブル全体を対象にする（範囲が無い場合の出力・トースト文言は変更前と同一＝無退行）。純粋関数へ分離: `extractCellTextMatrix(table, range)`（`isCellInRange`を再利用してセルテキストの二次元配列を抽出）、`buildTsvFromMatrix(matrix)`（タブ・改行結合）、`buildHtmlTableFragment(table, range)`（`cell.innerHTML`をそのまま使い装飾・リンク等を保った`<table>`断片を組み立てる）。書き込みは`writeToClipboard(tsv, html)`が`ClipboardItem`/`navigator.clipboard.write`の可用性をfeature-detectし、使える環境（Webview＝Chromiumベースなので通常は使える）では`text/plain`＋`text/html`の両方を、無い環境（jsdomでのテスト等）では従来の`writeText`（TSVのみ）へフォールバックする。**`/local-review`指摘A-1（medium）を同サイクルで修正**: `selectCell`は単一クリックで`currentCellRange`を解除するが、`addRow`/`addColumn`/`deleteRow`/`deleteColumn`はいずれも解除しないままだったため、「範囲確定→（単一セル選択を経ずに）行・列を増減→コピー」の順で操作すると、行・列インデックスがずれたstaleな範囲のままセルを抽出し誤った内容を黙ってコピーする不具合があった→新設の`invalidateRangeSelectionFor(table)`をこの4関数の冒頭で呼び、対象テーブルの範囲選択を無効化するよう修正（`selectCell`と同じ後始末パターン）。B（severity low、いずれもROADMAPへ）: `buildTsvFromMatrix`がセル内のタブ・改行をエスケープしない（旧実装から存在する既存制約・今回のリグレッションではない）／クリップボード書き込み失敗トーストのテスト欠如（同サイクルでテスト追加済み）。C（確信度低・対応不要）: `new ClipboardItem(...)`が同期的に例外を投げるブラウザ実装だと`copy()`の`.catch`が捕捉できない可能性（Chromiumでは通常発生しない）。ユニットテスト11件（純粋関数3つ＋範囲/別テーブル/stale範囲でのcopy分岐＋ClipboardItem利用可否での書き込み先切り替え＋失敗トースト）。`media/`のみ＝統合テスト非対象
  - 矩形範囲選択(3/3・選択範囲への矩形貼り付け・「表の矩形選択とコピー＆ペースト」完結): (1/3)の`currentCellRange`が確定している場合、貼り付け先セルに関わらず**その範囲の左上を起点**に貼り付けデータを展開するよう`pasteData(startCell, table, text)`を拡張（範囲が無ければ従来どおり貼り付け先セル自身が起点＝無退行）。純粋関数`computePasteTargets(anchorRow, anchorCol, data, rowCount, colCount)`（アンカーからのオフセットで書き込み先の行・列を列挙し、`rowCount`/`colCount`を超える位置＝はみ出し分は結果に含めない＝行・列の自動追加はしない）と、それをDOMへ適用する`writeMatrixIntoTable(table, anchorRow, anchorCol, data)`（見出しセル`<th>`は個別にスキップ・列数は`rows[0]`＝見出し行のセル数を全行共通の矩形と仮定して算出）へ分離。データの行列数が範囲より小さい場合は書き込まれなかった残りのセルを変更しない（Excel等と同じ・範囲を空欄でクリアしない）、大きい場合はデータ側の行列数がそのまま使われる（範囲はアンカー位置を決めるだけで敷き詰め先のサイズには使わない）。旧`pasteData`実装は貼り付け起点が見出しセルだと貼り付け全体を無条件でno-opにしていたが、新実装は見出しセル自体だけをスキップし同じ貼り付けに含まれる本文行側へは書き込む（意図した挙動変更・回帰テストで固定）。`/local-review`: Aなし。B（`writeMatrixIntoTable`の列数算出が全行同一セル数という暗黙の前提に依存／行ごとの`Array.from`再生成、いずれもseverity low）はROADMAPへ。C（コメント表現の不正確さは同サイクルで修正／範囲より小さいデータ貼り付け時に残りセルをクリアしない仕様はdocstringで明記）。ユニットテスト14件（`computePasteTargets`全分岐＋`writeMatrixIntoTable`のth除外/はみ出し無視＋`pasteData`の範囲優先/見出し起点/別テーブル非干渉/ドキュメント反映）。`media/`のみ＝統合テスト非対象。**Excel等からの実際の貼り付け・大きい/小さいデータでの見え方は実機（拡張機能開発ホスト）確認前提**
- **タスクリスト**: GFMのタスクリスト記法（`- [ ]` / `- [x]`）をチェックボックスとして表示し、クリックで完了/未完了を切り替え（Markdownソースへ即時反映）。入力中も `- [ ]` / `- []` / `-[]`（スペース有無を問わず）を打った時点でチェックボックスへライブ変換（`commands.js` の `convertTaskLists`）
- **シンタックスハイライト**: コードブロック内のコードを自動的に色付け。`highlight.min.js`（highlight.js commonビルド）が JavaScript/TypeScript/JSON/YAML/Go/Rust を含む約36言語を登録済みで、加えて PowerShell を個別バンドルで追加。未登録・未指定の言語は自動判定
- **画像のクリップボード貼り付け**: スクショ等の画像を `Ctrl/Cmd+V` で貼ると、編集中ドキュメントと同じフォルダへ画像ファイルを保存し `![](相対パス)` をキャレットへ挿入する。**拡張機能側(1/2)**: Webview からの `saveClipboardImage`（`{ base64, mime }`）を受け、`document.uri` 基準のフォルダへ一意名で書き込み `insertImagePath`（`{ path }`＝POSIX区切りの相対パス）を返す（`markdownEditor.ts` の `saveClipboardImage`）。保存は `saveMermaidPng` と同方式（`atob`→`Uint8Array`→`workspace.fs.writeFile`）だがダイアログ無し・自動命名。未保存（untitled/非file）は警告して中断・空base64は無視・`readDirectory` 失敗は衝突チェック無しで続行。純粋ロジックは vscode 非依存の `src/imagePaste.ts`（`mimeToExtension`/`buildImageFilename`＝`image-<yyyyMMdd-HHmmss>.<ext>`/`disambiguateFilename`＝衝突時 `-1`,`-2`…/`toMarkdownRelativePath`＝`path.relative`＋区切りの`/`正規化）へ分離しユニット＋統合テスト。`toMarkdownRelativePath` の区切り判定は**実行プラットフォームの `path.sep` のみ**を見る（`\`と`/`の両方を一律に区切り扱いすると、macOS/Linuxで正当なファイル名文字である`\`を含む `a\b.png` が `a/b.png` という別パスへ化ける）。`path.relative` の戻り値は必ずそのプラットフォームの `path.sep` 区切りになる（Windowsは入力に`/`が混ざっても出力は`\`へ正規化）ため `path.sep` だけで足りる。第3引数 `sep` はWindows挙動をmacOS/Linuxのテストから検証するための注入口。**Webview側(2/2)**: `editor.js` の paste ハンドラで、**`text/plain` を持たない**クリップボード（＝スクショ等の純粋な画像。Excel等のテキスト併載は従来の表/テキスト貼り付けを優先＝画像で潰さない）に限り `commands.findClipboardImageItem`（`type` が `image/` で始まる最初の item）で画像を検出し、`FileReader`→base64 化して `saveClipboardImage` を postMessage。貼り付け時のキャレット範囲を `pendingImagePasteRange` に控え、応答 `insertImagePath` を受けて範囲を復元（今もエディタ内に生きている場合のみ）してから `commands.insertImageMarkdown(path)` で `![](path)` を挿入。`buildImageMarkdown` は alt 空・パス中の記号を `IMAGE_PATH_ENCODE_MAP`（`%`・空白・`( )`・`< >`・`"`・`` ` ``・`$`・`#`・`?`・`\`・`[ ]` の14文字）で**1回の走査で**パーセントエンコード（連鎖 replace だと `%20` の `%` を再度拾い `%2520` になる）。対応表に無い空白（タブ・全角スペース＝`\s`にマッチ）は文字が変わらないよう `encodeURIComponent` へ委ねる。`#`/`?` は外部ビューア（VS Codeプレビュー・GitHub）がURL区切りとして解釈するため、`` ` ``/`$` は両変換関数がインラインコード・数式を**画像より先に**退避し画像より**後に**復元するため（`` ` `` は `src` の値が `a<code>b</code>c.png` に汚染、`$` は復元される `<span class="math-inline" …>` の `"` で属性値が途中終了しHTML構造ごと破壊）。`_`/`*`/`~`/`+` と非ASCII文字は**あえて非エンコード**（プレースホルダ退避で保護済み・可読性優先）。現在の唯一の呼び出し元は同一フォルダ・`image-<日時>.<ext>` 固定でエンコード対象を含まないため**サブフォルダ保存対応に備えた予防的実装**。**画像記法は現状レンダリング対象外**でリテラルテキストとして入り、直列化は `$` のみエスケープ＝往復不変（`markdown.js:110` のリンク正規表現は alt 必須のため `![]()` はリンク化されない）。純粋関数（`buildImageMarkdown`/`findClipboardImageItem`）＋`insertImageMarkdown`（キャレット挿入・末尾フォールバック・往復）をユニットテスト（`media/modules/commands.js` 変更）。`editor.js` の paste 結線・`FileReader`・実際の貼り付けは実機確認前提
- **画像記法 `![alt](url)` の Markdown⇔HTML（レンダリング＋往復）**: `convertInline`（読込・`markdown.js`）と `convertInlineText`（ライブ変換・`commands.js`）が `![alt](url)`→`<img src alt>` を**リンクより先に**変換（`![` を `!`＋リンクに割らない）、`serializeInline` が `<img>`→`![alt](url)`（`data-original-src` があればそれを優先＝ローカル表示で `src` を webview URI へ差し替えても元パスで往復）。alt 空 `![]()` はリンク正規表現が alt 必須のため誤リンク化しない。**URL/alt/title 中の記法文字保護**: 生成した `<img>` はインラインコード・数式と同じくプレースホルダ（`U+0003`）へ退避してから強調変換（`_`/`*`/`~`/`+`）を通し、`src="a<em>b</em>c"` のような属性破壊＝往復崩れを防ぐ（両パス）。**リンクも同様に退避する（`U+0005`）**が、`<img>` と違い中身を持つため退避するのは**開始タグ（属性）だけ**で、リンクテキストと `</a>` は残す（`[**太字**](url)` のようにテキスト内の強調は変換される必要があるため。`</a>` は記法文字を含まないので強調変換に巻き込まれない）。プレースホルダの割り当ては**両ファイルで共通**（0=コード / 1=`\$` / 2=数式 / 3=画像 / 4=脚注参照 / 5=リンク）。脚注参照は当初 `markdown.js` だけ実HTMLのまま埋めていて、ラベルが許す `_` を2つ以上含むとき（`[^a_b_c]`）に`data-footnote-label`・`href`・`id` が `a<em>b</em>c` へ化けて脚注リンクのジャンプが壊れていた（脚注一覧側の `<li>` は別経路で生成されるため無事＝参照側だけがずれる非対称）ため、`commands.js` に揃えて退避するようにした。**属性になる部分の退避戻し（`unstashToText`）**: コード・数式・`\$` の退避は画像/リンクより**前**に行われ復元は**後**なので、URL・タイトル・alt に紛れ込んだこれらのプレースホルダは属性値の中で展開されてしまう（`href="http://e/a<code>b</code>c"`。数式は復元される `<span …>` の `"` で属性値が途中終了し**タグ構造ごと破壊**され、往復結果が `["&gt;t](http://e/<span class=)` になる）。そこで画像・リンクの属性を組み立てる直前に `unstashToText` で**元のMarkdownテキストへ戻す**（コード→`` `…` ``、数式→`$…$`、エスケープ由来のドル記号→`\$`）。URL/タイトル中の `` `…` ``・`$…$` はコードでも数式でもなく単なる文字列なので、文字どおり保つのが往復にとっても正しい。**リンクテキスト・alt以外の本文は戻さない**ため `` [`code`](u) `` のリンクテキスト内コードは従来どおり変換される。エスケープ由来のドル記号は入力されたままの `\$` とゼロ幅スペース付きの `$` の2形態があるが、属性値は直列化時に再エスケープされないため**どちらも `\$` へ**戻す（これにより `serializeInline` の `case 'A'` が `href` に `stripZeroWidth` を掛けていなくてもゼロ幅スペースは混入しない）。属性の `"` は escapeAttr（読込＝escapeHtml済み）／escapeHtml＋`"`潰し（ライブ＝未エスケープ）で閉じ防止。往復・保護・ライブ変換をユニットテスト。**ローカル相対パス画像の実表示（2/2・実装済み）**: 拡張機能側（`markdownEditor.ts` の `resolveCustomTextEditor`）が **ドキュメントのフォルダを `webview.options.localResourceRoots` に追加**し、そのフォルダの webview URI（`webview.asWebviewUri(dirUri).toString()`。file スキームのみ）を `update` メッセージの `imageBaseUri` で Webview へ渡す。Webview 側（`editor.js` の `resolveLocalImages`）が各描画後（input・handleUpdateMessage・Rawからの復帰）に `<img>` を走査し、`markdown.isResolvableRelativeImageSrc(src)`（スキーム付き `http:`/`data:`/`vscode-webview:` 等・プロトコル相対 `//`・ルート絶対 `/` は false）が true の相対 src を、元パスを `data-original-src` に退避してから `markdown.resolveImageSrc(imageBaseUri, src)`（`base + '/' + rel`。先頭 `./` 除去・`#`/`?` を `%23`/`%3F` へエンコード・`%` は二重エンコードしない）で解決した webview URI へ差し替える。`data-original-src` があれば再解決しない（二重解決ガード）／`serializeInline` は `data-original-src` を優先して読むため**往復は元の相対パスで不変**／解決後srcは元相対パスと異なるが `getCleanHtmlFromEditor`→`htmlToMarkdown` は `data-original-src` を読むので `incoming===current` 比較が成立し再レンダリングループしない。CSP `img-src ${webview.cspSource}` が asWebviewUri のホストを許可済み（変更不要）。純粋関数（`isResolvableRelativeImageSrc`/`resolveImageSrc`）をユニットテスト、`src/markdownEditor.ts` 変更＝統合テスト対象（openEditor が新経路を通過）。`../` を含む相対や外部URL画像（`https://…`）の表示は実機確認前提（[[シールドバッジ画像の表示]] は外部 `img-src` CSPが別途必要）。**タイトル記法 `(url "title")`（実装済み）**: `markdown.js` の純粋関数 `parseLinkDestination(dest)` が丸括弧の中身を `{url, title}` へ分け（`"…"`/`'…'` の両方、**URLとの間に空白必須**＝`a"b".png` のような引用符入りパスを守る。閉じ引用符が末尾に無い不正形はタイトル無し扱い）、`buildTitleAttr` が ` title="…"` を組み立てる。画像・リンクの両方・読込（`convertInline`）とライブ変換（`convertInlineText`）の両パスで共有。直列化は `serializeTitle(node)` が `title` 属性を ` "title"` へ戻す（`"` を含むタイトルは `'…'` で囲む／**`)` を含むタイトルは捨てる**＝`([^)]+)` では表現できず、出力すると再パースでURLが途中で切れて記法の外まで文字列が漏れ文書が壊れるため、タイトルだけを失う方を選ぶ）。空タイトル `""` は保存時に落ちる既知の正規化。**修正前の実害**: `([^)]+)` が `url "title"` 全体をURLとして取り込んでいたため `src`/`href` にタイトルが混入し、**タイトル付き画像は表示されず**（存在しないパスへ解決）**リンク先も無効**になっていた（Markdownテキスト自体の往復は `getAttribute` が実体参照を戻すため偶然保たれており、ROADMAP の「往復も崩れる」という記述は不正確だった）。CommonMark の `(…)` 形式のタイトルは `)` 制約と両立しないため非対応
- **リアルタイム同期**: WYSIWYGビューとMarkdownソースの双方向同期。Webview→拡張は `edit`（150msデバウンス）、拡張→Webview は `onDidChangeTextDocument`→`update`。**編集中のキャレット保持**: `handleUpdateMessage` の innerHTML 全書き換えでキャレットが飛ばないよう、`utils.saveCursorPosition`/`restoreCursorPosition` が3段のアンカー（①ノード基準＝インプレース変更向け・②ブロック基準＝全書き換え向け・キャレットのトップレベルブロックのインデックス＋ブロック内オフセット。ブロックの挿入・並べ替えで index がずれても内容の署名（先頭テキスト64文字・`utils.blockSignatureOf`）が一意一致すればそのブロックを辿り、一致無し／曖昧なら index にフォールバック・③グローバル文字数オフセット＋最寄りフォールバック）で復元する。**非同期描画完了後の復元**: Mermaid の再描画は非同期（`mermaidModule.render()` は Promise を返す）で、描画完了前に復元するとブロックの高さ・ノード構成が未確定でアンカーがずれるため、`utils.restoreCaretAfterRender(renderPromise, restoreFn)` が render の Promise の解決（失敗時も DOM 構造は確定なので復元を試みる）を待ってからキャレットを復元する（Promise でない値は従来どおり `setTimeout(0)` 相当）。**競合する古いエコーの無視**: Webview は `edit` 送信ごとに単調増加する `state.editSeq` を採番（全送信を `editor.js` の `postEdit` へ集約し `seq` を載せる）し、拡張機能は `markdownEditor.ts` でその編集適用に起因するエコー `update` にだけ `seq` を反映（`pendingEchoSeq` を適用前に保持→`updateWebview` で一度きり消費・外部編集には付けない・無変更時は `updateTextDocument` が `false` を返すのでクリア）。Webview は `utils.shouldIgnoreStaleUpdate(message.seq, state.editSeq)` で「エコーの seq が最新送信 seq より小さい＝陳腐化」を無視し、タイプ中に古いエコーが新しいローカル内容を巻き戻すのを防ぐ（`src/markdownEditor.ts` 変更＝統合テスト対象）
- **単語数・文字数表示**: エディタ右下に固定表示するステータスバーで単語数・文字数（空白除外・コードポイント単位）をリアルタイム更新（数え上げは `utils.js` の `countText`、UIは `editor.js` が動的生成。`markdownEditor.ts` のHTMLには手を入れない）
- **Rawモードの行番号ガター**: Rawモード（`#rawEditor` textarea）左端にソース行番号を表示（`.raw-line-gutter`）。行番号を論理行と1対1で揃えるため textarea を `white-space: pre`（折り返しオフ・横スクロール＝VS Code既定と同じ）に変更。行数は純粋関数 `utils.countLines`（`\n` 区切り・空文字は1行・末尾改行は+1行）、番号列は `utils.buildLineNumberText`（`1\n…\nN`）で生成し `utils.test.ts` で検証。ガターは `white-space: pre` の1要素へ番号を流し込み、縦スクロールに `transform: translateY(-scrollTop)` で追従（横スクロールでは動かさない）。font/上padding(20px)を textarea と一致させ、**行高は整数px（`line-height: 22px`）で textarea と gutter を完全一致**させる（以前は unitless `1.6`＝22.4px の小数で、textarea のネイティブ行描画と div の行送りが食い違い下の行ほど番号がずれて「一部の行に番号が出ない」不具合になっていた。整数px化で解消）。UIは `editor.js` が `#rawEditor` を flex 行のラッパー（`.raw-editor-wrap`）で包む形で動的生成し、`markdownEditor.ts` は非変更（`media/` のみ＝統合テスト非対象）。入力・文書更新・Raw突入時に更新、モード可視制御はラッパーの `display` で行う。WYSIWYG側の行番号は ROADMAP に分割済み（(2/3)対応付けは実装済み・(3/3)ガター描画は残）
- **Rawモードの行折り返しON/OFFトグル**: ツールバーの`#toggleRawWrap`（`↩️ 折り返し`ボタン、`markdownEditor.ts`に追加）。`state.isRawWrapEnabled`（既定false、`media/modules/state.js`）をクリックでトグルし、`editor.js`の`applyRawWrapMode()`が反映する: `.raw-editor-wrap`へ`raw-wrap-on`クラスを付け外しし、`editor.css`の`.raw-editor-wrap.raw-wrap-on #rawEditor { white-space: pre-wrap; overflow-wrap: break-word; }`で折り返しを有効化。行番号ガター（`.raw-line-gutter`）は論理行1つ＝固定22px行高の前提で作られており折り返した視覚行とはずれるため、ON中はガター要素自体（`rawGutter`。ガター付きラッパー全体を隠す既存の`hideRawLineGutter`とは別物＝textareaは表示したまま）を`display:none`にして非表示化する（ROADMAPの「開始行のみ番号を出すミラー測定」までは実装せず、まず折り返しトグル＋折り返し時ガター非表示の簡易対応に留めた、というROADMAP記載の方針どおり）。`applyRawWrapMode`は`initRawLineGutter`の直後（初期化時）とボタンクリック時の両方で呼ばれ、Raw⇔プレビュー切替時の`showRawLineGutter`/`hideRawLineGutter`とは独立して折り返し設定を保持する。`state.js`の`isRawWrapEnabled`はユニットテスト対象（getter/setter）だが、`editor.js`側のDOM操作（`applyRawWrapMode`/`setupRawWrapToggle`）は`toggleRawMode`等の既存パターンと同様に`media/modules/`外＝ユニットテスト対象外。`markdownEditor.ts`変更のため統合テスト（12件）も実行しパス。折り返し設定はwebview再読み込みで永続化されない（既存の`isRawMode`と同じ非永続方針）。`/local-review`: Aなし。C（トグルボタンがWYSIWYGモード中も常時表示・クリック可能で効果が見えず誤解を招きうる、severity low）はROADMAPへ
- **WYSIWYG行番号の「ブロック→ソース開始行」対応付け（行番号表示 2/3）**: 純粋関数 `markdown.computeBlockStartLines(finalMarkdown, blockMarkdowns)`。`finalMarkdown`（`htmlToMarkdown` の確定ソース＝唯一の変換規則）と、各トップレベルブロックを `serializeBlockElement` 相当で直列化した文字列配列を受け、各ブロックの**1始まり開始行番号**の配列を返す（本文を持たない空ブロックは `null`）。各ブロックの本文行（`coreLinesOf` で前後の空行を落とした行列）は `finalMarkdown` 内に順序どおり連続で現れるため、直前に確定した位置（cursor）から前方一致で開始行を探す。これによりブロック間の空行畳み込み（`\n{3,}`→`\n\n`）・先頭空行除去に依存せず、同一本文が複数あっても順序で正しく対応づく。DOM非依存でユニットテスト済み（`markdown.test.ts`）。
- **WYSIWYG行番号の「表示ブロック→開始行」対応（行番号表示 3/3 橋渡し）**: `markdown.computeEditorLineMap(editorEl)` がライブの`#editor`から `{ block: 表示中のライブ要素, line: 開始行 }` の配列を返す（本文なしブロックは除外）。クリーン化を `getCleanHtmlFromEditor` と共有するため clone生成部を `getCleanEditorClone(editorEl)` へ切り出し（`getCleanHtmlFromEditor` はその `.innerHTML` を返すだけの薄いラッパへ）。クリーンなクローンから `finalMarkdown`（全体）と各トップレベルブロックの直列化を得て `computeBlockStartLines` で開始行を求め、それを「表示中のライブ要素列」（Mermaidの隠し `pre.mermaid-source` を除外）とインデックスで対応づける。クリーンのトップレベル数と表示中ライブ要素数は常に一致する（Mermaidの隠しpre↔可視 `.mermaid-container`、テーブルの `.table-container`→`table` アンラップを経ても）。レイアウト非依存＝jsdomでユニットテスト済み（Mermaid/テーブルの対応も検証）
- **WYSIWYG行番号ガター描画（行番号表示 3/3）**: `editor.js` が `#editor` を flex ラッパー（`.wysiwyg-editor-wrap`）で包み左に `.wysiwyg-line-gutter` を置く（Rawガターと同方式・`markdownEditor.ts` 非変更）。`computeEditorLineMap` の各 `{block, line}` について、ブロック上端を `getBoundingClientRect().top - editorRect.top + scrollTop`（offsetParent非依存）で `#editor` 内容座標へ変換し、その `top` に行番号を絶対配置。縦スクロールは `transform: translateY(-scrollTop)` で追従（`#editor` の `scroll`）、内容変化（input・デバウンス／文書更新後の `scheduleWysiwygGutterUpdate`）とリサイズで再配置。可視制御はラッパーの `display`（`toggleRawMode` の `editor.style.display` 直接操作を `showWysiwygLineGutter`/`hideWysiwygLineGutter` へ置換）。ピクセル整列はレイアウト依存のため実機確認前提（jsdomでは検証不可）
- **目次(TOC)生成**: 見出し(h1〜h6)から目次を生成してキャレット位置へ挿入（ツールバーの 📑 ボタン / `Ctrl+Shift+O` / `executeCommand('toc')`）。スラッグ生成 `markdown.slugify`・目次組み立て `markdown.buildTocMarkdown`（純粋関数）＋ `commands.insertToc`（DOM挿入）。重複見出しは `-1`/`-2` 付与、`> > text` 同様に往復変換で安定。`markdownToHtml` は見出しへ同一規則の `id` を付与し、目次リンク（`#slug`）を `Ctrl`/`Cmd`+クリックすると `commands.scrollToAnchor` が該当見出しへスクロール（contentEditableでは既定遷移が効かないため明示処理／`id` は往復に非出力）
- **見出しパンくずバー（スクロール位置の階層表示）**: ツールバー直下・`#editor` の直前へ動的生成する固定バー（`editor.js` の `initHeadingBreadcrumb`。`markdownEditor.ts` 非変更）。`#editor` の `scroll`・入力・外部編集（`handleUpdateMessage`）・Raw⇔プレビュー切替のたびに更新する。**更新の間引きは経路によって使い分ける**——スクロールは rAF（1フレーム1回。追従が遅れると体感で分かるため待たせない）、**入力は `utils.debounce` で150ms**（`updateHeadingBreadcrumb` は見出しの数だけ `getBoundingClientRect` を呼ぶため、キー入力のたびに同期実行すると見出しの多い文書でタイピングが引っかかる。同じ入力イベントで走る行番号ガター更新と同じ待ち時間）。外部編集・モード切替は即時。`utils.debounce(fn, waitMs, timers)` はタイマーを注入できる汎用ヘルパで、返す関数は保留中の実行を取り消す `cancel()` を持つ（`media/modules/` にあるためユニットテスト済み）。ロジックは3つの純粋関数（`commands.js`）に分離してユニットテスト: `collectHeadings(editor)` が `h1`〜`h6` を出現順に `{level, id, text, el}` へ、`findCurrentHeadingIndex(tops, scrollTop)` が各見出しの上端位置配列から「スクロール位置以前の最後の見出し」＝いま見ている節のインデックスを求め（文書先頭でどの見出しも通過していなければ `-1`）、`buildBreadcrumbChain(headings, currentIndex)` がそこから手前へ辿ってレベルが単調に小さくなる見出しだけを祖先として積み上げる（レベル飛びは存在するものだけ）。`el.getBoundingClientRect()` を使う位置計算は `editor.js` 側（DOM依存・テスト対象外）。各パンくず項目は `id` があるものだけクリック可能にし（`heading-breadcrumb-item-clickable` クラスでcursor/hoverも分離）、`commands.scrollToAnchor('#'+id)` で遷移する。**既知の制約**: `execCommand('formatBlock')` による見出し化やライブ変換直後の見出しは `id` を持たない（`markdownToHtml` の再パース時のみ付与）ため、その間はパンくずに表示されてもクリック不可（`/local-review` 指摘で同サイクル中に修正）。
- **「/」コマンドメニュー（Notion風。初期スコープ: 目次挿入・表の挿入）**: 空の段落で「/」だけを入力すると、その直後へポップアップメニューを表示する。判定は純粋関数 `commands.isSlashCommandTrigger(block)`（可視テキストが半角スラッシュ1文字だけ・`<br>`以外の子要素を含まない。`table.isBlockEmpty`と同じ考え方）。表示/非表示は `editor.js` の `setupSlashCommandMenuEvents` が `document` の `selectionchange`（`setupRawMarkdownCaretEvent`と同じ設計）を監視して駆動し、メニューは `mermaid`/`math`/`table` の各コンテキストメニューと見た目を共有（`.slash-command-menu`/`.slash-command-item`）。項目選択（`runSlashCommand`）は対象ブロックの末尾へキャレットを合わせてから、既存の挿入処理（`commands.insertToc()` / `table.showInsertDialog()`）に委譲する（どちらも「キャレットのあるブロックの直後」へ内容を挿す処理を流用）。**「/」の除去は挿入が実際に成功した後にのみ行う**（`/local-review`指摘A-1/A-2で発覚: 先に消すと、表ダイアログのキャンセルや見出し0件での目次挿入失敗時に入力していた「/」が復元されず消えるだけになる）。これを担保するため `commands.insertToc()` は実際に挿入できたかを`boolean`で返すよう変更し、`table.showInsertDialog(onInserted)` はOK確定（挿入成功）時にのみ渡されたコールバックを呼ぶよう拡張（キャンセル・Escapeでは呼ばない。`table.js`の`onInsertConfirmed`/`cancel()`）。表挿入はダイアログがフォーカスを奪うため、右クリックの挿入メニューと同じ「`table.setPendingInsertRange(range)`で退避→確定時に`restorePendingSelection`で復元」の仕組みを外部から使えるよう`setPendingInsertRange`をエクスポートで追加。**既知の制約**: `utils.findBlockAncestor`が`P`/`DIV`/`LI`でしか止まらないため見出し・引用・テーブルセル内では現状トリガーしない（低優先度でROADMAPへ）
- **リンクの挿入・編集ダイアログ**: `Ctrl+K`（Mac: `Cmd+K`）／ツールバーのリンクボタンで自前ダイアログ（`#linkDialog`）を表示（`commands.insertLink`/`applyLinkDialog`/`removeLinkFromDialog`/`closeLinkDialog`、配線は `editor.js` の `setupLinkDialogEvents`）。Webviewでは `prompt()` が使えず従来の `insertLink` は実質機能していなかった。既存リンク（`<a>`／生Markdown展開中のspan）内なら編集モード（「リンク解除」ボタンを表示）、選択中ならその文字列をリンクテキストの初期値に。入力欄へのフォーカスで選択が失われるため `state.linkDialogRange`/`linkDialogTarget` を保持。適用後のキャレットはリンク直後（内側だと生Markdown展開が走るため）。**`state.js` はDOM参照・状態を明示的なgetter/setterで公開しているため、フィールド追加時は state リテラル・`initDOMReferences`・getter の3箇所を更新すること**
- **リンクのクリック挙動**: 通常クリックはキャレット設置のみ（リンク先へ飛ばない・`cursor: text`）、`Ctrl`（Mac: `Cmd`）+クリックで遷移（`commands.handleLinkClick`）。**`click` ではなく `mousedown` で処理**（clickの時点ではキャレット設置→`selectionchange`→`syncRawMarkdownToCaret` で `<a>` が `span.raw-markdown` に置き換わり辿れないため）。展開済みspanも遷移対象（記法をパースしてhrefを取得）。修飾キー付きは `preventDefault` でキャレット移動も抑止、通常クリックは抑止しない（キャレットが動かなくなる）。`#slug` は `scrollToAnchor`、外部リンクは `openLink` メッセージ→`markdownEditor.ts` の `openLink` が `vscode.env.openExternal` で開く。スキームは http/https/mailto のホワイトリスト（`javascript:` 等は無視／Webviewを信頼せず拡張機能側でも再検証）。相対パスは未対応（キャレット設置のみ）
- **生Markdown表示（リンク・強調記法）**: キャレットがインライン装飾（リンク `[](…)`・太字 `**`・斜体 `*`・取り消し線 `~~`・下線 `++` とその入れ子）の内側にある間だけ生Markdownを薄く表示（`commands.syncRawMarkdownToCaret`／`editor.js` の `setupRawMarkdownCaretEvent` が `selectionchange` を監視・再入抑止）。`outermostInlineDecoration` で**最も外側の装飾要素ごと** `<span class="raw-markdown">` へ展開（内側だけだと `***` が壊れる）、外れたら復帰。展開は `markdown.serializeInline`、復帰は `markdown.convertInline`（どちらも htmlToMarkdown/markdownToHtml と同じ関数）に委譲するため生表示⇔レンダリングが通常変換と一致・往復非影響。展開中は `utils.shouldSkipInline`（`.raw-markdown` を除外）が `walkInline` の再変換を抑止。`RAW_INLINE_TAGS` に対象タグを定義（`CODE` は含めない＝コード内は記法解釈しない）。**数式**も同じ `raw-markdown` の仕組みを共有するが、数式コンテナは `contenteditable="false"` でキャレットが内側へ入れないため展開のトリガーだけは**クリック**（`commands.handleMathClick`／`expandMathToRaw`）で行う。インラインは `<span class="raw-markdown">$式$`、ブロックは `<div class="raw-markdown raw-math-block">$$…$$`。復帰は `collapseRawMarkdown` が担い、ブロックは `markdown.buildMathBlockHtml` で `math-block` を再生成＋`MathModule.render` で再描画、インラインは既存の `convertInline`（`$...$` 対応済み）＋再描画。直列化は `markdown.rawMarkdownText`（`raw-markdown` 要素の生テキスト＝`$` を非エスケープ・`<br>`→改行）で往復不変
- **数式（KaTeX）**: インライン `$...$` → `span.math-inline[data-math]`、ブロック `$$...$$` → `div.math-block[data-math]`（どちらも `contenteditable="false"`）。**生の式は `data-math` が唯一の正**で `htmlToMarkdown` は必ずそこから復元する（KaTeXの生成DOMは直列化に不使用＝描画失敗でも式が消えない）。`markdown.js` は空コンテナを出すだけでKaTeX非依存（変換は純粋関数のままテスト可能）、描画は `math.js` の `render(root)` が担当し `data-math-rendered` で二重描画を防ぐ。呼び出しは `editor.js`（読込時・input時・Rawからの復帰時。Mermaidと同じ箇所）。式の中身はプレースホルダ退避で他のインライン整形から保護（`$\alpha^*$` の `*` が斜体化するのを防ぐ）。**インライン数式のライブ変換**: `commands.js` の `convertInlineText`（`applyInlineFormatting`→`walkInline` 経由の入力イベント）が `markdown.js` の `convertInline` と同じ退避順序（`\$` 退避→`$...$` を `math-inline` コンテナへ退避→復元）で `$...$` を変換し、input末尾の `MathModule.render` が描画する。閉じ `$` を打つまで正規表現がマッチしないため入力途中で壊れない。入力テキストは未エスケープなので `data-math` は `escapeHtml`＋`"` の順でエスケープ（読込パスと同一の `data-math` を生成、突き合わせテスト有り）。**ブロック数式のライブ変換**: 複数行 `$$...$$` は `convertInlineText` の対象外（インライン正規表現は改行を含まない）。`commands.js` の `convertMathBlocks`（`applyInlineFormatting` から `walkInline` より前に実行）が、エディタ直下の平文ブロック（class無しの P/DIV）のうち単独 `$$` 行を開き・次の単独 `$$` 行を閉じとして扱い、間の平文ブロックの**生テキスト**（`$` 非エスケープ）を式本文に集めて `markdown.buildMathBlockHtml`〈読込時と同じ生成関数〉で `math-block` へ畳む。閉じ `$$` が無い入力途中は変換しない。キャレットが範囲内なら `contenteditable=false` の math-block へは入れないため直後に空段落を挿入して移す（`caretHandled=true` で `restoreCursorPosition` を回避）。**これが無いと**新規入力した `$$...$$` が段落のまま残り、書き戻し時に `serializeInline` が各 `$` を `\$` へエスケープして生ファイルが破損した（修正済みバグ）。**エスケープ**: 素の `$` は数式開始、通常のドル記号は `\$`。直列化時はテキストノードの `$` を `\$` へ戻す（CODE/PRE は textContent を使う別分岐なので影響なし）。KaTeXは `media/katex/`（js+css+`fonts/*.woff2`）へ同梱＝CSPで外部CDN不可。`katex.min.css` がフォントを相対 `fonts/` で参照するため配置固定。**ブロック数式のPNGコピー＋背景色選択**: `div.math-block` の右クリックで自前メニュー（`math.js` の `setupContextMenu`／`showContextMenu`／`hideContextMenu`。`markdownEditor.ts` は触らず初回に動的生成して `body` へ挿入・CSSは `.math-context-menu`/`.math-menu-item`/`.math-menu-bg-row`/`.math-bg-btn` が `.mermaid-context-menu` 系と同ルールを共有）を出し、メニュー上部の**背景トグル**（`.math-bg-btn` の `data-bg="transparent|white|black"`・既定 `white`）と**文字色トグル**（`.math-fg-btn` の `data-fg="auto|black|white"`・既定 `auto`）で選ぶ（両行とも `buildToggleRow(label, btnClass, dataAttr, options)` で生成＝クリックで自行内の active を1つだけ移す・メニューは閉じない）。「画像としてコピー」で背景を `resolveMenuBackground(menu)`（`.math-bg-btn.active` の `data-bg`・`transparent`/`black` 以外は `white`。Mermaid の同名関数と同契約）、文字色モードを `resolveMenuTextColor(menu)`（`.math-fg-btn.active` の `data-fg`・`black`/`white` 以外は `auto`）で解決し `copyBlockAsPng(block, background, textColor)`→`blockToPngBlob(block, undefined, background, textColor)`→`navigator.clipboard.write(ClipboardItem)`。**文字色**は純粋関数 `resolveTextColor(background, textColor)` が決める（`black`/`white` はその色、`auto` は背景追従＝黒背景のみ白・白/透過は黒。KaTeX は色を明示せず `currentColor` 継承のため採寸ラッパーの `wrapper.style.color` へ与える）。これで**黒背景の白文字**（黒地に黒文字で式が消えるのを防ぐ）に加え、**透過背景をダーク環境へ貼るとき文字色「白」を選べば埋もれない**（B-1 対応）。背景は `buildMathBlockSvgMarkup` の `<rect fill>`（透過は無描画）と html2canvas フォールバックの `backgroundColor` の両経路へ、文字色は `wrapper.style.color` が両経路へ伝播。既定（背景=白・文字=auto）＝従来動作と同一出力。ユニットは `resolveMenuBackground`/`resolveMenuTextColor`/`resolveTextColor` の全分岐・両トグルの独立した active 遷移・`copyBlockAsPng` に渡した背景/文字色がSVGの `fill` と文字色まで伝播することを検証。KaTeXはSVGでなくHTML+CSSのため `svgToPngBlob`（SVG→canvas）は使えず `html2canvas` で `math-block` のクローン（白背景・余白付き）を直接ラスタライズ。字形崩れ防止に `document.fonts.ready` を待つ。`editor.js` 初期化で `mathModule.setupContextMenu(state.editor)` を配線（`media/modules/` のみ変更＝統合テスト非対象。ユニットは `math.js` を helper の `MODULE_FILES` に追加して純粋関数〈`findMathBlock`/`computeMenuPosition`〉とメニュー表示・フォールバックを検証）。**foreignObject 方式への移行（配線済み・実機確認前提）**: html2canvas は `.katex` のCSS配置（`vertical-align`・負マージン・絶対配置・√overline）を再現できず画像が崩れるため、`blockToPngBlob` を `<foreignObject>`（ブラウザネイティブ描画）でSVG化→`data:image/svg+xml`→`<img>`→canvas→PNG 方式へ差し替えた。処理: ラッパー実寸採寸→`loadKatexFontFaceCss`（`KATEX_FONT_MANIFEST` の woff2 を `resolveKatexFontBaseUrl` で導いた `fonts/` から fetch→`arrayBufferToBase64`→`buildKatexFontFaceCss` で `@font-face` を組み立て。全成功時のみキャッシュ・部分失敗はリトライ）→`collectKatexCssText`（読み込み済み KaTeX スタイルシートのルール）＋フォントCSSを `buildMathBlockSvgMarkup` でSVG化→`svgMarkupToPngBlob`（Image→canvas・15秒タイムアウト保険）。foreignObject は隔離コンテキストで外部フォントを参照できないため base64 埋め込みが必須で、CSP は `font-src ${webview.cspSource} data:` に緩和。純粋/テスト可能な部分（`buildMathBlockSvgMarkup`・`buildKatexFontFaceCss`・`KATEX_FONT_MANIFEST`・`resolveKatexFontBaseUrl`・`arrayBufferToBase64`・`loadKatexFontFaceCss`）はユニットテスト済み。**⚠️ 重大リスク（実機未検証）**: Chromium は `<foreignObject>` を含む SVG を canvas に描くと canvas を taint し `toBlob()` が SecurityError を投げる既知挙動があり、VS Code webview でも起きると新経路は毎回失敗して **html2canvas フォールバック（＝従来の崩れた画像）へ退避**する（無リグレッションだが崩れ修正が発現しない）。実機で数式右クリック「画像としてコピー」の結果とコンソールの `falling back to html2canvas` 警告有無を確認する必要がある。taint する場合は数式レンダラを MathJax(SVG出力) へ切り替える案が ROADMAP に残置
- **Mermaid図のPNGコピー／保存＋背景色選択**: `.mermaid-preview-panel` の右クリックで `#mermaidContextMenu`（`markdownEditor.ts` に静的配置）を表示。メニュー上部の背景トグル（`.mermaid-bg-btn` の `data-bg="transparent|white|black"`・既定 `white` が `active`）で背景を選び、「画像をコピー」（`copyToClipboard`→`navigator.clipboard.write`）／「PNG画像として保存」（`saveAsPng`→base64化して拡張機能へ `saveMermaidPng` を postMessage）を実行。配線は `mermaid.js` の `setupContextMenuEvents`（`editor.js` init で一度だけ呼ぶ）が担い、選択背景は純粋関数 `resolveMenuBackground(menu)`（`.mermaid-bg-btn.active` の `data-bg` を読む・`transparent`/`black` 以外は `white`）で解決して `copyToClipboard`/`saveAsPng` の第2引数へ渡す。実際の画像化は `svgToPngBlob(svg, 4, background)` が純粋関数 `resolveImageBackground(background)`（`transparent`→alpha有効・塗り無し・html2canvas背景 null／`white`/`black`→不透明＋該当色／未知→白）で `getContext('2d',{alpha})`・`fillRect` 有無・html2canvas の `backgroundColor` を切り替える。**既定は白＝従来動作**（背景トグル追加前と同じ出力）。背景選択はメニューが持続するため直近の選択を記憶する。`src/markdownEditor.ts`（メニューHTML）変更＝統合テスト対象。ユニットは `resolveMenuBackground`・トグルの active 遷移・`resolveImageBackground` の全分岐を検証（PNGの透過/白/黒の見た目・クリップボード/保存は実機確認前提）
- **太字の表示**: `#editor strong` はウェイト 700（ダーク系テーマのみ 800・body の `vscode-dark`/`vscode-high-contrast` クラスで判定）に加え、`font-size: 1.05em`（**全テーマ共通**・`em` なので見出し内でも相対拡大）。`#editor` 配下の `line-height` は body の単位なし `1.6` を継承＝各要素の `font-size` 基準で計算されるため、拡大すると太字を含む行だけ行高が伸びる。`line-height: calc(1.6 / 1.05)` で補正して行高を据え置いている（`media/editor.css`）
- **書式ツールバー**: 太字・斜体・下線・見出し・リスト・引用・リンク・コードブロックのボタン列（選択範囲を保持したまま適用）
- **インラインライブ変換**: `**太字**` / `~~取り消し線~~` 等を入力中に即時変換（`commands.js` の `applyInlineFormatting`）。contenteditableが入力テキストを複数の隣接テキストノードに分割しても記法がマッチするよう、走査前に `editor.normalize()` でテキストノードを結合する（要素境界はまたがない）。インラインコード（`` `...` ``）内の記法は装飾せず保持する: `convertInline`（`markdown.js`）と `convertInlineText`（`commands.js`）はコードを先にプレースホルダ（NUL文字＋通し番号）へ退避し、他の整形適用後に `<code>` として復元する。これにより `` `**太字**` `` が装飾へ化けず、WYSIWYG往復での記法欠損を防ぐ
- **オートブロック変換**: 行頭の `- ` / `1. ` / `> ` をその場でリスト・引用へ変換（`commands.js` の `handleAutoBlock`）。引用ブロック内で `> ` を入力すると1段深いネスト引用を作る（`handleNestedQuote`。往復変換は `> > ` 形式）
- **箇条書きマーカーの往復保持**: 書き戻しは表示中の全ブロックを `htmlToMarkdown` で再直列化するため、`serializeList` が常に `* ` を吐くと `- ` で書いた既存行が編集と無関係に `* ` へ正規化されていた（触っていない行が書き換わる不具合）。対策として `markdownToHtml` のリストパースが元マーカー（`-`/`*`）を各 `<ul>` の `data-marker` に保持（`buildListHtml`）し、`serializeList` がそれを尊重して復元する（`data-marker` 無し/不正値は従来どおり `*`＝新規入力・execCommand 生成 ul の既定）。`getCleanEditorClone`/`getCleanHtmlFromEditor` は `data-marker` を除去しないため書き戻し経路でも有効。**既知の制約**: 空行なしで異なるマーカーが混在（`- a\n* b`）すると現状パーサーが1つの ul に束ね先頭マーカーへ統一（CommonMark では別リスト・非退行の既知挙動を `markdown.test.ts` で固定）
- **表の区切り行の往復保持**: 上記と同種の不具合が表区切り行 `\|---\|`→`\| --- \|` にもあった。`serializeTable` が区切り行を常に `\| --- \|` 形式（アライメントコロン `:---`/`---:`/`:---:` も `---` へ潰す）で出力していたため、表を一切触っていなくても書き戻しで元の表記が失われていた。対策として `markdownToHtml` の表パースが区切り行を（trimせず）列ごとの生テキストへ分解する `splitSeparatorRow` で解析し、列数がヘッダーと一致する場合のみ元表記を `<table>` の `data-sep` 属性（カンマ区切り。区切り行の許可文字は空白・`-`・`:`のみでカンマを含まないため属性値として安全）へ保持。`serializeTable` はこれがあり列数が一致していればそのまま復元し、無い（新規生成テーブル）／列数不一致（列追加・削除後）はデフォルト `---` 書式へフォールバックする。列の追加・削除では `table.js` の `addColumn`/`deleteColumn` が `markdown.insertSepColumn`/`removeSepColumn`（`data-sep` を列単位で splice する純粋関数）で属性も同時に更新するため、**変更していない他列のアライメント・空白は保たれる**（追加した列だけが既定の ` --- ` になる）。これらの純粋関数は、属性が無い場合や現在の列数と食い違っている（＝既に陳腐化している）場合は `null` を返して更新しない——陳腐化した値を splice すると偶然列数が一致して誤った書式が復元されるおそれがあるため。**既知の制約**: 行の追加・削除は区切り行に影響しないため対象外。セルの結合など列数が暗黙に変わる操作を将来入れる場合は同様の更新が必要
- **引用ブロックのEnter操作**: 引用の末尾で `Enter` は引用を抜けて後続段落へ、`Shift+Enter` は引用内改行（`<br>`）（`commands.js` の `handleBlockquoteEnter`。`editor.js` のkeydownでコードブロックEnter処理より前に配線）
- **GitHubアラート**: 引用の先頭行が `[!NOTE]`/`[!TIP]`/`[!IMPORTANT]`/`[!WARNING]`/`[!CAUTION]` のみのとき、色分けアラートboxへ変換（`markdown.js` の `tryBuildAlertHtml`）。`data-alert-type` を保持し `serializeAlert` で `> [!TYPE]\n> 本文` へ往復。マーカー大文字必須・行内に余分なテキストがあると通常引用（GitHub準拠）。色はテーマ変数（`--vscode-editorInfo/Warning/Error-foreground`・`--vscode-charts-*`）。手入力・ペーストは `commands.js` の `convertAlerts`（`applyInlineFormatting` 経由の入力イベント）がライブ変換: 対象ブロックをシリアライズ→再パースし単一アラートdivになる場合のみ置換（読込時パーサと判定が常に一致）、変換後キャレットは本文へ。本文内のEnterは `handleAlertEnter` が処理: 末尾ならboxを抜けて後続段落へ、途中およびShift+Enterなら `<br>` 改行（ブラウザ既定のEnterは本文divを分割しbox構造を壊すため常に自前処理）。`<br>` 挿入＋末尾プレースホルダ補完は `handleBlockquoteEnter` と共通の `insertLineBreak` を使用
- **脚注（`[^label]` / `[^label]: 本文`）**: `markdown.js`の`markdownToHtml`が読込時（Raw⇔プレビュー切替時も含む）に、行ベースの本パーサーとは別に2段階で処理する。**(1) 定義抽出**: `extractFootnoteDefinitions(lines)`が本文行配列を先読みし、コードフェンス（```）・ブロック数式（$$）の中を除いて`[^label]: 本文`形式の行を候補として拾う→**同じラベルが文書内のどこかで`[^label]`として（コード/数式ブロック外で）参照されている場合のみ**実際の定義として確定し、通常のブロックパース対象（`contentLines`）から取り除く（`FOOTNOTE_REF_PATTERN = /\[\^([A-Za-z0-9_-]+)\](?!:)/g`で参照を収集）。**この「参照必須」ガードが無いと、正規表現の説明文（`[^0-9]: 数字以外にマッチ`のような地の文）まで誤って脚注定義として剥がしてしまう（`/local-review`指摘・同サイクルで修正）**。重複ラベルは最初の定義を採用し、二番目以降は通常の段落として残る（データを失わない。ただしその段落の`[^label]:`部分は`(?!:)`ガードにより誤って参照変換されない）。定義は文書中のどこにあっても**保存時に文書末尾の`<section class="footnotes"><ol>`一覧へ集約**される（一般的な脚注表示慣習・既知の仕様。中程にあった定義は次の保存で末尾へ移動する）。**(2) 参照変換**: 本文段落の`convertInline(text, footnoteLabels)`（第2引数は定義済みラベルのSet。見出し・リスト・引用・テーブルセルには現状渡していない＝本文段落のみが対象）が`[^label]`（`(?!:)`付きで定義行自身を誤検出しない）を`<sup class="footnote-ref" data-footnote-label="label"><a href="#fn-label" id="fnref-label">label</a></sup>`へ変換（未定義ラベルはリテラルテキストのまま）。ラベルの見た目は連番へ振り直さず**入力した文字列をそのまま表示**する簡易実装（`[^1]`→上付き"1"、`[^note]`→上付き"note"）。直列化（`htmlToMarkdown`）は`SUP`（`data-footnote-label`から`[^label]`を復元）と`SECTION.footnotes`（各`<li data-footnote-label>`から戻りリンク`.footnote-backref`を除いて`[^label]: 本文`へ復元）の2ケースを追加（`BLOCK_TAGS`に`SECTION`を追加）。脚注参照・脚注一覧の`<a href="#fn-label">`/`<a href="#fnref-label">`は既存の`commands.handleLinkClick`（任意の`a[href^="#"]`をCtrl/Cmd+クリックで`scrollToAnchor`へ渡す汎用処理）にそのまま乗るため追加配線不要。定義行の`:`直後の空白は`FOOTNOTE_DEF_PATTERN = /^\[\^([A-Za-z0-9_-]+)\]:(\s*)(.*)$/`の第2グループ（`sep`）で個数ごと独立して取り出し、`buildFootnotesSectionHtml`が`<li data-footnote-sep="...">`属性へ保持、`htmlToMarkdown`のSECTIONケースがその属性値をそのまま`[^label]:${sep}${text}`へ差し戻すことで、空白0個・1個・2個以上のいずれでも変換往復で元の個数を保持する（属性が無い＝この対応以前に生成されたHTML等は半角スペース1つへフォールバック）。**定義同士の間の空行**も同型の仕組みで保持する: 定義行だけを本文から取り除くと定義の間にあった空行が本文側に残り、往復すると最初の定義の前へ空行が集まってしまう（`\n\n[^x]: A\n\n[^y]: B` → `\n\n\n[^x]: A\n[^y]: B`）ため、`extractFootnoteDefinitions`は定義直後の空行をいったん保留し、**次に来るのがまた定義なら**その定義の`blanksBefore`へ付け替えて本文から取り除く（次が本文行なら保留分を本文へ戻す／文書が「定義＋空行」で終わる場合も末尾で戻す）。`buildFootnotesSectionHtml`が`data-footnote-blanks`属性（0のときは属性自体を出さず後方互換）へ保持し、SECTIONケースが定義同士の連結を`'\n'.repeat(1 + blanks)`で復元する。なお**脚注定義の位置そのものは保たれない**（本文の途中に書いた定義も文書末尾の脚注一覧へ集約される＝一般的なMarkdownの脚注と同じ挙動）。空行の扱いは**「脚注一覧側の区切り（`blanksBefore`）の算出」と「本文側から空行を減らすか」を分離**している。前者は、直前も定義なら保留分をまるごと、直前が本文なら（定義の間に本文行が挟まっていても）直前に空行があれば1つ、として立てる。後者は**定義（の連なり）が前後の両方を空行に挟まれていたときだけ**1つ減らす（定義行だけが消えると前後の空行が両方残り本文側の空行が1つ増えるため。「定義行を消すときは隣の改行も一緒に消す」という手編集と同じ結果）。**後ろに空行が無い定義では減らしてはいけない**——リスト・引用・テーブルのブロックパーサは`seamIndices`を見ず空行だけが区切りなので、`- item1` / `` / `[^x]: A` / `- item2` のような入力で項目が融合し、連続空行（空段落）も1つ失われる。本文側の空行を残したまま`blanksBefore`を立てても、脚注一覧は文書末尾に独立して出るため二重計上にはならない
  - 入力時ライブ変換: `commands.js`の`convertInlineText`も`walkInline`が`section.footnotes li[data-footnote-label]`から既存の脚注ラベル集合を収集し、対応する定義があるラベルの`[^label]`をその場で`<sup class="footnote-ref">`へ変換する（`markdownToHtml`と同じ判定方針・`(?!:)`ガードも同様）。ただし`markdownToHtml`が段落（`<p>`）変換にのみラベル集合を渡す仕様に合わせ、`nearestBlockTag(node)`で直近のブロック祖先が`P`の場合のみ変換する（見出し・リスト・表・引用・アラート本文では変換しない＝編集中と保存後の見た目が食い違う非対称を避けるため）。他の記法（コード・数式・画像・エスケープ`$`）と同様、変換結果は一旦専用のデリミタ付きプレースホルダへ退避し、リンク変換の直後（他記法の復元より前）で退避、全復元の最後に実HTMLへ差し戻す（数式・画像の中身にたまたま`[^label]`相当の文字列が含まれていても属性値を壊さないため。`/local-review`指摘・同サイクルで修正）。参照・定義を同一セッションで両方新規入力した直後は、まだ脚注一覧セクションがDOM上に存在しないため変換されず、保存/Raw切替/再読込を経て初めて反映される（意図的なMVPスコープ）。同一段落内で同じラベルを複数回参照すると`id="fnref-label"`が重複する点はROADMAPへ（`markdown.js`側にも既存の同型挙動あり）
  - ホバーツールチップ表示: 脚注参照（`sup.footnote-ref`）にマウスを乗せると、対応する脚注定義本文をポップアップ表示する。`commands.getFootnoteDefinitionText(editor, label)`（純粋寄りのDOM参照関数。ユニットテスト対象）が`li[id="fn-label"]`から戻りリンク（`.footnote-backref`）を除いたテキストを取り出し、`editor.js`の`showFootnoteTooltip`が`table.computeMenuPosition`を流用して参照要素の直下へ配置する。表示/非表示の配線（`setupFootnoteTooltipEvents`）は`mouseover`/`mouseout`のイベント委譲＋300msデバウンス（`mouseenter`/`mouseleave`はバブリングしないため）。`<sup>`とその内側の`<a>`の間でマウスが出入りするたびに`mouseover`が再発火する問題は、`activeFootnoteRef`（現在処理中のref要素を保持するモジュール変数）で「既に同じrefを処理中」の再入場を検知し、その場合はタイマーの再設定をスキップして防いでいる（`hideFootnoteTooltip`が呼ばれた経路＝実際にref外へ出た場合・スクロール時には必ず`activeFootnoteRef`をリセットするため、正当な次のホバーを取りこぼさない）。**`/local-review`指摘（severity medium）を同サイクルで修正**: 外部更新（`handleUpdateMessage`がVS Code側からの変更で`state.editor.innerHTML`を丸ごと差し替える経路）でツールチップを閉じておらず、表示中に外部更新が来ると古い内容のまま残ったり、保留中の`mouseover`タイマーがdetachedになった旧`sup`要素に対して発火し`getBoundingClientRect()`がゼロ矩形を返して左上に誤表示される不具合があった→Raw⇔プレビュー切替の同種の全書き換え経路と同様に`hideFootnoteTooltip()`を追加。ユニットテスト5件（`getFootnoteDefinitionText`の本文取得・装飾除去・未定義ラベル・null安全・複数脚注での該当ラベル特定）。`editor.js`側のDOM/位置計算・イベント配線は`media/modules/`外＝ユニットテスト対象外（`markdownEditor.ts`非変更のため統合テストも対象外）。**実際のホバー表示位置・遅延の体感・外部更新時の挙動は実機（拡張機能開発ホスト）確認前提**
- **定義リスト（`用語`\n`: 定義本文`）**: `markdown.js`の`markdownToHtml`本文パースループに、リスト・引用と同様の連続行スキャンとして追加。`scanDefListTerms(lines, i, seamIndices)`が`lines[i]`から「用語候補（空行でも`: `定義行でも見出し/hr/リスト/引用/テーブル開始行でもない通常行）」を連続する限り集め、直後に`: `定義行（`DEFLIST_DEF_PATTERN = /^:(\s*)(.*)$/`）が最低1つ続く場合のみ確定させる（続かなければnullを返し、呼び出し側は通常の段落等に委ねる）。複数の用語行が同じ定義群を共有するケース（`Term A`\n`Term B`\n`: 定義`）、1つの用語に複数の`: `定義行が続くケースの両方に対応し、`buildDefListHtml(items)`が`<dl>`内へ用語の数だけ`<dt>`・定義の数だけ`<dd>`を並べる。空行を挟まず前の行に続けて定義リストが始まると、その行も共有の用語として取り込まれる（Pandoc等の定義リスト拡張と同じ規則。分離したい場合は空行を挟む）。直列化（`htmlToMarkdown`）は`DL`ケース（`BLOCK_TAGS`に`DL`を追加）を追加し、子要素の出現順をそのまま辿って`Term`/`: 定義`へ復元。`:`直後の元の空白（`FOOTNOTE_DEF_PATTERN`と同型の設計で第1キャプチャに分離）は`<dd data-def-sep="...">`属性へ保持し、変換往復で元の空白数（0個・1個・2個以上）を保つ。**`/local-review`指摘（severity high、複数ラウンドで検出・同サイクルで修正）**: 脚注定義行（`[^1]: ...`）は本文パース前に`contentLines`から取り除かれるため、除去後に前後の行が直接隣接し、本来無関係だった行同士が定義リストへ誤結合される回帰があった（例: `Term`\n`[^1]: 本文`\n`: Definition`で`[^1]: 本文`を除去すると`Term`と`: Definition`が隣接し誤結合）。最初に「除去箇所へプレースホルダを残す」方式を試みたが、これはリスト・引用・テーブル（各行が`-`/`>`/`|`等の記法で自己申告するため元々縫い目の影響を受けないはずのブロック）まで誤って分裂させる副作用があり撤回。最終的に`extractFootnoteDefinitions`が返す`seamIndices`（`contentLines`中で「直前の行との隣接性が失われている位置」の集合）を導入し、これを参照するのは`scanDefListTerms`（用語収集・複数`: `定義行の収集・グループ連鎖の3箇所すべて）と段落継続ループのみに限定（リスト・引用・テーブルは自身の記法判定だけで縫い目の影響を受けず従来通り正しく結合される）。ユニットテスト多数（用語/定義グループの各パターン、`data-def-sep`の往復、縫い目をまたぐ誤結合防止の複数パターン、リスト/引用が縫い目をまたいでも正しく結合されることの確認）。型/lint/compile・ユニット660件パス。`media/modules/`のみ＝統合テスト非対象。**既知の制約（ROADMAPへ）**: `utils.findBlockAncestor`が`DT`/`DD`を認識しないため定義リスト内では自動整形（`---`→`<hr>`化等）が無効／`buildDefListHtml`が`footnoteLabels`を渡さないため定義リスト内の`[^label]`は脚注参照へ変換されない（見出し・リスト・テーブルセルも同様の既存仕様）
- **YAML front matter の折りたたみ表示**: `markdown.js`の`markdownToHtml`本文パースループの先頭（`i === 0`のときのみ）に`parseFrontMatter(rawLines)`（文書先頭が`---`で、後続のどこかに`---`があれば`{raw, endIndex}`を返す純粋関数）を追加。`buildFrontMatterHtml(raw)`が折りたたみ可能な`<div class="frontmatter"><div class="frontmatter-header" contenteditable="false">...</div><pre class="frontmatter-body">...</pre></div>`を組み立てる（YAMLはMarkdownのインライン記法を持たないため`convertInline`は通さず`escapeHtml`のみ）。既定は折りたたみ状態（`editor.css`で`.frontmatter-body`を非表示）、ヘッダクリックで`commands.toggleFrontMatter`（`editor.click`のイベント委譲経由。折りたたみ状態はUI表示のみで文書には影響しないため往復対象にしない）が`frontmatter-expanded`クラスをトグルする。直列化（`htmlToMarkdown`）は`DIV`の`.frontmatter`分岐で`.frontmatter-body`（`<pre>`なのでcontenteditableでも`shouldSkipInline`により装飾されない・textContentがそのまま生YAML）から`---\n...\n---`を復元。HTML仕様上`<pre>`開始タグ直後の最初のLFはパース時に無視されるため、本文の先頭行が空行でも失われないよう常にダミーの改行を1つ先頭へ足している。**`/local-review`指摘（severity high、複数ラウンドで検出・同サイクルで修正）**: (1) `extractFootnoteDefinitions`が`markdownToHtml`本文パースとは別に`parseFrontMatter`を（脚注除去後の`contentLines`に対して）再度呼んでいたため、文書1行目が参照済み脚注定義行で除去後にたまたま`---`が現れるケースで前後の判定が食い違い、通常の内容を誤ってfront matterと認識する回帰があった→front matter判定を元の行配列（`rawLines`）に対して一度だけ行い、`extractFootnoteDefinitions(rawLines, frontMatterEndIndex)`と本文パースループの両方でその結果を共有する設計へ変更（`extractFootnoteDefinitions`の第2引数）。(2) front matter内が偶然`[^label]: ...`形式に一致すると脚注定義として剥がされてしまう問題→`extractFootnoteDefinitions`が`frontMatterEndIndex`以下の行をコードフェンス・ブロック数式と同様に非scannableとして保護。(3) 初版は「本文0行」と「空行1行だけ」の区別を`data-frontmatter-lines`属性で保持しようとしたが、`.frontmatter-body`はcontenteditableでユーザーが自由に編集できるため、この属性は編集直後に実際の内容と食い違う陳腐化した値になり、直列化時にユーザーが入力した内容そのものを誤って捨ててしまう重大なデータ消失リスクがあることが再レビューで判明→撤回し、この2ケースは保存のたびに「本文0行」へ正規化される仕様として受け入れることにした（既知の仕様としてコード内コメント・テストに明記）。(4) `insertToc`のフォールバック挿入位置（有効なキャレット位置が無い場合`state.editor.firstChild`の直前へ挿入）がfront matterの「文書絶対先頭」制約を壊しうる問題→先頭が`.frontmatter`ブロックならその直後へ挿入するよう修正。**既知の制約（ROADMAPへ）**: ヘッダのトグルがマウス操作限定でキーボード操作不可（既存の`.code-lang-selector`等と同じ制約）／ラベルが英語表記（`Front Matter`）で他のUI文言と不統一／`parseFrontMatter`が内容を検証せず位置関係のみで判定するため、文書が水平線から始まり後方にも水平線がある通常のMarkdown文書を誤ってfront matterと認識しうる（実際のJekyll/Hugo等も同様の設計）
- **キーボードショートカット**: 一般的なショートカット（Ctrl+B、Ctrl+Iなど）をサポート
- **テーマ対応**: VS Codeのカラーテーマに自動適応

## 技術スタック

- **言語**: TypeScript
- **プラットフォーム**: VS Code Extension API
- **ビルドツール**: esbuild
- **依存関係**:
  - highlight.js: コードブロックのシンタックスハイライト
  - mermaid.js: ダイアグラムレンダリング
  - html2canvas: SVGからPNG画像への変換（高解像度エクスポート）
- **開発依存関係**:
  - TypeScript 5.9.3
  - ESLint 9.39.2
  - @vscode/test-electron: 拡張機能のテスト

## プロジェクト構造

```
vscode-Markdown-WYSIWYG-/
├── src/
│   ├── extension.ts           # 拡張機能のエントリーポイント
│   ├── markdownEditor.ts      # カスタムエディタの実装
│   └── test/
│       └── extension.test.ts  # ユニットテスト
├── media/                     # Webviewのリソース（CSS、JSなど）
├── dist/                      # ビルド出力ディレクトリ
├── package.json               # 拡張機能のマニフェスト
├── tsconfig.json              # TypeScript設定
├── esbuild.js                 # ビルドスクリプト
├── eslint.config.mjs          # ESLint設定
└── README.md                  # ユーザー向けドキュメント
```

## アーキテクチャ

### 1. エントリーポイント（extension.ts）
- 拡張機能の初期化と登録
- コマンドの登録
- カスタムエディタプロバイダーの登録

### 2. カスタムエディタ（markdownEditor.ts）
- VS CodeのCustom Editor APIを使用したWYSIWYGエディタの実装
- Webviewを使用したリッチテキスト編集インターフェース
- Markdownとの双方向変換処理
- ドキュメントの保存・編集イベントの処理

### 3. Webview
- ContentEditable属性を使用したリッチテキスト編集
- ツールバーによる書式設定機能
- highlight.jsを使用したコードブロックのシンタックスハイライト
- Mermaid図の高解像度レンダリングとエクスポート
  - デフォルト4倍スケール
  - デバイスピクセル比を考慮した自動高解像度化
  - Canvas 2D APIの高品質レンダリング設定（imageSmoothingQuality: 'high'）
- VS Codeのメッセージングシステムを使用した拡張機能との通信

## 開発ワークフロー

### セットアップ

```bash
# 依存関係のインストール
npm install

# 型チェック
npm run check-types

# リント
npm run lint

# ビルド
npm run compile

# 開発モード（watch）
npm run watch
```

### デバッグ

1. VS Codeでプロジェクトを開く
2. F5キーを押して拡張機能開発ホストを起動
3. 新しいウィンドウでMarkdownファイルを開いて動作確認

### テスト

```bash
# ユニットテスト（jsdom上でWebviewモジュールを検証・高速）
npm run test:unit

# 統合テスト（VS Code実機でコマンド・カスタムエディタを検証）
npm run test

# 両方を実行
npm run test:all

# テストのコンパイルのみ
npm run compile-tests
```

- **ユニットテスト** (`src/test/unit/`): `media/modules/` のWebviewモジュール
  （Markdown⇔HTML変換、テーブル編集、検索、コマンド・オートブロック変換、
  ユーティリティ）をjsdomで構築したWebview相当のDOM環境で検証する。`src/test/unit/helper.ts` が実際のWebviewと
  同じ順序でモジュールを読み込み、`acquireVsCodeApi` をスタブする。
- **統合テスト** (`src/test/extension.test.ts`): `@vscode/test-cli` でVS Code本体を
  起動し、コマンド登録、WYSIWYG⇔テキストエディタ切り替え、`updateTextDocument` の
  最小範囲編集・EOL保持を検証する。

### ビルド

```bash
# プロダクションビルド
npm run package
```

## カスタムエディタAPI

このプロジェクトは、VS CodeのCustom Editor APIを活用しています。

- **viewType**: `markdownWysiwyg.editor`
- **優先度**: `default`（ユーザーはテキストエディタとWYSIWYGエディタを切り替え可能）
- **対象ファイル**: `*.md`（すべてのMarkdownファイル）

## 今後の拡張可能性

### 短期的な改善
- 画像の挿入機能
- より多くの言語のシンタックスハイライト対応
- テーブルのセル結合機能

（機能バックログの詳細は `docs/ROADMAP.md` を参照）

### 長期的な改善
- 脚注のサポート
- リアルタイムコラボレーション
- マークダウンテンプレート機能
- カスタムCSSテーマ

- **印刷用スタイル（`@media print`・PDFエクスポート 1/4）**: `media/editor.css` 末尾の `@media print` ブロック。画面専用UI（`.toolbar`・`.heading-breadcrumb-bar`・`.raw-line-gutter`・`#rawEditor`・`.word-count-status`・`.mermaid-toast`・`.link-dialog`・`.table-insert-dialog`・`.mermaid-context-menu`/`.math-context-menu`・`.table-toolbar`・`.find-widget`・`.slash-command-menu`・`.code-copy-btn`・`.code-lang-selector`・`.mermaid-toolbar`/`.mermaid-source-panel`・`.footnote-tooltip`）を `display:none !important` で隠し、`body`/`#editor` の画面用レイアウト制約（`height:100vh`・`display:flex`・`overflow-y:auto`・`min-height:0`）を解除して本文が用紙へ流れるようにする。**色は `body` と `#editor` の地の色だけを `#000`/`#fff` へ固定する**——テーマ変数のままだとダークテーマで「白紙に白文字」になる一方、全称セレクタ等で一括指定すると `#editor .hljs-*` のシンタックスハイライトが潰れるため。**幅**: `.wysiwyg-editor-wrap` は `flex-direction: row` なので `#editor` の `flex` は高さではなく**幅**を決める（`flex: none` を当てると flex base size が max-content になり本文が用紙幅を超えて切れる）。ラッパーを `display: block` へ落として `#editor` を通常のブロック＝用紙幅いっぱいにする（フレックスコンテナのままページ分割される問題も同時に避けられる）。編集中だけ意味を持つ装飾（選択セルのハイライト・検索ハイライト）も紙では消す（検索ハイライトは背景色に加えて `outline` を持ち、outline は背景色と違って印刷設定で落ちないため明示的に消す）。キャレット位置の生Markdown表示（`.raw-markdown`）は消すと本文が欠けるためそのまま出し、淡色を黒へ戻すだけにする。**検証**: 印刷結果は実機でしか目視できないため、`src/test/unit/print-css.test.ts` が「`@media print` が参照するクラス/idがコードベースに実在するか」を静的に検証する（存在しない名前を書いてもCSSはエラーにならず黙って何も隠さないため。実際に `.mermaid-view-toggle` という誤名をこの検証で検出した）。**既知の制約**: Rawモード表示中は `#wysiwygEditorWrap`（`#editor` の親ラッパー）がインラインスタイルで `display:none` になるため印刷すると白紙になる（救済は ROADMAP「PDFエクスポート (2/4)」）／改ページ制御は (3/4)／Mermaid図のテーマ追随による見えにくさは (3/4)／非同期描画の待ち合わせは (4/4)

- **PDF出力の導線（撤去済み）**: かつてツールバーの `#exportPdf`（🖨️ PDF）ボタンとコマンド `markdown-wysiwyg-editor.exportPdf` から `window.print()` を呼ぶ導線を実装していたが、**実機確認でどちらも無反応だったため撤去した**。VS Code の Webview は sandbox 付き iframe で動くため `window.print()` がブロックされる（コンソールに警告が出るだけで何も起きない）。`Ctrl+P` / `Cmd+P` も VS Code の「クイックオープン（ファイル検索）」が優先されるため使えない。撤去したもの: ツールバーボタン・コマンド登録（`package.json` / `extension.ts`）・`MarkdownEditorProvider.postToActivePanel` とパネル追跡の静的 Set・`editor.js` の `exportPdf` と `print` メッセージ分岐・`state.exportPdfBtn`。**残しているもの**: (1) `@media print` のスタイル一式（出力時の見た目の作り込み。代替方式でも使う）、(2) `utils.waitForRenderComplete`（**本番の呼び出し元は無い**。代替方式で「描画完了を待ってからHTMLを抜き出す」のに使うためテスト付きで保守している）、(3) `mermaid.render()` の直列化（印刷とは独立した correctness 修正で、`render()` の戻り値が「進行中の描画も含めて完了した」ことを表すようにするもの）。**代替方式**は「HTMLへ書き出して既定のブラウザで開き、ブラウザ側の印刷から PDF 保存する」（ROADMAP「PDF出力の代替方式」）。外部ブラウザなら `Ctrl+P` が普通に効く

- **ツールバーの表示/非表示**: 表示状態は **VS Code の設定 `markdownWysiwyg.showToolbar`**（`contributes.configuration`・既定 true）が持つ——セッション限りの `isRawMode` 等と違い「常に隠しておきたい」という使い方を想定しているため永続化する。コマンド `markdown-wysiwyg-editor.toggleToolbar`（`extension.ts`）が設定を反転させる。**書き込み先は読み取りと同じスコープ**——`get()` が返すのは実効値（フォルダ > ワークスペース > ユーザー > 既定）なので、ワークスペースに値がある状態でユーザー設定だけ書き換えると実効値が変わらず**押しても何も起きない**（撤去したPDFボタンと同種の体験になる）。`inspect()` で値が定義されているスコープを調べてそこへ書く。`markdownEditor.ts` が `onDidChangeConfiguration` を拾って `setToolbarVisible` メッセージを Webview へ送る（Webview は設定APIを直接読めないため。パネル生成時にも初期値を1度送る。購読は `onDidDispose` で破棄）。Webview 側は `state.isToolbarVisible` へ入れて `applyToolbarVisibility()` が `<body>` へ `toolbar-hidden` クラスを付け外しし、CSS `body.toolbar-hidden .toolbar { display: none }` で隠す。**`.toolbar` へインラインスタイルを書かない**のは、印刷用スタイル（`@media print` もツールバーを隠す）とカスケードで競合させないため。ツールバーを隠すと検索ウィジェット（`.find-widget { top: 48px }` はツールバー高さを見込んだ固定値）が本文へ食い込むため、`body.toolbar-hidden .find-widget { top: 0 }` で詰める。なおパンくずバーの再計算は**不要**（通常フローの flex 子要素で位置は自動追従し、`updateHeadingBreadcrumb` も相対値だけで判定する）。**ツールバーを隠しても操作は失われない**: 棚卸しの結果、ボタンのうち他に到達手段が無いのは**行折り返しトグル（`#toggleRawWrap`）だけ**だった（見出し・リスト・引用・コードブロックは行頭のMarkdown記法がライブ変換され、太字等・リンク・目次・Raw切替は既存ショートカットがある）。そこで **Alt+Z** を追加した。判定は純粋関数 `commands.isRawWrapShortcut(event)` で、**`key` ではなく `code` を見る**——macOS で Option+Z を押すと `key` は `'Ω'` になり `key === 'z'` では反応しないため（**検索オプションの `Alt+C`/`W`/`R` も同じ原因で macOS では発火していなかった**——`commands.matchSearchOptionShortcut(event, isMac)` として同じ `code` 基準へ統一済み。**キーの組み合わせは VS Code 本体に合わせてプラットフォームで変える**——macOS は `⌥⌘C`/`⌥⌘W`/`⌥⌘R`、その他は `Alt+C`/`W`/`R`。macOS で Option 単独にすると `ç`/`∑`/`®` の入力を奪い、**それらの文字を含む語を検索欄に打てなくなる**（Command を足せば文字入力にならない）。加えて**検索ウィジェットが開いている間だけ**有効にしている）。Ctrl/Cmd/Shift 同時押しと IME変換中（`isComposing`）は対象外。**発火は `state.isRawMode` のときだけ**にゲートしている——(1) 折り返し設定はRaw表示にしか効かないのでWYSIWYG中に押しても何も起きず、ツールバーを隠していればボタンの見た目も変わらないため**内部状態だけが黙って反転**してしまう、(2) `code` 基準にした結果 macOS で `Option+Z`（`Ω`）の入力を奪うので、効果のあるRawモードに限定する。ツールバーを戻す導線はコマンドパレット（と設定UI）のみ（ROADMAP項目）

- **表コピーの貼り付け品質（`text/html` の整形）**: `writeToClipboard` は `text/plain`（TSV）と `text/html` の両方を書くが、`buildHtmlTableFragment` がセルの `innerHTML` をそのまま使っていたため貼り付け先で崩れていた。`sanitizeCellForClipboard(clonedCell)`（`table.js`・**クローンを渡す前提で破壊的に書き換える**）で整える: インライン数式 `.math-inline` は `data-math` から `$式$` テキストへ／脚注参照 `sup.footnote-ref` はラベルだけの `<sup>` へ（リンク先 `#fn-…` は外で無効）／`.raw-markdown`・`.find-highlight` は**編集中だけ意味を持つ**ので中身のテキストだけ残す／画像は `src` が webview URI に解決済みで貼り付け先から見えないため `data:` URI 以外は alt テキストへ（alt が無ければ削除）／文書内アンカー `a[href^="#"]` はリンクを外す／`contenteditable`・`class`・`id`・`data-*` を削除。あわせて `<thead>`+`<th>`（範囲がヘッダー行から始まる場合）と、罫線が出る最低限のインラインスタイルを付ける（貼り付け先はこちらのCSSを持たない）。**テストは `buildHtmlTableFragment` 経由でも確認する**——サニタイズ関数を直接叩くテストだけだと、組み立て側から呼び出しを外しても全テストが通ってしまうため

## 自動開発ループ（`/evolve` × `/loop`）

このリポジトリは Claude Code のスキル `.claude/skills/evolve/SKILL.md` を使い、機能追加・バグ修正を半自動で少しずつ進めています。

- **実行方法**: Claude Code で `/loop 6h /evolve` を起動する（＝**6時間ごとに `/evolve` を1周**実行）。`/loop` はセッションを開いている間だけ動くセッションローカルなループです。1周だけ回したい場合は `/evolve` を直接呼びます。
- **1周の流れ**: **GitHub の open issue を ROADMAP へ取り込む** → ROADMAP から項目を1つ選ぶ → 実装 → テスト → **`/local-review`（サブエージェントによる客観レビュー）** → ドキュメント更新 → `evolve/NNN` ブランチへ commit/push → バックログ更新 → **解決した issue をクローズ**。**1周＝1コミット**（issue 取り込みのみ例外的に別コミット）、**main へは直接コミットしない**（人間がレビューして取り込む）。
- **GitHub issue 連携**: open issue はループが `docs/ROADMAP.md` に取り込みます。重複取り込みはメモ欄先頭の `[#12](issueのURL)` の番号で防ぐため、**ROADMAP / roadmap-done.md を手で編集するときも `#番号` を消さないでください**。`gh` コマンド（GitHub CLI）が未インストール・未認証の環境では、この工程はスキップされます。
- **作業ブランチ**: `evolve/001`, `evolve/002` … の連番（3桁ゼロ埋め）。既存の `evolve/NNN` の最大値 +1 を、**最新の main から**切ります。常に main の直系の子孫になるため `git merge --ff-only` で取り込めます。
- 詳しい運用（成果物の確認・取り込み・巻き戻し、設計意図）は [docs/dev-loop.md](./docs/dev-loop.md) を参照。

## 貢献ガイドライン

### コーディング規約
- TypeScriptの型安全性を重視
- ESLintルールに準拠
- コミット前に`npm run lint`と`npm run check-types`を実行

### プルリクエスト
1. フォークしてブランチを作成
2. 変更を実装
3. テストを追加/更新
4. リントと型チェックをパス
5. PRを作成して説明を記載

### 機能追加時の注意事項
- **ドキュメントの更新**: 新しい機能を追加した場合は、以下のドキュメントを必ず更新してください
  - `README.md`: ユーザー向けの機能説明
  - `AGENT.md`: 開発者向けの技術詳細（このファイル）
  - `sample.md`: 新機能のサンプルコードや使用例
- 特に`sample.md`には実際に動作確認できる例を追加することで、ユーザーと開発者の両方にとって有用なリファレンスになります

## トラブルシューティング

### ビルドエラー
- `npm install`で依存関係を再インストール
- `node_modules`と`dist`を削除して再ビルド

### 拡張機能が動作しない
- VS Codeのバージョンを確認（v1.108.0以上が必要）
- 開発者ツール（Help > Toggle Developer Tools）でエラーを確認
- 拡張機能ホストを再起動

