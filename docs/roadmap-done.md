# ROADMAP — 完了 (done)

[docs/ROADMAP.md](./ROADMAP.md) の完了項目アーカイブ。`/evolve` はサイクル完了時にここへ行を追記する（完了日・コミットハッシュ付き）。追記ルールは ROADMAP.md 側の凡例に従う。

| 完了日 | 機能 | コミット |
|--------|------|----------|
| 2026-07-20 | 機能(1/2・**実機確認前提**): 画像記法 `![alt](url)` の Markdown⇔HTML 対応（`<img>` レンダリング＋往復）。「ローカル画像のインライン表示」M を分割した前半（`media/modules/` のみ・純粋変換）。`markdown.js` の `convertInline`（読込パス）と `commands.js` の `convertInlineText`（ライブ変換パス）に `![alt](url)`→`<img src alt>` を**リンクより先に**追加（`![` を `!`＋リンクに割らない）、`serializeInline` に `<img>`→`![alt](url)`（`data-original-src` 優先＝2/2でsrcをwebview URIへ差し替えても元パスで往復）。**`/local-review` A-1（medium）を同サイクルで修正**: 生成した `<img>` を素のHTMLで挿すと URL/alt 中の `_`/`*`/`~`/`+` が後続の強調変換で `src="a<em>b</em>c"` のように壊れ往復が崩れるため、インラインコード・数式と同じく**プレースホルダ（`U+0003`）へ退避→強調変換後に復元**する保護を両パスへ追加。ユニットテスト（往復・属性エスケープ・`data-original-src`優先・`_`等を含むパスの非破壊・ライブ変換で img 化＆リンクと区別・通常リンク回帰）を追加。型/lint/compile・ユニット518件パス。B-1（タイトル記法 `![](url "title")` 未対応＝画像/リンク共通の既存制約）は ROADMAP へ。C-1（IMGシリアライズが `]`/`)` を含む属性を非防御）・C-2（空srcのimgは無音欠落）は生成名が安全なため報告のみ。**残り(2/2)＝ローカル相対パス画像の実表示（CSP/localResourceRoots/baseUri。現状は壊れた画像アイコン）は ROADMAP に残置**。`media/modules/` のみ＝統合テスト非対象。実機で `data:`/表示可能URL画像の描画と往復を確認 | `c1007b2` |
| 2026-07-20 | 機能(2/2・**実機確認前提**): 画像のクリップボード貼り付け — Webview側の paste 検出→保存要求→`![](path)` 挿入。(1/2, `3f52b0f`)の拡張機能側に続く後半。`editor.js` の paste ハンドラで、**`text/plain` を持たない**クリップボード（＝スクショ等の純粋な画像。Excel等のテキスト併載は従来の表/テキスト貼り付けを優先）に限り `commands.findClipboardImageItem`（`type` が `image/` で始まる最初の item）で検出→`FileReader`→base64→`saveClipboardImage` を postMessage。貼り付け時のキャレット範囲を `pendingImagePasteRange` に控え、応答 `insertImagePath` で範囲を復元（今もエディタ内に生きている場合のみ）→`commands.insertImageMarkdown(path)` で挿入。`buildImageMarkdown` は alt 空・パスの空白/丸括弧をエンコード。画像記法はレンダリング対象外でリテラルテキストとして入り往復不変（リンク正規表現は alt 必須で `![]()` はリンク化されない）。**`/local-review` C-1（medium）を同サイクルで修正**: 初版は `image/*` があればテキストより先に横取りし、Excel等の「テキスト＋画像」併載で表貼り付けが画像保存に化ける回帰があったため、`text/plain` が無いときだけ画像扱いへ変更。C-2（応答が来ない場合の範囲残存）も復元前の生存確認で対処。B-1（`buildImageMarkdown` の Markdownアクティブ文字未エンコード）と**ローカル画像のインライン表示**は ROADMAP へ。ユニットテスト9件・型/lint/compile・ユニット510件パス。`media/`（`editor.js`・`commands.js`）のみ＝統合テスト非対象。**実機で スクショ貼り付け→保存→挿入→保存Markdownの往復を確認**する必要あり | `7669d23` |
| 2026-07-20 | 機能(1/2・**実機確認前提**): 画像のクリップボード貼り付け — 拡張機能側の保存＋相対パス返却。L項目を分割した前半。Webview からの `saveClipboardImage`（`{ base64, mime }`）を `markdownEditor.ts` で受け、編集中ドキュメントと同じフォルダ（`document.uri` 基準）へ一意名（`image-<yyyyMMdd-HHmmss>.<ext>`・衝突時 `-1`,`-2`…）で書き込み、`insertImagePath`（POSIX区切りの相対パス）を postMessage で返す。保存は既存 `saveMermaidPng` と同方式（`atob`→`Uint8Array`→`workspace.fs.writeFile`）だがダイアログ無し。未保存（untitled/非file）は警告して中断・空base64は無視・`readDirectory` 失敗は衝突チェック無しで続行。純粋ロジックを vscode 非依存の新規 `src/imagePaste.ts`（`mimeToExtension`/`buildImageFilename`/`disambiguateFilename`/`toMarkdownRelativePath`）へ分離しユニットテスト11件、統合テスト3件（実書き込み・別名保存・untitled中断）を追加。型/lint/compile・ユニット502・統合12件パス。`/local-review`: A なし／B（medium・Webview結線が未実装でバックエンドは現状到達不能）は**意図した(1/2)分割**〈(2/2) は ROADMAP に残置〉／C-1（衝突統合テストの非決定性）はテスト名を実保証へ修正＋`-1`連番はユニットで決定的検証／C-2・C-3 は報告のみ。**(2/2)=Webview側 paste 検出→保存要求→`![](path)` 挿入・実機での貼り付け〜往復確認は残置**。`src/markdownEditor.ts` 変更＝統合テスト対象 | `3f52b0f` |
| 2026-07-20 | 機能(**実機確認前提**): 表の挿入UI（空行の右クリック→行数・列数ダイアログ）。中核（`buildEmptyTableMarkdown`／`insertTable`、`78b0295`）に対する呼び出しUIの配線。`table.js` に `setupContextMenu(editor)` を追加し `editor.js` init で数式メニューの後に配線。`contextmenu` を監視し**空行・エディタ余白**でのみ自前メニュー「➕ 表を挿入…」を出す（表示可否は純粋関数 `shouldShowInsertMenu`＝`findExcludedAncestor`〈数式/Mermaid/既存テーブル上は対象外〉＋`isBlockEmpty`〈テキスト無し・`<br>`以外の要素を含まない〉）。メニュー→行数・列数ダイアログ（`prompt()` 不可のため `.link-dialog` 系で動的生成・`markdownEditor.ts` 非変更）→OKで `clampTableDimensions`（空/非数値/0以下→1・上限 行50/列20）を通し `insertTable(rows, cols)`。挿入位置は右クリック時の `caretRangeFromPoint` を `pendingInsertRange` に退避し確定直前に選択へ復元。CSSは `.table-context-menu`/`.table-menu-item` を `.mermaid-context-menu`/`.math-context-menu` 系と同ルールへ追加。**`/local-review` A-1（medium）を同サイクルで修正**: 初版は本文全域で `preventDefault` し既定メニュー（貼り付け・スペル修正等）を潰す回帰があったため、ROADMAP 意図どおり空行・余白限定へ絞り本文テキスト上は既定メニューを尊重。C-1（`pendingInsertRange` 挿入後クリア）・C-2（キャンセルボタン上のEnterはキャンセル）も対応。B-1（ダイアログが body 直下で画面右上に出る＝位置基準ずれ）は ROADMAP 低優先へ。ユニットテスト16件・型/lint/compile/ユニット491件パス。**メニュー/ダイアログの見た目・操作感・右クリック位置への挿入は実機（拡張機能開発ホスト）確認前提**。`media/` のみ＝統合テスト非対象 | `018eb37` |
| 2026-07-20 | バグ修正(**実機確認前提**): 透過背景でコピーした数式画像の文字色（自動・黒・白）を選べるように。前サイクルの `/local-review` B-1 由来（バグ修正セクション最上位 todo）。ブロック数式PNGコピーは背景（透過/白/黒）を選べたが文字色は白・透過背景で黒固定（`blockToPngBlob` の `wrapper.style.color`）で、**透過PNGをダークな資料/チャットへ貼ると黒文字が埋もれて見えない**問題。右クリックメニューに**文字色トグル**（`.math-fg-btn` の `data-fg="auto|black|white"`・既定 `auto`）を背景トグル下へ追加。両行は新ヘルパ `buildToggleRow(label, btnClass, dataAttr, options)` で生成しクリックで**自行内 active のみ**移す（背景選択と文字色選択が独立）。文字色は純粋関数2段: `resolveMenuTextColor(menu)`（`black`/`white` 以外は `auto`）でモードを読み、`resolveTextColor(background, textColor)`（`black`/`white` はその16進、`auto` は背景追従＝黒背景のみ白）で決めて `wrapper.style.color` へ与える。`copyBlockAsPng(block, background, textColor)`→`blockToPngBlob(block, undefined, background, textColor)` を配線。**既定（背景=白・文字=auto）は従来と同一出力（無リグレッション）**＝前サイクルの黒背景白文字反転も `auto` の背景追従として維持。CSS は `.math-fg-btn` を既存 `.math-bg-btn`/`.mermaid-bg-btn` と同ルールへ追加。`/local-review` 指摘: A/B なし。C-1（`buildToggleRow` が両行に `math-menu-bg-row`/`-label` の「bg」命名を流用＝Mermaid と共有する既存CSSクラスの意図的再利用のため据え置き）・C-2（`resolveTextColor` の未知値は既に auto 分岐へ落ちる＝誤検知）は対応せず報告のみ。ユニットテスト8件（文字色トグルの既定active・行独立・`resolveMenuTextColor`/`resolveTextColor` 全分岐・透過＋白指定でSVGに rect 無し＆白文字が伝播）。型/lint/compile/ユニット473件パス。**透過＋白文字のダーク環境での見え方・メニュー操作感は実機（拡張機能開発ホスト）確認前提**。`media/` のみ＝統合テスト非対象 | `72f488e` |
| 2026-07-20 | 機能(**実機確認前提**): ブロック数式コピー画像の背景色（透過・白・黒）を選べるUI配線。基盤（`blockToPngBlob(block, scale, background)`）は実装済みで、残っていた呼び出しUIを配線した。ブロック数式の右クリックメニュー（`math.js` の `ensureContextMenu` が動的生成・`markdownEditor.ts` 非変更）はこれまで「📋 画像としてコピー」の1項目のみで常に既定の白で出力していた。メニュー上部に背景トグル（`.math-bg-btn` の `data-bg="transparent|white|black"`・既定 `white` が `active`・クリックで active を移すのみ＝メニューは閉じない）を追加し、「画像としてコピー」実行時に純粋関数 `resolveMenuBackground(menu)`（`.math-bg-btn.active` の `data-bg` を読む・`transparent`/`black` 以外は `white`。Mermaid の同名関数と同契約）で背景を解決して `copyBlockAsPng(block, background)`→`blockToPngBlob(block, undefined, background)` へ渡す。**`/local-review` 指摘 A-1（high）を同サイクルで修正**: 新規追加した「黒」背景は KaTeX が色を明示せず `currentColor` を継承するため採寸ラッパーの `wrapper.style.color` が黒固定＝黒地に黒文字で数式が不可視だった。`wrapper.style.color = (bg==='black')?'#ffffff':'#000000'` で黒背景時のみ文字色を白へ反転（foreignObject 経路・html2canvas フォールバックの両方を1行で修正）。CSS は `.math-menu-bg-row`／`.math-menu-bg-label`／`.math-bg-btn` を既存 `.mermaid-*` と同ルールへ追加。**既定は白＝背景トグル追加前と同一出力（無リグレッション）**。ユニットテスト7件（トグルの既定active・クリックでの active 遷移とメニュー非クローズ・`resolveMenuBackground` 全分岐＋null防御・黒選択でSVG `fill="black"`＋文字色白への反転／白選択で `fill="white"`＋黒文字の伝播）。型/lint/compile/ユニット465件パス。`/local-review` B-1（透過背景＋黒文字はダーク環境貼付で視認性低）は既存・別課題として ROADMAP バグ修正へ、C-1（配線の end-to-end テスト欠如）は追加した伝播テストで担保。**PNGの透過/白/黒の見た目・黒背景での白文字・クリップボード結果・メニュー操作感は実機（拡張機能開発ホスト）確認前提**。`media/` のみ＝統合テスト非対象 | `531757f` |
| 2026-07-20 | バグ修正: ブロック数式コピー画像の崩れ修正 — foreignObject 経路の canvas taint 実機確認と、確認過程で見つかった残存崩れの修正。**実機確認**: headless Chrome（実 Chromium エンジン。VS Code webview と同じ Chromium ベース＝canvas taint はレンダラ本体の挙動でありElectron埋め込みに固有ではないため妥当な代替）で `blockToPngBlob` を直接呼び出し、`∫_{-∞}^{∞} e^{-x²} dx = √π` を実際にPNG化して確認した結果、`foreignObject rasterize failed, falling back to html2canvas` の警告は一度も出ず＝**canvas taint は発生せず foreignObject 経路が毎回成功**（ROADMAPのtodoが警告していた懸念は杞憂と判明）。ただし生成画像を見本と比較すると別の崩れが残っていた: (1) CSP に `connect-src` が無く `loadKatexFontFaceCss` の woff2 fetch が全滅→フォント埋め込み無しで代替フォント描画（∫が細く小さく見えるなど見た目劣化）、(2) `wrapper.outerHTML`（HTML直列化）だとKaTeXが√記号に使うネストした`<svg>`にxmlnsが付かず、`data:image/svg+xml`としてXML解釈されるforeignObject内でSVGと認識されず**√記号が消える**、(3) `.katex-display`の既定`margin:1em 0`で上下余白が過大。対処: CSPへ`connect-src ${webview.cspSource}`追加（[markdownEditor.ts](../src/markdownEditor.ts)）／`outerHTML`を`XMLSerializer`直列化（新関数`serializeNodeToXhtml`）へ差し替え／クローン採寸前に`.katex-display`のmarginを0にリセット。修正後は見本と同等の見た目（√π・正しいKaTeXフォント・適切な余白）をheadless Chromeで再確認。回帰テスト2件追加（ネスト`<svg>`のxmlns保持）。既存456→458件パス・型/lint通過。**headless Chromeでの検証であり文字通りの拡張機能開発ホストではない点は留意**（Chromium統一エンジンのため技術的には同等の代替と判断してtodoをclose）。`src/markdownEditor.ts`変更＝統合テスト対象 | `d6ba754` |
| 2026-07-20 | バグ修正（**実機確認前提**）: Rawモードの行番号が下の行でずれ「一部の行に番号が出ない」問題。原因はガター（`.raw-line-gutter` の div）と textarea の行高が unitless `1.6`（=22.4px の小数）で、textarea のネイティブ行描画と div の行送りが端数の扱いで一致せず行を重ねるほど食い違って蓄積していたこと（番号生成 `utils.countLines`/`buildLineNumberText` の論理自体は正しく `utils.test.ts` で検証済み＝レイアウト側の問題）。`#rawEditor` と `.raw-line-gutter` の `line-height` を整数px（`22px`）に固定して行送りを完全一致させた（`media/editor.css` のみ）。`search.js` の `parseInt(lineHeight)` は 22.4→22／22px→22 で同値のため回帰なし（`/local-review` 確認）。CSSのみ＝ユニットテスト不能・型/lint/compile/既存ユニット453件パス。`/local-review` 指摘: A なし。B（Raw=px/WYSIWYG=unitless の二分）・C（22px は font-size 14px 前提）は害小として対応せず。**行番号と実際の行の一致・スクロール追従は実機（拡張機能開発ホスト）確認前提**。`media/` のみ＝統合テスト非対象 | `f2e684c` |
| 2026-07-20 | 機能(2/2・**実機確認前提**): Mermaid図の画像コピー／保存時に背景色（透過・白・黒）を選べるUI配線。中核(1/2, `9e8ea82`)の背景解決（`resolveImageBackground`＋`svgToPngBlob(svg, scale, background)`）に対する呼び出しUI。Mermaid右クリックメニュー（`#mermaidContextMenu`, `src/markdownEditor.ts`）上部に背景トグル（`.mermaid-bg-btn` の `data-bg="transparent|white|black"`・既定 `white` が `active`）を追加し、「画像をコピー」／「PNG画像として保存」実行時に選択背景を `copyToClipboard`/`saveAsPng` の第2引数へ渡す。配線は `mermaid.js` の `setupContextMenuEvents`（背景ボタンで `active` 移動・メニューは閉じない／アクション時に純粋関数 `resolveMenuBackground(menu)`＝`.mermaid-bg-btn.active` の `data-bg` を読む・`transparent`/`black` 以外は `white` で解決）。**既定は白＝無リグレッション**。背景選択はメニュー持続で直近選択を記憶。CSS（`.mermaid-menu-bg-row`／`.mermaid-bg-btn`）追加。ユニットテスト5件＋統合テスト9件パス。`/local-review` 指摘: A なし。C-1（背景選択が右クリック間で持続＝仕様判断）・C-2（アクション→背景引数の end-to-end テストは内部関数直呼びのため未追加＝個別ユニットで担保）は対応せず報告のみ。**PNGの透過/白/黒の見た目・クリップボード/保存・メニュー操作感は実機（拡張機能開発ホスト）確認前提**（特に html2canvas が透過時に白を焼き込まないか）。`src/markdownEditor.ts` 変更＝統合テスト対象 | `7a1f50e` |
| 2026-07-20 | バグ修正（配線・**実機確認前提**）: ブロック数式コピー画像の崩れ修正の foreignObject 方式への配線。`blockToPngBlob` を html2canvas 直接ラスタライズから `<foreignObject>`（ブラウザネイティブ描画）方式へ差し替え、html2canvas が再現できない `.katex` のCSS配置を正確に画像化する。(a) ラッパー実寸採寸→(b) `loadKatexFontFaceCss`（`KATEX_FONT_MANIFEST` の woff2 を `resolveKatexFontBaseUrl` で導いた `fonts/` から fetch→`arrayBufferToBase64`→`buildKatexFontFaceCss`。全成功時のみキャッシュ・部分失敗はリトライ）→(c) `collectKatexCssText`＋フォントCSSを `buildMathBlockSvgMarkup` でSVG化→(d) `svgMarkupToPngBlob`（`data:image/svg+xml`→Image→canvas→PNG・15秒ロードタイムアウト保険）。CSP へ `font-src … data:` を追加（`src/markdownEditor.ts`）。**foreignObject 失敗時は html2canvas フォールバックで無リグレッション**。ユニットテスト15件＋統合テスト9件パス。`/local-review` 指摘対応: A-1（フォント取得オーケストレーションのテスト欠如）→`loadKatexFontFaceCss` を export し fetch スタブで4件追加／B-1（部分失敗の恒久キャッシュ）→全成功時のみキャッシュへ修正／C-3（クエリ付きhref）→テスト追加。**⚠️ C-1（高・確信中〜高）＝ Chromium が foreignObject 入り SVG を canvas 描画時に taint し `toBlob` が SecurityError を投げる既知挙動により、実機で新経路が毎回フォールバックへ落ち崩れ修正が発現しない恐れ**。B-2（フォールバックの黒/色背景マッピングは現状 white 固定呼び出しのため未到達）・C-2（`font-src data:` は画像コンテキストSVGに親CSPが効かず不要な可能性）・C-4（CSSの`<style>`無エスケープのXML妥当性）は害小として対応せず（フォールバックが担保）。**実機確認**: 数式右クリック「画像としてコピー」で崩れが直るか／`falling back to html2canvas` 警告で taint しているかを確認。taint なら MathJax 切替 todo（ROADMAP）へ。`src/markdownEditor.ts` 変更＝統合テスト対象 | `c1741c6` |
| 2026-07-19 | バグ修正の基盤（配線前段）: ブロック数式コピー画像の崩れ修正（foreignObject 移行）に向けた**KaTeXフォント埋め込みの純粋コア**。foreignObject を `<img>` でラスタライズすると SVG が隔離コンテキストで描画され外部フォントを参照できないため、KaTeX の woff2 を base64 で `@font-face` として SVG 内へ埋め込む必要がある。`math.js` に純粋関数 `buildKatexFontFaceCss(fonts)`（base64 化 woff2 配列→`src:url(data:font/woff2;base64,…)` の `@font-face` 群を組み立て。`family` の引用符/バックスラッシュ除去・`style` は italic 以外→normal・`weight` は3桁数値以外→400・`base64` は base64 アルファベット以外を除去し空はスキップ、と防御的に正規化）と、埋め込む20フォントの静的一覧 `KATEX_FONT_MANIFEST`（family/style/weight＋ファイル名）を追加。**既定挙動は不変**（`blockToPngBlob` は html2canvas のまま＝無リグレッション）。ユニットテスト10件（`@font-face` 組み立ての全分岐＋マニフェストの各 woff2 がディスク実在＋`katex.min.css` の `@font-face` 群と過不足なく一致する網羅テスト）。`/local-review` の指摘（C-1 base64 の非対称なサニタイズ欠如／C-2 CSS網羅性テストの欠如）を同サイクルで反映（網羅テストは追加時に jsdom レルム配列の deepStrictEqual プロトタイプ不一致を検知し `Array.from` で修正）。**残り＝ランタイム配線（`blockToPngBlob` の foreignObject 差し替え・フォント fetch→base64・CSP へ `font-src … data:` 追加）は統合テスト対象サイクル＋実機確認前提**で ROADMAP に残置。`media/modules/` のみ＝統合テスト非対象 | `ecf34a5` |
| 2026-07-19 | バグ修正（根本対策の完結）: 「編集中にキャレットが飛ぶ」＝一連の根本対策の**最後の一手**として、innerHTML 全書き換え経路（`handleUpdateMessage`）で **Mermaid の非同期再描画完了を待ってからキャレットを復元**するようにした。従来は `setTimeout(0)` で復元しており、`mermaidModule.render()`（Promise を返す async）の描画完了前に走るとブロックの高さ・ノード構成が未確定でアンカー（ノード基準／ブロック基準・署名）がずれる余地があった。純粋ヘルパ `utils.restoreCaretAfterRender(renderPromise, restoreFn)` を追加し、Promise の**解決（または失敗）を待って**から復元・Promise でない値は従来どおり `setTimeout(0)` 相当・描画失敗時も DOM 構造は確定しているため復元を試みる（後方互換）。復元はブロック署名・オフセット（DOM構造）ベースでピクセルレイアウト非依存のため Promise 解決後のマイクロタスクで安全。これで応急処置（`cb86e8c`）＋ノード基準（`225ca19`）＋ブロック基準（`132bd22`）＋署名（`8750ec2`）＋競合エコー無視（`e60bee2`/`1aca68d`）に続く一連の対策が完結。ユニットテスト4件（Promise解決後に復元・reject時も復元・非Promiseはマクロタスク復元・不正コールバックで例外を出さない）。`/code-review` は当ループから起動不可（ユーザー起動専用・`disable-model-invocation`）のため手動レビューで代替。**高速入力・数式/Mermaid編集時のキャレット挙動の最終確認は実機（拡張機能開発ホスト）確認前提**。`media/`（`editor.js`・`modules/utils.js`）のみ＝統合テスト非対象 | `e877965` |
| 2026-07-19 | バグ修正（根本対策の一部）: 「編集中にキャレットが飛ぶ」＝ブロック基準アンカー（`132bd22`）の弱点だった**ブロックの挿入・並べ替えで `blockIndex` がずれる**問題への対処。全書き換えでキャレットのブロックより前に別ブロックが挿入されると `blockIndex` が別ブロックを指し、その中へ誤復元していた。`utils.saveCursorPosition` がブロックの**署名**（先頭テキスト64文字・空白畳み込み・純粋ヘルパ `utils.blockSignatureOf`）`blockSignature` も保持し、`utils.restoreCursorPosition` のブロック基準復元段で署名が**一意一致**するトップレベルブロックを優先。一致無し（キャレットのブロック自体が編集され署名が変化）／複数一致で曖昧なら従来どおり `blockIndex` で引く（後方互換・既存テスト全通過）。ユニットテスト5件（署名ヘルパの空白畳み込み/64字丸め/null・前挿入でindexずれ時の署名一致復元・キャレットブロック編集時のindexフォールバック）。`/code-review` は当ループから起動不可（ユーザー起動専用）のため手動レビューで代替。**残るはMermaid非同期描画完了後の復元のみ（優先度低め・実機確認前提）**で ROADMAP に残置。`media/modules/` のみ＝統合テスト非対象 | `8750ec2` |
| 2026-07-19 | バグ修正（根本対策の一部・2/2）: 「編集中にキャレットが飛ぶ」＝競合する古い `update` エコー無視ガードを**実効化**する拡張機能側の配線。(1/2, `e60bee2`) で入れたガードは `message.seq` が常に undefined で無効だった。`src/markdownEditor.ts` の `resolveCustomTextEditor` に `pendingEchoSeq` を保持し、Webview から受けた `edit` の `seq` を、その編集適用に**起因するエコー `update` にだけ**反映する（`applyEdit` が同期的に `onDidChangeTextDocument` を発火させるため適用前に保持→`updateWebview` で `seq` を載せて一度きり消費・`undefined` へリセット）。外部編集には seq を付けず常に適用。無変更時は `updateTextDocument` が `false`（従来 `true`）を返すよう変更し、発火しないエコーの seq を `edit` ハンドラが後始末（次の外部変更へ持ち越さない）。これでタイプ中に古いエコーが新しいローカル内容を巻き戻す＝キャレット巻き戻りを防ぐ。統合テスト9件パス（`updateTextDocument` の戻り値が「実際に編集を適用したか」を表すことの回帰テスト1件を追加）。`/code-review` は当ループから起動不可（ユーザー起動専用）のため手動レビューで代替。**実機での高速タイプ・数式/Mermaid編集時のキャレット挙動確認は実機確認前提**で残置。`src/markdownEditor.ts` 変更＝統合テスト対象サイクル | `1aca68d` |
| 2026-07-19 | バグ修正（根本対策の一部・1/2）: 「編集中にキャレットが飛ぶ」の残る根本原因＝**競合する古い `update` エコー**（タイプ中に「AB」を送った直後に「ABC」を送ると、遅れて届いた「AB」のエコーが現在の「ABC」を巻き戻しキャレットを乱す。既存の `incoming === lastSentMarkdown` ガードは `lastSentMarkdown` が既に更新済みで古いエコーを弾けない）への対策のWebview側インフラ。編集送信ごとに単調増加する `state.editSeq` を採番し、全送信経路を共通ヘルパ `postEdit(content)` へ集約して `seq` を載せる。純粋関数 `utils.shouldIgnoreStaleUpdate(messageSeq, currentEditSeq)`（エコーの seq が最新送信 seq より小さければ無視・未指定/同値は適用）を `handleUpdateMessage` 先頭ガードに配線。**拡張機能側が seq をエコーへ反映する(2/2)配線までは `message.seq` が常に undefined＝ガード無効で完全に従来動作**（無リグレッション）。ユニットテスト5件（seq判定の全分岐＋editSeq初期値/読み書き）。`/code-review` は当ループから起動不可（ユーザー起動専用）のため手動レビューで代替。**(2/2)＝拡張機能側のseqエコー配線（`src/markdownEditor.ts`・統合テスト対象）＋実機での高速タイプ時キャレット確認は実機確認前提**で ROADMAP に残置。`media/` のみ（`src/` 非変更）＝統合テスト非対象 | `e60bee2` |
| 2026-07-19 | バグ修正（根本対策の一部）: 「編集中にキャレットが先頭へ飛ぶ／ずれる」の根本原因＝グローバル文字数オフセット方式に対し、**ブロック基準アンカー**を導入（innerHTML全書き換え経路向け）。先行のノード基準（`225ca19`・インプレース経路）は全書き換えで保存ノードが破棄され使えないため、`utils.saveCursorPosition` が新たに `blockIndex`（キャレットを含むトップレベルブロックのインデックス）/`blockOffset`（ブロック内ローカルオフセット）を保持し、`utils.restoreCursorPosition` に第二候補の復元段を追加。ノード基準が使えない全書き換え時に、同インデックスのブロックが残っていてブロック内でローカルオフセットに到達できればそこへ復元する。これでキャレットより前の別ブロック（数式・Mermaid・テーブル）が再描画で文字量を変えても、グローバルオフセットのように別ブロックへドリフトしない。ブロックが無い／届かない（構造変化）場合は従来のグローバルオフセット走査＋最寄りフォールバックへ委譲（後方互換・既存テスト全通過）。オフセット走査は `scanOffset(root, targetOffset)` ヘルパへ切り出しブロック内／全体で共用。ユニットテスト3件（アンカー保持・前ブロック文字量変化での正確復元・blockIndex範囲外フォールバック）。`/code-review` は当ループから起動不可（ユーザー起動専用）のため手動レビューで代替。**残り（Mermaid非同期描画完了後の復元・競合update無視／バージョン付与・ブロックID安定化）は ROADMAP に残置＝実機確認前提**。`media/modules/` のみ＝統合テスト非対象 | `132bd22` |
| 2026-07-19 | 機能の基盤(1/2): Mermaid図の画像コピー／保存時の背景色選択（透過・白・黒）の中核。`mermaid.js` に純粋関数 `resolveImageBackground(background)` を追加（`'transparent'`→alpha有効・塗りつぶし無し・html2canvas背景 null／`'white'`/`'black'`→不透明＋該当色／未指定・未知→白フォールバック）し、`svgToPngBlob(svg, scale, background='white')` がこれを使って `getContext('2d',{alpha})`・`fillRect` 有無・html2canvas の `backgroundColor` を切り替えるように。`copyToClipboard`/`saveAsPng` も背景引数を受けて渡す（**既定 'white' で従来動作のまま＝無変更**）。テスト用に helper の `MODULE_FILES` へ `mermaid.js` を追加（`mermaid.min.js` 依存は関数内のみのため純粋関数は jsdom で検証可）。ユニットテスト5件。呼び出しUI（右クリックメニューでの背景選択）は(2/2)＝実機確認前提で ROADMAP に残置。`media/modules/` のみ＝統合テスト非対象 | `9e8ea82` |
| 2026-07-19 | バグ修正（根本対策の一部）: 「編集中にキャレットがずれる／先頭へ飛ぶ」の根本原因＝文字数オフセット方式に対し、**ノード基準アンカー**を導入。`utils.saveCursorPosition` が `range.endContainer`/`endOffset` も保持し、`utils.restoreCursorPosition` は保存ノードが**テキストノードとしてエディタ内に生きていれば**（＝インプレースDOM変更）文字数オフセット走査を経ずに直接そのノードへ復元する。これで入力イベント経路（`applyInlineFormatting` 後の復元）では、埋め込みウィジェット（数式・Mermaid・テーブル）の内部テキスト量が変わってもキャレットがずれない。ノードが消えている場合（innerHTML 全書き換え）は従来のオフセット方式＋最寄りフォールバックへ委譲（後方互換）。コードレビュー（`/code-review high`）指摘で、要素コンテナは childNodes インデックスが古くなり得るためノード基準の対象をテキストノードに限定。ユニットテスト4件。**残り（innerHTML全書き換え経路のブロックID基準アンカー・非同期描画後復元・競合update無視）は ROADMAP に残置＝実機確認前提**。`media/modules/` のみ＝統合テスト非対象 | `225ca19` |
| 2026-07-19 | バグ修正: マウスドラッグでの本文選択が装飾記号（`**` 等）に到達すると解除される問題。**根本原因**: `selectionchange` ごとに走る `commands.syncRawMarkdownToCaret` が、選択の開始位置（`startContainer`）の所属だけで判定して装飾（`**太字**` 等）を生Markdownへ**展開／折り畳み**＝DOM書き換えしていたため、ドラッグ中（範囲選択中）に装飾へ差しかかると選択が破棄されていた。**修正**: 生Markdown表示は「キャレット位置」を示す機能であり範囲選択中は不要なので、選択が非collapsed（範囲）の間は `syncRawMarkdownToCaret` を早期returnして一切DOMを触らないようにした。既に展開中のspanも範囲選択中は折り畳まれず、展開テキストのドラッグ編集は維持される。ユニットテスト3件（装飾をまたぐ範囲選択で非展開・選択保持／開始が装飾内でも非展開／展開中spanの非折り畳み）。`media/modules/` のみ＝統合テスト非対象 | `042b500` |
| 2026-07-19 | バグ修正の基盤(1/2): ブロック数式コピー画像の崩れ修正に向けた foreignObject SVG マークアップ組み立ての純粋関数 `math.buildMathBlockSvgMarkup(innerHtml, cssText, width, height, background)` を追加。**調査で根本原因を特定**（KaTeX を `output:'html'` で描画→画像化の html2canvas が `.katex` のCSS配置＝`vertical-align`/負マージン/絶対配置/√overline を再現できず崩れる）し、対策方式を「html2canvas をやめ foreignObject＝ブラウザネイティブ描画でラスタライズ」へ確定。本サイクルはその前段の文字列組み立て（SVG/xhtml名前空間・`<style>`インライン・寸法正規化・背景色 `transparent`可・属性エスケープ）を実装しユニットテスト6件で検証（実機不要）。実際の `blockToPngBlob` 差し替え・KaTeXフォントの base64 埋め込み・CSP `font-src data:` 追加は(2/2)＝実機確認前提で ROADMAP に残置。`media/modules/` のみ＝統合テスト非対象 | `36e6631` |
| 2026-07-19 | バグ修正（応急処置）: 「編集中に突然キャレットが先頭へ飛ぶ」問題の対症療法。`utils.restoreCursorPosition` が復元先の文字数オフセットに届かない（保存時より本文が短くなった）とき、従来は range を一切設定せず、直前の `innerHTML` 全書き換えで選択が失われているとブラウザがキャレットをエディタ先頭へ描画していた。復元失敗時、①エディタ内に妥当な選択が残っていればそれを尊重、②選択が失われている（rangeCount=0 または `(editor,0)` へ collapse＝先頭飛びそのもの）ときのみ、到達できた最寄り位置（最後のテキストノード末尾、本文が無ければエディタ末尾）へフォールバックするよう変更。コードレビュー（`/code-review high`）指摘で `(editor,0)` collapse を妥当な選択と誤判定するギャップを発見し同サイクルで修正。ユニットテスト4件。**根本原因（オフセット方式）は未解決のため ROADMAP に根本対策として残置。** `media/modules/` のみ＝統合テスト非対象 | `cb86e8c` |
| 2026-07-19 | バグ修正: 見出し行でEnterを押すと改行直後の新しい行にも見出しテキストが重複表示される問題。**根本原因**: `utils.findBlockAncestor` が見出し（h1〜h6）をブロックとして扱わず、`commands.handleHeadingConfirm` が「レンダリング済み見出し内でのEnter」を捕まえられずブラウザ既定のEnter処理（見出しの複製・分割）に委ねていた。`## ああ` 入力後 150ms のsyncで段落がh2へ再レンダリングされてから押されたEnterがこの経路に落ちていた。**修正**: `handleHeadingConfirm` に見出し祖先（`findAncestor` で h1〜h6）検出を追加し、`confirmRenderedHeading` でキャレット以降を新しい段落へ切り出して見出し直下に挿入・キャレットを移動し、`preventDefault` でブラウザ既定を抑止（末尾なら空段落・途中なら後半のみ移動でテキストは失われない）。ユニットテスト5件を追加。`media/modules/` のみ＝統合テスト非対象 | `bec3a24` |
| 2026-07-19 | バグ修正: 全選択への上書き貼り付け（Ctrl+A→Ctrl+V）で先頭に空見出しの殻 `# ` が残る問題。`commands.js` の `handleMarkdownPaste` の殻ブロック掃除が生 `textContent` で空判定していたため、`.heading-hash` スパン（`# `）を含む空見出しが除去されず残っていた。空判定を同関数内の `shellIsEmpty`（heading-hash を本文から除外）へ置換して解消。前サイクルで発見し `/evolve` のテストゲートを落としていた既存失敗テストが解消（回帰テスト1件追加）。`media/modules/` のみ＝統合テスト非対象 | `ec815e5` |
| 2026-07-19 | バグ修正: ブロック数式（`$$…$$`）を新規入力しても自動レンダリングされず生ファイルが `\$\$` に破損する問題。`commands.js` に `convertMathBlocks` を追加し `applyInlineFormatting`（入力イベント）から `walkInline` 前に実行。エディタ直下の平文ブロック（class無しP/DIV）の単独 `$$` 行を開き・次の単独 `$$` 行を閉じとして、間の平文ブロックの生テキスト（`$` 非エスケープ）を式本文に集め `markdown.buildMathBlockHtml`〈読込時と同じ〉で math-block へ畳む。閉じ `$$` 無しは非変換、キャレットが範囲内なら直後に空段落を挿入して移す（`caretHandled`）。ユニットテスト7件。`media/modules/` のみ＝統合テスト非対象 | `bebed92` |
| 2026-07-18 | 表挿入の中核: `table.buildEmptyTableMarkdown(rows, cols)`（空テーブルMarkdown生成・純粋関数）＋ `table.insertTable(rows, cols)`（キャレット位置の直後＝無ければ末尾へ挿入し `render()` で即インタラクティブ化。挿入方式は `commands.insertToc` と同じ）。ユニットテスト6件。呼び出しUI（右クリックメニュー＋行数・列数ダイアログ）はROADMAPに分割（実機確認前提） | `78b0295` |
| 2026-07-18 | 行番号表示 (3/3 描画): WYSIWYGモードの行番号ガター描画とスクロール同期。`#editor` を flex ラッパーで包み、`computeEditorLineMap` の各ブロック上端（`getBoundingClientRect`）へ開始行番号を絶対配置、`transform` で縦スクロール追従。input（デバウンス）/文書更新/リサイズで再配置。`markdownEditor.ts` 非変更。**これで行番号表示（元L項目の3分割）が一通り完了**。ピクセル整列は実機確認前提 | `5c82d73` |
| 2026-07-18 | 行番号表示 (3/3 橋渡し): ライブ`#editor`から「表示ブロック→開始行」を対応づける `markdown.computeEditorLineMap`。clone整形を `getCleanEditorClone` へ切り出して `getCleanHtmlFromEditor` と共有、Mermaidの隠しpreを除外し可視コンテナ・テーブルの `.table-container` へ対応。jsdomでユニットテスト（元L項目の分割(3/3)前段。残りはガター描画＝実機確認前提） | `092f2a3` |
| 2026-07-18 | 行番号表示 (2/3): WYSIWYG側「ブロック→ソース開始行」対応付け。純粋関数 `markdown.computeBlockStartLines(finalMarkdown, blockMarkdowns)`＋補助 `coreLinesOf` を追加。各ブロックの本文先頭行を確定ソース内から前方一致で探し1始まりの開始行を返す（空ブロックは `null`・空行畳み込みに非依存）。DOM非依存でユニットテスト（元L項目の分割(2/3)・UIは(3/3)） | `5b72280` |
| 2026-07-18 | 行番号表示 (1/3): Rawモード（textarea）の行番号ガター。左端にソース行番号を表示、行を揃えるため textarea を `white-space: pre`（折り返しオフ）へ変更。行数/番号列は純粋関数 `utils.countLines`/`buildLineNumberText`（ユニットテスト）、ガターは `transform` で縦スクロール追従。UIは `#rawEditor` をラッパーで包む動的生成で `markdownEditor.ts` 非変更。ユーザー要望（元L項目の分割(1/3)） | `fcf630e` |
| 2026-07-18 | ブロック数式を右クリックでPNG画像としてクリップボードにコピー。`math.js` に自前コンテキストメニュー（動的生成・`markdownEditor.ts` 非変更）＋`html2canvas` での `math-block`→PNG化（`document.fonts.ready` 待機）を追加。CSSは `.mermaid-context-menu` 系と共有。ユーザー要望 | `44b4031` |
| 2026-07-18 | 数式のライブ変換（入力・ペースト時点で即時レンダリング）。`commands.convertInlineText` に `$...$` の分岐を追加し、`markdown.convertInline` と同じ退避順序で `math-inline` コンテナへ変換→入力イベント末尾の `MathModule.render` で描画。閉じ `$` を打つまで非変換で入力途中に壊れない。`data-math` は読込パスと同一（突き合わせテスト有り）。ユーザー要望 | `45d5bc2` |
| 2026-07-18 | 数式の内側にカーソルがある間、生Markdownを表示する（数式コンテナは `contenteditable="false"` でキャレットが入れないため、クリックで `$...$` / `$$...$$` の生Markdownへ展開＝`commands.handleMathClick`／`expandMathToRaw`。復帰・再変換抑止は既存の `syncRawMarkdownToCaret`／`raw-markdown` クラスを共有し、`collapseRawMarkdown` にブロック数式（`raw-math-block`→`buildMathBlockHtml`＋`MathModule.render`）分岐を追加。直列化は `markdown.rawMarkdownText` で `$` を非エスケープ・`<br>`→改行。ユーザー要望） | `f3426e1` |
| 2026-07-18 | 強調記法（太字・斜体・取り消し線・下線）とリンクの生Markdown表示（`syncRawMarkdownToCaret` を汎用化・`outermostInlineDecoration`・展開は `serializeInline`／復帰は `convertInline` に委譲・入れ子は最外要素ごと展開） | `7c82d3b` |
| 2026-07-17 | 数式（インライン `$...$` / ブロック `$$...$$`）の表示・双方向変換（KaTeXを `media/katex/` へ同梱・生の式は `data-math` に保持・描画は `math.js`・`\$` エスケープ対応。ユーザー要望） | `fd2a368` |
| 2026-07-17 | エディタ右上の「Markdown: WYSIWYGエディタで開く」ボタンの削除（`menus.editor/title` のエントリを削除。コマンド定義とコマンドパレットは維持。ユーザー要望） | `4fb531b` |
| 2026-07-17 | ダークモードで太字がまだ視認しにくい問題の改善（ウェイトに加え `font-size: 1.05em` へ拡大・全テーマ共通。`line-height: calc(1.6 / 1.05)` で行高を据え置き。ユーザー再報告） | `0306fcf` |
| 2026-07-17 | リンクの挿入・編集ダイアログ（`Ctrl+K`・選択テキストのリンク化／既存リンクの編集・解除。自前ダイアログ＝Webviewでは `prompt()` が使えないため） | `c7b61b4` |
| 2026-07-17 | リンクのクリック挙動を Ctrl/Cmd+クリックでの遷移へ変更（通常クリックはキャレット設置のみ。`handleLinkClick`＋拡張機能側 `openLink`／`vscode.env.openExternal`。ユーザー要望・実機確認済み） | `528cbfb` |
| 2026-07-17 | リンク上にカーソルがある間、生Markdownを薄く表示（`syncRawMarkdownToCaret`・`selectionchange`監視・`span.raw-markdown`へ展開／復帰。実機確認済み） | `407a321` |
| 2026-07-17 | アラートbox本文内のEnter操作（末尾でboxを抜けて段落へ／途中とShift+Enterは`<br>`改行。`handleAlertEnter`・共通ヘルパ`insertLineBreak`） | `318194a` |
| 2026-07-16 | 検索ウィジェットが Cmd+F で開かない問題の修正（macOSの `metaKey` 対応・`Ctrl/Cmd+F/G//`。ユーザー報告バグ） | `123edab` |
| 2026-07-16 | GitHubアラートのライブ変換（手入力・ペーストで即時反映。`convertAlerts`・ユーザー報告バグの修正） | `a8d69d4` |
| 2026-07-16 | 検索ウィジェットに置換機能（`replaceCurrent`/`replaceAll`・WYSIWYG/RAW両対応・リテラル置換） | `d304706` |
| 2026-07-16 | 目次(TOC)生成をツールバーの 📑 ボタンからも実行できるように（`data-command="toc"`） | `297c41a` |
| 2026-07-16 | GitHubアラート記法（`> [!NOTE]`/`[!TIP]`/`[!IMPORTANT]`/`[!WARNING]`/`[!CAUTION]`）の表示・双方向変換対応 | `31dcbe8` |
| 2026-07-16 | 冗長な個別ハイライトバンドル（python/bash/c/sql）の読み込み・ファイル削除（commonビルドに既存・PowerShellのみ残す） | `38d686e` |
| 2026-07-16 | 下端スクロールでツールバーが画面外に消える問題の修正（`#editor`/`#rawEditor` の `min-height` を `100%`→`0`） | `c7b2f9f` |
| 2026-07-16 | ダークモードで太字が視認しにくい問題の改善（`#editor strong` を 700、ダーク系は 800 へ・テーマ別ルール） | `fd898ed` |
| 2026-07-16 | インラインコード内の記法（`**`/`~~`/`++`/`[]()`）が装飾に化けて往復欠損するバグの修正（`convertInline`/`convertInlineText` でコードを退避・復元） | `482e72d` |
| 2026-07-16 | TOCの目次リンク（`[見出し](#slug)`）クリックで該当見出しへ遷移（見出しへの`id`付与＋`commands.scrollToAnchor`） | `e8b89aa` |
| 2026-07-15 | タスクリスト（`- [ ]` / `- [x]`）のチェックボックス表示とクリックでのトグル | `cf42195` |
| 2026-07-15 | ネスト引用（`> >` / `>>`）の双方向変換対応 | `9e1ab6b` |
| 2026-07-15 | 水平線（`---` / `***` / `___`）の双方向変換とEnterでの自動変換 | `904bba7` |
| 2026-07-15 | 書式ツールバー（太字・斜体・下線・見出し・リスト・引用・リンク・コードブロック）の設置 | `7f78fdc` |
| 2026-07-15 | 取り消し線（`~~text~~`、Ctrl+Shift+X、ツールバーボタン） | `4cfbb21` |
| 2026-07-15 | タスクリストのライブ入力対応（`- [ ]` / `- []` / `-[]` を入力した時点でチェックボックス化） | `9246e08` |
| 2026-07-15 | 太字・取り消し線のライブ変換が分割テキストノードでマッチしない問題の修正（走査前に `normalize()`） | `67080b7` |
| 2026-07-15 | 引用内での `> ` 入力によるネスト引用の作成（`handleNestedQuote`） | `19ff5cc` |
| 2026-07-15 | 引用ブロックのEnter（引用を抜ける）/Shift+Enter（引用内改行）対応（`handleBlockquoteEnter`） | `cc7a1d6` |
| 2026-07-15 | JS/TS/JSON/YAML/Go/Rust のシンタックスハイライト対応の明文化（commonビルドに既存・回帰テスト追加） | `9db72a8` |
| 2026-07-15 | 単語数・文字数のステータス表示（右下バー・`utils.countText`） | `4b86e1c` |
| 2026-07-15 | 見出しから目次(TOC)を生成・挿入するコマンド（`Ctrl+Shift+O`・`slugify`/`buildTocMarkdown`/`insertToc`） | `83433dc` |
