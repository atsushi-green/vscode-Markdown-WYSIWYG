# ROADMAP — 完了 (done)

[docs/ROADMAP.md](./ROADMAP.md) の完了項目アーカイブ。`/evolve` はサイクル完了時にここへ行を追記する（完了日・コミットハッシュ付き）。追記ルールは ROADMAP.md 側の凡例に従う。

| 完了日 | 機能 | コミット |
|--------|------|----------|
| 2026-07-31 | バグ修正: 検索オプションのショートカット `Alt+C`/`Alt+W`/`Alt+R` が macOS で効かない問題を修正（ROADMAPバグ修正セクション。前サイクルの `/local-review` B-1 由来）。`editor.js` の判定が `e.key === 'c'|'w'|'r'` だったため、macOS の Option+C/W/R では `key` が `ç`/`∑`/`®` になり**まったく発火していなかった**。それでいて README と `docs/commands-and-shortcuts.md` は `Opt+C` 等として公開しており「効くと書いてあるのに効かない」状態だった。**実装**: 純粋関数 `commands.matchSearchOptionShortcut(event, isMac)` を新設し `event.code` 基準へ統一（前サイクルの `isRawWrapShortcut` と同じ方針）。検索ウィジェットが開いている間だけ有効にし、重複していた開閉判定は `isFindWidgetOpen()` に集約した。**`/local-review`指摘（medium 1件・low 2件、すべて同サイクルで対応）**: **A-1（medium）— 当初の実装は `Option+C` 単独で発火させていたため、`code` 基準にした結果****検索欄に `ç`/`∑`/`®` を打てなくなる＝それらの文字を含む語を検索・置換できないという機能的な回帰**を持ち込んでいた。「検索ウィジェットが開いている間だけ」というゲートも想定より広く、ウィジェットを出したまま本文を編集する通常の使い方では本文でも奪われ続ける。レビューが挙げた選択肢のうち **(a) VS Code 本体と同じキー割り当てに揃える**を採用し、**macOS は `⌥⌘C`/`⌥⌘W`/`⌥⌘R`（Option+Command）、Windows/Linux は `Alt+C`/`W`/`R`** とした（Command を足せば文字入力にならないので何も奪わず、検索欄にフォーカスがあるときも効く。入力欄にフォーカスがあるときは奪わない案(b)は、`Ctrl+F` 直後はフォーカスが検索欄にあるため実用性が下がる）。A-2（low）— `docs/features.md` の検索セクションだけ制約の追記が漏れていた→更新。A-3（low）— ROADMAPの該当項目が `todo` のままで、「判定を純粋関数へ揃える」項目も残件（`Ctrl+/`・`Ctrl+F`・`F3`/`Ctrl+G`）へ絞る必要があった→両方更新。**B（ROADMAPへ）**: 検索オプション名→ボタンの対応が3箇所（`editor.js` の `SEARCH_OPTION_BUTTONS`・`search.js` のクリックリスナー・`commands.js` の switch）に重複しており、**`editor.js` 側はどのテストにも守られていない**（ハーネスが `media/modules/` しか読まないため）。`toggleOption` が第2引数省略時に自分でボタンを引くようにすれば対応表がテスト可能領域へ移る。**C（対応不要と判断）**: Windows/Linux で「検索を開く前にオプションを事前設定できなくなる」挙動変更は、切り替えてもUIが見えず内部状態が黙って変わるだけなので意図的な変更として受け入れ、ドキュメントにも明記した。ユニットテスト9件追加（816→824）。`media/modules/` と `media/editor.js` のみ＝`src/` 非変更のため統合テスト非対象。型/lint/compile・ユニット824件パス。**実機確認してほしい点**: (a) macOS で検索を開き `⌥⌘C`/`⌥⌘W`/`⌥⌘R` がオプションを切り替えるか、(b) **検索欄に `ç` や `∑` を入力できるか**（A-1の修正点）、(c) Windows/Linux で `Alt+C` 等が従来どおり効くか、(d) Windows で Alt 単独押下がメニューバーを起こさないか | `d8b7e6b` |
| 2026-07-31 | 機能（**実機確認前提**）: ツールバーを隠しても操作に困らないよう導線を揃えた（優先度:中・前サイクルのツールバー非表示とセットの項目）。**まず棚卸しをした結果、ROADMAPの想定と違っていた**——項目には「H1〜H3・箇条書き/番号付き/引用・リンク挿入・コードブロックはツールバーにしか到達手段が無い」と書かれていたが、実際には**行頭のMarkdown記法を入力すればライブ変換される**ため到達可能で、リンク挿入は `Ctrl+K`・目次は `Ctrl+Shift+O`・Raw切替は `Ctrl+/`・太字等は `Ctrl+B` 等がある。**本当に手段が無いのは行折り返しトグル（`#toggleRawWrap`）だけ**だった。そこで **Alt+Z**（Mac: Option+Z）を追加し、判定を純粋関数 `commands.isRawWrapShortcut(event)` として`media/modules/` へ置いてユニットテスト可能にした。**`key` ではなく `code` で判定する**——macOS で Option+Z を押すと `key` は `'Ω'` になり `key === 'z'` では反応しないため。**`/local-review`指摘（medium 2件・low〜medium 1件・low 2件、すべて同サイクルで対応）**: **A-2（medium）— `code` 基準にした結果、macOS で `Ω` がどこにも入力できなくなる回帰を持ち込んでいた**。`document` レベルの keydown で無条件に `preventDefault()` するため、WYSIWYG本文・Rawテキストエリア・検索/置換入力・リンクダイアログ・Mermaidソース編集のすべてで `Option+Z` が奪われる（既存の `Alt+C/W/R` は `key` 比較のため macOS では発火せず、この副作用が無かった＝本変更が新規に持ち込んだもの）。**A-4（low）— WYSIWYGモードでは押しても何も起きないうえ、ツールバーを隠していればボタンの見た目も変わらず内部状態だけが黙って反転する**（後でRawに入ると折り返し設定が意図せず変わっている）。→**発火を `state.isRawMode` のときだけにゲートすることで両方を同時に解消**した（折り返し設定はRaw表示にしか効かないので、効果のある場面に限定するのが素直で、WYSIWYG本文では従来どおり `Ω` を入力できる）。A-3（low〜medium）— IME変換中（`isComposing`）のガードが無く、日本語入力の変換操作を `preventDefault` が妨げうる（このファイルの他のキーハンドラは軒並みガードしている）→追加。A-1（medium）/C-1— ドキュメントとROADMAPの記述が新事実と矛盾（features.md が「行折り返しは実行できなくなる」のまま、README と commands-and-shortcuts.md のショートカット表に `Alt+Z` が無い、ROADMAPは「到達手段が無い」と列挙したまま）→すべて更新。A-5（low）— `code` が無いイベントと `isComposing` のテストが未カバー→追加。**B（ROADMAPへ）**: (1) **検索オプションの `Alt+C`/`Alt+W`/`Alt+R` が macOS で効いていない**（`key` 比較のため。それでいてドキュメントは `Opt+C` 等として公開している＝"効くと書いてあるのに効かない"）→バグ修正セクションへ。(2) ツールバーを隠したときに戻す導線が Webview 内に無い。(3) グローバルショートカットの判定が `Alt+Z` だけ純粋関数で二重基準になっている。**C（対応不要と判断）**: VS Code本体の Alt+Z（折り返し切替）との二重発火は、Webviewがキーを消費する限り起きない想定だが確信度が低いため実機確認項目とした。関数の配置位置（HTML変換系ヘルパーの間にある）は純粋な整理なので据え置き。ユニットテスト8件追加（808→816）。`media/modules/` と `media/editor.js` のみ＝`src/` 非変更のため統合テスト非対象。型/lint/compile・ユニット816件パス。**実機確認してほしい点**: (a) Rawモードで `Alt+Z`（Mac: Option+Z）を押すと折り返しが切り替わるか、(b) **WYSIWYG本文では `Option+Z` で `Ω` が入力できるか**（A-2の修正点）、(c) 日本語入力の変換中に `Alt+Z` を押しても変換が妨げられないか、(d) **押したときに背後のVS Codeエディタの折り返し設定まで変わっていないか**（二重発火の確認） | `16cee76` |
| 2026-07-31 | 機能（**実機確認前提**）: ツールバーを非表示にできるようにした（**利用者からの要望**・優先度:中の先頭項目）。表示状態は VS Code の設定 `markdownWysiwyg.showToolbar`（`contributes.configuration` を新設・既定 true）が持ち、**セッションを跨いで保たれる**（「常に隠しておきたい」という使い方を想定しているため、`isRawMode` のような非永続フラグにはしなかった）。コマンド `markdown-wysiwyg-editor.toggleToolbar` が設定を反転させ、`markdownEditor.ts` が `onDidChangeConfiguration` を拾って `setToolbarVisible` メッセージを Webview へ送る（Webview は設定APIを直接読めないため。パネル生成時にも初期値を1度送り、購読は `onDidDispose` で破棄）。Webview 側は `state.isToolbarVisible` に入れて `<body>` へ `toolbar-hidden` クラスを付け外しし、CSS `body.toolbar-hidden .toolbar { display: none }` で隠す。**`.toolbar` へインラインスタイルを書かない**方針にしたのは、16周目に `showWysiwygLineGutter` が `display:flex` をインラインで書いていたせいで印刷用スタイルが無効化された前例があるため。**`/local-review`指摘（medium 2件・low 4件、A/B/Cとも対応）**: **A-1（medium）— 設定の読み取りと書き込みでスコープが食い違っていた**。`get()` が返すのは実効値（フォルダ > ワークスペース > ユーザー > 既定）なのに書き込み先を常に `Global` にしていたため、`.vscode/settings.json` に値がある利用者は**コマンドを押しても何も起きない**（しかも成功したように通知が出る）＝**直前のサイクルで撤去した「押しても何も起きないPDFボタン」と同種の体験**になっていた→`inspect()` で値が定義されているスコープを調べ、そこへ書き戻すよう修正。A-2（low）— 設定キーが `package.json` とコード定数で二重管理なのに、片方をリネーム/タイプミスしてもTypeScriptもテストも検出できず「設定UIには出るが効かない」状態になる→`packageJSON.contributes.configuration` と `MarkdownEditorProvider.showToolbarSetting`・既定値を突き合わせる統合テストを追加（キーをリネームして実際に赤くなることを確認済み）。**C-5（low）— ツールバーを隠すと検索ウィジェットが本文へ食い込む**（`.find-widget { top: 48px }` はツールバー高さを見込んだ固定値）→`body.toolbar-hidden .find-widget { top: 0 }` を追加。本機能が可能にした表示崩れなので同サイクルで対応した。C-1（low）— `applyToolbarVisibility` の `updateHeadingBreadcrumb()` 呼び出しとその根拠コメントが誤りだった（パンくずバーは通常フローの flex 子要素で位置は自動追従し、計算も相対値だけ）→呼び出しを削除し理由を書き直した。C-2（low）— CSSコメントの「@media print と競合しない」という説明が不正確（印刷側は `!important` なのでインラインでも隠れる）→body クラス方式の本当の理由（過去の回帰を踏まえた方針）へ書き直し。C-3（low）— 印刷回帰テストがファイル末尾までの部分文字列一致で、`.toolbar-*` のような別セレクタでも通ってしまう→`@media print` ブロックを切り出してセレクタ境界で見る形へ。B-3（low）— トグルのたびに情報通知が出るのは煩わしい→通知を削除（表示が即座に変わるので不要）。**B（ROADMAPへ）**: (1) ツールバーを隠すと到達手段が無くなる操作の具体リスト（`#toggleRawWrap`・H1〜H3・箇条書き/番号付き/引用・リンク挿入・コードブロック。**戻す導線もコマンドパレットだけ**）を既存項目へ反映して具体化。(2) `toggleToolbar` を commandPalette の `when` 句で絞るか（既存3コマンドは `resourceLangId == markdown` で絞っている）。ユニットテスト3件追加（805→808）・統合テスト2件追加（14→15）。型/lint/compile・ユニット808件・統合15件パス。**実機確認してほしい点**: (a) コマンドでツールバーが消える/戻るか、(b) VS Code を再起動しても状態が保たれるか、(c) ワークスペース設定（`.vscode/settings.json`）に値を書いた状態でもトグルが効くか（A-1の修正点）、(d) 隠した状態で検索（`Ctrl+F`）を開いたとき本文と重ならないか（C-5の修正点） | `591abc9` |
| 2026-07-31 | バグ修正: 押しても何も起きない 🖨️ PDF ボタンとコマンド「PDFとして出力」を撤去（ROADMAPバグ修正セクションの項目。利用者の実機確認により判明）。**経緯**: PDFエクスポート (1/4)〜(4/4) で印刷用スタイル・出力導線・改ページ制御・描画待ち合わせを実装したが、利用者の実機確認で **`Ctrl+P`/`Cmd+P` は VS Code のクイックオープンが優先され、ツールバーの 🖨️ ボタンとコマンドパレット経由でも無反応**であることが判明した。VS Code の Webview は sandbox 付き iframe で動くため `window.print()` がブロックされており、**印刷ダイアログ方式そのものが使えない**。**判断**: ROADMAPには「(a) 一旦外す」「(b) 押したときに案内トーストを出す」の二択を書いていたが、**(a) 外す**を選んだ——「できません」としか言わないボタンは、無反応なボタンよりむしろ邪魔なため。**撤去**: ツールバーボタン `#exportPdf`・コマンド `markdown-wysiwyg-editor.exportPdf`（`package.json` / `extension.ts`）・`MarkdownEditorProvider.postToActivePanel` とパネル追跡の静的Set・`editor.js` の `exportPdf`/`setupExportPdf`/`PRINT_RENDER_TIMEOUT_MS`/`print` メッセージ分岐・`state.exportPdfBtn`・テスト用フィクスチャのボタン。**残したもの**: (1) `@media print` のスタイル一式、(2) `utils.waitForRenderComplete`（**本番の呼び出し元は無くなった**が、代替方式で「描画完了を待ってからHTMLを抜き出す」のに使うためテスト付きで保守。JSDocに呼び出し元が無いことを明記し、ROADMAPの代替方式項目からも「この関数を流用する」と紐付けて『使われていないから消す』事故を防いだ）、(3) `mermaid.render()` の直列化（印刷とは独立した correctness 修正）。**`/local-review`指摘（high 1件・medium 2件・low 2件、すべて同サイクルで対応）**: **A-1（high）— `AGENTS.md` が削除済みの実装を『現在の設計』として記述したままだった**（`#exportPdf`・コマンド・`postToActivePanel`・パネル追跡・`afterprint` 復帰をすべて『ある』前提で説明。末尾に「動作しない」という追記があるだけで**撤去した事実は書かれていなかった**）。AGENTS.md はアーキテクチャの正典なので、放置すると次サイクルが存在しないコードを前提に作業する→全面的に書き換え。A-2（medium）— 撤去で無効になった3つのtodo（印刷導線を`media/modules/`へ移す／二重クリック／画像待ちのスナップショット）が存在しないコードを指したまま残っていた→削除し、**代替方式の項目へ「実装時に注意する点」として畳んで引き継いだ**。**A-3（medium）— 撤去したコマンドが復活しないことを守るテストが無かった**。`package.json` の宣言と `extension.ts` の登録は二重管理で、**宣言だけ残るとコマンドパレットには出るのに押すと `command not found`**＝今回直したのと同種の不具合になる。当初 `getCommands` で「登録されていないこと」だけ確認するテストを書いたが、**実際に宣言だけ戻して試したところ検出できなかった**（`getCommands` は登録済みのものしか返さない）ため、`packageJSON.contributes.commands` を直接突き合わせるテストを追加。両方向とも赤くなることを確認済み。A-4/B-1/B-2/C-2（low）— README の句点欠落、`waitForRenderComplete` のJSDocが `window.print()` 前提のまま、mermaidテストのスイート名が「印刷前の待ち合わせ」のまま、features.md の見出しが「使える機能」に見える点→すべて修正。**B（ROADMAPへ）**: Webview の HTML（`markdownEditor.ts`）とテスト用フィクスチャ（`helper.ts`）の二重管理を検知できるようにする（🖨️ボタンの追加・撤去の両方で毎回2ファイルを手直ししている）。ユニット805件（変更なし）・統合14件パス（12→14。撤去分2件を削除し、宣言/登録の突き合わせとコマンド復活検知の2件を追加）。型/lint/compile も成功。**利用者向けドキュメントも訂正**: README・features.md が「🖨️ボタンで印刷できます」と動作しない機能を案内していたため、撤去した事実と代替方式の予定を明記した | `b44a889` |
| 2026-07-30 | 性能改善: 見出しパンくずバーの更新をデバウンス化（ROADMAP優先度:低の項目）。`updateHeadingBreadcrumb` は見出しの数だけ `getBoundingClientRect` を呼ぶため、入力イベントから同期・無条件に実行すると見出しの多い文書でタイピングが引っかかる。入力由来の更新を150msでまとめた（同じ入力イベントで走る行番号ガター更新と同じ待ち時間）。**スクロール由来の更新は rAF 間引きのまま**にしている——スクロールは追従が遅れると体感で分かるため150ms待たせない。**実装場所の判断**: 単純な `setTimeout` を `editor.js` に直書きするとテストハーネスが読み込まず検証不能になる（直近2サイクルのレビューで繰り返し指摘された点）ため、汎用ヘルパ `utils.debounce(fn, waitMs, timers)` として `media/modules/utils.js` に置いた。タイマーを注入でき、返す関数は保留中の実行を取り消す `cancel()` を持つ。**`/local-review`指摘（medium 3件・low 1件、すべて同サイクルで対応）**: A-1（medium）— `AGENTS.md` の同じ箇条書きに旧文「入力イベントでの更新は現状デバウンスされていない（低優先度でROADMAPへ）」が残り、前半の新実装の説明と**自己矛盾**していた（後続サイクルが「未実装」と誤読して二重実装しうる）→削除。A-3（medium）— テストの偽タイマーが `setTimeoutFn` の第2引数を捨てており、**`waitMs` が渡っていなくても／別の値に化けても全テストが緑のまま通る**状態だった（姉妹関数 `waitForRenderComplete` のテストは待ち時間を検証しており非対称）→待ち時間を記録して検証する形へ。変更後、`delay` を渡さない実装にすると実際に赤くなることを確認済み。A-4（low）— `cancel()` 後の再スケジュール経路のテストが無かった→追加（ただし `timerId = null` のリセット自体は、stale な id を `clearTimeout` しても無害なため**公開APIからは観測できない**。追加したのは「cancel 後も再びスケジュールできる」という挙動保証）。B-4（low）— `editor.js` 側の無用なラッパー関数→`utils.debounce(updateHeadingBreadcrumb, 150)` へ簡約。B-2/C-3（low）— 同一モジュール内で防御方針が非対称だった点（`waitForRenderComplete` は NaN を弾くのに `debounce` は素通し）と、注入された `setTimeout` が `undefined` を返す場合に番兵 `!== null` が崩れる点→`Number.isFinite` ガード（ただし 0 は「次のタスクへ回す」正当な指定なので許す）と `!= null` へ。**B（ROADMAPへ）**: 手書きのデバウンス実装が各所に残っている（`scheduleWysiwygGutterUpdate`・`editSyncTimeout` 2箇所・脚注ツールチップ・`mermaid.js`・`table.js`）→`utils.debounce` へ寄せる項目を追加。**C（対応不要と判断）**: trailing-only デバウンス（maxWait 無し）のため、キー間隔が150msを切り続ける限りパンくずが一度も更新されない点は、キャレット追従で `#editor` の `scroll` が発火し rAF 経路で更新されるケースが多いため意図的な設計として据え置き（レビューも「指摘というより確認事項」としている）。ユニットテスト8件追加（797→805件）。`media/modules/` と `media/editor.js` のみ＝`src/` 非変更のため統合テスト非対象。型/lint/compile・ユニット805件パス。**実機確認してほしい点**: 見出しの多い文書でタイピングが軽くなったか、入力を止めてから150ms後にパンくずが正しく追従するか、連続タイピング中の表示に違和感が無いか | `0dfaf06` |
| 2026-07-30 | 機能（**実機確認前提**）: PDFエクスポート (4/4) 非同期レンダリング完了を待ってから印刷する。Mermaid図（`mermaid.render()` が非同期）・数式（KaTeXは描画自体は同期だが**Webフォントの読み込みが終わるまで字形が確定しない**）・ローカル画像（webview URIへ差し替えた後に読み込まれる）の完了を待ってから `window.print()` を呼ぶ。**実装場所の判断**: 前サイクルのレビューで「印刷導線が `editor.js` にあり構造的にユニットテスト不能」と指摘されていたため、待ち合わせロジックは依存をすべて引数で受け取る `utils.waitForRenderComplete()` として `media/modules/utils.js` に置いた（ハーネスが読み込む対象なのでテスト可能）。**決して reject せず上限（既定5秒）で必ず解決する**——印刷は「多少崩れても実行される」ほうが「押しても何も起きない」より良いため、個々の待ち対象の失敗（画像のerror・renderの同期例外含む）は握りつぶす。**`/local-review`指摘（high 1件・medium 2件・low 2件、すべて同サイクルで対応）**: **A-1（high）— 待ち合わせが主要ケースで効いていなかった**。`mermaid.js` の `render()` は「コンテナが既にある `pre` はスキップ」するが、そのコンテナは**SVG生成の `await` より前**に挿入される。そのため `exportPdf` が `toggleRawMode()` で始めた描画の最中に `render()` を呼ぶと、進行中のブロックはスキップされ**SVGの完成を待たずに解決**し、空の図が印刷されうる（＝「Rawモードから戻した直後」というまさに想定していた経路で待ち漏れる）→モジュールレベルの `renderChain` に繋いで呼び出しを直列化し、戻り値が「進行中の描画も含めて完了した」ことを表すようにした。直列化を外すと新テストが赤くなることを確認済み。**A-3（medium）— 「全部を待つ」テストが実質無検証だった**。マイクロタスク1回後に未解決を確認するだけで、実装が `Promise.all`→`race`→`then` と数ホップ挟むため**3つのうち1つしか待たない実装でも通る**状態だった→「3つのうち2つだけ解決させても未解決のまま」を3パターン全てについて確認する形へ作り直した。A-2（medium）— `exportPdf` のJSDocに「描画完了まで待ち合わせる対応は (4/4) で行う」という陳腐化した記述が残っていた（本コミットがその (4/4)）→更新。C-1（low）— `typeof d.timeoutMs === 'number'` は `NaN` も通し `setTimeout(fn, NaN)` は即時発火＝**待ち合わせが丸ごと無効になる**→`Number.isFinite` かつ正数のチェックへ変更しテストも追加。B-1（low）— 待ち対象が先に終わっても上限タイマーが残っていた→`clearTimeout` で解除しテストも追加。**B（ROADMAPへ）**: 待機中の二重クリックで待ち合わせと `print()` が二重に走る（Rawからの実行では `afterprint` の復帰リスナも二重登録される）／画像の待ち対象がクリック時点のスナップショットで待機中に増えた画像を待たない。**C（対応不要と判断）**: `document.fonts.ready` がクリック時点で既に解決済みだと待ちにならない理論上のケースは、Raw→WYSIWYG経路では `mathModule.render()` が同期で先に走るため実際には概ね pending になるとレビュー自身が判断しており据え置き。ユニットテスト11件追加（786→797件）。`media/modules/` と `media/editor.js` のみ＝`src/` 非変更のため統合テスト非対象。型/lint/compile・ユニット797件パス。**実機確認してほしい点**: (a) Mermaid図・数式・画像を含む文書で🖨️を押し、**図や式が欠けずに**印刷プレビューへ出るか (b) Rawモードから実行した場合も同様か (c) 待ち時間が体感で長すぎないか。**なお13周目から未確認の前提が残っている**——VS Code の Webview は sandbox 付き iframe で動くため `window.print()` 自体がブロックされ無反応になる可能性がある。これが該当する場合、本サイクルの待ち合わせも含めて印刷方式ごと再検討（ヘッドレス変換方式）が必要 | `5bc7879` |
| 2026-07-30 | 機能（**実機確認前提**）: PDFエクスポート (3/4) 改ページ制御と用紙幅の調整。`media/editor.css` の `@media print` へ、(1)`break-inside: avoid`（旧 `page-break-inside` も併記。Chromiumは両方解釈）を `#editor pre`/`table`/`blockquote`/`img`/`.math-block`/`.frontmatter`/`section.footnotes li`/`.mermaid-container` へ、(2)`break-after: avoid` を `h1〜h6` へ、(3)`orphans`/`widows: 3` を `p`/`li` へ、(4)`@page { margin: 15mm }`、(5)用紙幅からのはみ出し対策、(6)Mermaid図のダークテーマ対策、を追加した。**Mermaid**は `resolveTheme()` でVS Codeのテーマに追随して描画されるためダークテーマでは明色の線・文字になり、白紙に出すと読めない。コンテナの暗い背景を `print-color-adjust: exact` で紙にも残す方針にした（印刷のたびにライトテーマで再描画する方式は待ち合わせが要るため (4/4) とあわせて検討）。**`h1 { break-before: page }`（章ごとの改ページ）は入れていない**——文書の性質で好みが分かれ、通しで印刷する用途では冒頭に空ページが出る懸念もあるため、要望が出てから判断する（判断理由をAGENTS.mdに記録）。**`/local-review`指摘（high 1件・medium 2件・low 3件、A/Bとも対応）**: **A-1（high）— 表の用紙幅対策が実際には効いていなかった**。レンダリング済みの表は `.table-container(overflow:hidden) > .table-wrapper(overflow:auto) > table` という構造で、紙にはスクロールバーが無いため**はみ出した列が無音でクリップされて印刷結果から消える**。さらに `.table-container th/td { min-width: 100px }` が残っているとセルが縮まず、A4の本文幅≒680pxに対し7列で下限700pxを超えて右側の列が丸ごと欠落する（`#editor pre` には `overflow: visible` を当てていたのに表側の同等の解除が抜けていた）→スクロールコンテナを `overflow: visible` にし、`#editor th/td { min-width: 0 }`（詳細度1,0,1が0,1,1に勝つ）で下限も外した。あわせて折り返しを `break-word` から **`overflow-wrap: anywhere`** へ変更——`break-word` は min-content幅を縮めないため、長いURLを含む列が実際には狭くならない。A-3（medium）— 縦に長い表も同じスクロールコンテナ（monolithic扱い）に支配されて行が失われうる→同じ修正で解消。**A-2（medium）— 追加した `break-inside` のテストが実質無検証だった**。`hasSelector` は `@media print` ブロック全体を検索するため、`#editor pre`（折り返し・背景の両ルールに出現）・`#editor img`（max-width ルールにも出現）・`.mermaid-container`（print-color-adjust にも出現）は **break-inside ルールから消しても通ってしまう**状態だった→ルールを `[セレクタ, 宣言]` に分解して突き合わせる `ruleHas()` を導入し、ルール単位の検証へ作り直した（実際に `.mermaid-container` をbreak-insideルールから外して赤くなることを確認）。C-1（medium）— テストのコメント除去が波括弧カウントの**後**だったため、今後 `@media print` 内のコメントに `@page { margin: 0 }` のような例示コードを書くと抽出が壊れる→除去を計数の前へ移した（今サイクルで踏んだ「コメント中の `table-layout: fixed` を誤検出する」問題も同時に解消）。B-1（low）— `#editor img`/`.mermaid-container svg` の `max-width:100%` は既存指定と完全重複→削除。B-2（low）— `#editor pre` が印刷ブロック内の2箇所に分散していた→1箇所へ集約。B-3（low）— `word-break: break-word` は非推奨エイリアス→`overflow-wrap: anywhere` へ統一。**B（ROADMAPへ）**: 1ページを超えるコードブロックは `break-inside: avoid` により直前に半ページ近い空白が出る可能性があり、目立つようなら `pre` だけ `break-inside: auto` に留める判断もある（`/local-review` C-3、実機確認待ち）。ユニットテスト7件追加（785→786件）。`media/editor.css` と `src/test/` のみ＝統合テスト非対象。型/lint/compile・ユニット786件パス。**実機確認してほしい点**: 印刷プレビューで (a) **列数の多い表が右端まで印刷されるか**（A-1の修正点。従来は無音で欠けていた） (b) 長いコードブロックの前に不自然な空白が出ないか (c) ダークテーマのMermaid図が読める状態か（コンテナの暗い背景ごと出る見た目が許容できるか。テーマによっては「やや暗いグレー地に中間色の線」で読みづらい可能性がレビューで指摘されている） (d) 改ページ位置が不自然でないか | `7b9dcf3` |
| 2026-07-30 | 機能（**実機確認必須**）: PDFエクスポート (2/4) 「PDFとして出力」コマンド／ツールバーボタンの追加。ツールバーの `#exportPdf`（🖨️ PDF）とコマンド `markdown-wysiwyg-editor.exportPdf` の2経路を用意した。印刷は Webview 側でしか実行できない（拡張機能ホストに `window.print` が無い）ため、コマンドは`MarkdownEditorProvider.postToActivePanel({type:'print'})` でアクティブなパネルへ送り `editor.js` が実行する。パネル追跡は `resolveCustomTextEditor` で追加し `onDidDispose` で削除する静的 Set。**Rawモード中はWYSIWYGへ戻してから印刷**し（Rawのままだと `@media print` が `#rawEditor` を隠すため白紙になる）、印刷後は `afterprint` で元のRaw表示へ戻す。**統合テスト環境の問題も併せて解消**: `npm test` が `spawn … Electron ENOENT` で起動すらできなかった。VS Code 1.13x 以降は macOS の実行ファイル名が `Contents/MacOS/Electron` → `Code` へ変わったのに `@vscode/test-electron` 2.5.2 が前者を決め打ちで探すことが原因。`.vscode-test.mjs` で `version: '1.108.0'`（`engines.vscode` の最小サポート版）へ固定して解決した。途中で誤って `1.95.0` を指定したところ**既存コマンドも含め全て `not found`** になったが、`git stash` して変更前のコードでも同じ5件が失敗することを確認し、自分の変更が原因でないと切り分けたうえで「1.95.0 では `engines.vscode: ^1.108.0` を満たさず拡張機能が activate しない」ことを突き止めた。**`/local-review`指摘（high 1件・medium 3件・low 2件、すべて同サイクルで対応）**: **A-1（high）— `showWysiwygLineGutter()` がインラインで `display:flex` を書いており、カスケード上クラスより強いため `@media print` の `display:block` に勝ってしまっていた**。`exportPdf` が通るRaw→WYSIWYG復帰は必ずこの経路なので、**(1/4) で入れた用紙幅いっぱいのブロック配置が無効化される回帰**だった（「一度Rawへ切り替えて戻した後の印刷」全ケースで発生）→クラスが既に `display:flex` を持つのでインラインスタイルを**消す**（`style.display = ''`）形に変更。A-2（medium）— `wasRawMode` を取得しているのに印刷後に復元しておらず、Raw編集中に印刷すると勝手にWYSIWYGへ切り替わったままになっていた→`afterprint`（`{once:true}`）で元へ戻す。A-3（medium）— 統合テストの「すべてのコマンドが登録されている」に新コマンドを追加し忘れていた→追加。あわせて「非アクティブでも例外を投げない」ケースも追加（統合13件へ）。C-2/B-3（medium/low）— コマンドパレット表示中に `panel.active` が落ちる可能性→**表示中のパネルがちょうど1枚ならそれへ送る**フォールバックを追加。B-1（medium）— `.vscode-test.mjs` のコメントが「ROADMAPに記録」と書いているのに未記載だった→test-electron 更新の項目を追加（最新版での回帰をCIで検知できなくなる副作用も明記）。B-4（low）— `await` の無い `async` ハンドラ→同期関数へ。**B（ROADMAPへ）**: 印刷導線のロジックが `media/editor.js` にあり、テストハーネスが `media/modules/` 配下しか読み込まないため**構造的にユニットテスト不能**（`/local-review` A-4）。**C-3を反映**: Rawからの印刷はフル再描画が走るぶん図欠けの確率が高い旨を (4/4) の項目へ追記。ユニット780件・統合13件パス。型/lint/compile も成功。**⚠️ 実機確認が必須（この機能の成否そのものが懸かる）**: VS Code の Webview は sandbox 付き iframe で動くため、`allow-modals` の有無によっては `window.print()` が**ブロックされて無反応**になる可能性がレビューで指摘されている（確信度中・静的には断定不可）。🖨️ ボタン押下で**印刷プレビューが実際に出るか**を最優先で確認してほしい。無反応の場合は (4/4) の待ち合わせ実装ではなく、ヘッドレス変換方式（ROADMAPの `puppeteer-core` 案）ごと再検討が必要。あわせてコマンドパレット経由の動作、Rawモードから実行したときの往復も確認したい | `3a08902` |
| 2026-07-29 | 機能（**実機確認前提**）: PDFエクスポート (1/4) 印刷用スタイル（`@media print`）の整備（ROADMAP優先度:低の先頭項目。前サイクルでサイズLの「PDFエクスポート機能」をS/M 5件へ分割したうちの1件目）。`media/editor.css` 末尾に `@media print` ブロックを新設（それまで印刷用スタイルは1つも無かった）。画面専用UI（ツールバー・パンくず・**WYSIWYG/Raw両方の行番号ガター**・Rawのtextarea・文字数表示・トースト・リンク/表挿入ダイアログ・表/Mermaid/数式のコンテキストメニュー・表ツールバー・検索ウィジェット・「/」コマンドメニュー・コードのコピーボタン/言語セレクタ・Mermaidツールバー/ソースペイン・脚注ツールチップ）を`display:none !important` で隠し、`body` の画面高制約（`height:100vh`・`display:flex`）を解除する。**色**は `body`/`#editor` の地の色だけを `#000`/`#fff` へ固定する（テーマ変数のままだとダークテーマで「白紙に白文字」になる一方、一括指定すると `#editor .hljs-*` のハイライトが潰れるため）。**検証**: 印刷結果は実機でしか目視できないため `src/test/unit/print-css.test.ts` を新設し、(1)必須セレクタが `@media print` で扱われているか (2)書いたクラス/idが**コードベースに実在するか** (3)色の固定方針 (4)コードブロック背景の保持 (5)幅の扱い、を静的に検証する。存在しないクラス名を書いてもCSSはエラーにならず**黙って何も隠さない**ため、この検証が無いと実機で印刷するまで気づけない（実際に `.mermaid-view-toggle` という誤名をこの検証が検出した）。**`/local-review`指摘（high 2件・medium 4件・low 1件、すべて同サイクルで修正）**: **A-1（high）— `#editor { flex: none }` が本文を用紙幅からはみ出させていた**。`.wysiwyg-editor-wrap` は`flex-direction: row` なので `#editor` の `flex` は高さではなく**幅**を決めており、`flex: none` はflex base size を max-content にして段落が用紙幅を超えて右端で切れる（画面高の制約を作っていたのは `body { height:100vh }` の方で、`flex` の解除は高さに何も効いていなかった）→ラッパーを `display: block` へ落とす形に変更（フレックスコンテナのままページ分割される問題も同時に解消）。**A-2（high）— 既定モードであるWYSIWYGの行番号ガター `.wysiwyg-line-gutter` を隠し忘れていた**（隠していたのはRawモード用の `.raw-line-gutter` だけ）。通常の使い方で印刷すると左端40pxの行番号カラムと罫線がそのまま紙に出る状態で、ドキュメントの「行番号は印刷されず」という記述とも矛盾していた→追加。A-3（medium）— `.raw-markdown` にコメントは「紙に出さない」と書きながら実装は `opacity: 1` で逆になっており、さらに淡色（`--vscode-descriptionForeground`）が詳細度で勝つためダークテーマではグレーのまま印刷される→消すと本文が欠けるためそのまま出す方針に決め、色を黒へ戻してコメントも一致させた。A-4（medium）— 検索ハイライト `.find-highlight` が印刷対象のままだった（`.find-widget` を隠してもDOM上のspanは残る。とくに `outline` は背景色と違い印刷設定で落ちない）→背景とoutlineを消す指定を追加。A-5（medium）— 「hljsは触らない」方針は、**vs2015系が暗背景前提の明るいトークン色**である点を見落としており、`#editor pre` の背景が印刷で落ちると白地に薄いグレーの文字で判読不能になる→`print-color-adjust: exact` でコードブロックの背景を紙にも残すようにした。A-6（medium）— テストが「書いた名前が実在するか」しか見ておらず**「隠すべきものを書き忘れた」ケースを検出できない**（実際にA-2/A-4を通過させていた）／自前ソース以外に同梱ライブラリ（数MBのミニファイ済みコード）まで走査対象にしており偶然一致で誤合格しうる→必須セレクタ一覧を13件へ拡充し、走査対象を自前ソース（`editor.js`・`modules/*.js`・`src/*.ts`）に限定。A-7（low）— `.table-context-menu` だけ隠し漏れ→追加。B-1（low）— コメントの「`#editor` が `display:none`」は不正確で実際は `#wysiwygEditorWrap`→修正。B-4（low）— `min-height: 0` が元ルールと同値の無効な再指定→削除。C-2/C-3（low）— テストが最初の `@media print` しか見ない／部分文字列判定が他セレクタに誤合格しうる→全ブロックを連結して検証し、セレクタ境界を見る判定へ変更。修正後、A-1/A-2/A-4 の状態を一時的に再現して**3件とも新テストが赤くなる**ことを確認済み。**B（ROADMAPへは追加せず）**: `#editor pre` の折り返し・ページ分割・`@page` 余白・Mermaidのダークテーマ図は、いずれも既に (3/4) に記載済みのため重複追加せず。ユニットテスト6件追加（新規ファイル）。`media/editor.css` と `src/test/` のみ＝`media/modules/`・`src/` 本体は非変更のため統合テスト非対象。型/lint/compile・ユニット780件パス。**実機確認してほしい点**: Ctrl+P の印刷プレビューで (a) 本文が用紙幅に収まっているか（A-1の確信度は中〜高で最終的な見え方はChromiumの印刷レイアウト実装に依存する） (b) 行番号カラムが消えているか (c) ダークテーマでもコードブロックが読めるか (d) 編集用UIが1つも出ていないか。**Rawモード表示中は白紙になる**（救済は (2/4)） | `5b86a05` |
| 2026-07-29 | 機能改善: 表の列追加・削除時に他列のセパレーター書式（スペース有無・アライメント）を維持（ROADMAP優先度:低の先頭項目）。区切り行の元表記は`data-sep`属性に保持しているが、列の追加・削除で属性を更新していなかったため列数がヘッダーと食い違い、`serializeTable`が**全列をデフォルト書式へフォールバック**して、触っていない列のアライメント（`:---`/`---:`/`:---:`）や空白の有無まで失われていた。**実装**: `markdown.js`に純粋関数`insertSepColumn`/`removeSepColumn`（`data-sep`を列単位でspliceする）と定数`DEFAULT_SEP_CELL = ' --- '`（`serializeTable`のフォールバック書式と一致）を新設し、`table.js`の`addColumn`/`deleteColumn`から呼んで属性を同時更新する。**属性が無い場合・現在の列数と食い違っている（＝既に陳腐化している）場合・indexが範囲外の場合は`null`を返して更新しない**——陳腐化した値をspliceすると偶然列数が一致して誤った書式が復元されるおそれがあるため、更新せず列数不一致でフォールバックさせるほうが安全側。**`/local-review`指摘（medium 1件・low 4件、A/Bとも対応）**: A-1（low）— 新規の定数・関数を`serializeTable`のJSDocと関数定義の**間**に挿入してしまい、`serializeTable`が説明を失い`DEFAULT_SEP_CELL`が誤ったJSDocを持つ形になっていた→JSDocの位置を修正。A-2（medium）— `docs/features.md`の「（列の追加・削除を行った場合を除く）」という記述が本変更で事実と食い違うようになった→更新。C-1（low, 確信度低）— `insertSepColumn`だけが範囲外indexを**黙ってクランプ**しており`removeSepColumn`と非対称だった。DOM側は`insertBefore(headers[-1])`＝末尾追加になるのに対しクランプは先頭挿入になるため、**列数だけ一致してアライメントが1列ずれた表が書き出される（サイレントなデータ破損）**余地があった（実際に到達する経路は見つからなかったが防御として）→`removeSepColumn`と同じく`null`返却へ統一し、末尾追加（index===列数）は正当として通すテストも追加。B-1（low）— テスト変数名`secondHeaderCell`が実体（tbodyのセル）と食い違っていた→`secondBodyCell`へ。B-3（low）— 往復の冪等性テストが無く、`splitSeparatorRow`がcellをtrimしない実装への暗黙依存だった→書き出したMarkdownを読み直して同じ`data-sep`に戻ることを確認するアサーションを追加。**B（ROADMAPへ）**: 表のアライメントがWYSIWYG表示に反映されない（`style="text-align"`未付与。書式が保たれるようになったぶん可視化の価値が上がった）／コンパクト表記（`|---|`）の表に列を追加すると追加列だけ` --- `で浮く（既存セルから書式を推定する案）。**既知の制約**: `setAttribute`はcontenteditableのネイティブundoスタックに乗らないため、列追加をCtrl+Zで戻すとDOMの列数だけ戻り`data-sep`は取り残される（列数不一致で全列デフォルト書式へフォールバック＝**修正前の挙動に戻るだけで破損はしない**）。レビューはcorrectnessバグを検出せず、`splitSeparatorRow`がtrimしないため`' --- '`がそのまま復元され往復が冪等であること、列数変化が`addColumn`/`deleteColumn`以外の経路（貼り付け・Tab）では起きず更新漏れが無いことも確認。ユニットテスト7件追加。`media/modules/`のみ＝統合テスト非対象。型/lint/compile・ユニット774件パス。**実機確認してほしい点**: 表の列を追加・削除したあと、他列の右寄せ・中央寄せ記法が保存後のMarkdownで保たれているか（WYSIWYG上ではアライメントが見えないため、保存したファイルを確認する必要がある） | `8e8f815` |
| 2026-07-29 | バグ修正: 本文行を挟んだ脚注定義同士の間の空行が失われる問題を修正（ROADMAPバグ修正セクションの最後の項目、前サイクルの`/local-review` B-2由来）。未参照の「定義行らしき行」や通常の段落が定義の間に挟まると、直前が定義でなくなり`afterDef`がリセットされるため`blanksBefore`が0になり、脚注一覧で`[^a]: A\n[^b]: B`と詰まっていた。**実装**: 「脚注一覧側の区切り（`blanksBefore`）の算出」と「本文側から空行を減らすか」を**分離**した。前者は直前も定義なら保留分をまるごと、直前が本文でも直前に空行があれば1つ立てる。後者は定義（の連なり）が**前後の両方を空行に挟まれていたときだけ**1つ減らす。本文側の空行を残したまま`blanksBefore`を立てても、脚注一覧は文書末尾に独立して出るため二重計上にはならない。**`/local-review`指摘（high 1件・medium 2件・low 1件、すべて同サイクルで対応）**: **A-1（high）— 初版は「定義行は直前の空行1つを必ず持っていく」という1つの規則に統一したが、これは前後の両方に空行がある場合にしか成り立たない類推で、後ろに空行が無い定義で本来必要な区切りまで削除していた**。実測での回帰: `- item1[^x]\n\n[^x]: A\n- item2` → リスト項目が融合、`> a[^x]\n\n[^x]: A\n> b` → 引用が融合、表では段落が表の行として吸収され、`本文[^x]\n\n\n[^x]: A\n後書き` では連続空行（空段落）が1つ失われていた（リスト・引用・テーブルのブロックパーサは`seamIndices`を見ず空行だけが区切りのため）→上記の分離設計へ作り直し、3系統の回帰テストを追加して修正前に赤くなることを確認。A-2（medium）— 新規テストが「前後どちらにも空行がある」ケースしか踏んでおらず、既存の往復テスト（リスト・引用の間に脚注定義行）も定義の前に空行が無い入力のためこの回帰を検出できなかった→上記3系統を追加。A-3（medium）— JSDoc・実装コメントが旧アルゴリズムのまま食い違っていた（`afterDef &&`ガードの削除で「すべての空行を一旦保留する」仕様に変わった点を含む）→実装に合わせて更新。C-2（low）— `else`分岐が`pendingSeam === false`を暗黙に仮定している点を不変条件としてコメントに明記。**B（ROADMAPへ）**: 空行の保留バッファが`entry.scannable`を見ずコードフェンス・数式ブロック・front matter内の空行も通している（現状の挙動は変わらないが将来の事故につながりやすい）。**C（対応不要と判断）**: 同一文書内で脚注一覧の区切り方が混在しうる点（`[^b]`の前に空行が無ければ詰めて出る）は、「元の書式を保つ」方針からむしろ正しく往復も冪等なため据え置き。順序付きリストの番号が往復で失われる既存問題は本サイクル起因ではないため触れず。実測11ケース（目的のケース＋前サイクルまでの全回帰ケース）が期待どおりかつ3周まで冪等であることを確認。ユニットテスト2件追加・既存6件の期待値を更新。`media/modules/`のみ＝統合テスト非対象。型/lint/compile・ユニット767件パス。**実機確認は不要**（空行構成は往復・冪等テストで検証済み）。**これでROADMAPのバグ修正セクションは空になった** | `786f942` |
| 2026-07-28 | バグ修正: 文書途中の脚注定義を末尾へ集約する際に本文側の空行が1つ増える問題を修正（ROADMAPバグ修正セクションの先頭項目、前サイクルの`/local-review` B-1由来）。`本文[^x]です。\n\n[^x]: A\n\n後書き。` が往復後に `本文[^x]です。\n\n\n後書き。\n\n[^x]: A` となり、定義行だけが取り除かれて**前後の空行が両方残る**ため本文側の空行が1つ増えていた（定義が末尾の脚注一覧へ移動すること自体は設計＝一般的なMarkdownの脚注と同じ挙動）。**実装**: `flushBlankBuffer`に「定義（の連なり）の直前にも空行があるなら、戻す空行を1つ減らす」判定を追加。定義行とその片側の空行で1つのまとまりとみなし、「定義行を消すときは隣の改行も一緒に消す」という手編集と同じ結果にする考え方。前に空行が無い場合は減らす対象が無く従来どおり。**連続空行の保持機能との関係**: `markdownToHtml`は連続空行を`<p><br></p>`（空段落）として保持する＝空行はユーザーのコンテンツだが、減らすのは定義自身の区切り空行1つだけなので空行が2つ以上並んでいた場合は必ず1つ以上残る（衝突しない）。実測でも`本文[^x]。\n\n\n[^x]: A\n\n後書き。`・`本文[^x]。\n\n[^x]: A\n\n\n後書き。`とも空段落が保持され3周まで冪等であることを確認。**`/local-review`指摘（medium 2件・low 2件、すべて同サイクルで対応）**: A-1（medium）— 実装コメントが「文書末尾の定義は従来どおり」と書いていたが、**最終flushにも同じ判定が効くため「定義の前に空行があり後ろに空行が残ったまま文書が終わる」入力では末尾でも空行が1つ減る**（既存テストが通り続けたのは、その入力の定義の前に空行が無かったからにすぎない）→コメントを実態に合わせ、この差分ケースの単体テストを追加（`contentLines`末尾の空行は`markdownToHtml`側の`\n+$`除去で結局落ちるため出力自体は変わらないことも明記）。A-2（medium）— 連続空行の保持機能との相互作用が最重要地点なのに、追加テストは空行がすべて1つのケースしか往復確認していなかった→定義の前後に空行が2つあるケースの往復＋冪等テストを追加。A-3（low）— 関数レベルのJSDoc（公開APIの契約）に新しい規則が書かれていなかった→追記。A-4（low）— 複数定義の冪等テストと「定義が文書先頭（`contentLines`が空で規則が非対称）」のテストが無かった→追加。C-1（low）— front matter直後に定義を置いた文書の往復テストが無かった（実測では安全）→追加。C-2（low）— 空行を1つ減らした結果seamが空行ではなく後続の本文行に立つようになった点は、seam利用側が行indexで判定するため**むしろ意味的に正しい**方向の変化としてテストコメントに記録。**B（対応せず）**: `blankBuffer.shift()`が先頭要素を落とすため空行の中身（`''`と`'   '`の混在）が入力順に依存するが、空白のみの行なので往復結果に差は出ず修正不要。ユニットテスト9件追加。`media/modules/`のみ＝統合テスト非対象。型/lint/compile・ユニット764件パス。**実機確認は不要**（空行構成は往復・冪等テストで検証済み） | `c33691a` |
| 2026-07-28 | バグ修正: 脚注定義同士の間の空行が往復で失われる問題を修正（ROADMAPバグ修正セクションの先頭項目、前サイクルの`/local-review` B-1由来）。定義行だけが本文から取り除かれるため、定義の**間**にあった空行が本文側に残り、往復すると最初の定義の前へ空行が集まっていた（`\n\n[^x]: A\n\n[^y]: B` → `\n\n\n[^x]: A\n[^y]: B`）。**実装**: `extractFootnoteDefinitions`が定義直後の空行をいったん保留し、**次に来るのがまた定義なら**その定義の`blanksBefore`へ付け替えて本文から取り除く（次が本文行・文書末尾なら保留分を本文へ戻す）。`buildFootnotesSectionHtml`が`data-footnote-blanks`属性へ保持し（0のときは属性自体を出さず後方互換）、`htmlToMarkdown`のSECTIONケースが`'\n'.repeat(1 + blanks)`で復元する。既存の`data-footnote-sep`（コロン直後の空白保持）と同型の設計に揃えた。`defs`の返り値に`blanksBefore`が加わったため、形を比較していた既存テスト3件の期待値も更新。**途中で気づいた自分のテストの誤り**: 当初「定義の後に本文が続く場合の空行は本文側に残る」という回帰確認テストを書いたが、これは**脚注定義が常に文書末尾へ集約される**という既存設計（一般的なMarkdownの脚注と同じ挙動）を「定義の位置が保たれる」と誤って仮定したものだった→設計を明示するテストへ書き換えた。**`/local-review`指摘（medium 2件・low 2件、すべて同サイクルで対応）**: A-1（medium）— 定義間の空行を`contentLines`から取り除くことで「本来隣接していなかった本文行同士が隣接する」ケースが増え、`scanDefListTerms`等が`seamIndices`で防いでいる誤結合の危険域に触れる（過去2度回帰した領域）にもかかわらず、その保証を守るテストが1件も無かった→`seamIndices`が次の本文行に正しく立つことの単体テストと、定義リストへ誤結合しないことの実効テストを追加。A-2（medium）— 対になる`data-footnote-sep`には生成側テストと後方互換テストが両方あるのに`data-footnote-blanks`は往復経由の間接検証しか無かった→属性が`blanks>0`のときだけ出ること、属性の無い`<li>`が従来どおり改行1つで連結されることのテストを追加。A-3（low）— `@returns`の型注釈と関数先頭の説明コメントが`blanksBefore`に触れていなかった→追記。C-1（low）— `'\n'.repeat()`に上限が無く、外部由来HTMLの貼り付けで巨大値が入ると`String.repeat`がRangeErrorを投げて**保存経路（`htmlToMarkdown`）ごと落ちる**→`Math.min(raw, 20)`でクランプし、不正値（`abc`/`-3`/巨大値）でも落ちないことのテストを追加。C-3（low）— `reduce`より`map`+`join`のほうがセパレータの意図が明確という指摘を受け書き換え。**B（ROADMAPへ）**: (1) 文書途中の定義を末尾へ集約する際に本文側の空行が1つ増える（定義の移動自体は設計だが空行増加は単なるロス。severity medium、旧版と同挙動）。(2) 未参照の定義風行が定義の間に挟まると空行がまだ失われる（`afterDef`が非空行でリセットされる限界）。(3) SECTIONの直列化が`querySelectorAll('li')`でネストしたリストの`<li>`まで拾い、脚注本文中に箇条書きがあると`[^]: nested`というゴミ行が生成される（既存）。(4) C-2の「脚注一覧の`<li>`をEnterで分割するとcontenteditableが属性を複製する」は既存の`id`重複項目へ追記。レビューは旧版との比較コーパス12ケースで**変更により出力が変わったのは改善方向の4ケースのみ・残りは完全同一・全ケースで冪等性も確認**。ユニットテスト10件追加・既存3件更新。`media/modules/`のみ＝統合テスト非対象。型/lint/compile・ユニット755件パス。**実機確認は不要**（空行構成の保持は往復テストで検証済み） | `5c09597` |
| 2026-07-28 | バグ修正: 脚注ラベルに含まれる `_` が強調変換に食われて脚注リンクが壊れる問題を修正（ROADMAPバグ修正セクションの先頭項目、前サイクル中に自分で発見して記録した項目）。`markdown.js`の`convertInline`は脚注参照の`<sup>`を**実HTMLのまま**埋め込んでから強調変換を通していたため、ラベルが許す`_`（`[A-Za-z0-9_-]`）を2つ以上含むと`data-footnote-label`・`href`・`id`が化けて**脚注リンクのジャンプが壊れて**いた（脚注一覧側の`<li>`は別経路で生成されるため無事＝参照側だけがずれる非対称）。実測での壊れ方は経路によって2種類あり、斜体経路（`(^|[^_])_([^_]+)_(?!_)`）では`本文[^a<em>b</em>c]です。`、**太字経路（`__(.+?)__`の非貪欲マッチが`<sup>`を跨ぐ）では`本文[^a<strong>b]です。`と`</strong>`すら残らずラベルの一部が消失する、より重い破壊**になっていた。**実装**: `commands.js`の`convertInlineText`が既に行っていたのと同じプレースホルダ退避（番号4）へ揃えた。これでプレースホルダの割り当てが**両ファイルで完全に一致**する（0=コード/1=`\$`/2=数式/3=画像/4=脚注参照/5=リンク）。**`/local-review`指摘（medium 2件・low 2件、すべて同サイクルで対応）**: **A-1（medium）— 追加した往復テストのうち`[^a*b*c]`のケースが脚注コードを1度も通っていない死んだテストだった**。ラベルは`[A-Za-z0-9_-]`に限定されるため`*`はラベル文字ではなく、`[^a*b*c]`はそもそも脚注として成立しない（起きているのは段落テキストの`*b*`が斜体になって戻るだけで、修正をrevertしても緑のまま）→そのケースは「`*`はラベル文字ではないので脚注にならない」という仕様確認テストへ分離し、テスト名からも`*`を外した。A-2（medium）— 斜体経路とは壊れ方の異なる`__`（太字経路）のラベルが未テストだった→`[^a__b]`を往復テストへ追加。A-3（low）— 退避の**インデックス機構**自体（複数参照・隣接プレースホルダ・強調が参照を跨ぐケース）が未検証だった→`[^a_1][^b_2]と[^a_1]`で採番が混ざらないことと`*強調[^a_b_c]中*`の往復を追加。差し替え後のテストが実際に回帰を捕まえることを、修正を一時的に戻して**3件が赤くなる**ことで確認済み。C-1（low）— `markdown.js`だけ脚注の復元位置が復元群の先頭で`commands.js`（最後＝コード復元の直前）と非対称だった（デリミタ付きなので挙動は同一）→「ほぼ同一の2関数を並べて読む運用では位置がずれていると片側だけ直す事故を招く」という指摘を受け、`commands.js`側へ揃えた。C-3（low）— ライブ変換側のテストが「対称性」を名乗りながら読込パスと突き合わせていなかった→`markdownToHtml`が生成する`<sup>`と`applyInlineFormatting`後の`<sup>`の`outerHTML`を実比較する形へ変更。**B（ROADMAPへ）**: (1) 脚注定義が複数あると往復で空行構成が崩れる（`\n\n[^x]: A\n\n[^y]: B` → `\n\n\n[^x]: A\n[^y]: B`。修正前後とも同じ既存欠陥だがラウンドトリップ保持という必須要件に反するためバグ修正セクションへ、severity medium）。(2) 段落以外のブロック（見出し・リスト・表・引用・定義リスト・脚注本文）では`footnoteLabels`が渡されず脚注参照が変換されないうえ、ラベルに`_`を含むと強調変換だけが効いて文字列自体が変わる（実測: `- 項目[^a_b_c]` → `- 項目[^a*b*c]`）。段落だけを正しくした結果、ブロック種別による非対称が可視化された。**C（対応不要と判断）**: 原文にU+0004が含まれると脚注が二重化する（全プレースホルダに共通する設計上の性質で、Markdownファイルに制御文字が入る現実的可能性は極小）。レビューはHEAD版と作業ツリー版をjsdomへ並行ロードしランダム4000ケースで往復比較しており、**退行0件・新たに直った36件・プレースホルダ残留0件**を確認。ユニットテスト5件追加（うち1件は仕様確認）。`media/modules/`のみ＝統合テスト非対象。型/lint/compile・ユニット745件パス。**実機確認は不要**（脚注リンクのジャンプ先が一致することは参照側の`href`と一覧側の`id`の突き合わせで検証済み） | `9f8727f` |
| 2026-07-28 | バグ修正: リンク・画像のURL/タイトル/alt に含まれる `` ` `` と `$` が、コード・数式の退避に巻き込まれて壊れる問題を修正（ROADMAPバグ修正セクションの先頭項目、前サイクルの`/local-review` B-1由来）。インラインコード・数式・`\$` の退避は画像/リンクより**前**に行われ復元は**後**に行われるため、URL・タイトル・alt に紛れ込んだこれらのプレースホルダが属性値の中で展開されていた。実測での壊れ方: `` [t](http://e/a`b`c) `` → `href="http://e/a<code>b</code>c"`、`[t](http://e/$a_1$)` は復元される `<span class="math-inline" …>` が `"` を含むため属性値がそこで途中終了し**タグ構造ごと破壊**され往復結果が `["&gt;t](http://e/<span class=)` になっていた。**実装**: 画像・リンクの属性を組み立てる直前に `unstashToText` で**元のMarkdownテキストへ戻す**（コード→`` `…` ``、数式→`$…$`、エスケープ由来のドル記号→`\$`）。URL/タイトル中の `` `…` ``・`$…$` はコードでも数式でもなく単なる文字列なので、文字どおり保つのが往復にとっても正しいという判断。**戻すのは属性になる部分（URL・タイトル・alt）だけ**で、リンクテキストはHTMLの流れに残して従来どおり変換する（`` [`code`](u) `` のリンクテキスト内コードは変換される／コード内に書いたリンク記法 `` `[t](u)` `` は従来どおり変換されない、の両方を回帰テストで固定）。エスケープ由来のドル記号は入力されたままの `\$` とゼロ幅スペース付きの `$` の2形態があるが、属性値は直列化時に再エスケープされないため**どちらも `\$` へ**戻す（これによりROADMAPに併記されていた「`serializeInline`の`case 'A'`が`href`に`stripZeroWidth`を掛けていないためゼロ幅スペースが混入する」問題も、混入元が無くなることで解消。`href`の完全一致テストで担保）。**`/local-review`指摘（severity high 1件・medium 1件、いずれも同サイクルで修正）**: **A-1（high）— 初版の`unstashToText`は復元順序が「コード→`\$`→数式」で、退避順（コード→`\$`→数式）と同じ向きだった。数式の退避はコード・`\$` の後に行われるため控えている元テキストの中にはそれらのプレースホルダが残っており、最後に差し戻した数式ソース中の分を誰も復元しないまま属性値へ入って、修正したはずの不具合が入れ子ケースでそのまま再発していた**（実測: `` [t](http://e/$a`b`c$) `` → `` [t](http://e/$a<code>b</code>c$) ``、`[t](http://e/$a\$b$)` → ゼロ幅スペースがファイルへ混入、`` [t](http://e/$a`"`b$) tail `` → 往復結果が `` [b\$">t](http://e/$a<code>) tail `` とタグ構造ごと破壊）→ 復元順を**数式→コード→`\$`**へ反転して解決（入れ子は1段しか起こらない＝コードの中身と`\$`にはプレースホルダが入り得ないため単一パスで足りる）。A-2（medium）— 入れ子ケースのテストが無かった（初版のテストはコード・数式・`\$`が単独で現れる場合しか見ていなかった）→入れ子の往復テストと、属性値の早期終了を検出する `` [t](http://e/$a`"`b$) tail `` の回帰テストを追加。いずれも**修正前に赤くなることを確認**してから順序を直した。B-1の「2モジュールに完全重複していて片方だけ直す事故が起きやすい」という指摘はまさにこの順序バグで現実になったため、修正は両ファイル同時に適用した。**B（ROADMAPへ）**: `unstashToText`の2モジュール重複をファクトリ化して共有する／URL中の`\$`をhrefにバックスラッシュ付きで入れる現仕様は往復優先でCommonMark解釈（`http://e/$100`）と異なり実際の遷移先がずれうる／`$…$`が`)`をまたぐとリンク記法自体が変換されない（退避順序に元からある制約）。**C（対応不要と判断）**: 復元テキストが`parseLinkDestination`の入力になることによる分割判定への影響はレビュー自身が実害を再現できず誤検知の可能性が高いと判断（むしろ`[t](http://e/$a b$ "T")`が正しく分割される）。ユニットテスト9件追加。`media/modules/`のみ＝統合テスト非対象。型/lint/compile・ユニット740件パス。**実機確認してほしい点**: URLに`` ` ``や`$`を含むリンクを実際にCtrl/Cmd+クリックしたときの遷移先（とくに`\$`入りURLは上記のとおり往復優先の設計でバックスラッシュがhrefに残る） | `801a055` |
| 2026-07-27 | バグ修正: リンクのURL・タイトルに含まれる `_`/`*` が強調変換に食われて往復が崩れる問題を修正（ROADMAPバグ修正セクションの先頭項目、前サイクルの`/local-review` B-1由来）。`markdown.js`の`convertInline`と`commands.js`の`convertInlineText`は、画像は生成した`<img>`をプレースホルダへ退避してから強調変換（`**`/`_`/`~~`/`++`）を通すのに対し、**リンクは実HTMLを文字列に埋めたまま**後続の強調置換を通していた。このため`[t](https://e.com/a_b_c)`のように`_`を2つ以上含むURL（Wikipediaの記事URLなど日常的に現れる）は`href="https://e.com/a<em>b</em>c"`に化け、往復が崩れていた。タイトル記法対応で`title`属性にも同じ影響が及ぶことが判明したが、URL側は以前から存在する欠陥。**実装**: リンクも画像と同じプレースホルダ方式（`U+0005`）へ退避する。ただし`<img>`はvoid要素で中身を持たないのに対しリンクは中身を持つため、**退避するのは開始タグ（属性）だけ**にしてリンクテキストと`</a>`はそのまま残す（`[**太字**](url)`のようにテキスト内の強調は従来どおり変換される必要があるため。`</a>`は記法文字を含まないので強調変換に巻き込まれない）。プレースホルダ番号は`markdown.js`が0=コード/1=`\$`/2=数式/3=画像/5=リンク、`commands.js`はこれに4=脚注参照を加えたもの（`U+0005`は両ファイルとも未使用だったことを確認）。**検証手順**: 前サイクルまでに確立した手順どおり、修正前に再現テストを書いて**両パスで実際に赤くなること**を確認してから実装した。**`/local-review`: A（今サイクルで直すべき問題）なし**。レビューはHEAD版と作業ツリー版の両方をjsdomでロードし、個別ケース＋ランダム生成3000〜4000ケースで新旧の往復結果を直接比較。挙動が変わった31ケースのうち14ケースが「往復が壊れていた→往復するようになった」で、**往復が壊れる方向への退行は0件**、プレースホルダ`U+0005`のDOMへの漏れも0件（`[t](u_a_b`のような閉じ括弧の無い入力途中を含む）。`</a>`を残した副作用（`*[t](u)*`・`[a **b](u) c** d`・`[![alt](img_1.png)](url)`等の強調のまたぎや不正ネスト）も新旧で出力が完全一致することを確認。**C（レビュー指摘を受けて対応）**: 追加テストが`_`/`*`しか固定しておらず、同じ退避で新たに守られるようになった`~~`/`++`/`__`/`***`のケースが無かったため往復テストへ4件追加。両ファイルのリンク正規表現の書き分けの不揃い（`]`のエスケープ有無。動作は同一）も、コメントで「両パスで同じ規則」と謳っている以上揃えた。**B（ROADMAPへ）**: (1) URL/タイトル中の`` ` ``と`$`はコード・数式の退避（リンク/画像より前に退避し後で復元）に巻き込まれて壊れる別系統の穴で、`$`のケースは属性が途中終了してタグ構造ごと破壊される（severity medium、画像も同型）。あわせて`serializeInline`の`case 'A'`が`href`に`stripZeroWidth`を掛けていない点も記載。(2) 本サイクル中に自分で発見した**脚注ラベルの`_`/`*`が読込パスでのみ壊れる**問題（`markdown.js`は`<sup>`を実HTMLのまま埋め込み、`commands.js`は`U+0004`で退避済みという非対称。実測で再現確認済み）。**C（対応不要と判断）**: `.claude/settings.json`の`Skill(local-review *)`という許可記法が実際にマッチするかは未実測（ユーザーの手元設定のため本サイクルでは変更せず、報告で伝える）。ユニットテスト6件追加。`media/modules/`のみ＝統合テスト非対象。型/lint/compile・ユニット729件パス。**実機確認は不要**（`_`を含むURLのリンクが正しく機能するかは自動テストで往復・属性値とも検証済み） | `f82d755` |
| 2026-07-27 | 機能（**実機確認前提**）: 画像・リンクのタイトル記法 `![alt](url "title")` / `[text](url "title")` に対応（ROADMAP優先度:低の項目）。`markdown.js`に純粋関数 `parseLinkDestination(dest)`（丸括弧の中身を`{url, title}`へ分ける。`"…"`と`'…'`の両方を受け付け、**URLとの間に空白を必須**とすることで`a"b".png`のような引用符入りパスをタイトルと誤認しない。閉じ引用符が末尾に無い不正形はタイトル無し扱い）、`buildTitleAttr(title, escape)`（` title="…"`の組み立て）、`serializeTitle(node)`（`title`属性→` "title"`の逆変換）を新設し、**画像・リンクの両方**を**読込（`convertInline`）とライブ変換（`convertInlineText`）の両パス**で共通の規則に統一した。直列化時の引用符は中身に応じて選ぶ（`"`を含むタイトルは`'…'`で囲む）。**ROADMAPの記述の訂正（実測で判明）**: ROADMAPは「`"`が`&quot;`化されて往復も崩れる」としていたが、`getAttribute`が実体参照を戻すため**Markdownテキスト自体の往復は偶然保たれていた**。真の実害は`([^)]+)`が`url "title"`全体をURLとして取り込むために`src`/`href`へタイトルが混入し、**タイトル付き画像が表示されず**（存在しないパスへ解決される）**リンク先も無効になる**ことだった。**`/local-review`指摘（A 4件、すべて同サイクルで修正）**: (1) severity medium — リンク編集ダイアログ（`applyLinkDialog`）は既存`<a>`を新規要素で丸ごと置換するが`title`を引き継がず、タイトル付きリンクにキャレットを置いてCtrl+Kでテキストだけ直すと**ユーザーが触っていないタイトルがファイルから消える**データ喪失があった（今回の機能が生んだ新規経路）→`state.linkDialogLinkTitle`へ控えて適用時に復元するよう修正（既存の`linkDialogTitle`はダイアログ見出しのDOM参照で別物のため名前を分けた）。(2) severity medium — `parseRawLink`が丸括弧の中身をそのまま`href`として返すため、生Markdown表示中のリンクでは`handleLinkClick`がタイトル込みの不正URLを開こうとし、`insertLink`のURL欄にもタイトルが混入していた→`parseLinkDestination`へ委譲し`{text, href, title}`を返すよう修正。(3) severity low〜medium — `serializeTitle`のガードが`)`のみで、改行を含むタイトルも同様に段落を分断して文書を壊す→`/[)\r\n]/`へ拡張。(4) severity low — 壊れている経路（ダイアログ）が未カバーだったテストの穴→ダイアログ経路のテスト3件を追加し、**タイトル引き継ぎを一時的に外して2件が実際に赤くなること**を確認（前サイクルで確立した検証手順）。**B（ROADMAPへ）**: リンクだけプレースホルダ保護が無く`_`/`*`を含むURL・タイトルが強調変換に食われて往復が崩れる（severity medium、URL側は以前から存在する欠陥のためバグ修正セクションへ）／属性値エスケープ処理の重複を1箇所へ寄せる（simplification）。**C（対応不要と判断）**: 引用符が2組ある`(u "a" "b")`の解釈（往復は保たれるためdocstringに明記）／`buildTitleAttr`の`undefined`非対応（`parseLinkDestination`は必ず`null|string`を返すため到達不能）／空タイトル`""`の消失（既知の正規化としてテストで明示）。**既知の仕様**: URLの前後の空白は落ちる（CommonMark同様の正規化。docstringに明記）／URL・タイトルのどちらにも`)`は含められない（リンク記法に元からある制約でCommonMarkの`(…)`形式タイトルも非対応）。ユニットテスト24件追加（`parseLinkDestination`の全分岐・`serializeTitle`の引用符選択と`)`/改行ガード・画像/リンクの往復・data URL（内部に空白と`'`を含む）＋タイトルの往復・ライブ変換・ダイアログ経路）。`media/modules/`のみ＝統合テスト非対象。型/lint/compile・ユニット724件パス。**実機確認してほしい点**: タイトル付きリンク・画像にマウスカーソルを合わせたときツールチップが出るか（`sample.md`に例を追加済み）、生Markdown表示（リンク内へキャレット移動）でタイトルが正しく見えるか、Ctrl+Kでタイトル付きリンクを編集してもタイトルが残るか | `9b302b9` |
| 2026-07-27 | バグ修正: `toMarkdownRelativePath` がファイル名中の `\` をパス区切りとして壊す問題を修正（ROADMAP優先度:低の先頭項目、前サイクルの`/local-review` B-1由来）。`src/imagePaste.ts`の`toMarkdownRelativePath`は`rel.split(/[\\/]/).join('/')`で`\`と`/`の**両方**を一律に区切りとみなしていたが、macOS/Linuxでは`\`は**ファイル名に使える普通の文字**のため、`a\b.png`というファイル名が`a/b.png`という別パス（存在しないサブフォルダ配下）へ化けていた。`path.relative`の戻り値は必ずそのプラットフォームの`sep`区切りになる（Windowsは入力に`/`が混ざっていても出力を`\`へ正規化する）ため、区切り判定を`pathImpl.sep`のみに限定し、`sep === '/'`（POSIX）なら無変換で返す実装へ変更した。**設計上の判断**: テスト容易性のため第3引数`pathImpl: PathModule`（`Pick<typeof path, 'relative' \| 'sep'>`）で`path`実装を注入できるようにし、既定は実行プラットフォームの`path`。これによりmacOS上から`path.win32`を渡してWindows挙動を、`path.posix`を渡してPOSIX挙動を、**実行環境に依存せず**検証できる。**`/local-review`指摘（すべて同サイクルで修正）**: (1) severity medium — 初版のテストは第3引数を`sep: string`（区切り文字だけ）にしていたため、`sep='\\'`を渡すテストは`path.relative`自体がPOSIXのまま動き`rel`に`\`区切りが現れず、**実装が`return rel;`でも旧実装でも通るデッドアサーション**になっていた（前サイクル`ed34975`で指摘された`img.innerHTML`と同類型の再発）→注入口を`sep`から`path`モジュールそのものへ変え、Windows側テストを`path.win32`によるエンドツーエンド検証に作り直した。(2) severity low〜medium — 初版のPOSIX側テストは`path.relative`が注入されておらず実行プラットフォーム依存で、Windowsで実行すると`path.win32.relative`が`a\b.png`を`a\b.png`（`\`を区切りと解釈）にするため失敗する状態だった→既存3件を含む全テストへ`path.posix`/`path.win32`を明示的に渡し、スイートを「POSIX」「Windows」に分けてプラットフォーム非依存にした。(3) severity low — ROADMAP該当項目が`todo`のまま旧実装の記述で残り`AGENTS.md`だけ更新済みという不整合→本サイクルのドキュメントフェーズで解消。**回帰検出能力の確認**: 前サイクルで確立した手順どおり、修正を一時的に旧実装（`split(/[\\/]/)`）へ戻して実行し、POSIX側の`\`保持テストだけが赤くなることを確認した。Windows側3件は旧実装でも通る（Windowsのファイル名に`\`は使えないため両実装で挙動が同じ）ため**回帰テストではなく、POSIX側の修正でWindowsの正規化を壊していないことの裏付け**である旨をテストのコメントに明記した。**B（severity low、ROADMAPへ）**: Windowsで別ドライブに画像がある場合`path.relative`が相対化できず絶対パスを返す（現在の呼び出し元からは到達不能）。**C（対応不要と判断）**: `sep === '/'`早期リターンによりWindows上で`rel`に`/`が紛れた場合に素通しするが、出力先がMarkdown（`/`区切りが正解）なので結果は正しくレビューも「回帰なし・確信度高」と確認。ユニットテスト7件（既存3件を明示注入へ更新＋新規4件）。`src/imagePaste.ts`は`markdownEditor.ts`/`extension.ts`のいずれでもないため統合テスト非対象。型/lint/compile・ユニット701件パス。**実機確認は不要**（自動生成ファイル名`image-<日時>.<ext>`に`\`は含まれず、現在の唯一の呼び出し元では挙動が変わらない予防的修正） | `1d64d4e` |
| 2026-07-27 | 予防的堅牢化: 画像貼り付けの`buildImageMarkdown`がMarkdown/URLとしてあいまいな文字もパーセントエンコードするようにした（ROADMAP優先度:低の先頭項目）。従来は半角スペース・丸括弧のみを`%20`/`%28`/`%29`へ変換していたところへ、`IMAGE_PATH_ENCODE_MAP`（14文字の対応表）を新設し`%`・`<`・`>`・`"`・`` ` ``・`$`・`#`・`?`・`\`・`[`・`]`を追加した。置換は`.replace()`の連鎖ではなく**1回の走査**で行う（連鎖だと先に生成した`%20`の`%`が後段の`%`エンコードに拾われて`%2520`になる）。対応表に無い空白（タブ・全角スペース等。正規表現の`\s`にマッチする）は文字が変わらないよう`encodeURIComponent`へ委ねる。**エンコードする根拠は2系統**: (a) 生Markdownとしての正しさ — `#`（フラグメント）・`?`（クエリ）はURLの区切りなので、外部ビューア（VS Codeのプレビュー・GitHub等）が読んだときその位置以降がパスから切り落とされる（エディタ内表示だけなら`resolveImageSrc`が結合時に`%23`/`%3F`へ逃がすため既に壊れないが、保存されるMarkdownファイル自体を正しく保つのは`buildImageMarkdown`の責務）。(b) インライン変換の退避順序 — `convertInline`（`markdown.js`）と`convertInlineText`（`commands.js`）はインラインコード`` ` ``と数式`$…$`を**画像より先に**プレースホルダへ退避し、画像の復元より**後で**復元するため、パスにこれらが含まれると画像を組み立てたあとの属性値の中でプレースホルダが展開される（`` ` ``は`<img src="a<code>b</code>c.png">`となり属性値は引用符内なのでHTML構造は壊れず`src`の値のみ汚染、`$`は復元される`<span class="math-inline" data-math="…">`が`"`を含むため属性値がそこで途中終了し**HTML構造ごと壊れて残骸が本文へ落ちる**）。**あえてエンコードしない文字**: `_`・`*`・`~`・`+`等の強調記法文字（両変換関数が画像をプレースホルダへ退避してから強調変換を走らせるため化けず、エンコードすると`image_1.png`→`image%5F1.png`と読みにくくなるだけ）、および日本語などの非ASCII文字（ブラウザ側でエンコードされて解決される）。**`/local-review`指摘（severity medium 5件、すべて同サイクルで修正）**: (1) 往復テストの`assert.strictEqual(img.innerHTML ?? '', '')`は`<img>`がvoid要素のため常に`''`＝**回帰が起きても緑のままになるデッドアサーション**であり、さらに実際の壊れ方（`` ` ``は値の汚染・`$`は構造破壊）の説明とも食い違っていた→`querySelector('code')`/`.math-inline`が無いことと`textContent`に残骸が無いことを見るアサーションへ差し替え、コード側コメントの壊れ方の説明も2系統に分けて正確化。差し替え後のアサーションが本当に回帰を捕まえることを、未エンコードのパスを一時的に流して2件とも失敗する（`src`が`a<code>b</code>c <span class=`まで汚染される）ことで確認済み。(2) `encodeURIComponent`フォールバック分岐（`\s`のうち対応表に無い文字）を新規テストが1件も通っておらず、とくに全角スペースは日本語ファイル名で普通に出る→タブ・全角スペースのテストを追加。(3) README/features.mdが「フォルダ名に記号が含まれていても壊れない」と**現在の効能として言い切っていた**が、`saveClipboardImage`は保存先をドキュメントと同一フォルダに固定し`toMarkdownRelativePath`が返すのは`image-<日時>[-n].<ext>`という生成ファイル名のみのため、この恩恵を受ける経路は現状存在しない→「将来サブフォルダ保存へ対応したときのための予防措置」という表現へ修正。(4) `#`/`?`の理由付けコメントが陳腐化していた（`resolveImageSrc`は前サイクル`52bef01`で既に`%23`/`%3F`エンコード済み）→根拠を外部ビューア向けの生Markdownの正しさへ書き換え。(5) コメントがライブ変換（`convertInlineText`）を主目的に挙げているのに往復テストが`markdownToHtml`（＝`convertInline`）しか通していなかった→`applyInlineFormatting`経由のライブ変換テストを追加。**B（severity low、ROADMAPへ）**: `src/imagePaste.ts`の`toMarkdownRelativePath`が`rel.split(/[\\/]/)`でmacOS/Linuxでは正当なファイル名文字である`\`をパス区切りとして扱い`a\b.png`を`a/b.png`に化けさせる（このため`\`→`%5C`エンコードは現在の呼び出し元からは到達不能）。**C（対応不要と判断）**: 未エンコードの`&`・`'`・`\|`・`+`は問題なし（`&`は両経路とも`escapeHtml`を通り`getAttribute`で戻る＝往復不変、`'`は属性が`"`引用なので無害、`\|`は表セル内で`escapeCell`が`\\\|`へ逃がす）とレビュー自身が確信度高で確認。予防的実装であること自体（サブディレクトリ保存の実装時まで待つ選択肢との比較）はROADMAP記載の方針どおりとして受け入れ。ユニットテスト11件（既存2件＋新規9件）。`media/modules/`のみ＝統合テスト非対象。型/lint/compile・ユニット697件パス | `ed34975` |
| 2026-07-23 | バグ修正（**実機確認前提**）: 表の挿入ダイアログの表示位置を右クリック位置の近くにする（ROADMAP優先度:低の項目）。`table.js`の`ensureInsertDialog`が生成するダイアログは従来`.link-dialog`の固定CSS（`position:absolute; top:48px; right:20px`）により、右クリック位置に関わらず常に画面右上に出ていた。`showInsertDialog(onInserted, anchor)`に`anchor`（`{x,y}`）引数を追加し、新設の純粋関数`computeDialogPosition(anchor, dialogWidth, dialogHeight, viewportWidth, viewportHeight)`（`anchor`があれば既存の`computeMenuPosition`と同じ規則＝ビューポート右端・下端からはみ出す場合は内側へ寄せる、でその近くへ配置し、`anchor`が無ければビューポート中央へ配置する）で算出した`left`/`top`をダイアログ表示のたびにインラインスタイルで設定するよう変更した。CSS側は`.table-insert-dialog`に`position: fixed; top: 0; right: auto;`を追加し、`.link-dialog`が持つ`position: absolute; top: 48px; right: 20px;`の固定配置を（このクラスを併用する要素に限って）上書きする（`#linkDialog`（Ctrl+Kのリンク挿入・編集ダイアログ）自体は`.table-insert-dialog`クラスを持たないため非変更・従来どおり右上固定のまま）。呼び出し元は2箇所あり、右クリックメニュー経由（`table.js`の`contextmenu`ハンドラ）は右クリック時に記録した`lastContextMenuPoint`（新設のモジュール変数）を、「/」コマンドメニュー経由（`editor.js`）はキャレット位置（挿入対象ブロックの末尾に置いた折りたたみ済みRangeの`getBoundingClientRect()`）をそれぞれ`anchor`として渡す。**`/local-review`指摘（複数ラウンドにわたり段階的に検出・同サイクルで修正）**: (1) severity medium — 初回レビューで`computeDialogPosition`という純粋関数自体の単体テスト（境界値・中央フォールバック等）は用意していたが、`showInsertDialog`が実際にその計算結果を`dialog.style.left`/`top`へ正しく反映しているかを検証する統合テストが無く、配線部分（`table.js`内の引数順序や`rect.width`/`rect.height`の受け渡し）を壊しても既存テストがすべて通ってしまう抜けがあった→右クリックメニューを経由してダイアログを開き、`dialog.style.left`/`top`が右クリック座標と一致することを確認する回帰テストを追加。(2) severity low〜medium、確信度中 — `editor.js`側で使っている`range.getBoundingClientRect()`は、稀に（レイアウトのタイミング等により）collapsed Rangeに対して全て0の矩形を返すことがあり、その場合`anchor`が`{x:0, y:4}`相当になってダイアログが画面左上へ意図せず表示される可能性があった（ROADMAPが問題視していた「常に固定位置に出る」バグの別形態が残ってしまう懸念）→矩形のleft/top/width/heightが全て0の場合は「キャレット位置不明」とみなし、`anchor`を渡さずビューポート中央フォールバックへ委ねるガードを`editor.js`に追加（`editor.js`はDOM/イベント配線のみで`media/modules/`外のためこのガード自体に対する直接のユニットテストは無いが、既存の`toggleRawMode`等と同様のプロジェクト既存の慣行に沿う）。**B（severity low, PLAUSIBLE、ROADMAPへ低優先todo追加）**: `computeMenuPosition`/`computeDialogPosition`は右端・下端のはみ出し補正のみで、`anchor.x`/`anchor.y`が負の場合（左端・上端方向のはみ出し。マウス右クリック由来では通常発生しないが、キャレット由来のanchorではエディタが水平スクロールしている場合等に理論上発生し得る）の補正が無い。ユニットテスト追加（`computeDialogPosition`の全分岐＝anchorありでのcomputeMenuPosition委譲・右端/下端はみ出し補正・anchor無しでの中央配置・中央配置時の負値防止、および右クリックメニュー経由でダイアログを開いた際に`dialog.style.left`/`top`が右クリック座標と一致することを確認する回帰テスト）。`media/modules/table.js`・`media/editor.js`・`media/editor.css`のみの変更で`src/`は非変更のため統合テスト非対象。型/lint/compile・ユニット689件パス。**実際のダイアログ表示位置（右クリック位置・キャレット位置への追従、画面端での内側寄せ調整）の見た目は実機（拡張機能開発ホスト）確認前提** | `fc3e5db` |
| 2026-07-23 | 機能（**実機確認前提**）: Rawモードの行折り返しON/OFFトグル（ROADMAP優先度:低の項目）。ツールバーに`↩️ 折り返し`ボタン（`#toggleRawWrap`、`markdownEditor.ts`に追加）を新設し、`media/modules/state.js`の`isRawWrapEnabled`（既定false）をクリックでトグルする。`editor.js`の`applyRawWrapMode()`が`.raw-editor-wrap`へ`raw-wrap-on`クラスを付け外しし、`editor.css`の対応ルール（`.raw-editor-wrap.raw-wrap-on #rawEditor { white-space: pre-wrap; overflow-wrap: break-word; }`）で`#rawEditor`の折り返しを`pre`⇔`pre-wrap`で切り替える。行番号ガター（`.raw-line-gutter`）は論理行1つ＝固定22px行高の前提で作られており、折り返し後の視覚行とは番号送りがずれてしまうため、ON中はガター要素自体（`rawGutter`。ガター付きラッパー全体を隠す既存の`hideRawLineGutter`とは別物で、textarea自体は表示したまま）を`display:none`にして非表示化する（ROADMAPに記載されていた「開始行のみ番号を出すミラー測定」までは実装せず、まず「折り返しトグル＋折り返し時はガター非表示」という簡易対応に留めるという記載どおりの方針）。`applyRawWrapMode`は初期化時（`initRawLineGutter`直後）とボタンクリック時の両方で呼ばれ、Raw⇔プレビュー切替時の`showRawLineGutter`/`hideRawLineGutter`とは独立して折り返し設定を保持する。**`/local-review`**: A（correctnessバグ・回帰）なし。C（トグルボタンが`#toggleView`と異なりモードに応じた表示/無効化制御を持たず、WYSIWYGモード中も常時表示・クリック可能で、Rawモードに入るまで効果が見えないため初見のユーザーが「効いていない」と誤解しうる。title属性で補足説明済みのため実害は小さいと判断・severity low, 確信度中）はROADMAPへ低優先todo追加。ユニットテスト追加（`media/modules/state.js`の`isRawWrapEnabled`のgetter/setter、およびテストハーネス`helper.ts`のフィクスチャHTMLに`#toggleRawWrap`ボタンを追加し`initDOMReferences`の参照設定確認テストを更新）。`src/markdownEditor.ts`（ツールバーへのボタン追加）を変更したため型/lint/compile・ユニット684件パスに加えて統合テスト（`npm test`）12件も実行しすべてパス。`editor.js`側のDOM操作（`applyRawWrapMode`/`setupRawWrapToggle`）は`toggleRawMode`等の既存パターンと同様に`media/modules/`外＝ユニットテスト対象外。折り返し設定はwebview再読み込みで永続化されない（既存の`isRawMode`と同じ非永続方針）。**実際の折り返し表示・ガター非表示の見た目・クリック操作感は実機（拡張機能開発ホスト）確認前提** | `6ae850f` |
| 2026-07-23 | 機能: YAML front matter の折りたたみ表示（ROADMAP優先度:低の項目）。`markdown.js`の`markdownToHtml`本文パースループの先頭（`i===0`のときのみ）に`parseFrontMatter(rawLines)`（文書先頭が`---`で後続のどこかに`---`があれば`{raw, endIndex}`を返す純粋関数）を追加し、`buildFrontMatterHtml(raw)`が折りたたみ可能な`<div class="frontmatter"><div class="frontmatter-header" contenteditable="false">...</div><pre class="frontmatter-body">...</pre></div>`を構築する（YAMLはMarkdownのインライン記法を持たないため`convertInline`は通さず`escapeHtml`のみ）。既定は折りたたみ状態（`editor.css`で`.frontmatter-body`を非表示）、ヘッダクリックで`commands.toggleFrontMatter`（`state.editor`のクリックイベント委譲経由）が`frontmatter-expanded`クラスをトグルする（折りたたみ状態はUI表示のみで文書には影響しないため往復対象にしない）。直列化（`htmlToMarkdown`）は`DIV`の`.frontmatter`分岐で`.frontmatter-body`（`<pre>`のtextContentがそのまま生YAML）から`---\n...\n---`を復元する。HTML仕様上`<pre>`開始タグ直後の最初のLFはパース時に無視されるため、本文の先頭行が空行でも失われないよう常にダミーの改行を1つ先頭へ足している。**`/local-review`指摘（severity high、複数ラウンドにわたり段階的に検出・同サイクルで修正）**: (1) `extractFootnoteDefinitions`が`markdownToHtml`本文パースとは別に、脚注除去後の行配列（`contentLines`）に対して`parseFrontMatter`を再度呼んでいたため、文書の1行目が参照済み脚注定義行で、除去後にたまたま`---`が新たに先頭へ現れるケースで前後の判定が食い違い、通常の内容を誤ってfront matterと認識してしまう回帰があった→front matter判定を元の行配列（`rawLines`）に対して一度だけ行い、`extractFootnoteDefinitions(rawLines, frontMatterEndIndex)`（第2引数追加）と本文パースループの両方でその結果を共有する設計へ変更。(2) front matter内が偶然`[^label]: ...`形式に一致すると脚注定義として剥がされてしまう問題→`extractFootnoteDefinitions`が`frontMatterEndIndex`以下の行をコードフェンス・ブロック数式と同様に非scannableとして保護。(3) `insertToc`のフォールバック挿入位置（有効なキャレット位置が無い場合`state.editor.firstChild`の直前へ挿入）がfront matterの「文書絶対先頭」という制約を壊しうる問題→先頭が`.frontmatter`ブロックならその直後へ挿入するよう修正。**設計判断（一度実装したが撤回・データ消失リスクを優先した既知の仕様）**: 「本文0行（`---\n---`）」と「空行1行だけ（`---\n\n---`）」はどちらも生成される`raw`が空文字列になり区別できない問題に対し、当初`data-frontmatter-lines`属性でパース時の実際の行数を保持する対応を実装したが、`.frontmatter-body`は`contenteditable="false"`を持たずユーザーが自由に編集できるため、この属性は編集直後に実際の内容と食い違う陳腐化した値になり、直列化時にユーザーが入力した内容そのものを誤って捨ててしまう重大なデータ消失リスクがあることが再レビューで判明した→撤回し、保存のたびに「空行1行だけ」は「本文0行」へ正規化される仕様として受け入れることにした（既知の仕様としてコード内コメント・テストに明記）。**B（severity low、いずれもROADMAPへ低優先todo追加）**: ヘッダの折りたたみ/展開トグルがマウス操作限定でキーボード操作不可（既存の`.code-lang-selector`・数式クリック展開と同じ制約）／ヘッダラベルが英語表記（`Front Matter`）で他のUI文言と不統一／`parseFrontMatter`が内容を検証せず位置関係のみで判定するため、文書が水平線から始まり後方にも水平線がある通常のMarkdown文書を誤ってfront matterと認識しうる（実際のJekyll/Hugo等も同様に内容検証をしない設計のため妥当な面もある）。ユニットテスト多数追加（`parseFrontMatter`単体の全分岐、`markdownToHtml`/`htmlToMarkdown`の変換往復（本文複数行・空・空行1つ・YAML内の記号がインライン記法へ変換されないこと）、脚注・定義リストとの相互作用（front matter内の`[^label]:`形式行の保護、文書1行目が脚注定義行のケースでの誤認識防止）、`commands.toggleFrontMatter`のクリックトグル挙動、`insertToc`のfront matter先頭割り込み防止）。型/lint/compile・ユニット683件パス。`media/modules/`のみ＝統合テスト非対象。**実際の折りたたみ/展開のクリック操作感・見た目は実機（拡張機能開発ホスト）確認前提** | `956b7ca` |
| 2026-07-23 | 機能: 定義リスト（`用語`\n`: 定義本文`）のサポート（ROADMAP優先度:低の項目）。`markdown.js`の`markdownToHtml`本文パースループへ、リスト・引用と同様の連続行スキャンとして`scanDefListTerms(lines, i, seamIndices)`を追加。用語候補（見出し/hr/リスト/引用/テーブル開始行でない通常行）を連続する限り集め、直後に`: `定義行（`DEFLIST_DEF_PATTERN = /^:(\s*)(.*)$/`）が最低1つ続く場合のみ`<dl>`として確定する（続かなければ通常の段落等に委ねる）。複数の用語行が同じ定義群を共有するケース（`Term A`\n`Term B`\n`: 定義`）、1つの用語に複数の`: `定義行が続くケースの両方に対応し、`buildDefListHtml`が用語の数だけ`<dt>`・定義の数だけ`<dd>`を並べる。空行を挟まず前の行に続けて定義リストが始まると、その行も共有の用語として取り込まれる（Pandoc等の定義リスト拡張と同じ規則。分離したい場合は空行を挟む）。`:`直後の空白は`data-def-sep`属性へ保持し変換往復で元の個数（0個・1個・2個以上）を保つ（脚注定義行の`data-footnote-sep`と同型の設計）。**`/local-review`指摘（severity high、複数ラウンドにわたり段階的に検出・同サイクルで修正）**: 脚注定義行（`[^1]: ...`）は本文パース前に`contentLines`から除去されるため、除去後に前後の行が直接隣接し、本来無関係だった行同士が定義リストへ誤結合される回帰があった（例: `Term`\n`[^1]: 本文`\n`: Definition`で`[^1]: 本文`を除去すると`Term`と`: Definition`が隣接し誤結合）。**修正の経緯**: (1) 最初に「除去箇所へプレースホルダ文字を残し、それを他ブロック開始行として扱う」方式を試みたが、再レビューでリスト・引用・テーブル（各行が`-`/`>`/`|`等の記法で自己申告するため元々縫い目の影響を受けないはずのブロック）まで誤って分裂させる副作用が見つかり撤回（さらにこのプレースホルダの実装時、生のNUL制御バイトをソースへ直接埋め込んでしまいファイルがバイナリ判定される問題も発生し、あわせて解消）。(2) 最終的に`extractFootnoteDefinitions`が返す新フィールド`seamIndices`（`contentLines`中で「直前の行との隣接性が失われている位置」の集合）を導入し、これを参照するのは用語収集・複数`: `定義行の収集・グループ連鎖・段落継続ループの4箇所のみに限定した（リスト・引用・テーブルは各行自身の記法判定だけで完結するため縫い目の影響を受けず、除去前と同様に正しく結合される）。段階的なレビューで(a)用語↔単一定義行の誤結合、(b)複数`: `定義行の一部が縫い目をまたいで誤結合、(c)グループ連鎖が縫い目をまたいで誤結合、(d)段落継続が縫い目をまたいで誤結合、の4パターンをそれぞれ検出・修正した。最終ラウンドの`/local-review`はAなし。**B（severity low、いずれもROADMAPへ低優先todo追加）**: `utils.findBlockAncestor`が`DT`/`DD`を認識しないため定義リスト内では自動整形（`---`→`<hr>`化等）が無効／`buildDefListHtml`が`footnoteLabels`を渡さないため定義リスト内の`[^label]`は脚注参照へ変換されない（見出し・リスト・テーブルセルも同様の既存仕様）。ユニットテスト多数追加（`markdownToHtml`の用語/定義グループ各パターン・`scanDefListTerms`単体・`buildFootnotesSectionHtml`同様の属性出力確認・`htmlToMarkdown`のDLケース・変換往復・縫い目をまたぐ誤結合防止の複数パターン・リスト/引用が縫い目をまたいでも正しく結合されることの確認）。既存の脚注関連テストは`extractFootnoteDefinitions`の返り値形状変更（`seamIndices`追加・除去行の扱い変更）に合わせて更新。型/lint/compile・ユニット660件パス。`media/modules/`のみ＝統合テスト非対象 | `957aa9d` |
| 2026-07-22 | バグ修正（`/local-review`指摘由来）: 脚注ホバーツールチップの`mouseover`/`mouseout`が`<sup>`内側の`<a>`との出入りで再発火する問題の修正（ROADMAP優先度:中の項目）。`editor.js`の`setupFootnoteTooltipEvents`は`sup.footnote-ref`への`mouseover`をイベント委譲で監視しているが、`<sup>`とその内側の`<a>`の間でマウスが出入りするたびにバブリングで再発火し、300msデバウンス用の`clearTimeout`/`setTimeout`が毎回リセットされていた（機能停止には至らない軽微な非効率）。新設のモジュール変数`activeFootnoteRef`で「現在処理中のref要素」を追跡し、`mouseover`ハンドラは`ref === activeFootnoteRef`（同一refへの再入場）ならタイマー再設定をスキップするようにした。`hideFootnoteTooltip()`（実際にref外へ出た場合・スクロール時・外部更新時など、ツールチップを閉じる全経路の集約点）で`activeFootnoteRef`を必ずリセットするため、次の正当なホバーを取りこぼす状態異常は起きない。`/local-review`: A（correctnessバグ・回帰）なし。C（無関係な`.claude/settings.json`が同梱・コメント表現の精度に関する軽微な指摘、いずれもseverity low、確信度低）は対応不要と判断。`editor.js`はDOM/イベント配線のみで`media/modules/`外＝既存の慣習どおりユニットテスト対象外（`markdownEditor.ts`非変更のため統合テストも対象外）。型/lint/compile・ユニット632件パス（既存回帰なし）。**実際のホバー挙動（デバウンス解消の体感・複数脚注間の切り替え）は実機（拡張機能開発ホスト）確認前提** | `c7cca7f` |
| 2026-07-22 | バグ修正（`/local-review`指摘由来）: 脚注定義行（`[^label]: 本文`）のコロン直後の空白数が変換往復で1つに正規化される問題の修正（ROADMAP優先度:中の項目）。従来 `FOOTNOTE_DEF_PATTERN`（`markdown.js`）は`:`の後の空白を`\s?`（0または1文字のみ）で吸収していたため、`[^1]:  本文`のように空白が2つ以上あると余分な空白が本文側の先頭空白として残り、直列化時の`trim()`で失われていた（往復後は常に`[^label]: 本文`＝空白1つに正規化）。パターンを`:(\s*)(.*)$`へ変更し`:`直後の空白（0個・1個・2個以上いずれも）を独立した`sep`として捕捉するようにし、`extractFootnoteDefinitions`の返り値`def`へ`sep`フィールドを追加。`buildFootnotesSectionHtml`が`<li data-footnote-sep="...">`属性へその空白文字列をそのまま保持し（空白のみで構成されるため属性値として安全）、`htmlToMarkdown`のSECTIONケースが属性値を`[^label]:${sep}${text}`の形でそのまま差し戻すことで元の空白数を保つ（`data-footnote-sep`属性が無い＝この対応より前に生成されたHTML等は半角スペース1つへフォールバックし後方互換）。`/local-review`: A（correctnessバグ・回帰）なし。C（`data-footnote-sep`属性値がエスケープ無しで直接連結されており安全性は`FOOTNOTE_DEF_PATTERN`が空白のみしか捕捉しないという不変条件に依存する・severity low, 確信度中）は、既存のテーブル区切り書式保持`data-sep`属性と同じ設計方針であり対応不要と判断。ユニットテスト7件追加（`extractFootnoteDefinitions`のsep捕捉0/1個/3個・`buildFootnotesSectionHtml`の属性出力・変換往復3パターン）。既存の脚注関連テスト3件は新属性`data-footnote-sep`を含む期待値へ更新。型/lint/compile・ユニット632件パス。`media/modules/`のみ＝統合テスト非対象 | `80d42cb` |
| 2026-07-22 | 機能（**実機確認前提**）: 脚注参照（`[^label]`）の入力時ライブ変換（ROADMAP優先度:中の項目。前サイクルまでの脚注サポート`8a07c28`・ホバーツールチップ`bd0a94f`に続く対応）。これまで`markdownToHtml`（読込・Raw⇔プレビュー切替・保存後の再読込）でのみ`[^label]`を`<sup class="footnote-ref">`へ変換しており、`commands.js`の`convertInlineText`（太字・取り消し線・数式等が即時反映される入力中ライブ変換の経路）には未配線だった。`walkInline`が`root.querySelectorAll('section.footnotes li[data-footnote-label]')`で「既にエディタ内にレンダリング済みの脚注定義」からラベル集合を軽量収集し、対応する定義が存在するラベルの`[^label]`だけをその場で`<sup>`へ変換する（意図的なMVPスコープ: 同一セッションで参照・定義を両方新規入力した直後はまだ変換されず、保存/Raw切替/再読込を経て初めて変換される。定義がまだ無い参照はリテラルテキストのまま残す）。**`/local-review`指摘A-1（high）を同サイクルで修正**: 初版は脚注参照の変換をコード/数式/画像/エスケープ`$`の復元より後段に置いていたため（誤った思い込み: これらの復元がデリミタ無しの裸の数字マッチだと勘違いしていたが実際は`\u0000`〜`\u0003`で適切にデリミタ化されていた）、数式・画像の中身に`[^label]`（既存ラベルと一致する文字列）がたまたま含まれていると、既に復元済みの`data-math`/`src`属性値の途中に`<sup>`が誤って挿入されデータが破壊される不具合があった（例: `$a[^1]b$`＋既存の脚注ラベル`1`→`data-math`属性の途中に`<sup>`混入）。→他の記法と同じ方式（コード=`\u0000`, エスケープ`$`/数式/画像=`\u0001`〜`\u0003`で同様にデリミタ化していた既存3種はそのまま踏襲しつつ）脚注参照専用の連番プレースホルダへ一旦退避し、リンク変換の直後（画像・数式・エスケープ`$`の復元より前、`markdown.js`の`convertInline`と同じ位置関係）で退避、全復元が終わった最後に実HTMLへ差し戻すよう修正。**`/local-review`指摘A-2（high、再レビューで検出）を同サイクルで修正**: `markdown.js`の`markdownToHtml`は`footnoteInfo.labels`を段落（`<p>`）変換にしか渡しておらず、見出し・リスト項目・テーブルセル・引用・GitHubアラート本文はいずれも脚注参照を変換しない仕様（各タグ直下に本文が置かれ`<p>`で包まれないため）だったが、ライブ変換側はブロック種別を問わずエディタ全体のテキストノードを無条件に変換対象にしていたため、見出し等で`[^label]`を入力すると編集中はリンクに見えるのに保存・再読込で消える非対称な回帰があった→`nearestBlockTag(node)`で直近のブロック祖先タグを判定し、`P`の場合のみ脚注ラベル集合を渡すよう修正（`markdown.js`の段落限定仕様と挙動を一致させた）。**B（severity low、いずれもROADMAPへ低優先todo追加）**: 同一段落内で同じラベルを複数回参照すると`id="fnref-label"`が重複する（`markdown.js`側にも既存の同型挙動があり今回起因の新規バグではない）／`footnoteLabels`収集の`querySelectorAll`がほぼ毎キー入力で走る（軽量だが未キャッシュ）。ユニットテスト7件追加（既存の見た目確認3件・保存往復1件に加え、数式/画像/コード混在時の数字誤消費防止1件、数式内・画像URL内に`[^label]`相当の文字列が含まれても属性値を破壊しない再現テスト1件、見出し/リスト/表セル/引用内では変換しないことを確認する非対称防止テスト1件）。型/lint/compile・ユニット629件パス。`media/modules/`のみ＝統合テスト非対象。**実際のタイピング体感・見出し等での意図的な無変換の見た目は実機（拡張機能開発ホスト）確認前提** | `986346c` |
| 2026-07-22 | 機能（**実機確認前提**）: 脚注のホバーツールチップ表示（ROADMAP優先度:中の項目。前サイクルの脚注サポート`8a07c28`により前提解消済み）。脚注参照（`sup.footnote-ref`）にマウスを乗せると、少し待ってから対応する脚注定義の本文をポップアップ表示する。`commands.getFootnoteDefinitionText(editor, label)`（`li[id="fn-label"]`から戻りリンク`.footnote-backref`を除いたテキストを取り出す純粋寄りのDOM参照関数・ユニットテスト対象）＋`editor.js`の`showFootnoteTooltip`（`table.computeMenuPosition`を流用して参照要素の直下へ配置）。表示/非表示の配線（`setupFootnoteTooltipEvents`）は`mouseover`/`mouseout`のイベント委譲＋300msデバウンス（`mouseenter`/`mouseleave`はバブリングしないため）。**`/local-review`指摘（severity medium）を同サイクルで修正**: VS Code側からの外部更新（`handleUpdateMessage`が`state.editor.innerHTML`を丸ごと差し替える経路）でツールチップを閉じておらず、表示中に外部更新が来ると古い内容のまま残ったり、保留中の`mouseover`タイマーがdetachedになった旧`sup`要素に対して発火し`getBoundingClientRect()`がゼロ矩形を返して画面左上に誤表示される不具合があった→Raw⇔プレビュー切替の同種の全書き換え経路と同様に`hideFootnoteTooltip()`を追加。**B（severity low・ROADMAPへ）**: `<sup>`と内側の`<a>`の出入りで`mouseover`/`mouseout`が再発火しデバウンスタイマーがリセットされる軽微な非効率。ユニットテスト5件（`getFootnoteDefinitionText`の本文取得・装飾除去・未定義ラベル・null安全・複数脚注での該当ラベル特定）。型/lint/compile・ユニット622件パス。`editor.js`側のDOM/位置計算・イベント配線は`media/modules/`外＝ユニットテスト対象外（`markdownEditor.ts`非変更のため統合テストも対象外）。**実際のホバー表示位置・遅延の体感・外部更新時の挙動は実機（拡張機能開発ホスト）確認前提** | `bd0a94f` |
| 2026-07-22 | 機能: 脚注（`[^label]` / `[^label]: 本文`）のサポート（ROADMAP優先度:低の項目。優先度:中の「脚注のホバーツールチップ表示」の前提解消のため着手）。`本文[^1]`という参照と`[^1]: 脚注の本文`という定義行から成る脚注記法に対応。`markdownToHtml`が読込・Raw⇔プレビュー切替時に(1)定義抽出`extractFootnoteDefinitions`（コードフェンス・ブロック数式の中は除外）→(2)本文段落の`convertInline(text, footnoteLabels)`が参照`[^label]`を`<sup class="footnote-ref"><a href="#fn-label" id="fnref-label">label</a></sup>`へ変換（未定義ラベルはリテラルテキストのまま）→(3)定義を文書末尾の`<section class="footnotes"><ol>`一覧へ集約（脚注表示の一般的慣習・既知の仕様＝中程にあった定義は保存で末尾へ移動）。直列化（`htmlToMarkdown`）は`SUP`/`SECTION.footnotes`ケースを追加（`BLOCK_TAGS`に`SECTION`を追加）し`[^label]`/`[^label]: 本文`へ復元。参照・脚注一覧の`<a href="#fn-label">`/`<a href="#fnref-label">`は既存の`commands.handleLinkClick`（任意の`a[href^="#"]`をCtrl/Cmd+クリックで`scrollToAnchor`へ渡す汎用処理）にそのまま乗るため参照⇔定義間の追加配線は不要。ラベルの見た目は連番へ振り直さず入力した文字列をそのまま表示する簡易実装（`[^1]`→上付き"1"、`[^note]`→上付き"note"）。**`/local-review`指摘（severity high）を同サイクルで修正**: 初版は「参照の有無を問わずどんな行でも`[^label]: 本文`の見た目なら定義として剥がす」実装だったため、正規表現の説明文（`[^0-9]: 数字以外にマッチ`のような無関係な地の文）まで誤って脚注定義として扱ってしまう不具合があった→**同じラベルが文書内のどこかで実際に`[^label]`として（コード/数式ブロック外で）参照されている場合のみ**定義として確定するよう`extractFootnoteDefinitions`を2パス化して修正（あわせて`convertInline`側の参照変換にも`(?!:)`ガードを追加し、無効化された重複定義の`[^label]:`部分が誤って参照変換される副作用も解消）。**既知の制約（低優先度でROADMAPへ）**: 定義行のコロン直後の空白が2つ以上あると往復で1つに正規化される／入力中のライブ変換（`commands.convertInlineText`）には未配線でRaw切替・保存・再読込まで見た目が変わらない（脚注は文書全体の参照状態に依存するため意図的なMVPスコープ）。ユニットテスト19件（`extractFootnoteDefinitions`の参照ガード・フェンス/数式除外・重複ラベル、`convertInline`の参照変換・未定義ラベル、`SUP`/`SECTION`の直列化、往復6パターン、地の文の誤検知防止）。既存の`sample.md`全体往復テストに合わせ、追加したサンプル脚注定義もファイル末尾に配置。型/lint/compile・ユニット617件パス。`media/modules/`のみ＝統合テスト非対象。**脚注一覧の見た目・参照⇔定義間のCtrl/Cmd+クリック遷移の操作感は実機（拡張機能開発ホスト）確認前提** | `8a07c28` |
| 2026-07-22 | 機能: 表の矩形範囲選択への矩形貼り付け対応(3/3・完結)（ROADMAP優先度:中の先頭項目。「表の矩形選択とコピー＆ペースト」を(1/3, `38ca924`)(2/3, `f3d5800`)に続き完結）。(1/3)で導入した矩形範囲選択（`currentCellRange`）がこのテーブルに対して確定している場合、貼り付け先セルに関わらずその範囲の左上を起点に貼り付けデータを展開するよう`pasteData(startCell, table, text)`を拡張（範囲が無ければ従来どおり貼り付け先セル自身が起点＝無退行）。純粋関数`computePasteTargets(anchorRow, anchorCol, data, rowCount, colCount)`（アンカーからのオフセットで書き込み先の行・列を列挙し、`rowCount`/`colCount`を超える位置＝はみ出し分は結果に含めない＝行・列の自動追加はしない）と、それをDOMへ適用する`writeMatrixIntoTable(table, anchorRow, anchorCol, data)`（見出しセル`<th>`は個別にスキップ）へ分離。データの行列数が範囲より小さい場合は書き込まれなかった残りのセルを変更せず（Excel等と同じ・範囲を空欄でクリアしない）、大きい場合はデータ側の行列数がそのまま使われる（範囲はアンカー位置を決めるだけで敷き詰め先のサイズには使わない）。**旧`pasteData`実装は貼り付け起点が見出しセルだと貼り付け全体を無条件でno-opにしていたが、新実装は見出しセル自体だけをスキップし同じ貼り付けに含まれる本文行側へは書き込む（意図した挙動変更・回帰テストで固定）**。`/local-review`: Aなし（correctnessバグ・ラウンドトリップ破壊は見つからず）。**B（severity low、いずれもROADMAPへ低優先todo追加）**: `writeMatrixIntoTable`の列数算出（`rows[0]`＝見出し行のセル数）が全行同一セル数という暗黙の前提に依存を強めている（現状の生成・行列追加削除ロジックが保っている前提で今回のリグレッションではない）／行ごとに`Array.from(rows[row].cells)`を再生成する軽微な非効率。**C（判断が分かれる・低確信度）**: コメントが「既存の単一セル貼り付けの挙動を踏襲」としていたのが実際は意図した挙動変更だった点は同サイクルで表現を修正、範囲より小さいデータ貼り付け時に残りセルをクリアしない仕様（Excel等と同様の一般的挙動）はdocstringで明記。ユニットテスト14件（`computePasteTargets`全分岐＋`writeMatrixIntoTable`のth除外/はみ出し無視＋`pasteData`の範囲優先/見出し起点/別テーブル非干渉/ドキュメント反映）。既存の`pasteData`にはテストが一件も無かったため、単一セル起点の従来動作についても新規にテストを追加。型/lint/compile・ユニット598件パス。`media/`のみ＝統合テスト非対象。**Excel等からの実際の貼り付け・大きい/小さいデータでの見え方は実機（拡張機能開発ホスト）確認前提** | `ecb38fd` |
| 2026-07-21 | 機能: 表の矩形範囲選択をTSV＋text/htmlでクリップボードへコピー(2/3)（ROADMAP優先度:中の先頭項目。「表の矩形選択とコピー＆ペースト」Lを分割した2番目。(1/3, `38ca924`)で導入した矩形範囲選択に依存。(3/3)矩形貼り付けはROADMAPに残置）。`copy(table)`が`currentCellRange.table === table`ならその範囲だけを、無ければ従来どおりテーブル全体をTSV＋`text/html`（装飾・リンクを保った`<table>`断片）の両方でクリップボードへ書き込むようにした（範囲が無い場合の出力・トースト文言は変更前と同一＝無退行）。純粋関数へ分離: `extractCellTextMatrix(table, range)`（既存の`isCellInRange`を再利用してセルテキストを二次元配列で抽出）、`buildTsvFromMatrix(matrix)`（タブ・改行結合）、`buildHtmlTableFragment(table, range)`（`cell.innerHTML`をそのまま使い`<strong>`やリンク等の装飾を保つ）。書き込みは`writeToClipboard(tsv, html)`が`ClipboardItem`/`navigator.clipboard.write`の可用性をfeature-detectし、使える環境（Webview＝Chromiumベースなので通常は使える）では両方を、無い環境（jsdomでのテスト等）では従来の`writeText`（TSVのみ）へフォールバックする。**`/local-review`指摘A-1（medium）を同サイクルで修正**: `selectCell`は単一クリックで`currentCellRange`を解除するが、`addRow`/`addColumn`/`deleteRow`/`deleteColumn`はいずれも解除しないままだったため、「範囲確定→（単一セル選択を経ずに）行・列を増減→コピー」の順で操作すると、行・列インデックスがずれたstaleな範囲のままセルを抽出し誤った内容を黙ってコピーしてしまう不具合があった（再現: ①セルをクリックしてcurrentEditingCellを設定→②別セルへドラッグして範囲確定（ドラッグはclickを発火しないため①の選択は残る）→③ツールバー操作で行・列を増減）→新設の`invalidateRangeSelectionFor(table)`をこの4関数の冒頭で呼び、対象テーブルの範囲選択を無効化するよう修正（`selectCell`と同じ後始末パターン）。**B（severity low、いずれもROADMAPへ低優先todo追加）**: `buildTsvFromMatrix`がセル内のタブ・改行をエスケープしない（旧実装から存在する既存制約・今回のリグレッションではない）／クリップボード書き込み失敗トーストのテスト欠如（同サイクルでテスト追加済みのため解消）。**C（確信度低・対応不要と判断）**: `new ClipboardItem(...)`が同期的に例外を投げるブラウザ実装だと`copy()`の`.catch`が捕捉できない可能性（Chromiumでは通常発生しない）。ユニットテスト11件（純粋関数3つの全分岐＋range/別テーブル/stale範囲でのcopy分岐＋ClipboardItem利用可否での書き込み先切り替え＋失敗トースト）。型/lint/compile・ユニット584件パス。`media/`のみ＝統合テスト非対象。**実際のExcel等へのTSV貼り付け・text/htmlでの装飾保持・失敗時のトースト表示は実機（拡張機能開発ホスト）確認前提** | `f3d5800` |
| 2026-07-21 | 機能: 表のマウスドラッグによる矩形範囲選択とハイライト表示(1/3)（ROADMAP優先度:中の先頭項目。「表の矩形選択とコピー＆ペースト」Lを分割した基盤部分。(2/3)コピー・(3/3)貼り付けはROADMAPに残置）。既存の単一セル選択（`selectCell`）に加え、セルをクリックしたままドラッグして複数セルにまたがる矩形範囲を選択・ハイライト（`table-cell-range-selected`）できるようにした（現時点ではハイライトのみ・クリップボード連携は無し）。`table.js`の`setupCellRangeDrag(cell, table)`が各セルへ`mousedown`（主ボタンのみ）で開始セルの行・列（`cellPosition`が`table.rows`/`row.cells`のindexOfで算出＝既存の矢印キーナビゲーションと同じ考え方）を記録し、ドラッグ中（`e.buttons===1`）に別セルへ`mouseenter`すると`computeCellRange`（開始・終了の前後を問わず正規化）で矩形範囲を求め`applyRangeHighlight`が範囲内の全セルへハイライトを付与（既存の単一選択は解除）。`document`の`mouseup`（`setupRangeSelectionMouseUp`・`editor.js`のinitEditorで一度だけ配線）でドラッグ追跡のみ終了し、確定した範囲（`currentCellRange`）とそのハイライトは次のクリック・別範囲開始まで保持（将来の(2/3)コピーが参照できるよう`getCurrentCellRange()`を公開）。単なるクリック（mousedown/mouseupが同一セル）は別セルへの`mouseenter`が発生しないためこの配線は無反応＝既存の単一セル選択と競合しない。**`/local-review`指摘A-1（medium-high）・A-2（medium）・A-3（low-medium）を同サイクルで修正**: (A-1)複数テーブルを持つ文書で、表1に範囲確定→表2で新たに範囲確定すると表1側のハイライトが誰にも解除されず残留し、さらに表1のセルを単純クリックすると本来解除すべき表1ではなく`currentCellRange.table`（表2）を誤って解除してしまう不具合があった→`mousedown`時に`currentCellRange.table !== table`なら即座にその別テーブルのハイライトを解除するよう修正（clickは必ずmousedownの後に発火するため、単純クリック・ドラッグ開始のどちらの経路でも機能する）。(A-2)`.table-cell-selected`/`.table-cell-range-selected`が単体（1クラス）だと詳細度で`.table-cell:hover`（クラス+疑似クラス）に負け、ドラッグ中まさにマウスが乗っているセルだけホバー色に上書きされる問題→両クラスを`.table-cell`と複合させ詳細度を`:hover`と同等以上に揃えて修正。(A-3)`cleanup()`が新設の`table-cell-range-selected`を除去せず・モジュール状態もリセットしていなかった→除去対象とrange状態リセットを追加。**B（severity low、いずれもROADMAPへ低優先todo追加）**: `makeEditable`が行・列追加のたびに全セルへリスナーを多重登録する既存問題が今回の2種類のリスナー追加で悪化／`applyRangeHighlight`が大きな表でO(セル数×行数)。**C（確信度中・対応不要と判断）**: セル間のドラッグは通常のブラウザ文字列選択より矩形選択を優先する挙動になるが、Excel的なセル選択優先UXとして意図的な仕様と判断。ユニットテスト18件（純粋関数`cellPosition`/`computeCellRange`/`isCellInRange`の全分岐＋DOM配線のドラッグ・mouseup後の追跡終了・単純クリックとの非競合・複数テーブル間の非干渉2件＋`cleanup`でのクラス除去）。型/lint/compile・ユニット572件パス。`media/`のみ＝統合テスト非対象。**実際のドラッグ操作感・ハイライト色の見た目（`:hover`とのCSS優先順位を含む）は実機（拡張機能開発ホスト）確認前提** | `38ca924` |
| 2026-07-21 | 機能: 「/」入力によるNotion風コマンドメニューの表示（初期スコープ: 目次挿入・表の挿入。ROADMAP優先度:中の先頭項目）。空の段落で「/」だけを入力すると直後にポップアップメニューが出て、「📑 目次を挿入」「➕ 表を挿入…」を選べる（`mermaid`/`math`/`table`の各コンテキストメニューとCSSを共有）。判定は純粋関数`commands.isSlashCommandTrigger(block)`（可視テキストが「/」1文字だけ・`<br>`以外の子要素を含まない。`table.isBlockEmpty`と同じ考え方）。表示/非表示は`editor.js`の`setupSlashCommandMenuEvents`が`document`の`selectionchange`を監視して駆動（`setupRawMarkdownCaretEvent`と同じ設計で、入力・クリック・キャレット移動いずれでも追従する）。項目選択（`runSlashCommand`）は対象ブロック末尾へキャレットを合わせてから、既存の`commands.insertToc()`/`table.showInsertDialog()`（「キャレットのあるブロックの直後へ挿入」処理）に委譲。**`/local-review`指摘A-1（high）・A-2（medium）を同サイクルで修正**: 初版は挿入処理を呼ぶ前に無条件で「/」を消していたため、(1)表挿入ダイアログのキャンセル（Escape／キャンセルボタン）、(2)見出しが1つも無い文書での「目次を挿入」失敗、のどちらでも入力していた「/」が復元されず無言で消えるだけだった。**修正方針**: 「/」の除去を挿入が実際に成功した後にのみ行う。担保のため`commands.insertToc()`を実際に挿入できたかを`boolean`で返す契約へ変更（既存呼び出し元は戻り値を無視しているため後方互換）、`table.showInsertDialog(onInserted)`にOK確定（挿入成功）時にのみ呼ばれるコールバック引数を追加（キャンセルでは呼ばない・`onInsertConfirmed`＋`cancel()`ヘルパで一元化）。表挿入はダイアログが数値入力へフォーカスを奪うため、右クリックの挿入メニューが内部でやっているのと同じ「キャレット位置を`pendingInsertRange`へ退避→確定時に`restorePendingSelection`で復元」の仕組みを外部（スラッシュメニュー）からも使えるよう`table.setPendingInsertRange(range)`をエクスポート追加。A-3（low・`isSlashCommandTrigger`の非BR子要素分岐のテスト網羅性）も同サイクルで追加。**C（severity low・確信度中）**: `utils.findBlockAncestor`が`P`/`DIV`/`LI`タグでしか停止しないため、見出し・引用・テーブルセル内（例: 空の`<h1>/</h1>`）では現状「/」メニューがトリガーしない（初期実装の意図的スコープ限定と判断）→ROADMAPへ低優先todo追加。ユニットテスト19件（`isSlashCommandTrigger`全分岐＋`insertToc`の返り値2件＋`table.setPendingInsertRange`によるライブ選択上書きからの復元＋`showInsertDialog`コールバックの確定/キャンセル呼び分け＋後方互換の無引数呼び出し）。型/lint/compile・ユニット555件パス。`media/editor.js`（DOM/selectionchange依存部）は統合テスト非対象、`media/modules/commands.js`・`table.js`の純粋関数・契約変更部分はユニットテスト対象。**メニューの実際の表示位置（キャレット直後への出現・画面端での寄せ）・クリック操作感・「/」除去後の見た目は実機（拡張機能開発ホスト）確認前提** | `31003ef` |
| 2026-07-21 | 機能: スクロール時に現在位置の見出しをパンくず状に上部固定表示（ROADMAP優先度:中の先頭項目）。ツールバー直下・`#editor`の直前へ動的生成した固定バー（`editor.js`の`initHeadingBreadcrumb`。`markdownEditor.ts`非変更＝統合テスト非対象）が、スクロール（`scroll`イベントをrAFで間引き）・入力・外部編集（`handleUpdateMessage`）・Raw⇔プレビュー切替のたびに更新される。ロジックは3つの純粋関数（`commands.js`）へ分離してユニットテスト対象にした: `collectHeadings(editor)`（`h1`〜`h6`を出現順に`{level,id,text,el}`へ）、`findCurrentHeadingIndex(tops,scrollTop)`（各見出しの上端位置配列とスクロール位置から「スクロール位置以前の最後の見出し」＝いま見ている節のインデックスを求める。文書先頭でどの見出しも未通過なら`-1`）、`buildBreadcrumbChain(headings,currentIndex)`（そこから手前へ辿ってレベルが単調に小さくなる見出しだけを祖先として積み上げる。レベル飛び＝存在するものだけを並べる）。`el.getBoundingClientRect()`を使う実際の位置計算は`editor.js`側（DOM/レイアウト依存でテスト対象外）。各パンくず項目をクリックすると`commands.scrollToAnchor('#'+id)`で該当見出しへ遷移。**`/local-review`指摘A-1（medium）を同サイクルで修正**: `execCommand('formatBlock')`による見出し化やライブ変換直後の見出しは`markdownToHtml`の再パースを経るまで`id`を持たないため、パンくず項目がクリック可能な見た目（cursor/hover）になるのに実際は無反応というサイレントな不具合があった→`id`があるものだけ`heading-breadcrumb-item-clickable`クラスでcursor/hoverを分離して修正。B（3件・いずれもseverity low）: 入力イベントでの更新が行番号ガターと違いデバウンスされていない／スクロールのたびにバーDOMを毎回全消去→再構築（差分描画なし）／パンくず項目がキーボード操作不可（`<span>`+click のみで`tabindex`/`role`無し）は、いずれもROADMAPへ低優先todoとして追加。ユニットテスト12件（純粋関数3つの全分岐・jsdomレルムの配列は`Array.from`でNodeレルムへ正規化してから比較）。型/lint/compile・ユニット544件パス。**スクロール時の実際の表示位置・体感パフォーマンス・パンくずクリックの遷移感は実機（拡張機能開発ホスト）確認前提**（`getBoundingClientRect`ベースの位置計算はjsdomでは検証できないレイアウト依存処理のため） | `ca1da0f` |
| 2026-07-21 | バグ修正: 触っていない行の表区切り `\|---\|`→`\| --- \|` が勝手に書き換わる問題（ユーザー報告・箇条書きマーカー側は対応済み・本項目で表側も解消）。**原因**: `serializeTable` が区切り行を常に `\| --- \|` 形式（アライメントコロンも `---` へ潰す）で出力していたため、表を一切触っていなくても書き戻しで区切り行の表記（スペース有無・`:---`/`---:`/`:---:`）が失われていた。**修正**: `markdownToHtml` の表パース時に区切り行を（trimせず）列ごとの生テキストへ分解する `splitSeparatorRow` を追加し、列数がヘッダーと一致する場合のみ元表記を `<table data-sep="...">`（カンマ区切り。区切り行の許可文字は空白・`-`・`:`のみでカンマを含まないため属性値として安全）へ保持。`serializeTable` はこれがあり列数が一致していればそのまま復元し、無い／列数不一致（列追加・削除後）はデフォルト `---` 書式へフォールバック（新規生成テーブルや構造変更後は非退行）。**回帰テスト**: コンパクト（`\|---\|`）・左/右/中央アライメント（`:---`/`---:`/`:---:`）の各バリアントで往復バイト一致・列数不一致時のフォールバックを追加。`/local-review`: A なし。C-1（列追加・削除で列数が変わると他列の書式も含め丸ごとデフォルトへ落ちる＝UX上は部分保持の余地。severity low, PLAUSIBLE）→ROADMAPへ低優先todo追加。C-2（`data-sep` 属性値の未サニタイズ埋め込み＝現状は許可文字が空白/-/:のみで到達不能・実害なしと判断）→対応不要。型/lint/compile・ユニット534件パス。`media/modules/` のみ＝統合テスト非対象 | `5d5c574` |
| 2026-07-21 | バグ修正: 触っていない行の箇条書き `- ` が `* ` に勝手に書き換わる問題（ユーザー報告・`631060f` 起票の箇条書き側）。**原因**: Webview→ドキュメント書き戻しは表示中の全ブロックを `htmlToMarkdown` で再直列化するが、`serializeList` が箇条書きマーカーを常に `* ` で出力していたため、`- ` で書いた既存行が編集と無関係に `* ` へ正規化されていた。**修正**: `markdownToHtml` のリストパースが元マーカー（`-`/`*`）を各 `<ul>` の `data-marker` 属性に保持（`buildListHtml`）し、`serializeList` がそれを尊重して復元（無い/不正値は従来どおり `*`＝新規入力・execCommand 生成の ul の既定を維持）。`getCleanHtmlFromEditor` は `data-marker` を除去しないため書き戻し経路で有効。**回帰テスト**: `- `/`* `/ネスト/タスクリストの各バリアントで往復バイト一致を検証（既存の往復テストは正規化後の形で見ており本不具合を検出できていなかった）。既存の markdownToHtml 出力アサーション2件を `data-marker` 付きへ更新。`/local-review`: A なし／B（表区切り `\|---\|`→`\| --- \|` の側は未対応＝ROADMAP に残置）／C-1（空行なしのマーカー混在 `- a\n* b` は先頭マーカーへ統一＝非退行の既知制約・現状挙動固定テストを追加）・C-2（`+` はパーサーがリスト認識しないため往復不変＝非該当）。型/lint/compile・ユニット527件パス。`media/modules/` のみ＝統合テスト非対象（書き戻し経路は実機で目視確認推奨） | `d74c1ba` |
| 2026-07-20 | 機能(2/2・**実機確認前提**): ローカル相対パス画像 `![](path)` をエディタ内で実表示。(1/2, `c1007b2`)の画像記法⇔`<img>`往復に続く後半。**拡張機能側**（`markdownEditor.ts`）: ドキュメントのフォルダを `webview.options.localResourceRoots` に追加し、そのフォルダの webview URI（`asWebviewUri`・file スキームのみ）を `update` の `imageBaseUri` で渡す。**Webview側**（`editor.js` の `resolveLocalImages`）: 各描画後に `<img>` を走査し、`markdown.isResolvableRelativeImageSrc`（スキーム付き/プロトコル相対/ルート絶対は false）が true の相対 src を、元パスを `data-original-src` に退避してから `markdown.resolveImageSrc`（`base + '/' + rel`・`./`除去・`#`/`?` エンコード・`%`非再エンコード）で解決した webview URI へ差し替える。`data-original-src` があれば再解決せず、`serializeInline` はそれを優先＝**往復は元相対パスで不変**・`incoming===current` で再レンダリングループなし。CSP は変更不要（`img-src ${webview.cspSource}` が asWebviewUri ホストを許可）。**`/local-review` A-1（low）を同サイクルで修正**: `resolveImageSrc` が `#`/`?` を素通しし表示が壊れる問題→エンコード。B-1（陳腐化コメント）も更新。C-1（`../` 相対の URL 正規化）・C-2（Windows `\`）は報告のみ。純粋関数2つをユニットテスト・統合12件パス（openEditor が新経路）・ユニット523件。**実機で 相対パス（同フォルダ/`assets`/`../`）・`data:`・貼り付け画像の表示と往復を確認**。`src/markdownEditor.ts` 変更＝統合テスト対象 | `52bef01` |
| 2026-07-20 | 機能(1/2・**実機確認前提**): 画像記法 `![alt](url)` の Markdown⇔HTML 対応（`<img>` レンダリング＋往復）。「ローカル画像のインライン表示」M を分割した前半（`media/modules/` のみ・純粋変換）。`markdown.js` の `convertInline`（読込パス）と `commands.js` の `convertInlineText`（ライブ変換パス）に `![alt](url)`→`<img src alt>` を**リンクより先に**追加（`![` を `!`＋リンクに割らない）、`serializeInline` に `<img>`→`![alt](url)`（`data-original-src` 優先＝2/2でsrcをwebview URIへ差し替えても元パスで往復）。**`/local-review` A-1（medium）を同サイクルで修正**: 生成した `<img>` を素のHTMLで挿すと URL/alt 中の `_`/`*`/`~`/`+` が後続の強調変換で `src="a<em>b</em>c"` のように壊れ往復が崩れるため、インラインコード・数式と同じく**プレースホルダ（`U+0003`）へ退避→強調変換後に復元**する保護を両パスへ追加。ユニットテスト（往復・属性エスケープ・`data-original-src`優先・`_`等を含むパスの非破壊・ライブ変換で img 化＆リンクと区別・通常リンク回帰）を追加。型/lint/compile・ユニット518件パス。B-1（タイトル記法 `![](url "title")` 未対応＝画像/リンク共通の既存制約）は ROADMAP へ。C-1（IMGシリアライズが `]`/`)` を含む属性を非防御）・C-2（空srcのimgは無音欠落）は生成名が安全なため報告のみ。**残り(2/2)＝ローカル相対パス画像の実表示（CSP/localResourceRoots/baseUri。現状は壊れた画像アイコン）は ROADMAP に残置**。`media/modules/` のみ＝統合テスト非対象。実機で `data:`/表示可能URL画像の描画と往復を確認 | `c1007b2` |
| 2026-07-20 | 機能(2/2・**実機確認前提**): 画像のクリップボード貼り付け — Webview側の paste 検出→保存要求→`![](path)` 挿入。(1/2, `3f52b0f`)の拡張機能側に続く後半。`editor.js` の paste ハンドラで、**`text/plain` を持たない**クリップボード（＝スクショ等の純粋な画像。Excel等のテキスト併載は従来の表/テキスト貼り付けを優先）に限り `commands.findClipboardImageItem`（`type` が `image/` で始まる最初の item）で検出→`FileReader`→base64→`saveClipboardImage` を postMessage。貼り付け時のキャレット範囲を `pendingImagePasteRange` に控え、応答 `insertImagePath` で範囲を復元（今もエディタ内に生きている場合のみ）→`commands.insertImageMarkdown(path)` で挿入。`buildImageMarkdown` は alt 空・パスの空白/丸括弧をエンコード。画像記法はレンダリング対象外でリテラルテキストとして入り往復不変（リンク正規表現は alt 必須で `![]()` はリンク化されない）。**`/local-review` C-1（medium）を同サイクルで修正**: 初版は `image/*` があればテキストより先に横取りし、Excel等の「テキスト＋画像」併載で表貼り付けが画像保存に化ける回帰があったため、`text/plain` が無いときだけ画像扱いへ変更。C-2（応答が来ない場合の範囲残存）も復元前の生存確認で対処。B-1（`buildImageMarkdown` の Markdownアクティブ文字未エンコード）と**ローカル画像のインライン表示**は ROADMAP へ。ユニットテスト9件・型/lint/compile・ユニット510件パス。`media/`（`editor.js`・`commands.js`）のみ＝統合テスト非対象。**実機で スクショ貼り付け→保存→挿入→保存Markdownの往復を確認**する必要あり | `7669d23` |
| 2026-07-20 | 機能(1/2・**実機確認前提**): 画像のクリップボード貼り付け — 拡張機能側の保存＋相対パス返却。L項目を分割した前半。Webview からの `saveClipboardImage`（`{ base64, mime }`）を `markdownEditor.ts` で受け、編集中ドキュメントと同じフォルダ（`document.uri` 基準）へ一意名（`image-<yyyyMMdd-HHmmss>.<ext>`・衝突時 `-1`,`-2`…）で書き込み、`insertImagePath`（POSIX区切りの相対パス）を postMessage で返す。保存は既存 `saveMermaidPng` と同方式（`atob`→`Uint8Array`→`workspace.fs.writeFile`）だがダイアログ無し。未保存（untitled/非file）は警告して中断・空base64は無視・`readDirectory` 失敗は衝突チェック無しで続行。純粋ロジックを vscode 非依存の新規 `src/imagePaste.ts`（`mimeToExtension`/`buildImageFilename`/`disambiguateFilename`/`toMarkdownRelativePath`）へ分離しユニットテスト11件、統合テスト3件（実書き込み・別名保存・untitled中断）を追加。型/lint/compile・ユニット502・統合12件パス。`/local-review`: A なし／B（medium・Webview結線が未実装でバックエンドは現状到達不能）は**意図した(1/2)分割**〈(2/2) は ROADMAP に残置〉／C-1（衝突統合テストの非決定性）はテスト名を実保証へ修正＋`-1`連番はユニットで決定的検証／C-2・C-3 は報告のみ。**(2/2)=Webview側 paste 検出→保存要求→`![](path)` 挿入・実機での貼り付け〜往復確認は残置**。`src/markdownEditor.ts` 変更＝統合テスト対象 | `3f52b0f` |
| 2026-07-20 | 機能(**実機確認前提**): 表の挿入UI（空行の右クリック→行数・列数ダイアログ）。中核（`buildEmptyTableMarkdown`／`insertTable`、`78b0295`）に対する呼び出しUIの配線。`table.js` に `setupContextMenu(editor)` を追加し `editor.js` init で数式メニューの後に配線。`contextmenu` を監視し**空行・エディタ余白**でのみ自前メニュー「➕ 表を挿入…」を出す（表示可否は純粋関数 `shouldShowInsertMenu`＝`findExcludedAncestor`〈数式/Mermaid/既存テーブル上は対象外〉＋`isBlockEmpty`〈テキスト無し・`<br>`以外の要素を含まない〉）。メニュー→行数・列数ダイアログ（`prompt()` 不可のため `.link-dialog` 系で動的生成・`markdownEditor.ts` 非変更）→OKで `clampTableDimensions`（空/非数値/0以下→1・上限 行50/列20）を通し `insertTable(rows, cols)`。挿入位置は右クリック時の `caretRangeFromPoint` を `pendingInsertRange` に退避し確定直前に選択へ復元。CSSは `.table-context-menu`/`.table-menu-item` を `.mermaid-context-menu`/`.math-context-menu` 系と同ルールへ追加。**`/local-review` A-1（medium）を同サイクルで修正**: 初版は本文全域で `preventDefault` し既定メニュー（貼り付け・スペル修正等）を潰す回帰があったため、ROADMAP 意図どおり空行・余白限定へ絞り本文テキスト上は既定メニューを尊重。C-1（`pendingInsertRange` 挿入後クリア）・C-2（キャンセルボタン上のEnterはキャンセル）も対応。B-1（ダイアログが body 直下で画面右上に出る＝位置基準ずれ）は ROADMAP 低優先へ。ユニットテスト16件・型/lint/compile/ユニット491件パス。**メニュー/ダイアログの見た目・操作感・右クリック位置への挿入は実機（拡張機能開発ホスト）確認前提**。`media/` のみ＝統合テスト非対象 | `018eb37` |
| 2026-07-20 | バグ修正(**実機確認前提**): 透過背景でコピーした数式画像の文字色（自動・黒・白）を選べるように。前サイクルの `/local-review` B-1 由来（バグ修正セクション最上位 todo）。ブロック数式PNGコピーは背景（透過/白/黒）を選べたが文字色は白・透過背景で黒固定（`blockToPngBlob` の `wrapper.style.color`）で、**透過PNGをダークな資料/チャットへ貼ると黒文字が埋もれて見えない**問題。右クリックメニューに**文字色トグル**（`.math-fg-btn` の `data-fg="auto|black|white"`・既定 `auto`）を背景トグル下へ追加。両行は新ヘルパ `buildToggleRow(label, btnClass, dataAttr, options)` で生成しクリックで**自行内 active のみ**移す（背景選択と文字色選択が独立）。文字色は純粋関数2段: `resolveMenuTextColor(menu)`（`black`/`white` 以外は `auto`）でモードを読み、`resolveTextColor(background, textColor)`（`black`/`white` はその16進、`auto` は背景追従＝黒背景のみ白）で決めて `wrapper.style.color` へ与える。`copyBlockAsPng(block, background, textColor)`→`blockToPngBlob(block, undefined, background, textColor)` を配線。**既定（背景=白・文字=auto）は従来と同一出力（無リグレッション）**＝前サイクルの黒背景白文字反転も `auto` の背景追従として維持。CSS は `.math-fg-btn` を既存 `.math-bg-btn`/`.mermaid-bg-btn` と同ルールへ追加。`/local-review` 指摘: A/B なし。C-1（`buildToggleRow` が両行に `math-menu-bg-row`/`-label` の「bg」命名を流用＝Mermaid と共有する既存CSSクラスの意図的再利用のため据え置き）・C-2（`resolveTextColor` の未知値は既に auto 分岐へ落ちる＝誤検知）は対応せず報告のみ。ユニットテスト8件（文字色トグルの既定active・行独立・`resolveMenuTextColor`/`resolveTextColor` 全分岐・透過＋白指定でSVGに rect 無し＆白文字が伝播）。型/lint/compile/ユニット473件パス。**透過＋白文字のダーク環境での見え方・メニュー操作感は実機（拡張機能開発ホスト）確認前提**。`media/` のみ＝統合テスト非対象 | `72f488e` |
| 2026-07-20 | 機能(**実機確認前提**): ブロック数式コピー画像の背景色（透過・白・黒）を選べるUI配線。基盤（`blockToPngBlob(block, scale, background)`）は実装済みで、残っていた呼び出しUIを配線した。ブロック数式の右クリックメニュー（`math.js` の `ensureContextMenu` が動的生成・`markdownEditor.ts` 非変更）はこれまで「📋 画像としてコピー」の1項目のみで常に既定の白で出力していた。メニュー上部に背景トグル（`.math-bg-btn` の `data-bg="transparent|white|black"`・既定 `white` が `active`・クリックで active を移すのみ＝メニューは閉じない）を追加し、「画像としてコピー」実行時に純粋関数 `resolveMenuBackground(menu)`（`.math-bg-btn.active` の `data-bg` を読む・`transparent`/`black` 以外は `white`。Mermaid の同名関数と同契約）で背景を解決して `copyBlockAsPng(block, background)`→`blockToPngBlob(block, undefined, background)` へ渡す。**`/local-review` 指摘 A-1（high）を同サイクルで修正**: 新規追加した「黒」背景は KaTeX が色を明示せず `currentColor` を継承するため採寸ラッパーの `wrapper.style.color` が黒固定＝黒地に黒文字で数式が不可視だった。`wrapper.style.color = (bg==='black')?'#ffffff':'#000000'` で黒背景時のみ文字色を白へ反転（foreignObject 経路・html2canvas フォールバックの両方を1行で修正）。CSS は `.math-menu-bg-row`／`.math-menu-bg-label`／`.math-bg-btn` を既存 `.mermaid-*` と同ルールへ追加。**既定は白＝背景トグル追加前と同一出力（無リグレッション）**。ユニットテスト7件（トグルの既定active・クリックでの active 遷移とメニュー非クローズ・`resolveMenuBackground` 全分岐＋null防御・黒選択でSVG `fill="black"`＋文字色白への反転／白選択で `fill="white"`＋黒文字の伝播）。型/lint/compile/ユニット465件パス。`/local-review` B-1（透過背景＋黒文字はダーク環境貼付で視認性低）は既存・別課題として ROADMAP バグ修正へ、C-1（配線の end-to-end テスト欠如）は追加した伝播テストで担保。**PNGの透過/白/黒の見た目・黒背景での白文字・クリップボード結果・メニュー操作感は実機（拡張機能開発ホスト）確認前提**。`media/` のみ＝統合テスト非対象 | `531757f` |
| 2026-07-20 | バグ修正: ブロック数式コピー画像の崩れ修正 — foreignObject 経路の canvas taint 実機確認と、確認過程で見つかった残存崩れの修正。**実機確認**: headless Chrome（実 Chromium エンジン。VS Code webview と同じ Chromium ベース＝canvas taint はレンダラ本体の挙動でありElectron埋め込みに固有ではないため妥当な代替）で `blockToPngBlob` を直接呼び出し、`∫_{-∞}^{∞} e^{-x²} dx = √π` を実際にPNG化して確認した結果、`foreignObject rasterize failed, falling back to html2canvas` の警告は一度も出ず＝**canvas taint は発生せず foreignObject 経路が毎回成功**（ROADMAPのtodoが警告していた懸念は杞憂と判明）。ただし生成画像を見本と比較すると別の崩れが残っていた: (1) CSP に `connect-src` が無く `loadKatexFontFaceCss` の woff2 fetch が全滅→フォント埋め込み無しで代替フォント描画（∫が細く小さく見えるなど見た目劣化）、(2) `wrapper.outerHTML`（HTML直列化）だとKaTeXが√記号に使うネストした`<svg>`にxmlnsが付かず、`data:image/svg+xml`としてXML解釈されるforeignObject内でSVGと認識されず**√記号が消える**、(3) `.katex-display`の既定`margin:1em 0`で上下余白が過大。対処: CSPへ`connect-src ${webview.cspSource}`追加（[markdownEditor.ts](../src/markdownEditor.ts)）／`outerHTML`を`XMLSerializer`直列化（新関数`serializeNodeToXhtml`）へ差し替え／クローン採寸前に`.katex-display`のmarginを0にリセット。修正後は見本と同等の見た目（√π・正しいKaTeXフォント・適切な余白）をheadless Chromeで再確認。回帰テスト2件追加（ネスト`<svg>`のxmlns保持）。既存456→458件パス・型/lint通過。**headless Chromeでの検証であり文字通りの拡張機能開発ホストではない点は留意**（Chromium統一エンジンのため技術的には同等の代替と判断してtodoをclose）。`src/markdownEditor.ts`変更＝統合テスト対象 | `d6ba754` |
| 2026-07-20 | バグ修正（**実機確認前提**）: Rawモードの行番号が下の行でずれ「一部の行に番号が出ない」問題。原因はガター（`.raw-line-gutter` の div）と textarea の行高が unitless `1.6`（=22.4px の小数）で、textarea のネイティブ行描画と div の行送りが端数の扱いで一致せず行を重ねるほど食い違って蓄積していたこと（番号生成 `utils.countLines`/`buildLineNumberText` の論理自体は正しく `utils.test.ts` で検証済み＝レイアウト側の問題）。`#rawEditor` と `.raw-line-gutter` の `line-height` を整数px（`22px`）に固定して行送りを完全一致させた（`media/editor.css` のみ）。`search.js` の `parseInt(lineHeight)` は 22.4→22／22px→22 で同値のため回帰なし（`/local-review` 確認）。CSSのみ＝ユニットテスト不能・型/lint/compile/既存ユニット453件パス。`/local-review` 指摘: A なし。B（Raw=px/WYSIWYG=unitless の二分）・C（22px は font-size 14px 前提）は害小として対応せず。**行番号と実際の行の一致・スクロール追従は実機（拡張機能開発ホスト）確認前提**。`media/` のみ＝統合テスト非対象 | `f2e684c` |
| 2026-07-20 | 機能(2/2・**実機確認前提**): Mermaid図の画像コピー／保存時に背景色（透過・白・黒）を選べるUI配線。中核(1/2, `9e8ea82`)の背景解決（`resolveImageBackground`＋`svgToPngBlob(svg, scale, background)`）に対する呼び出しUI。Mermaid右クリックメニュー（`#mermaidContextMenu`, `src/markdownEditor.ts`）上部に背景トグル（`.mermaid-bg-btn` の `data-bg="transparent|white|black"`・既定 `white` が `active`）を追加し、「画像をコピー」／「PNG画像として保存」実行時に選択背景を `copyToClipboard`/`saveAsPng` の第2引数へ渡す。配線は `mermaid.js` の `setupContextMenuEvents`（背景ボタンで `active` 移動・メニューは閉じない／アクション時に純粋関数 `resolveMenuBackground(menu)`＝`.mermaid-bg-btn.active` の `data-bg` を読む・`transparent`/`black` 以外は `white` で解決）。**既定は白＝無リグレッション**。背景選択はメニュー持続で直近選択を記憶。CSS（`.mermaid-menu-bg-row`／`.mermaid-bg-btn`）追加。ユニットテスト5件＋統合テスト9件パス。`/local-review` 指摘: A なし。C-1（背景選択が右クリック間で持続＝仕様判断）・C-2（アクション→背景引数の end-to-end テストは内部関数直呼びのため未追加＝個別ユニットで担保）は対応せず報告のみ。**PNGの透過/白/黒の見た目・クリップボード/保存・メニュー操作感は実機（拡張機能開発ホスト）確認前提**（特に html2canvas が透過時に白を焼き込まないか）。`src/markdownEditor.ts` 変更＝統合テスト対象 | `7a1f50e` |
| 2026-07-20 | バグ修正（配線・**実機確認前提**）: ブロック数式コピー画像の崩れ修正の foreignObject 方式への配線。`blockToPngBlob` を html2canvas 直接ラスタライズから `<foreignObject>`（ブラウザネイティブ描画）方式へ差し替え、html2canvas が再現できない `.katex` のCSS配置を正確に画像化する。(a) ラッパー実寸採寸→(b) `loadKatexFontFaceCss`（`KATEX_FONT_MANIFEST` の woff2 を `resolveKatexFontBaseUrl` で導いた `fonts/` から fetch→`arrayBufferToBase64`→`buildKatexFontFaceCss`。全成功時のみキャッシュ・部分失敗はリトライ）→(c) `collectKatexCssText`＋フォントCSSを `buildMathBlockSvgMarkup` でSVG化→(d) `svgMarkupToPngBlob`（`data:image/svg+xml`→Image→canvas→PNG・15秒ロードタイムアウト保険）。CSP へ `font-src … data:` を追加（`src/markdownEditor.ts`）。**foreignObject 失敗時は html2canvas フォールバックで無リグレッション**。ユニットテスト15件＋統合テスト9件パス。`/local-review` 指摘対応: A-1（フォント取得オーケストレーションのテスト欠如）→`loadKatexFontFaceCss` を export し fetch スタブで4件追加／B-1（部分失敗の恒久キャッシュ）→全成功時のみキャッシュへ修正／C-3（クエリ付きhref）→テスト追加。**⚠️ C-1（高・確信中〜高）＝ Chromium が foreignObject 入り SVG を canvas 描画時に taint し `toBlob` が SecurityError を投げる既知挙動により、実機で新経路が毎回フォールバックへ落ち崩れ修正が発現しない恐れ**。B-2（フォールバックの黒/色背景マッピングは現状 white 固定呼び出しのため未到達）・C-2（`font-src data:` は画像コンテキストSVGに親CSPが効かず不要な可能性）・C-4（CSSの`<style>`無エスケープのXML妥当性）は害小として対応せず（フォールバックが担保）。**実機確認**: 数式右クリック「画像としてコピー」で崩れが直るか／`falling back to html2canvas` 警告で taint しているかを確認。taint なら MathJax 切替 todo（ROADMAP）へ。`src/markdownEditor.ts` 変更＝統合テスト対象 | `c1741c6` |
| 2026-07-19 | バグ修正の基盤（配線前段）: ブロック数式コピー画像の崩れ修正（foreignObject 移行）に向けた**KaTeXフォント埋め込みの純粋コア**。foreignObject を `<img>` でラスタライズすると SVG が隔離コンテキストで描画され外部フォントを参照できないため、KaTeX の woff2 を base64 で `@font-face` として SVG 内へ埋め込む必要がある。`math.js` に純粋関数 `buildKatexFontFaceCss(fonts)`（base64 化 woff2 配列→`src:url(data:font/woff2;base64,…)` の `@font-face` 群を組み立て。`family` の引用符/バックスラッシュ除去・`style` は italic 以外→normal・`weight` は3桁数値以外→400・`base64` は base64 アルファベット以外を除去し空はスキップ、と防御的に正規化）と、埋め込む20フォントの静的一覧 `KATEX_FONT_MANIFEST`（family/style/weight＋ファイル名）を追加。**既定挙動は不変**（`blockToPngBlob` は html2canvas のまま＝無リグレッション）。ユニットテスト10件（`@font-face` 組み立ての全分岐＋マニフェストの各 woff2 がディスク実在＋`katex.min.css` の `@font-face` 群と過不足なく一致する網羅テスト）。`/local-review` の指摘（C-1 base64 の非対称なサニタイズ欠如／C-2 CSS網羅性テストの欠如）を同サイクルで反映（網羅テストは追加時に jsdom レルム配列の deepStrictEqual プロトタイプ不一致を検知し `Array.from` で修正）。**残り＝ランタイム配線（`blockToPngBlob` の foreignObject 差し替え・フォント fetch→base64・CSP へ `font-src … data:` 追加）は統合テスト対象サイクル＋実機確認前提**で ROADMAP に残置。`media/modules/` のみ＝統合テスト非対象 | `ecf34a5` |
| 2026-07-19 | バグ修正（根本対策の完結）: 「編集中にキャレットが飛ぶ」＝一連の根本対策の**最後の一手**として、innerHTML 全書き換え経路（`handleUpdateMessage`）で **Mermaid の非同期再描画完了を待ってからキャレットを復元**するようにした。従来は `setTimeout(0)` で復元しており、`mermaidModule.render()`（Promise を返す async）の描画完了前に走るとブロックの高さ・ノード構成が未確定でアンカー（ノード基準／ブロック基準・署名）がずれる余地があった。純粋ヘルパ `utils.restoreCaretAfterRender(renderPromise, restoreFn)` を追加し、Promise の**解決（または失敗）を待って**から復元・Promise でない値は従来どおり `setTimeout(0)` 相当・描画失敗時も DOM 構造は確定しているため復元を試みる（後方互換）。復元はブロック署名・オフセット（DOM構造）ベースでピクセルレイアウト非依存のため Promise 解決後のマイクロタスクで安全。これで応急処置（`cb86e8c`）＋ノード基準（`225ca19`）＋ブロック基準（`132bd22`）＋署名（`8750ec2`）＋競合エコー無視（`e60bee2`/`1aca68d`）に続く一連の対策が完結。ユニットテスト4件（Promise解決後に復元・reject時も復元・非Promiseはマクロタスク復元・不正コールバックで例外を出さない）。`/code-review` は当ループから起動不可（ユーザー起動専用・`disable-model-invocation`）のため手動レビューで代替。**高速入力・数式/Mermaid編集時のキャレット挙動の最終確認は実機（拡張機能開発ホスト）確認前提**。`media/`（`editor.js`・`modules/utils.js`）のみ＝統合テスト非対象 | `e877965` |
| 2026-07-19 | バグ修正（根本対策の一部）: 「編集中にキャレットが飛ぶ」＝ブロック基準アンカー（`132bd22`）の弱点だった**ブロックの挿入・並べ替えで `blockIndex` がずれる**問題への対処。全書き換えでキャレットのブロックより前に別ブロックが挿入されると `blockIndex` が別ブロックを指し、その中へ誤復元していた。`utils.saveCursorPosition` がブロックの**署名**（先頭テキスト64文字・空白畳み込み・純粋ヘルパ `utils.blockSignatureOf`）`blockSignature` も保持し、`utils.restoreCursorPosition` のブロック基準復元段で署名が**一意一致**するトップレベルブロックを優先。一致無し（キャレットのブロック自体が編集され署名が変化）／複数一致で曖昧なら従来どおり `blockIndex` で引く（後方互換・既存テスト全通過）。ユニットテスト5件（署名ヘルパの空白畳み込み/64字丸め/null・前挿入でindexずれ時の署名一致復元・キャレットブロック編集時のindexフォールバック）。`/code-review` は当ループから起動不可（ユーザー起動専用）のため手動レビューで代替。**残るはMermaid非同期描画完了後の復元のみ（優先度低め・実機確認前提）**で ROADMAP に残置。`media/modules/` のみ＝統合テスト非対象 | `8750ec2` |
| 2026-07-19 | バグ修正（根本対策の一部・2/2）: 「編集中にキャレットが飛ぶ」＝競合する古い `update` エコー無視ガードを**実効化**する拡張機能側の配線。(1/2, `e60bee2`) で入れたガードは `message.seq` が常に undefined で無効だった。`src/markdownEditor.ts` の `resolveCustomTextEditor` に `pendingEchoSeq` を保持し、Webview から受けた `edit` の `seq` を、その編集適用に**起因するエコー `update` にだけ**反映する（`applyEdit` が同期的に `onDidChangeTextDocument` を発火させるため適用前に保持→`updateWebview` で `seq` を載せて一度きり消費・`undefined` へリセット）。外部編集には seq を付けず常に適用。無変更時は `updateTextDocument` が `false`（従来 `true`）を返すよう変更し、発火しないエコーの seq を `edit` ハンドラが後始末（次の外部変更へ持ち越さない）。これでタイプ中に古いエコーが新しいローカル内容を巻き戻す＝キャレット巻き戻りを防ぐ。統合テスト9件パス（`updateTextDocument` の戻り値が「実際に編集を適用したか」を表すことの回帰テスト1件を追加）。`/code-review` は当ループから起動不可（ユーザー起動専用）のため手動レビューで代替。**実機での高速タイプ・数式/Mermaid編集時のキャレット挙動確認は実機確認前提**で残置。`src/markdownEditor.ts` 変更＝統合テスト対象サイクル | `1aca68d` |
| 2026-07-19 | バグ修正（根本対策の一部・1/2）: 「編集中にキャレットが飛ぶ」の残る根本原因＝**競合する古い `update` エコー**（タイプ中に「AB」を送った直後に「ABC」を送ると、遅れて届いた「AB」のエコーが現在の「ABC」を巻き戻しキャレットを乱す。既存の `incoming === lastSentMarkdown` ガードは `lastSentMarkdown` が既に更新済みで古いエコーを弾けない）への対策のWebview側インフラ。編集送信ごとに単調増加する `state.editSeq` を採番し、全送信経路を共通ヘルパ `postEdit(content)` へ集約して `seq` を載せる。純粋関数 `utils.shouldIgnoreStaleUpdate(messageSeq, currentEditSeq)`（エコーの seq が最新送信 seq より小さければ無視・未指定/同値は適用）を `handleUpdateMessage` 先頭ガードに配線。**拡張機能側が seq をエコーへ反映する(2/2)配線までは `message.seq` が常に undefined＝ガード無効で完全に従来動作**（無リグレッション）。ユニットテスト5件（seq判定の全分岐＋editSeq初期値/読み書き）。`/code-review` は当ループから起動不可（ユーザー起動専用）のため手動レビューで代替。**(2/2)＝拡張機能側のseqエコー配線（`src/markdownEditor.ts`・統合テスト対象）＋実機での高速タイプ時キャレット確認は実機確認前提**で ROADMAP に残置。`media/` のみ（`src/` 非変更）＝統合テスト非対象 | `e60bee2` |
| 2026-07-19 | バグ修正（根本対策の一部）: 「編集中にキャレットが先頭へ飛ぶ／ずれる」の根本原因＝グローバル文字数オフセット方式に対し、**ブロック基準アンカー**を導入（innerHTML全書き換え経路向け）。先行のノード基準（`225ca19`・インプレース経路）は全書き換えで保存ノードが破棄され使えないため、`utils.saveCursorPosition` が新たに `blockIndex`（キャレットを含むトップレベルブロックのインデックス）/`blockOffset`（ブロック内ローカルオフセット）を保持し、`utils.restoreCursorPosition` に第二候補の復元段を追加。ノード基準が使えない全書き換え時に、同インデックスのブロックが残っていてブロック内でローカルオフセットに到達できればそこへ復元する。これでキャレットより前の別ブロック（数式・Mermaid・テーブル）が再描画で文字量を変えても、グローバルオフセットのように別ブロックへドリフトしない。ブロックが無い／届かない（構造変化）場合は従来のグローバルオフセット走査＋最寄りフォールバックへ委譲（後方互換・既存テスト全通過）。オフセット走査は `scanOffset(root, targetOffset)` ヘルパへ切り出しブロック内／全体で共用。ユニットテスト3件（アンカー保持・前ブロック文字量変化での正確復元・blockIndex範囲外フォールバック）。`/code-review` は当ループから起動不可（ユーザー起動専用）のため手動レビューで代替。**残り（Mermaid非同期描画完了後の復元・競合update無視／バージョン付与・ブロックID安定化）は ROADMAP に残置＝実機確認前提**。`media/modules/` のみ＝統合テスト非対象 | `132bd22` |
| 2026-07-19 | 機能の基盤(1/2): Mermaid図の画像コピー／保存時の背景色選択（透過・白・黒）の中核。`mermaid.js` に純粋関数 `resolveImageBackground(background)` を追加（`'transparent'`→alpha有効・塗りつぶし無し・html2canvas背景 null／`'white'`/`'black'`→不透明＋該当色／未指定・未知→白フォールバック）し、`svgToPngBlob(svg, scale, background='white')` がこれを使って `getContext('2d',{alpha})`・`fillRect` 有無・html2canvas の `backgroundColor` を切り替えるように。`copyToClipboard`/`saveAsPng` も背景引数を受けて渡す（**既定 'white' で従来動作のまま＝無変更**）。テスト用に helper の `MODULE_FILES` へ `mermaid.js` を追加（`mermaid.min.js` 依存は関数内のみのため純粋関数は jsdom で検証可）。ユニットテスト5件。呼び出しUI（右クリックメニューでの背景選択）は(2/2)＝実機確認前提で ROADMAP に残置。`media/modules/` のみ＝統合テスト非対象 | `9e8ea82` |
| 2026-07-19 | バグ修正（根本対策の一部）: 「編集中にキャレットがずれる／先頭へ飛ぶ」の根本原因＝文字数オフセット方式に対し、**ノード基準アンカー**を導入。`utils.saveCursorPosition` が `range.endContainer`/`endOffset` も保持し、`utils.restoreCursorPosition` は保存ノードが**テキストノードとしてエディタ内に生きていれば**（＝インプレースDOM変更）文字数オフセット走査を経ずに直接そのノードへ復元する。これで入力イベント経路（`applyInlineFormatting` 後の復元）では、埋め込みウィジェット（数式・Mermaid・テーブル）の内部テキスト量が変わってもキャレットがずれない。ノードが消えている場合（innerHTML 全書き換え）は従来のオフセット方式＋最寄りフォールバックへ委譲（後方互換）。コードレビュー（`/code-review high`）指摘で、要素コンテナは childNodes インデックスが古くなり得るためノード基準の対象をテキストノードに限定。ユニットテスト4件。**残り（innerHTML全書き換え経路のブロックID基準アンカー・非同期描画後復元・競合update無視）は ROADMAP に残置＝実機確認前提**。`media/modules/` のみ＝統合テスト非対象 | `225ca19` |
| 2026-07-19 | バグ修正: マウスドラッグでの本文選択が装飾記号（`**` 等）に到達すると解除される問題。**根本原因**: `selectionchange` ごとに走る `commands.syncRawMarkdownToCaret` が、選択の開始位置（`startContainer`）の所属だけで判定して装飾（`**太字**` 等）を生Markdownへ**展開／折り畳み**＝DOM書き換えしていたため、ドラッグ中（範囲選択中）に装飾へ差しかかると選択が破棄されていた。**修正**: 生Markdown表示は「キャレット位置」を示す機能であり範囲選択中は不要なので、選択が非collapsed（範囲）の間は `syncRawMarkdownToCaret` を早期returnして一切DOMを触らないようにした。既に展開中のspanも範囲選択中は折り畳まれず、展開テキストのドラッグ編集は維持される。ユニットテスト3件（装飾をまたぐ範囲選択で非展開・選択保持／開始が装飾内でも非展開／展開中spanの非折り畳み）。`media/modules/` のみ＝統合テスト非対象 | `042b500` |
| 2026-07-19 | バグ修正の基盤(1/2): ブロック数式コピー画像の崩れ修正に向けた foreignObject SVG マークアップ組み立ての純粋関数 `math.buildMathBlockSvgMarkup(innerHtml, cssText, width, height, background)` を追加。**調査で根本原因を特定**（KaTeX を `output:'html'` で描画→画像化の html2canvas が `.katex` のCSS配置＝`vertical-align`/負マージン/絶対配置/√overline を再現できず崩れる）し、対策方式を「html2canvas をやめ foreignObject＝ブラウザネイティブ描画でラスタライズ」へ確定。本サイクルはその前段の文字列組み立て（SVG/xhtml名前空間・`<style>`インライン・寸法正規化・背景色 `transparent`可・属性エスケープ）を実装しユニットテスト6件で検証（実機不要）。実際の `blockToPngBlob` 差し替え・KaTeXフォントの base64 埋め込み・CSP `font-src data:` 追加は(2/2)＝実機確認前提で ROADMAP に残置。`media/modules/` のみ＝統合テスト非対象 | `36e6631` |
| 2026-07-19 | バグ修正（応急処置）: 「編集中に突然キャレットが先頭へ飛ぶ」問題の対症療法。`utils.restoreCursorPosition` が復元先の文字数オフセットに届かない（保存時より本文が短くなった）とき、従来は range を一切設定せず、直前の `innerHTML` 全書き換えで選択が失われているとブラウザがキャレットをエディタ先頭へ描画していた。復元失敗時、①エディタ内に妥当な選択が残っていればそれを尊重、②選択が失われている（rangeCount=0 または `(editor,0)` へ collapse＝先頭飛びそのもの）ときのみ、到達できた最寄り位置（最後のテキストノード末尾、本文が無ければエディタ末尾）へフォールバックするよう変更。コードレビュー（`/code-review high`）指摘で `(editor,0)` collapse を妥当な選択と誤判定するギャップを発見し同サイクルで修正。ユニットテスト4件。**根本原因（オフセット方式）は未解決のため ROADMAP に根本対策として残置。** `media/modules/` のみ＝統合テスト非対象 | `cb86e8c` |
| 2026-07-19 | バグ修正: 見出し行でEnterを押すと改行直後の新しい行にも見出しテキストが重複表示される問題。**根本原因**: `utils.findBlockAncestor` が見出し（h1〜h6）をブロックとして扱わず、`commands.handleHeadingConfirm` が「レンダリング済み見出し内でのEnter」を捕まえられずブラウザ既定のEnter処理（見出しの複製・分割）に委ねていた。`## ああ` 入力後 150ms のsyncで段落がh2へ再レンダリングされてから押されたEnterがこの経路に落ちていた。**修正**: `handleHeadingConfirm` に見出し祖先（`findAncestor` で h1〜h6）検出を追加し、`confirmRenderedHeading` でキャレット以降を新しい段落へ切り出して見出し直下に挿入・キャレットを移動し、`preventDefault` でブラウザ既定を抑止（末尾なら空段落・途中なら後半のみ移動でテキストは失われない）。ユニットテスト5件を追加。`media/modules/` のみ＝統合テスト非対象 | `bec3a24` |
| 2026-07-19 | バグ修正: 全選択への上書き貼り付け（Ctrl+A→Ctrl+V）で先頭に空見出しの殻 `# ` が残る問題。`commands.js` の `handleMarkdownPaste` の殻ブロック掃除が生 `textContent` で空判定していたため、`.heading-hash` スパン（`# `）を含む空見出しが除去されず残っていた。空判定を同関数内の `shellIsEmpty`（heading-hash を本文から除外）へ置換して解消。前サイクルで発見し `/evolve` のテストゲートを落としていた既存失敗テストが解消（回帰テスト1件追加）。`media/modules/` のみ＝統合テスト非対象 | `ec815e5` |
| 2026-07-19 | バグ修正: ブロック数式（`$$…$$`）を新規入力しても自動レンダリングされず生ファイルが `\$\$` に破損する問題。`commands.js` に `convertMathBlocks` を追加し `applyInlineFormatting`（入力イベント）から `walkInline` 前に実行。エディタ直下の平文ブロック（class無しP/DIV）の単独 `$$` 行を開き・次の単独 `$$` 行を閉じとして、間の平文ブロックの生テキスト（`$` 非エスケープ）を式本文に集め `markdown.buildMathBlockHtml`〈読込時と同じ〉で math-block へ畳む。閉じ `$$` 無しは非変換、キャレットが範囲内なら直後に空段落を挿入して移す（`caretHandled`）。ユニットテスト7件。`media/modules/` のみ＝統合テスト非対象 | `bebed92` |
| 2026-07-18 | 表挿入の中核: `table.buildEmptyTableMarkdown(rows, cols)`（空テーブルMarkdown生成・純粋関数）＋ `table.insertTable(rows, cols)`（キャレット位置の直後＝無ければ末尾へ挿入し `render()` で即インタラクティブ化。挿入方式は `commands.insertToc` と同じ）。ユニットテスト6件。呼び出しUI（右クリックメニュー＋行数・列数ダイアログ）はROADMAPに分割（実機確認前提） | `78b0295` |
| 2026-07-18 | 行番号表示 (3/3 描画): WYSIWYGモードの行番号ガター描画とスクロール同期。`#editor` を flex ラッパーで包み、`computeEditorLineMap` の各ブロック上端（`getBoundingClientRect`）へ開始行番号を絶対配置、`transform` で縦スクロール追従。input（デバウンス）/文書更新/リサイズで再配置。`markdownEditor.ts` 非変更。**これで行番号表示（元L項目の3分割）が一通り完了**。ピクセル整列は実機確認前提 | `5c82d73` |
| 2026-07-18 | 行番号表示 (3/3 橋渡し): ライブ`#editor`から「表示ブロック→開始行」を対応づける `markdown.computeEditorLineMap`。clone整形を `getCleanEditorClone` へ切り出して `getCleanHtmlFromEditor` と共有、Mermaidの隠しpreを除外し可視コンテナ・テーブルの `.table-container` へ対応。jsdomでユニットテスト（元L項目の分割(3/3)前段。残りはガター描画＝実機確認前提） | `092f2a3` |
| 2026-07-18 | 行番号表示 (2/3): WYSIWYG側「ブロック→ソース開始行」対応付け。純粋関数 `markdown.computeBlockStartLines(finalMarkdown, blockMarkdowns)`＋補助 `coreLinesOf` を追加。各ブロックの本文先頭行を確定ソース内から前方一致で探し1始まりの開始行を返す（空ブロックは `null`・空行畳み込みに非依存）。DOM非依存でユニットテスト（元L項目の分割(2/3)・UIは(3/3)） | `5b72280` |
| 2026-07-18 | 行番号表示 (1/3): Rawモード（textarea）の行番号ガター。左端にソース行番号を表示、行を揃えるため textarea を `white-space: pre`（折り返しオフ）へ変更。行数/番号列は純粋関数 `utils.countLines`/`buildLineNumberText`（ユニットテスト）、ガターは `transform` で縦スクロール追従。UIは `#rawEditor` をラッパーで包む動的生成で `markdownEditor.ts` 非変更。ユーザー要望（元L項目の分割(1/3)） | `fcf630e` |
| 2026-07-18 | ブロック数式を右クリックでPNG画像としてクリップボードにコピー。`math.js` に自前コンテキストメニュー（動的生成・`markdownEditor.ts` 非変更）＋`html2canvas` での `math-block`→PNG化（`document.fonts.ready` 待機）を追加。CSSは `.mermaid-context-menu` 系と共有。ユーザー要望 | `44b4031` |
| 2026-07-18 | 数式のライブ変換（入力・ペースト時点で即時レンダリング）。`commands.convertInlineText` に `$...$` の分岐を追加し、`markdown.convertInline` と同じ退避順序で `math-inline` コンテナへ変換→入力イベント末尾の `MathModule.render` で描画。閉じ `$` を打つまで非変換で入力途中に壊れない。`data-math` は読込パスと同一（突き合わせテスト有り）。ユーザー要望 | `45d5bc2` |
| 2026-07-18 | 数式の内側にカーソルがある間、生Markdownを表示する（数式コンテナは `contenteditable="false"` でキャレットが入れないため、クリックで `$...$` / `$$...$$` の生Markdownへ展開＝`commands.handleMathClick`／`expandMathToRaw`。復帰・再変換抑止は既存の `syncRawMarkdownToCaret`／`raw-markdown` クラスを共有し、`collapseRawMarkdown` にブロック数式（`raw-math-block`→`buildMathBlockHtml`＋`MathModule.render`）分岐を追加。直列化は `markdown.rawMarkdownText` で `$` を非エスケープ・`<br>`→改行。ユーザー要望） | `f3426e1` |
| 2026-07-18 | 強調記法（太字・斜体・取り消し線・下線）とリンクの生Markdown表示（`syncRawMarkdownToCaret` を汎用化・`outermostInlineDecoration`・展開は `serializeInline`／復帰は `convertInline` に委譲・入れ子は最外要素ごと展開） | `7c82d3b` |
| 2026-07-17 | 数式（インライン `$...$` / ブロック `$$...$$`）の表示・双方向変換（KaTeXを `media/katex/` へ同梱・生の式は `data-math` に保持・描画は `math.js`・`\$` エスケープ対応。ユーザー要望） | `fd2a368` |
| 2026-07-17 | エディタ右上の「Markdown: WYSIWYGエディタで開く」ボタンの削除（`menus.editor/title` のエントリを削除。コマンド定義とコマンドパレットは維持。ユーザー要望） | `4fb531b` |
| 2026-07-17 | ダークモードで太字がまだ視認しにくい問題の改善（ウェイトに加え `font-size: 1.05em` へ拡大・全テーマ共通。`line-height: calc(1.6 / 1.05)` で行高を据え置き。ユーザー再報告） | `0306fcf` |
| 2026-07-17 | リンクの挿入・編集ダイアログ（`Ctrl+K`・選択テキストのリンク化／既存リンクの編集・解除。自前ダイアログ＝Webviewでは `prompt()` が使えないため） | `c7b61b4` |
| 2026-07-17 | リンクのクリック挙動を Ctrl/Cmd+クリックでの遷移へ変更（通常クリックはキャレット設置のみ。`handleLinkClick`＋拡張機能側 `openLink`／`vscode.env.openExternal`。ユーザー要望・実機確認済み） | `528cbfb` |
| 2026-07-17 | リンク上にカーソルがある間、生Markdownを薄く表示（`syncRawMarkdownToCaret`・`selectionchange`監視・`span.raw-markdown`へ展開／復帰。実機確認済み） | `407a321` |
| 2026-07-17 | アラートbox本文内のEnter操作（末尾でboxを抜けて段落へ／途中とShift+Enterは`<br>`改行。`handleAlertEnter`・共通ヘルパ`insertLineBreak`） | `318194a` |
| 2026-07-16 | 検索ウィジェットが Cmd+F で開かない問題の修正（macOSの `metaKey` 対応・`Ctrl/Cmd+F/G//`。ユーザー報告バグ） | `123edab` |
| 2026-07-16 | GitHubアラートのライブ変換（手入力・ペーストで即時反映。`convertAlerts`・ユーザー報告バグの修正） | `a8d69d4` |
| 2026-07-16 | 検索ウィジェットに置換機能（`replaceCurrent`/`replaceAll`・WYSIWYG/RAW両対応・リテラル置換） | `d304706` |
| 2026-07-16 | 目次(TOC)生成をツールバーの 📑 ボタンからも実行できるように（`data-command="toc"`） | `297c41a` |
| 2026-07-16 | GitHubアラート記法（`> [!NOTE]`/`[!TIP]`/`[!IMPORTANT]`/`[!WARNING]`/`[!CAUTION]`）の表示・双方向変換対応 | `31dcbe8` |
| 2026-07-16 | 冗長な個別ハイライトバンドル（python/bash/c/sql）の読み込み・ファイル削除（commonビルドに既存・PowerShellのみ残す） | `38d686e` |
| 2026-07-16 | 下端スクロールでツールバーが画面外に消える問題の修正（`#editor`/`#rawEditor` の `min-height` を `100%`→`0`） | `c7b2f9f` |
| 2026-07-16 | ダークモードで太字が視認しにくい問題の改善（`#editor strong` を 700、ダーク系は 800 へ・テーマ別ルール） | `fd898ed` |
| 2026-07-16 | インラインコード内の記法（`**`/`~~`/`++`/`[]()`）が装飾に化けて往復欠損するバグの修正（`convertInline`/`convertInlineText` でコードを退避・復元） | `482e72d` |
| 2026-07-16 | TOCの目次リンク（`[見出し](#slug)`）クリックで該当見出しへ遷移（見出しへの`id`付与＋`commands.scrollToAnchor`） | `e8b89aa` |
| 2026-07-15 | タスクリスト（`- [ ]` / `- [x]`）のチェックボックス表示とクリックでのトグル | `cf42195` |
| 2026-07-15 | ネスト引用（`> >` / `>>`）の双方向変換対応 | `9e1ab6b` |
| 2026-07-15 | 水平線（`---` / `***` / `___`）の双方向変換とEnterでの自動変換 | `904bba7` |
| 2026-07-15 | 書式ツールバー（太字・斜体・下線・見出し・リスト・引用・リンク・コードブロック）の設置 | `7f78fdc` |
| 2026-07-15 | 取り消し線（`~~text~~`、Ctrl+Shift+X、ツールバーボタン） | `4cfbb21` |
| 2026-07-15 | タスクリストのライブ入力対応（`- [ ]` / `- []` / `-[]` を入力した時点でチェックボックス化） | `9246e08` |
| 2026-07-15 | 太字・取り消し線のライブ変換が分割テキストノードでマッチしない問題の修正（走査前に `normalize()`） | `67080b7` |
| 2026-07-15 | 引用内での `> ` 入力によるネスト引用の作成（`handleNestedQuote`） | `19ff5cc` |
| 2026-07-15 | 引用ブロックのEnter（引用を抜ける）/Shift+Enter（引用内改行）対応（`handleBlockquoteEnter`） | `cc7a1d6` |
| 2026-07-15 | JS/TS/JSON/YAML/Go/Rust のシンタックスハイライト対応の明文化（commonビルドに既存・回帰テスト追加） | `9db72a8` |
| 2026-07-15 | 単語数・文字数のステータス表示（右下バー・`utils.countText`） | `4b86e1c` |
| 2026-07-15 | 見出しから目次(TOC)を生成・挿入するコマンド（`Ctrl+Shift+O`・`slugify`/`buildTocMarkdown`/`insertToc`） | `83433dc` |
