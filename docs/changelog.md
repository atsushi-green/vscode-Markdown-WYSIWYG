# 実装の変遷（gitログベース）

`git log` の履歴（時系列順）と、各コミットで追加・変更された内容の要約です。あわせて、現時点でまだコミットされていない作業中の変更（`git status` 時点）も記載します。

## コミット履歴

| 日付 | コミット | 内容 |
|---|---|---|
| 2026-01-13 | `7422a1d` first commit | プロジェクト初期構築。CustomTextEditorProviderによるWYSIWYG編集、Markdown⇔HTML変換、Mermaid図サポート、シンタックスハイライト（highlight.js）、基本的なキーボードショートカット、検索ウィジェットなど中核機能一式。 |
| 2026-01-13 | `df5b421` mermaid図保存時の余白の調整 | Mermaid図をPNGとしてエクスポートする際の余白（パディング）計算を調整。 |
| 2026-01-13 | `7443dd9` インストール方法の記載 | README.mdにWindows/macOS向けのインストール手順を追記。 |
| 2026-01-13 | `e27289c` sampleに表の追加 | `sample.md` にテーブル記法のサンプルを追加。 |
| 2026-01-15 | `4ee2ab5` 表機能の追加 | Markdownテーブルをインタラクティブに編集できるテーブル機能を実装（セル編集、キーボードナビゲーション、行列の追加削除、Excelデータの貼り付け・コピー）。 |
| 2026-01-15 | `e1acb88` fix: 拡張機能画面で意図しない編集が起きるバグを修正 | Webviewと文書の同期処理における意図しない編集発生の不具合を修正。 |
| 2026-01-15 | `62c4000` エディタ切り替えショートカットの実装 | `toggleEditor` コマンドとキーバインド（`Ctrl+Shift+M`/`Cmd+Shift+M`）を追加し、WYSIWYG⇔テキストエディタの相互切り替えを実装。 |
| 2026-07-15 | `cf42195` feat: タスクリスト（チェックボックス）の追加 | GFMタスクリスト記法（`- [ ]` / `- [x]`）をチェックボックス付きリストとして表示し、クリックで完了/未完了を切り替え可能に。状態はMarkdownソースへ即時反映。あわせて自動開発ループ用の `docs/ROADMAP.md` と `/evolve` コマンド定義を追加。 |
| 2026-07-15 | `9e1ab6b` feat: ネスト引用（> >）の追加 | 入れ子の引用ブロック（`> > text` / `>> text`）をMarkdown⇔HTML双方向で保持するように。ツールバーへの引用ボタン設置は書式ツールバー全体の追加としてROADMAPに分離。 |
| 2026-07-15 | `904bba7` feat: 水平線（---）の追加 | `---` / `***` / `___` の単独行を `<hr>` として双方向変換。`---` 行でEnterを押すとその場で水平線に自動変換。commands.jsをユニットテスト対象に追加。 |
| 2026-07-15 | `7f78fdc` feat: 書式ツールバーの追加 | エディタ上部に太字・斜体・下線・見出しH1〜H3・リスト・引用・リンク・コードブロックのボタン列を設置。`executeCommand` の配線は既存で、ボタンのみ未設置だった。mousedown抑止で選択範囲を保持。 |
| 2026-07-15 | `4cfbb21` feat: 取り消し線（~~text~~）の追加 | GFMの取り消し線を双方向変換・ライブ変換に対応（`<del>`/`<s>`/`<strike>` からの復元も可）。Ctrl+Shift+Xショートカットとツールバーボタンを追加。 |
| 2026-07-15 | `9246e08` feat: タスクリストのライブ入力対応 | エディタ上で `- [ ]` / `- []` / `-[]`（スペース有無を問わず）を入力した時点でチェックボックスGUIへ即時変換するように。従来はファイル読込時のパーサでしか解釈されず入力中はテキストのまま残っていた。`commands.js` に `convertTaskLists` を追加し、入力イベントの整形フローへ組み込み。 |
| 2026-07-15 | `67080b7` fix: 太字・取り消し線のライブ変換が実機で効かない問題を修正 | contenteditableが入力中のテキストを複数の隣接テキストノードに分割するため、`**` や `~~` の開始と終了が別ノードに割れて `convertInlineText` の正規表現にマッチしないことがあった。`applyInlineFormatting` で走査前に `editor.normalize()` して隣接テキストノードを結合（要素境界はまたがない）することで解消。分割ノード相当のユニットテストを追加。 |
| 2026-07-15 | `19ff5cc` feat: 引用内での「> 」入力によるネスト引用の作成 | 引用ブロック内の行頭で `> ` を入力すると1段深いネスト引用（`> > `）が作られるように。従来は `findBlockAncestor` が BLOCKQUOTE を返さず `handleAutoBlock` が発火しなかった。`commands.js` に `handleNestedQuote` を追加。ネスト構造は既存の `buildQuoteHtml`/`serializeBlockquoteLines` が往復対応済み。引用UX改善（Enterで抜ける等）の残タスクはROADMAPに分割。 |
| 2026-07-15 | `cc7a1d6` feat: 引用ブロックのEnter/Shift+Enter対応 | 引用ブロックの末尾で `Enter` を押すと引用を抜けて後続の段落へ移り、`Shift+Enter` では引用内で改行（`<br>`）して引用を継続するように。`commands.js` に `handleBlockquoteEnter` を追加し、`editor.js` のkeydown処理（コードブロックEnter処理の前）に配線。`<br>` は `> 行1` / `> 行2` として往復変換される。 |
| 2026-07-15 | `9db72a8` docs+test: JS/TS/JSON/YAML/Go/Rust のシンタックスハイライト対応を明文化 | 同梱の `highlight.min.js` は highlight.js の commonビルドで、これら6言語（と `js`/`ts`/`yml`/`golang`/`rs` 別名）を含む約36言語を登録済みであることを確認。個別バンドルの追加は不要だった。回帰テスト `syntax-highlight.test.ts` を追加し、README/AGENTS/features のハイライト対応言語の記述を実態（旧: Python/Bash/PowerShell/C/SQLのみ）に合わせて更新。 |
| 2026-07-15 | `4b86e1c` feat: 単語数・文字数のステータス表示 | エディタ右下に固定表示するステータスバーで、単語数と文字数（空白除外・Unicodeコードポイント単位）をリアルタイム表示。数え上げは純粋関数 `utils.countText` として実装しユニットテストを追加、UIは `editor.js` が動的生成（`markdownEditor.ts` は変更しない）。通常/Rawモード両方に対応。 |
| 2026-07-15 | `83433dc` feat: 見出しから目次(TOC)を生成・挿入するコマンド | `Ctrl+Shift+O`（Mac: `Cmd+Shift+O`）で見出し(h1〜h6)からGitHub風アンカーリンク付きの目次を生成し、キャレット位置へ挿入。`markdown.slugify`/`markdown.buildTocMarkdown`（純粋関数）＋ `commands.insertToc`（DOM挿入）を追加。重複見出しは `-1`/`-2` 付与、往復変換で安定。ツールバーボタンは未設置（キーボードショートカットのみ）。 |
| 2026-07-16 | `e8b89aa` fix: TOC目次リンクの見出し遷移バグを修正 | 目次のアンカーリンク（`[見出し](#slug)`）をクリックしても該当見出しへ遷移しなかった問題を修正。`markdownToHtml` が見出しへ `buildTocMarkdown` と同一のスラッグ生成・重複連番（`-1`/`-2`）で `id` を付与するようにし、contentEditable内で既定遷移が効かないアンカークリックを `commands.scrollToAnchor` で該当 `id` へスクロールするよう明示処理。`id` は `htmlToMarkdown` で出力されず往復に非影響。ユニットテスト（markdown/commands）を追加。 |
| 2026-07-16 | `482e72d` fix: インラインコード内の記法が往復で欠損するバグを修正 | `` `**太字**` `` のようにインラインコード内へ書いた `**`/`~~`/`++`/`[]()` が装飾として解釈され `<code><strong>太字</strong></code>` に化け、WYSIWYG往復で記法が失われていた問題を修正。`convertInline`（`markdown.js`）と `convertInlineText`（`commands.js`）でインラインコードを先にプレースホルダ（NUL文字＋通し番号）へ退避し、他のインライン整形適用後に `<code>` として復元することでコード内を保護。ユニットテスト（往復含む5件）を追加。 |
| 2026-07-16 | `fd898ed` fix: ダークモードで太字が視認しにくい問題の改善 | `#editor strong` のウェイトを 600 から 700 へ引き上げ、ダークテーマ・ダーク系ハイコントラストでは 800 へさらに強調するテーマ別ルールを `media/editor.css` に追加。テーマ判定は body の `vscode-dark`/`vscode-high-contrast(-light)` クラス（`mermaid.js` の判定と同規則）を利用。 |
| 2026-07-16 | `c7b2f9f` fix: 下端スクロールでツールバーが画面外に消える問題の修正 | `#editor`/`#rawEditor` の `min-height` を `100%` から `0` へ変更。flexの子に `min-height:100%` があるとツールバー分を無視して伸び、body側がはみ出してツールバーごとスクロールしていた。`min-height:0` で残り領域内に収め、スクロールを本文領域内部（`overflow-y:auto`）へ閉じ込めることで、ツールバーが常時上部固定されるように修正。 |
| 2026-07-16 | `38d686e` refactor: 冗長な個別ハイライトバンドルの読み込み整理 | commonビルド `highlight.min.js` に既に含まれる `hljs-python`/`hljs-bash`/`hljs-c`/`hljs-sql` の個別読み込み（`src/markdownEditor.ts`）と同梱ファイルを削除。commonビルド外の PowerShell のみ個別バンドルを残す。対応言語は変わらず（`syntax-highlight.test.ts` で回帰確認済み）、拡張機能サイズを約15KB削減。統合テスト（`npm test`）8件で拡張機能のアクティベート・コマンド登録・エディタ起動を確認。 |
| 2026-07-16 | `31dcbe8` feat: GitHubアラート記法（`> [!NOTE]` など）の表示対応 | 引用の先頭行が `[!NOTE]`/`[!TIP]`/`[!IMPORTANT]`/`[!WARNING]`/`[!CAUTION]` のみの場合、タイプ別に色分けしたアラートボックスへ変換（`markdown.js` の `tryBuildAlertHtml`/`serializeAlert`）。`data-alert-type` を保持し `> [!TYPE]\n> 本文` へ双方向変換（往復テスト済み）。マーカー大文字必須・行内に余分なテキストがあると通常引用（GitHub準拠）。色はVS Codeテーマ変数を参照。ユニットテスト9件を追加。 |
| 2026-07-16 | `297c41a` feat: 目次(TOC)生成をツールバーボタンに追加 | 書式ツールバーに 📑 ボタン（`data-command="toc"`）を追加し、キーボードショートカット `Ctrl+Shift+O` に加えてクリックからも目次を挿入可能に。ボタンの配線は既存の汎用ハンドラ（`data-command`→`executeCommand`）で自動的に有効化。`src/markdownEditor.ts` のツールバーHTMLへの追加のため統合テスト（`npm test`）8件で拡張機能のアクティベート等を確認。 |
| 2026-07-16 | `d304706` feat: 検索ウィジェットに置換機能を追加 | 検索欄の下に置換欄・「置換」「全置換」ボタンを追加。`search.js` に `replaceCurrent`（現在のマッチ）/`replaceAll`（全マッチ）を実装し、WYSIWYG（ハイライト要素をテキストへ置換）・RAW（検索正規表現で一括置換）の両モードに対応。置換はリテラル（`$&` 等を展開しない）。置換後は `input` イベントで書き戻し・再検索を実行。find widgetを検索行/置換行の2段構成へ再構成（`src/markdownEditor.ts`・`media/editor.css`）。ユニットテスト7件＋統合テスト8件で確認。 |
| 2026-07-16 | `a8d69d4` fix: GitHubアラートのライブ変換対応（手入力・ペーストで即時反映） | ユーザー報告: アラート記法を手入力・ペーストしてもレンダリングされず、Raw切替や再読込までアラートboxにならなかった（読込時パーサ `markdownToHtml` のみ対応でライブ変換が無かった）。`commands.js` に `convertAlerts` を追加し `applyInlineFormatting`（入力イベント）へ配線: 対象ブロック（blockquote / 平文の `>[!NOTE]`）をシリアライズ→再パースし「単一のアラートdiv」になる場合のみ置換するため判定は読込時と常に一致。変換後キャレットは本文へ移動（空本文はゼロ幅文字で受け）。ユニットテスト10件を追加。 |
| 2026-07-16 | `123edab` fix: 検索ウィジェットが Cmd+F で開かないバグを修正（macOS） | ユーザー報告: `Ctrl+F`/`Cmd+F` を押しても検索ウィジェットが表示されなかった。`editor.js` の `setupGlobalKeyboardShortcuts` が `Ctrl+F`/`Ctrl+/`/`Ctrl+G` を `e.ctrlKey` のみで判定しており、macOSの `Cmd`（`e.metaKey`）を受け付けていなかった。書式系ショートカット（太字等）と同様に `e.ctrlKey || e.metaKey` で両対応に修正し、文字キーは大文字小文字を無視。カスタムエディタは `enableFindWidget` 未設定（既定false）のためVS Code側のfindには奪われない。 |
| 2026-07-17 | `318194a` feat: アラートbox本文内のEnter操作に対応 | アラートboxの本文（`.markdown-alert-body`）内でEnterがブラウザ既定挙動のままで、boxをキーボードだけで抜ける手段が分かりにくかった問題に対応。`commands.js` に `handleAlertEnter` を追加し `editor.js` のkeydownへ配線（引用の `handleBlockquoteEnter` の前）: 本文末尾のEnterはboxを抜けて後続の段落へ、本文の途中のEnterとShift+Enterは `<br>` 改行。`<br>` 挿入と末尾プレースホルダ補完は `handleBlockquoteEnter` から共通ヘルパ `insertLineBreak` へ切り出して共用。ユニットテスト10件を追加（往復変換の確認を含む）。 |
| 2026-07-17 | `407a321` feat: リンク上にカーソルがある間の生Markdown表示 | カーソルがリンク（`<a>`）の内側のどこかにある間ずっと、生Markdown記法（`[text](url)`）を薄く（等幅・`--vscode-descriptionForeground`）表示し、URLやリンクテキストを直接修正できるように。`commands.js` に `syncRawMarkdownToCaret` を追加し、`editor.js` の `setupRawMarkdownCaretEvent` が `selectionchange`（documentのみ発火）を監視して配線（切り替えが選択を変えて再発火するため再入抑止）。対象リンクを `<span class="raw-markdown">` の生テキストへ展開し、カーソルが外れたら記法をパースしてリンクへ復帰（壊れていればプレーンテキストとして残す）。展開中は `utils.shouldSkipInline` が `walkInline` の再変換を抑止。spanの中身は生Markdownそのもので `serializeInline` のSPAN分岐が素通しするため、展開中でもMarkdownは展開前と同一（往復に非影響・書き戻し不要）。あわせて `utils.placeCaretAt` の末尾の `focus()` を「未フォーカス時のみ」へ限定（ブラウザでは既にフォーカス済みなら no-op のため実機の挙動は不変。jsdomでは選択がリセットされキャレット位置をテストできなかった）。ユニットテスト12件を追加。実機（拡張機能開発ホスト）で見た目・カーソル挙動を確認済み。 |
| 2026-07-18 | `7c82d3b` feat: 強調記法（太字・斜体・取り消し線・下線）の生Markdown表示 | リンクで実装済みの生Markdown表示（`fd2a368` 以前・`407a321`）を汎用化し、太字（`**`）・斜体（`*`）・取り消し線（`~~`）・下線（`++`）とその入れ子（`***太字斜体***` 等）にも対応。カーソルが装飾の内側にある間だけ生Markdown記法を薄く表示し、記法ごと直接編集できる（`commands.js` の `syncRawMarkdownToCaret`）。リンク専用だった `expandLinkToRaw`/`parseRawLink` ベースの展開・復帰を、`markdown.js` の `serializeInline`（展開）と `convertInline`（復帰）へ委譲する形へ置き換え（どちらも `htmlToMarkdown`/`markdownToHtml` と同じ関数のため、生表示⇔レンダリングが通常の変換結果と必ず一致し往復に影響しない）。そのため `markdown.js` の `serializeInline`/`convertInline`/`escapeHtml` を公開APIへ追加。`outermostInlineDecoration` で**最も外側の装飾要素ごと**展開する（内側だけ展開すると `***` の外側が壊れるため）。対象タグは `RAW_INLINE_TAGS`（A/STRONG/B/EM/I/DEL/S/STRIKE/U。`CODE` は含めない）。展開後のキャレットは記法プレフィックス（`**` や `[`）の分だけ後ろへずらして装飾内の相対位置を保つ。展開中は `utils.shouldSkipInline`（`.raw-markdown` 除外・既存）が `walkInline` の再変換を抑止。ユニットテスト10件を追加（各記法・入れ子・往復・壊れた記法の復帰・walkInline非変換）、既存のリンク生Markdownテストは全通過で回帰なし。実機（開発ホスト）で見た目・カーソル挙動を確認予定。 |
| 2026-07-18 | `f3426e1` feat: 数式の内側にカーソルがある間の生Markdown表示 | 生Markdown表示（`407a321`・`7c82d3b`）を数式へ拡張。ただし数式コンテナ（`math-inline`/`math-block`）は `contenteditable="false"`（KaTeXの生成DOM保護）でキャレットが内側へ入れず、リンク・強調のようなキャレット所属判定では展開できない。そこで展開トリガーだけを**クリック**とした（`commands.js` の `handleMathClick`→`expandMathToRaw`。エディタの `click` リスナーに `.math-inline, .math-block` 分岐を追加）: インライン数式は `<span class="raw-markdown">$式$`、ブロック数式は `<div class="raw-markdown raw-math-block">$$\n式\n$$` へ展開し、開き記法の直後へキャレットを置く。再変換抑止（`utils.shouldSkipInline` の `.raw-markdown` 除外・既存）とキャレット離脱時の復帰（`syncRawMarkdownToCaret`→`collapseRawMarkdown`）は既存の `raw-markdown` の仕組みを共有（復帰対象クエリを `span.raw-markdown`→`.raw-markdown` に拡張しdivも対象化）。`collapseRawMarkdown` を汎用化: ブロック数式は `markdown.buildMathBlockHtml` で `math-block` を再生成し `MathModule.render` で再描画（`convertInline` は `$$` を解釈しないため専用処理。壊れた記法は `markdownToHtml` に委ね内容を保全）、インライン数式（`$...$`）は既存の `convertInline`＋再描画で復帰。直列化は新設の `markdown.rawMarkdownText`（`raw-markdown` 要素の生テキスト＝`$` を非エスケープ・`<br>`→改行）を `serializeInline`（SPAN）と `serializeBlockElement`（DIV）の `raw-markdown` 分岐が使い、展開の前後でMarkdownが不変（往復非影響）。`markdown.js` の `buildMathBlockHtml`/`rawMarkdownText` を公開APIへ追加。ブロックの生表示は `.raw-math-block` に `white-space: pre-wrap` で複数行を保持（`editor.css`）。ユニットテスト14件を追加（commands 11＋markdown 3。展開・復帰・編集反映・往復不変・`$` 非エスケープ・`<br>`→改行）。実機（開発ホスト）でクリック展開・カーソル挙動を確認予定。 |
| 2026-07-17 | `fd2a368` feat: 数式（インライン `$...$` / ブロック `$$...$$`）の表示・双方向変換対応 | ユーザー要望。KaTeX を `media/katex/` へ同梱（`katex.min.js`＋`katex.min.css`＋`fonts/*.woff2` 20ファイル・計約590KB。WebviewはCSPで外部CDNを読めないため。`katex.min.css` がフォントを相対パス `fonts/` で参照するため配置場所が固定。CSPは `font-src` で許可済み）。`markdown.js` はインライン `$...$` を `span.math-inline[data-math]`、ブロック `$$...$$`（複数行・1行 `$$ x $$` 両対応）を `div.math-block[data-math]` として出力し、**生の式を `data-math` に保持**。`htmlToMarkdown` は常に `data-math` から復元するため、KaTeXの生成DOMは直列化に影響せず描画失敗でも式が失われない（Mermaidの `data-*` 保持と同じ方針）。式の中身はプレースホルダへ退避して他のインライン整形から保護（`$\alpha^*$` の `*` が斜体に、`$a_1$` の `_` が強調に化けるのを防ぐ）。コンテナは `contenteditable="false"`（KaTeXの生成DOMを編集で壊さないため）。描画は新規モジュール `media/modules/math.js` の `render(root)` が担当（`data-math-rendered` で二重描画を防止・`throwOnError:false`）、`editor.js` の読込時・input時・Rawからの復帰時に呼ぶ。`markdown.js` 自体はKaTeXに依存しないため変換は純粋関数のままユニットテスト可能。**エスケープ仕様（ユーザー確定）**: 素の `$` は数式開始として扱い、通常のドル記号は `\$100` と書く。`\$` はリテラルの `$` として描画し、直列化時はテキストノードの `$` を `\$` へ戻す（次回読込で数式化しないように。CODE/PRE は textContent を使う別分岐のため影響なし）。ユニットテスト15件（往復・エスケープ・属性を壊すクォート等を含む）＋統合テスト8件で確認。ライブ変換（入力中の即時数式化）と生Markdown表示は別項目としてROADMAPに残す。 |
| 2026-07-17 | `4fb531b` エディタ右上の「Markdown: WYSIWYGエディタで開く」ボタンの削除 | ユーザー要望: 不要なため削除。`package.json` の `contributes.menus` → `editor/title` にあった `markdown-wysiwyg-editor.openEditor` のエントリ（`"group": "navigation"` によりタイトルバーへボタンとして表示されていた）を削除し、`editor/title` メニュー自体を空にした。`customEditors` の `priority: "default"` により `.md` は既定でWYSIWYGエディタで開くため、このボタンは実質不要だった。**コマンド定義（`contributes.commands`）とコマンドパレットのエントリ（`menus.commandPalette`）は従来どおり残す**ため、コマンドパレットの「Markdown: WYSIWYGエディタで開く」と `Ctrl+Shift+M`（`toggleEditor`）からは引き続き開ける。README・features・commands-and-shortcuts の記述を実態へ更新。統合テスト8件（「すべてのコマンドが登録されている」を含む）でコマンドが失われていないことを確認。 |
| 2026-07-17 | `0306fcf` fix: ダークモードで太字がまだ視認しにくい問題の改善（フォントサイズの拡大） | ユーザー再報告: `fd898ed` でウェイトを 600→700（ダーク系は 800）へ引き上げたが、まだダークモードだと太字が分かりにくかった。ウェイトに加えて `#editor strong` の `font-size` を `1.05em` へわずかに拡大（`media/editor.css`）。拡大は**全テーマ共通**（テーマによって太字の大きさが変わらないようにするため。ウェイトのみ従来どおりテーマ別に出し分け）。`px` 固定ではなく `em` 指定とし、見出し内の太字も見出しサイズに対して相対的に拡大されるようにした。`#editor` 配下の `line-height` は body の単位なし `1.6` を継承し各要素の `font-size` 基準で計算されるため、拡大すると太字を含む行だけ行の高さが伸びて前後の行がずれる。これを避けるため `strong` の `line-height` を `calc(1.6 / 1.05)` へ補正して行の高さを据え置いた。CSSのみの変更のためユニットテストは追加せず、実機（拡張機能開発ホスト）でダーク／ライト両テーマの見え方と行送りが跳ねないことを確認。 |
| 2026-07-17 | `c7b61b4` feat: リンクの挿入・編集ダイアログ（Ctrl+K） | 従来の `insertLink` はWebviewで使えない `prompt()` を呼んでおり、ツールバーのリンクボタンが実質機能していなかった。自前のダイアログ（`#linkDialog`＝`markdownEditor.ts` のWebview HTML＋`editor.css`。配色・角丸は検索ウィジェットに合わせた）を追加し、`Ctrl+K`（Mac: `Cmd+K`）とツールバーのリンクボタンから開くように。`commands.js` に `insertLink`（開く・初期値決定）/`applyLinkDialog`（適用）/`removeLinkFromDialog`（リンク解除）/`closeLinkDialog` を実装し、`editor.js` の `setupLinkDialogEvents` で配線（Enterで適用・Escapeでキャンセル）。既存リンク（`<a>`／生Markdown展開中のspan）内にキャレットがあれば編集モード、テキスト選択中なら選択文字列をリンクテキストの初期値に、それ以外は新規挿入。入力欄へフォーカスすると選択が失われるため `state.linkDialogRange`/`linkDialogTarget` を保持して適用時に使う。既存リンクは要素ごと置換するため二重リンクにならない。テキスト未入力ならURLをリンクテキストにし、URL未入力では適用しない。適用後のキャレットはリンク直後（内側だと生Markdown展開が走るため）。`state.js` はDOM参照をgetterで公開する作りのため、stateリテラル・`initDOMReferences`・getterの3箇所を更新（getter追加漏れで `state.linkDialog` がundefinedになりダイアログが出ない不具合をテストで検出）。ユニットテスト12件を追加＋統合テスト8件（`src/markdownEditor.ts` 変更のため）で確認。 |
| 2026-07-17 | `528cbfb` feat: リンクのクリック挙動を Ctrl/Cmd+クリックでの遷移へ変更 | ユーザー要望: 編集中は通常クリックでカーソルを合わせたいのに、目次のアンカーリンクは通常クリックで遷移していた。`commands.js` の `handleLinkClick`（クリックリスナーから切り出し・テスト可能に）で、修飾キー無しのクリックはキャレット設置のみとし、`Ctrl`（Mac: `Cmd`）+クリックのときだけ遷移するよう変更。遷移の処理は `click` ではなく `mousedown` で行う（clickの時点ではキャレット設置→`selectionchange`→`syncRawMarkdownToCaret` により `<a>` が `span.raw-markdown` へ置き換わっており辿れず、実機で「Cmd+クリックしても飛ばない」不具合になったため）。既にキャレットがリンク内にあり展開済みのspanも記法をパースして遷移対象とする。修飾キー付きのクリックは `preventDefault` でキャレット移動も抑止（VS Code本体と同様）、通常クリックでは抑止しない（キャレットが動かなくなるため／既定遷移の抑止は `click` 側で実施）。ページ内アンカー（`#slug`）は従来通り `scrollToAnchor`、外部リンクは新設の `openLink` メッセージ→`markdownEditor.ts` の `openLink` が `vscode.env.openExternal` で開く（contentEditableでは既定遷移が効かないため従来は外部リンクを開く手段が無かった）。スキームは http/https/mailto のホワイトリストに限定し `javascript:` 等は無視、Webviewからのメッセージは信頼せず拡張機能側でも同じ検証を実施。`#editor a` に `cursor: text` を設定し通常クリック＝キャレット設置であることを示す。ユニットテスト14件を追加＋統合テスト8件（`src/markdownEditor.ts` 変更のため）で確認。 |
| 2026-07-18 | `45d5bc2` feat: インライン数式のライブ変換（入力時に即時レンダリング） | ユーザー要望（ROADMAP最優先）: WYSIWYG上で `$x$` を手入力しても読込時パーサ（`markdownToHtml`）だけが数式を解釈するためその場では数式にならず、Raw切替や再読込が必要だった（GitHubアラートを `convertAlerts`（`a8d69d4`）で解決したのと同じ構図）。`commands.js` の `convertInlineText`（`applyInlineFormatting`→`walkInline` 経由の入力イベント）に数式の分岐を追加: `markdown.js` の `convertInline` と同じ退避順序（`\$` 退避→`$...$` を `math-inline` コンテナへ退避→強調等の整形→数式・`\$`・コードの順で復元）で `$...$` を `span.math-inline[data-math][contenteditable=false]` へ変換し、`editor.js` 入力イベント末尾の既存 `MathModule.render` が描画する。閉じ `$` を打つまで正規表現がマッチしないため入力途中で式が壊れない。入力テキストは未エスケープなので `data-math` は `markdown.escapeHtml`＋`"`→`&quot;` の順でエスケープし、読込パスと同一の `data-math` を生成（突き合わせテスト有り）。ユニットテスト7件を追加（変換・`_`/`^*` 保護・閉じ無しは非変換・`\$` エスケープ・コード内非変換・クォート属性・読込パスとの一致）。`media/modules/` のみの変更のため統合テストは非対象。 |
| 2026-07-18 | `44b4031` feat: ブロック数式を右クリックでPNG画像としてクリップボードにコピー | ユーザー要望（ROADMAP優先度・高）。レンダリング済みの `div.math-block` を右クリックすると自前のコンテキストメニューを表示し「画像としてコピー」でPNGをクリップボードへ書き込む。UIはMermaid図の既存メニューを踏襲するが、`markdownEditor.ts`（Webview HTML）は触らず `math.js` の `ensureContextMenu` が初回に動的生成して `body` へ挿入（単語数バーと同じ方針）。CSSは `.math-context-menu`/`.math-menu-item` を `.mermaid-context-menu` 系と同ルール（`editor.css` のセレクタへ追記）で共有。KaTeXの出力はSVGではなくHTML+CSSのためMermaidの `svgToPngBlob`（SVG→canvas）は使えず、同梱の `html2canvas` で `math-block` のクローン（白背景・`16px 24px` 余白・中央寄せ/横スクロール解除）を直接ラスタライズする（`blockToPngBlob`。`scale` はデバイスピクセル比考慮で最低4x）。Webフォント（KaTeXのwoff2）の読み込み未完了だと字形が崩れるため `document.fonts.ready` を待ってから描画し、`navigator.clipboard.write(ClipboardItem)` で書き込む。失敗（html2canvas未読込・Blob生成失敗等）でも例外は投げずトーストで通知。`editor.js` 初期化で `mathModule.setupContextMenu(state.editor)` を配線（対象が `.math-block` のときだけ `preventDefault` で既定メニューを抑止）。`media/modules/` のみの変更のため統合テストは非対象。ユニットテスト12件を追加（`findMathBlock`／`computeMenuPosition` の純粋関数・メニュー動的生成・右クリック表示と非数式での非抑止・項目クリックで閉じる・html2canvas未読込時のフォールバック）。テスト用に helper の `MODULE_FILES` へ `math.js` を追加。実機（拡張機能開発ホスト）でPNGコピーの見え方を確認予定。 |
| 2026-07-18 | `1f4415e` docs: 行番号表示（L）をS/M子項目3件へ分割 | ROADMAP最優先の「行番号を表示する」はサイズLで着手前に分割が必要だったため、`/evolve` の分割サイクルとして `docs/ROADMAP.md` を書き換え（コード変更なし）。(1) Rawモード（textarea）の行番号ガター〈S・依存なし〉、(2) WYSIWYG側「トップレベルブロック→ソース開始行」対応付けの純粋関数＋ユニットテスト〈M・依存なし〉、(3) WYSIWYG側の行番号ガター描画と(2)の同期〈M・(2)依存〉の3件に分割し、各行へ設計方針（`htmlToMarkdown` での直列化＋空行積算で開始行を厳密化・折り返し行に番号を出さないVS Code準拠・動的生成で `markdownEditor.ts` を触らない方針）を記載。次サイクル以降で(1)から実装予定。 |
| 2026-07-18 | `fcf630e` feat: Rawモードの行番号ガター（行番号表示 1/3） | 分割項目(1/3)の実装。Rawモード（`#rawEditor` textarea）の左端にMarkdownソースの行番号を表示するガター（`.raw-line-gutter`）を追加。行番号を論理行と1対1で正確に揃えるため textarea を `white-space: pre`（折り返しオフ・横スクロール）へ変更（VS Codeの `editor.wordWrap: off` と同じ挙動。旧 `pre-wrap`＋`word-wrap:break-word` から変更）。行数の数え上げは純粋関数 `utils.countLines`（`\n` 区切り・空文字は1行・末尾改行は次の空行を+1行・保険で `\r\n?` も1改行）、番号列の生成は `utils.buildLineNumberText`（`1\n2\n…\nN`・0以下でも最低"1"）で `utils.js` に追加。ガターは `white-space: pre` の1要素へ番号列を流し込み、textareaの縦スクロールに `transform: translateY(-scrollTop)` で追従（横スクロールでは動かさない）。font-family/font-size/line-height/上padding(20px)をtextareaと厳密一致させて行位置を合わせる。UIは `editor.js` が `#rawEditor` を flex 行のラッパー（`.raw-editor-wrap`）で包む形で動的生成し、`markdownEditor.ts`（Webview HTML）は非変更（`media/` のみ＝統合テスト非対象）。可視制御はラッパーの `display`（Rawで `flex`／プレビューで `none`）で行い、`toggleRawMode` の `rawEditor.style.display` 直接操作を `showRawLineGutter`/`hideRawLineGutter` へ置換。入力・文書更新（VS Codeからのpush含む）・Raw突入時に行番号を更新。ユニットテスト9件を追加（`countLines` 5件・`buildLineNumberText` 4件）。DOM配線（`editor.js`）はhelperが読み込まない領域のため単語数バーと同じく純粋関数のみ検証。実機（拡張機能開発ホスト）で番号の整列・スクロール追従を確認予定。 |
| 2026-07-18 | `5b72280` feat: WYSIWYG行番号の「ブロック→ソース開始行」対応付け（行番号表示 2/3） | 分割項目(2/3)。行番号表示の中核となる純粋関数 `markdown.computeBlockStartLines(finalMarkdown, blockMarkdowns)` を `markdown.js` に追加（DOM非依存＝ユニットテスト可能・UIは含めず(3/3)で使用）。`finalMarkdown`（`htmlToMarkdown` の確定ソース＝唯一の変換規則）と、各トップレベルブロックを `serializeBlockElement` 相当で直列化した文字列配列を受け、各ブロックの**1始まり開始行番号**の配列を返す（本文を持たない空段落等は `null`）。実装方針: 各ブロックの本文行（補助純粋関数 `coreLinesOf` で前後の空行を落とした行列）は `finalMarkdown` 内に順序どおり連続で現れるため、直前に確定した位置（cursor）から前方一致で開始行を探す。これによりブロック間の空行畳み込み（`htmlToMarkdown` の `\n{3,}`→`\n\n`）・先頭空行除去に依存せず「定義上一致」し、同一本文の段落が並んでも順序（cursor前進）で正しく対応づく。`coreLinesOf`/`computeBlockStartLines` を公開APIへ追加。ユニットテスト10件（`coreLinesOf` 4件・`computeBlockStartLines` 6件＝段落2つ[1,3]・見出し＋段落・コードブロックの空行ずれ・空ブロックnull・同一本文の順序対応・実DOM経由でmarkdownToHtml→ブロック直列化→開始行が本文行と一致）。`media/modules/` のみ＝統合テスト非対象。次サイクルで(3/3)ガター描画を本関数で実装予定。 |
| 2026-07-18 | `092f2a3` feat: WYSIWYG行番号の「表示ブロック→開始行」対応（行番号表示 3/3 橋渡し） | (3/3)ガター描画の前段として、ライブの `#editor` から各表示ブロックのソース開始行を対応づける `markdown.computeEditorLineMap(editorEl)` を追加（`{ block: 表示中のライブ要素, line: 開始行 }` の配列を返す・本文なしは除外）。クリーン化ロジックを `getCleanHtmlFromEditor` と共有するため、clone生成・整形部を `getCleanEditorClone(editorEl)` へ切り出し（`getCleanHtmlFromEditor` はその `.innerHTML` を返す薄いラッパへ・挙動不変。`state.editor` 固定参照を引数 `editorEl` 化）。実装: クリーンなクローンから `finalMarkdown`（全体）と各トップレベルブロックの直列化を得て (2/3) の `computeBlockStartLines` で開始行を求め、「表示中のライブ要素列」（Mermaidの隠し `pre.mermaid-source` を除外）とインデックスで対応づける。クリーンのトップレベル数と表示中ライブ要素数は常に一致する（Mermaidの隠しpre↔可視 `.mermaid-container`、テーブルの `.table-container`→`table` アンラップを経ても）ことを利用。レイアウト非依存でjsdomユニットテスト4件を追加（見出し＋段落の実要素対応・コードブロックの空行ずれ・Mermaidは可視コンテナへ対応し隠しpreは対象外・テーブルは `.table-container` へ対応）。`getCleanEditorClone`/`computeEditorLineMap` を公開APIへ追加。`media/modules/` のみ＝統合テスト非対象。残るガター描画（各 `block.offsetTop` へ絶対配置＋スクロール/リサイズ同期）はブラウザ専用のため別項目（実機確認前提）。 |
| 2026-07-18 | `5c82d73` feat: WYSIWYG行番号ガターの描画とスクロール同期（行番号表示 3/3 完了） | (3/3)の描画部。WYSIWYG（プレビュー）モードでも `#editor` 左端に行番号ガターを表示する。`editor.js` が `#editor` を flex ラッパー（`.wysiwyg-editor-wrap`）で包み左に `.wysiwyg-line-gutter` を置く（Rawガターと同方式・`markdownEditor.ts` 非変更）。`markdown.computeEditorLineMap`（3/3橋渡し）が返す各 `{block, line}` について、ブロック上端を `getBoundingClientRect().top - editorRect.top + scrollTop`（offsetParent非依存）で `#editor` 内容座標へ変換し、その `top` に開始行番号を絶対配置（複数行ブロックは先頭行のみ＝VS Code準拠）。縦スクロールは `transform: translateY(-scrollTop)` で追従（`#editor` の `scroll`）、内容変化（input・150msデバウンス／文書更新後の `scheduleWysiwygGutterUpdate`）とウィンドウリサイズで再配置。モード可視制御はラッパーの `display` で行い、`toggleRawMode` の `editor.style.display` 直接操作を `showWysiwygLineGutter`/`hideWysiwygLineGutter` へ置換（Raw突入時は隠す）。`media/`（editor.js/editor.css）のみで `markdownEditor.ts` 非変更＝統合テスト非対象。ブロック→行の対応は既存ユニットテスト（`computeEditorLineMap`）で担保、ピクセル整列・スクロール追従はレイアウト依存のため実機（拡張機能開発ホスト）確認前提。これで行番号表示（元Lの3分割）が一通り完了。 |
| 2026-07-18 | `78b0295` feat: 空テーブルのMarkdown生成とカーソル位置への挿入（表挿入の中核） | 「右クリックメニューから表を挿入」(M) の中核（データ層）を先に実装。`table.js` に純粋関数 `buildEmptyTableMarkdown(rows, cols)`（ヘッダ1行＋区切り行＋本文rows行の空テーブルMarkdownを生成・0以下でも最低1行1列を保証）と `insertTable(rows, cols)`（生成した空テーブルを `markdownToHtml` に通し、キャレットのあるトップレベルブロック直後＝無ければ末尾へ挿入し、`render()` で即インタラクティブ化。挿入方式は `commands.insertToc` と同じ）を追加し公開APIへ。ユニットテスト5件（`buildEmptyTableMarkdown` 3件＝列数・本文行数・区切り行の形・0以下フォールバック／`insertTable` 2件＝キャレット直後への挿入とレンダリング済み確認・キャレット無しで末尾挿入）。`media/modules/` のみ＝統合テスト非対象。右クリックメニュー＋行数・列数入力ダイアログのUI（これらから `insertTable` を呼ぶ）は実機確認前提の別項目としてROADMAPに分割。 |
| 2026-07-19 | `bebed92` fix: ブロック数式（`$$…$$`）新規入力時の自動レンダリングと生ファイル破損の修正 | ROADMAP最優先バグ。空行に `$$`→Enter→式→Enter→`$$` と**新規入力**したブロック数式が描画されず、書き戻し時に各 `$` が `\$` へエスケープされ生ファイルが `\$\$`／式／`\$\$`（段落が空行で分断）に破損していた。**根本原因**: 複数行 `$$…$$` にはライブ編集時の変換経路が無く（インライン `$…$` は `convertInlineText` が変換するが改行を含む `$$…$$` は対象外、`handleAutoBlock` も `- * 1. >` のみ）、読込時 `markdownToHtml` か既存 `raw-math-block` の collapse でしか math-block 化されなかった。**修正**: `commands.js` に `convertMathBlocks` を追加し `applyInlineFormatting`（入力イベント）から `walkInline` の前に実行。エディタ直下の平文ブロック（class無しの P/DIV）のうち単独 `$$` 行を開き・次の単独 `$$` 行を閉じとして扱い、間の平文ブロックの**生テキスト**（`$` を非エスケープ）を式本文に集めて `markdown.buildMathBlockHtml`〈読込時と同じ生成関数〉で math-block へ畳む（＝ライブ変換と読込時の結果が一致・往復で `$$…$$` に戻り `$` が壊れない）。閉じ `$$` がまだ無い入力途中は変換しない。範囲内に非平文ブロック（テーブル等）が挟まれば打ち切る。キャレットが変換範囲内なら `contenteditable="false"` の math-block へは入れないため直後に空段落を挿入して移し、`caretHandled=true` で従来のオフセット式 `restoreCursorPosition` を回避。`media/modules/` のみ＝統合テスト非対象。ユニットテスト7件を追加（3ブロック変換・往復で `\$` 非破損の回帰・入力途中は非変換・複数行本文の集約・非平文で打ち切り・キャレット移動・`applyInlineFormatting` 統合経路）。実機（拡張機能開発ホスト）で入力時の即時描画を確認予定。あわせて既存テスト調査中に別バグ（全選択上書き貼り付けで空の殻ブロックが残る＝`commands.test.ts` が main で1件fail）を発見しROADMAPへ追記。 |
| 2026-07-19 | `ec815e5` fix: 全選択への上書き貼り付け（Ctrl+A→Ctrl+V）で先頭に空見出しの殻が残る問題の修正 | 前サイクルで発見・ROADMAP最優先化したバグ（`commands.test.ts` の「全選択への上書き貼り付けで空の殻ブロックが残らない」が失敗し `/evolve` のテストゲートを1件落としていた）を修正。**根本原因**: 見出しの一部から選択して貼り付けると `range.deleteContents()` が見出し本文だけを削り、`# ` マーク表示用スパン（`.heading-hash`）を残す。`handleMarkdownPaste` の選択削除後の殻ブロック掃除は生の `el.textContent.trim() === ''` で空判定していたため、`# ` を含む空見出しが「本文あり」と誤判定され除去されずに残り、書き戻しで `# ` 段落が混入していた（実測 `# \n\n段落1…` / 期待 `段落1…`）。**修正**: 掃除の空判定を、既に同関数内にある `shellIsEmpty`（`.heading-hash` スパンを本文から除外する純粋ヘルパ）へ置き換え。これで空見出しの殻も空と判定され除去される。見出しに本文が残る部分選択のケースは `shellIsEmpty` が false を返すため誤除去しない。`media/modules/` のみ＝統合テスト非対象。既存の失敗テストが解消（380 passing・0 failing）、加えて「見出しの一部だけ選択して貼り付けても残る見出しは消えない」回帰テスト1件を追加。 |
| 2026-07-19 | `bec3a24` fix: 見出し行でEnterを押すと改行直後にも見出しテキストが重複表示される問題の修正 | ROADMAP最優先バグ。`## ああ` を入力してEnterを押すと、確定後の新しい行にも「ああ」が複製表示されていた。**根本原因**: `utils.findBlockAncestor` は P/DIV/LI しかブロックとして返さず見出し（h1〜h6）を対象外にしていたため、`commands.handleHeadingConfirm` が「レンダリング済み見出しの内側でのEnter」を捕まえられず `return false`→ブラウザ既定のEnter処理（contenteditable上での見出しの複製・分割）に委ねていた。`## ああ` は入力後150msのsync（`onDidChangeTextDocument`→`update`）で段落がh2へ再レンダリングされるため、Enterがこの未処理経路に落ちていた（生の `<p>## ああ</p>` から直接Enterする経路は従来どおり正しく変換されていた）。**修正**: `handleHeadingConfirm` に見出し祖先検出（`utils.findAncestor` で `H[1-6]`）を追加し、見出し内でのEnterは新設 `confirmRenderedHeading` が処理する。キャレット位置から見出し末尾までを `Range.extractContents` で切り出し、内容があれば新しい `<p>` へ・空なら `<p><br></p>` を見出し直下へ挿入してキャレットをそこへ移し、処理できた場合のみ `event.preventDefault()`/`stopPropagation()` でブラウザ既定を抑止する（末尾Enter＝空段落、途中Enter＝後半のみ新段落へ移動しテキストは失われない）。`media/modules/` のみ＝統合テスト非対象。ユニットテスト5件を追加（生`## ああ`段落の見出し化・レンダリング済み見出し末尾での非複製と単一見出し維持・途中Enterでの後半分割・非見出し段落は非処理・Shift+Enterは非処理）。 |
| 2026-07-19 | `cb86e8c` fix: 編集中にキャレットが先頭へ飛ぶ問題の応急処置（先頭飛び防止のフォールバック） | ROADMAP最優先バグの**対症療法**。`utils.restoreCursorPosition` はキャレット保持を「エディタ先頭からの文字数オフセット」で行うが、保存時より本文が短くなると目的オフセットに届かず `found=false` になり、従来は**range を一切設定せず**に抜けていた。直前に `handleUpdateMessage` の `editor.innerHTML = …`（全書き換え）で選択が破棄されていると、選択が無い＝ブラウザがキャレットをエディタ先頭へ描画し「一番上へ飛ぶ」症状になっていた。**修正**: 復元失敗時、(1) エディタ内に妥当な選択が残っていればそれを尊重（従来動作を維持し不要にキャレットを動かさない）、(2) 選択が失われている時だけ、走査で到達できた最寄り位置（最後のテキストノード末尾、本文が無ければエディタ末尾）へフォールバックして先頭飛びを防ぐ。**コードレビュー（`/code-review high`）指摘**により、`innerHTML` 全書き換え後にブラウザが `(editor, 0)` へ collapse した選択を「妥当な選択」と誤判定して先頭に留めてしまうギャップを発見し、`startContainer === state.editor` を妥当な選択から除外して同サイクルで修正（回帰テスト1件追加）。**根本原因（文字数オフセット方式・埋め込みウィジェットや競合updateでの文字数ずれ）は未解決で、ROADMAP にノード基準アンカー化の根本対策として残置。** `media/modules/` のみ＝統合テスト非対象。ユニットテスト4件を追加（先頭飛び防止・妥当な選択の維持・`(editor,0)` collapse からの復帰・空本文フォールバック）。 |
| 2026-07-19 | `36e6631` feat: ブロック数式コピー画像の崩れ修正に向けた foreignObject SVG 組み立て純粋関数の追加（1/2） | ROADMAP最優先バグ「ブロック数式を右クリックでコピーした画像が崩れる（上付き/下付きが潰れ・√が左上へ飛ぶ）」の分割(1/2)。**調査で根本原因を特定**: 数式は KaTeX を `output: 'html'`（[math.js:55](../media/modules/math.js#L55)）でHTML+CSS配置として描画しており、画像化に使う **html2canvas がこのCSS配置（`.katex` の `vertical-align`・負マージン・絶対配置、√の overline 罫線）を再現できない**ため崩れる。html2canvas 側の限界のため設定調整では直らず、**html2canvas をやめて `.math-block` のHTMLを SVG `<foreignObject>` に入れ、ブラウザ自身のレンダリングでラスタライズする方式**（mermaid の `svgToPngBlob` と同じ Image→canvas の流れ）へ移行する。**本(1/2)**: そのラスタライズ前段の純粋関数 `math.buildMathBlockSvgMarkup(innerHtml, cssText, width, height, background)` を追加。SVG/`foreignObject`/xhtml名前空間・`<style>`インライン・寸法正規化（最低1px・切り上げ）・背景色（`transparent`／未指定で透過、色指定で全面 `rect`）・背景属性のエスケープを組み立てる。まだ呼び出し側からは未使用（現行のコピー挙動は不変）で安全。ユニットテスト6件を追加（構造・透過・背景塗り・エスケープ・style省略・寸法正規化）。**実機不要**（文字列組み立てのみ）。**(2/2)＝`blockToPngBlob` の差し替え・KaTeXフォントの base64 埋め込み・CSP `font-src … data:` 追加・実機での見た目確認**は ROADMAP に残置（`img-src … data:` は許可済みだが `font-src` は `data:` 不許可のため、foreignObject の隔離描画でフォントを出すにはフォント埋め込み＋CSP変更の両方が要ることも調査で判明）。`media/modules/` のみ＝統合テスト非対象。 |

| 2026-07-19 | `042b500` fix: マウスドラッグでの本文選択が装飾記号（`**`等）に到達すると解除される問題の修正 | ROADMAP最優先バグ。マウスで本文を範囲選択していくと、`**`（太字）等の装飾マーカー部分に差しかかった瞬間に選択が解除され、装飾をまたいだテキストを選択・コピペできなかった。**根本原因**: `selectionchange` ごとに走る `commands.syncRawMarkdownToCaret`（[commands.js:1289](../media/modules/commands.js#L1289)）が、選択の開始位置（`startContainer`）の所属だけで判定し、キャレット／選択が装飾（`<strong>` 等）の内側に入ると生Markdown（`**太字**`）へ**展開**＝`editor.innerHTML` 相当のDOM書き換えを行い、進行中のドラッグ選択（Range）を破棄していた。**修正**: 生Markdown表示は本来「キャレット位置」を示す機能で範囲選択中は不要なため、選択が非collapsed（範囲選択）の間は `syncRawMarkdownToCaret` を早期 `return false` して展開・折り畳みを一切行わないようにした（新規stateやmousedown/mouseup監視は追加せず、範囲選択の判定だけで解決）。これによりドラッグは装飾をまたいでも継続し、装飾はレンダリング表示のまま選択・コピー（`getSelectedMarkdown` が `<strong>`→`**太字**` へ直列化）できる。既に展開中のspanも範囲選択中は折り畳まれないため、従来の「展開テキストをドラッグ選択して編集」も維持される（選択が collapse した次の `selectionchange` で通常どおり展開・折り畳みが再開）。`media/modules/` のみ＝統合テスト非対象。ユニットテスト3件を追加（装飾をまたぐ範囲選択で非展開かつ選択保持・開始が装飾内でも非展開・展開中spanの非折り畳み）。実機（拡張機能開発ホスト）でドラッグ選択の連続性を確認予定。 |

| 2026-07-19 | `225ca19` fix: 編集中にキャレットがずれる問題への根本対策（ノード基準アンカーの導入・インプレース経路） | ROADMAP最優先バグ「編集中に突然キャレットが先頭へ飛ぶ／ずれる」の根本原因＝キャレット保持が**エディタ先頭からの文字数オフセット**方式（保存時と復元時でキャレット前の文字数が変わると失敗）への対策。**修正**: `utils.saveCursorPosition`（[utils.js:83](../media/modules/utils.js#L83)）が従来の `offset`/`text` に加えて**ノード基準アンカー** `node`（=`range.endContainer`）/`nodeOffset`（=`range.endOffset`）を保持し、`utils.restoreCursorPosition` はまず保存ノードが**テキストノードとしてエディタ内に生きていれば**、文字数オフセット走査を経ずにそのノードの相対位置へ直接復元する。これにより入力イベント経路（[editor.js:373-377](../media/editor.js#L373)・`applyInlineFormatting` 後の復元）では、埋め込みウィジェット（KaTeX数式・Mermaid・テーブル）の内部テキスト量が保存時と復元時で変わってもキャレットが別の場所へずれない（`normalize` で保存ノードが消えた場合はオフセット方式へ自動フォールバック）。ノードが消えている場合（`handleUpdateMessage` の innerHTML 全書き換え）は従来のオフセット方式＋最寄りフォールバック（`cb86e8c`）に委譲するため**後方互換**（既存テスト全通過）。`nodeOffset` はノード長へクランプして例外を出さない。**コードレビュー（`/code-review high`）指摘**により、要素コンテナ（キャレットが子ノード境界にある場合）は `nodeOffset` が childNodes インデックスで、子のインプレース増減で古くなり得るため、ノード基準の適用対象を**テキストノードに限定**（要素境界は従来のオフセット方式に委ねる方が安全）して同サイクルで修正。ユニットテスト4件を追加（アンカーの保持・前方文字数が変わっても正確復元・ノード消失時のオフセットフォールバック・範囲外nodeOffsetのクランプ）。**残る根本対策（innerHTML全書き換え経路のブロックID基準アンカー・Mermaid非同期描画完了後の復元・競合する古いupdateの無視）は ROADMAP に残置＝実機確認前提**。`media/modules/` のみ＝統合テスト非対象。 |

| 2026-07-19 | `9e8ea82` feat: Mermaid画像コピー／保存の背景色選択（透過・白・黒）の中核（1/2） | ユーザー要望（ROADMAP優先度・高）の分割(1/2)。現状 Mermaid図のPNG化は背景が**白で固定**だった（`svgToPngBlob` が `getContext('2d',{alpha:false})`＋`fillStyle='#ffffff'`／`fillRect`、html2canvas も `backgroundColor:'#ffffff'`）。**本(1/2)**: `mermaid.js` に純粋関数 `resolveImageBackground(background)`（[mermaid.js:337](../media/modules/mermaid.js#L337)）を追加し、`'transparent'`→`{alpha:true, fillColor:null, html2canvasBackground:null}`（透過）／`'white'`・`'black'`→不透明＋該当色／未指定・未知→白（従来動作）へ解決する。`svgToPngBlob(svg, scale, background='white')` がこの結果で `getContext` の `alpha`・`fillRect` の有無（透過時は塗らない）・html2canvas の `backgroundColor` を切り替え、`copyToClipboard(previewPanel, background)`／`saveAsPng(previewPanel, background)` が背景を受けて渡すようにした。**既定は 'white' で従来と完全に同一の出力**（現行の呼び出し箇所は背景未指定＝'white'）のため無変更・無リグレッション。透過時に html2canvas 側で白を焼き込まないよう中間ラスタライズの背景も `null` にする。テスト用に `src/test/unit/helper.ts` の `MODULE_FILES` へ `mermaid.js` を追加（`mermaid.min.js` への依存は関数内のみで、読み込み時は `window.MermaidModule` を定義するだけのため jsdom で純粋関数を検証可能）。ユニットテスト5件を追加（背景解決の全分岐: 透過・白・黒・未指定フォールバック・未知値フォールバック）。**呼び出しUI（右クリックメニューで透過／白／黒を選ぶ配線）と PNG の見た目・クリップボード/保存の実機確認は(2/2)＝実機（拡張機能開発ホスト）確認前提**で ROADMAP に残置。`media/modules/` のみ＝統合テスト非対象。 |

| 2026-07-19 | `3768284` fix: 見出し行のIME変換確定Enterで見出しテキストが複製される問題の修正（再発対応） | 「## ああ」と日本語入力してEnterを押すと改行後の行にも「ああ」が複製表示される不具合の**再発対応**。先の修正（`bec3a24`）はレンダリング済み見出し内のEnterをDOM構造面で処理したが、**IME（日本語入力）の変換確定Enterを考慮していなかった**のが真因。`ああ` を変換確定するEnterは `keydown` が `event.isComposing === true` で発火し、この時点で `handleHeadingConfirm` がDOMを書き換えて新しい段落へキャレットを移すと、直後にIMEが確定テキスト「ああ」をその新段落へ挿入して**複製**が起きていた（jsdom単体テストではIMEが無いため再現せず素通りしていた）。**修正**: `handleHeadingConfirm`（[commands.js:1664](../media/modules/commands.js#L1664)）のガードに `event.isComposing` を追加し、変換確定のEnterでは処理せず（`preventDefault` もせず）IMEの確定挙動に委ねる。既存の `handleAlertEnter`／`handleTaskListEnter` が既に持っていた `isComposing` ガードと同じ対処。`media/modules/` のみ＝統合テスト非対象。ユニットテスト1件を追加（isComposing時は非処理・非preventDefault・DOM不変）。テスト用の `fakeKeyEvent` に `isComposing` を追加。 |
| 2026-07-19 | `132bd22` fix: 編集中にキャレットが飛ぶ問題の根本対策（ブロック基準アンカーの導入・innerHTML全書き換え経路） | ROADMAP最優先バグ「編集中に突然キャレットが先頭へ飛ぶ／ずれる」の根本対策のうち、**innerHTML 全書き換え経路**（`handleUpdateMessage` の `editor.innerHTML = …`, [editor.js:611](../media/editor.js#L611)）向けの一手。先行するノード基準アンカー（`225ca19`）はインプレース経路を救うが、全書き換えでは保存ノードが破棄されるため**エディタ先頭からのグローバル文字数オフセット**方式に戻り、キャレットより前にある別ブロック（数式・Mermaid・テーブル）の再描画で文字量が変わるとキャレットが別ブロックへドリフトしていた。**修正**: `utils.saveCursorPosition`（[utils.js:83](../media/modules/utils.js#L83)）が新たに**ブロック基準アンカー** `blockIndex`（キャレットを含むトップレベルブロック＝エディタ直接の子のインデックス）/`blockOffset`（そのブロック内のローカル文字数オフセット）を保持し、`utils.restoreCursorPosition` に第二候補の復元段を追加。ノード基準が使えない（全書き換え）場合、同インデックスのブロックが残っていてブロック内でローカルオフセットに到達できればそこへ復元する。キャレットのブロックに閉じたオフセットなので、**他ブロックのウィジェット再描画による文字量変化ではドリフトしない**。ブロックが存在しない／届かない（構造が変わった）場合は従来のグローバルオフセット走査＋最寄りフォールバック（`cb86e8c`）へ委ねるため**後方互換**（既存テスト全通過）。オフセット走査ロジックは `scanOffset(root, targetOffset)` ヘルパへ切り出してブロック内／エディタ全体で共用。ユニットテスト3件を追加（blockIndex/blockOffsetの保持・前ブロックの文字量が変わってもブロック基準で正確復元・blockIndex範囲外でのグローバルフォールバック）。**残る根本対策（Mermaid非同期描画完了後の復元・競合する古いupdateの無視／バージョン付与）は ROADMAP に残置＝実機確認前提**。`media/modules/` のみ＝統合テスト非対象。 |
| 2026-07-19 | `e60bee2` fix: 編集中にキャレットが飛ぶ問題の根本対策（競合する古いupdateエコーの無視・シーケンス番号の導入 1/2） | ROADMAP最優先バグ「編集中にキャレットが飛ぶ」の根本対策の続き（(1/2) Webview側インフラ）。**競合レース**: タイプ中に「AB」を送った直後に「ABC」を送ると、拡張機能から遅れて届いた「AB」のエコー `update`（＝より古い編集の反映）が現在の「ABC」を巻き戻し、`handleUpdateMessage` の innerHTML 全書き換えでキャレットを乱していた（既存の `incoming === lastSentMarkdown` ガードは `lastSentMarkdown` が既に「ABC」に更新済みのため古いエコー「AB」を弾けない）。**本(1/2)**: Webview が編集を送るたびに単調増加する **編集シーケンス番号** `state.editSeq`（[state.js](../media/modules/state.js)）を採番し、全送信経路を共通の `postEdit(content)`（[editor.js:348](../media/editor.js#L348)）へ集約して `seq` を載せる。判定は純粋関数 `utils.shouldIgnoreStaleUpdate(messageSeq, currentEditSeq)`（[utils.js](../media/modules/utils.js)）＝エコーの seq が最新送信 seq より小さければ無視・未指定（外部編集/初期ロード）や同値は適用、を `handleUpdateMessage` の先頭ガードに配線。**拡張機能側が seq をエコーへ反映する配線は(2/2)**（`src/markdownEditor.ts`＝統合テスト対象）で、**それまでは `message.seq` が常に undefined＝本ガードは無効で完全に従来動作**（無リグレッション）。ユニットテスト5件を追加（seq判定の全分岐＋editSeqの初期値/読み書き）。`media/` のみ（`src/` 非変更）＝統合テスト非対象。**(2/2)＝拡張機能側のseqエコー配線＋実機での高速タイプ時キャレット挙動確認は実機確認前提**で ROADMAP に残置。 |
| 2026-07-19 | `1aca68d` fix: 編集中にキャレットが飛ぶ問題の根本対策（競合updateエコー無視の拡張機能側seqエコー配線 2/2） | (1/2, `e60bee2`) で入れた「競合する古いエコーの無視」ガードを**実効化**する拡張機能側の配線。前回まで `message.seq` が常に undefined でガードは無効（従来動作）だった。**本(2/2)**: `src/markdownEditor.ts` の `resolveCustomTextEditor` に `pendingEchoSeq` を保持し、Webview から受けた `edit` の `seq`（[markdownEditor.ts:76](../src/markdownEditor.ts#L76)）を、その編集適用に**起因するエコー `update` にだけ**反映する。`edit` ハンドラは `applyEdit` が同期的に `onDidChangeTextDocument` を発火させるため、**適用前に** `pendingEchoSeq = e.seq` を保持し、`updateWebview` が `seq: pendingEchoSeq` を載せて**一度きり消費**（`undefined` へリセット）する。外部編集（Webview 起因でない変更）は `pendingEchoSeq` が付かず常に適用。内容が同一で変更が起きない場合は `updateTextDocument` が **`false`**（従来は `true`）を返すよう変更し、エコーが発火せず消費されない seq を `edit` ハンドラが後始末する（次の外部変更へ誤って持ち越さない）。これでタイプ中に古いエコーが新しいローカル内容を巻き戻す＝キャレット巻き戻りが防がれる。`src/markdownEditor.ts` 変更のため統合テストを実行（9件パス。`updateTextDocument` の戻り値が「実際に編集を適用したか」を表すことの回帰テスト1件を追加）。**実機での高速タイプ・数式/Mermaid編集時のキャレット挙動確認は実機（拡張機能開発ホスト）前提**で残置。 |
| 2026-07-19 | `8750ec2` fix: 編集中にキャレットが飛ぶ問題の根本対策（ブロック基準アンカーの署名によるindexずれ耐性） | 「編集中にキャレットが飛ぶ」根本対策の一つ、ブロック基準アンカー（`132bd22`）の弱点だった**ブロックの挿入・並べ替えで `blockIndex` がずれる**問題への対処。全書き換えでキャレットのブロックより前に別ブロックが挿入されると、`blockIndex` が別ブロックを指し、その中のローカルオフセットへ誤って復元していた。**修正**: `utils.saveCursorPosition` がブロックの**署名**（先頭テキスト64文字・空白畳み込み）`blockSignature` も保持し（純粋ヘルパ `utils.blockSignatureOf`, [utils.js](../media/modules/utils.js)）、`utils.restoreCursorPosition` のブロック基準復元段で、署名が**一意に一致**するトップレベルブロックがあればそれを優先して復元する。一致が無い（＝キャレットのブロック自体が編集されて署名が変わった）／複数一致で曖昧な場合は従来どおり `blockIndex` で引く（**後方互換**・既存テスト全通過）。これでキャレットより前にブロックが挿入・並べ替えされても、キャレットのブロックを内容で辿って正しく復元できる。ユニットテスト5件を追加（署名ヘルパの空白畳み込み/64字丸め/null・前挿入でindexずれ時の署名一致復元・キャレットブロック編集時のindexフォールバック）。`media/modules/` のみ＝統合テスト非対象。**残るはMermaid非同期描画完了後の復元のみ**（実機確認前提で ROADMAP に残置）。 |
| 2026-07-19 | `e877965` fix: 編集中にキャレットが飛ぶ問題の根本対策（Mermaid非同期描画完了後にキャレットを復元） | 「編集中にキャレットが飛ぶ」根本対策の**最後の一手**。`handleUpdateMessage` の innerHTML 全書き換え経路（[editor.js:631](../media/editor.js#L631)）では、Mermaid の**非同期**再描画（`mermaidModule.render()` は Promise を返す）が完了する前に `setTimeout(0)` でキャレットを復元していたため、描画完了前はブロックの高さ・ノード構成が未確定でアンカー（ノード基準／ブロック基準・署名）がずれ、キャレットが飛ぶ余地が残っていた。**修正**: 純粋ヘルパ `utils.restoreCaretAfterRender(renderPromise, restoreFn)`（[utils.js](../media/modules/utils.js)）を追加し、`mermaidModule.render()` の戻り Promise の**解決（または失敗）を待ってから**キャレットを復元するようにした。Promise でない値（同期描画のみ／描画対象なし）が渡された場合は従来どおり `setTimeout(0)` 相当で復元し、描画が失敗しても DOM 構造自体は確定しているため復元は試みる（後方互換）。復元はブロック署名・オフセット（DOM構造）ベースでピクセルレイアウトに依存しないため、Promise 解決後のマイクロタスクで安全に走る。これで応急処置（`cb86e8c`）＋ノード基準（`225ca19`）＋ブロック基準＋署名（`132bd22`/`8750ec2`）＋競合エコー無視（`e60bee2`/`1aca68d`）に続く一連の対策が完結。ユニットテスト4件を追加（Promise解決後に復元・reject時も復元・非Promiseはマクロタスク復元・不正コールバックで例外を出さない）。`media/`（`editor.js`・`modules/utils.js`）のみ＝統合テスト非対象。**高速入力・数式/Mermaid編集時のキャレット挙動の最終確認は実機（拡張機能開発ホスト）前提**。 |
| 2026-07-19 | `ecf34a5` fix: ブロック数式コピー画像の崩れ修正 — KaTeXフォント埋め込みの純粋コア（配線前段） | ブロック数式（`$$…$$`）を右クリック「画像としてコピー」した際、html2canvas が KaTeX の CSS 配置（`.katex` の `vertical-align`・負マージン・絶対配置・√overline）を再現できず画像が崩れる問題への根本対策（foreignObject 方式移行）の前段。foreignObject を `<img>` 経由でラスタライズすると SVG は**隔離コンテキスト**で描画され外部の CSS/フォントを参照できないため、KaTeX の woff2 フォントを base64 で `@font-face` として SVG 内へ埋め込む必要がある。**本サイクル**: `math.js` に純粋関数 `buildKatexFontFaceCss(fonts)`（base64 化した woff2 配列から `src: url(data:font/woff2;base64,…) format("woff2")` の `@font-face` 群を組み立てる。`family` は引用符・バックスラッシュ除去、`style` は italic 以外→normal、`weight` は3桁数値以外→400、`base64` は base64 アルファベット以外を除去、空はスキップ、と防御的に正規化）と、埋め込む20フォントの静的一覧 `KATEX_FONT_MANIFEST`（family/style/weight とファイル名）を追加。既定挙動は不変（`blockToPngBlob` は現状 html2canvas のまま＝無リグレッション）。ユニットテスト10件を追加（`@font-face` 組み立ての全分岐＋マニフェストの各 woff2 がディスクに実在すること＋`katex.min.css` の `@font-face` 群と過不足なく一致することの網羅テスト）。**残り＝ランタイム配線**（`blockToPngBlob` の foreignObject 差し替え・フォントの fetch→base64・CSP へ `font-src … data:` 追加）は `src/markdownEditor.ts` を触る統合テスト対象サイクル＋**実機確認前提**で ROADMAP に残置。`media/modules/` のみ＝統合テスト非対象。 |
| 2026-07-20 | `c1741c6` fix: ブロック数式コピー画像の崩れ修正 — foreignObject 方式への配線（実機確認前提） | 前段（純粋コア `36e6631`/`ecf34a5`）に続くランタイム配線。`blockToPngBlob`（[math.js](../media/modules/math.js)）を html2canvas 直接ラスタライズから **`<foreignObject>`（ブラウザネイティブ描画）方式へ差し替え**、html2canvas が再現できない `.katex` のCSS配置（`vertical-align`・負マージン・絶対配置・√overline）を正確に画像化する。処理: (a) 画面外ラッパー（余白付き・左寄せ）の実寸を `getBoundingClientRect` で採寸、(b) `loadKatexFontFaceCss` が `KATEX_FONT_MANIFEST` の各 woff2 を webview URI（`resolveKatexFontBaseUrl` で導出した `fonts/` ベース）から fetch→`arrayBufferToBase64`→`buildKatexFontFaceCss` で `@font-face` を組み立て（全取得成功時のみキャッシュ・部分失敗はリトライ）、(c) `collectKatexCssText`（読み込み済み KaTeX スタイルシートのルール）＋(b) を `buildMathBlockSvgMarkup` に渡してSVG化、(d) `svgMarkupToPngBlob` が `data:image/svg+xml` を `new Image()`→canvas→PNG Blob（load 15秒タイムアウトの保険付き）。foreignObject は隔離コンテキストのため外部フォントを参照できず、フォント埋め込みが要る。**CSP変更**: `font-src ${webview.cspSource}` → `font-src ${webview.cspSource} data:`（[markdownEditor.ts](../src/markdownEditor.ts)）。**フォールバック**: foreignObject 経路が失敗（後述の canvas taint 等）した場合は従来の html2canvas 方式へ退避＝**無リグレッション**。ユニットテスト15件（`resolveKatexFontBaseUrl`4／`arrayBufferToBase64`3／`loadKatexFontFaceCss`4＝link無し・全成功・キャッシュ・部分失敗スキップ＋リトライ／copyBlockAsPng の失敗トースト）。統合テスト9件パス（CSP変更のため実行）。**⚠️ 重大な実機確認事項**: Chromium は `<foreignObject>` を含む SVG を canvas に `drawImage` すると canvas を taint し `toBlob()` が SecurityError を投げる既知の挙動があり、VS Code webview でも起きると新経路は毎回フォールバックへ落ち崩れ修正が発現しない恐れがある。実機で「崩れが直った画像がコピーされるか／コンソールに `falling back to html2canvas` 警告が出て taint しているか」の確認が必須（taint する場合の MathJax 切替は ROADMAP の実機確認 todo に残置）。`src/markdownEditor.ts` 変更＝統合テスト対象。 |
| 2026-07-20 | `7a1f50e` feat: Mermaid図の画像コピー／保存時に背景色（透過・白・黒）を選べるUI（実機確認前提） | 中核(1/2, `9e8ea82`)で実装済みの背景解決（`resolveImageBackground`＋`svgToPngBlob(svg, scale, background)`）に対する**呼び出しUIの配線(2/2)**。Mermaid右クリックメニュー（`#mermaidContextMenu`, [markdownEditor.ts](../src/markdownEditor.ts)）上部に**背景トグル**（`.mermaid-bg-btn` の `data-bg="transparent|white|black"`・既定 `white` が `active`）を追加し、「画像をコピー」／「PNG画像として保存」実行時に選択背景を `copyToClipboard`/`saveAsPng` の第2引数へ渡す。配線は `mermaid.js` の `setupContextMenuEvents`: 背景ボタンのクリックで `active` を移し（メニューは閉じない）、アクション実行時に純粋関数 `resolveMenuBackground(menu)`（`.mermaid-bg-btn.active` の `data-bg` を読む・`transparent`/`black` 以外は `white` へフォールバック）で背景を解決。**既定は白＝背景トグル追加前と同一出力（無リグレッション）**。背景選択はメニューが持続するため直近の選択を記憶する（右クリック間で保持）。CSS（`.mermaid-menu-bg-row`／`.mermaid-bg-btn`）を追加。ユニットテスト5件（`resolveMenuBackground` の全分岐＋null防御・トグルクリックで active が1つだけ移り解決に反映）。統合テスト9件パス（`src/markdownEditor.ts` のメニューHTML変更のため実行）。**PNGの透過/白/黒の見た目・クリップボード/保存・メニュー操作感は実機（拡張機能開発ホスト）確認が前提**（特に html2canvas が透過時に白を焼き込まないか）。`/local-review` 指摘: A なし。C-1（背景選択が右クリック間で持続＝仕様判断）・C-2（アクション→背景引数受け渡しの end-to-end テストは内部関数直呼びのため未追加＝個別ユニットで担保）は対応せず報告のみ。 |
| 2026-07-20 | `f2e684c` fix: Rawモードの行番号が下の行でずれる／一部の行に出ない問題の修正（実機確認前提） | Rawモード（`#rawEditor` textarea）の行番号ガター（`fcf630e` で実装）で、行を下るほど番号が実際の行からずれ、下の方で「一部の行に番号が出ない」ように見える不具合。**原因**: ガター（`.raw-line-gutter` の div）と textarea の行高が unitless `1.6`（=14px×1.6=22.4px の**小数**）で、textarea のネイティブな行描画と div の行送りが端数の扱いで一致せず、行を重ねるほど両者の行位置が食い違って蓄積していた（行番号生成 `utils.countLines`/`buildLineNumberText` の論理自体は正しく `utils.test.ts` で検証済み＝レイアウト側の問題）。**修正**: `#rawEditor` と `.raw-line-gutter` の `line-height` を**整数px（`22px`）に固定**し、両者の行送りを完全一致させた（`media/editor.css` のみ）。`search.js` が読む `parseInt(getComputedStyle(...).lineHeight)` は 22.4→22／22px→22 で同値のためジャンプ挙動に回帰なし（`/local-review` で確認）。**CSSのみ＝ユニットテスト不能・型/lint/compile/既存ユニット453件パス**。`/local-review` 指摘: A なし。B（Raw=px/WYSIWYG=unitless で行高指定が二分）・C（22pxは font-size 14px 前提の暗黙依存）は害小として対応せず報告のみ。**行番号と実際の行の一致・スクロール追従は実機（拡張機能開発ホスト）でしか検証できない**ため実機確認前提。`media/` のみ＝統合テスト非対象。 |
| 2026-07-20 | `531757f` feat: ブロック数式コピー画像の背景色（透過・白・黒）を選べるUI（実機確認前提） | 基盤（`blockToPngBlob(block, scale, background)`）は実装済みで、残っていた**呼び出しUIの配線**。ブロック数式の右クリックメニュー（`math.js` の `ensureContextMenu` が動的生成・`markdownEditor.ts` 非変更）はこれまで「📋 画像としてコピー」の1項目のみで、Mermaidメニューにある背景トグルが無く常に既定の白で出力していた。**変更**: メニュー上部に背景トグル（`.math-bg-btn` の `data-bg="transparent|white|black"`・既定 `white` が `active`。クリックで active を移すだけ＝メニューは閉じない）を追加し、「画像としてコピー」実行時に純粋関数 `resolveMenuBackground(menu)`（`.math-bg-btn.active` の `data-bg` を読む・`transparent`/`black` 以外は `white`。Mermaid の同名関数と同契約）で背景を解決して `copyBlockAsPng(block, background)`→`blockToPngBlob(block, undefined, background)` へ渡す。**`/local-review` 指摘 A-1（high）を同サイクルで修正**: 新規追加した「黒」背景は、KaTeX が色を明示せず `currentColor` を継承するため採寸ラッパーの `wrapper.style.color` が黒固定のままで**黒地に黒文字＝数式が不可視**になっていた。`wrapper.style.color = (bg==='black')?'#ffffff':'#000000'` で黒背景時のみ文字色を白へ反転（foreignObject 経路・html2canvas フォールバックの両方を1行で修正）。CSS は `.math-menu-bg-row`／`.math-menu-bg-label`／`.math-bg-btn` を既存の `.mermaid-*` と同ルールへ追加。**既定は白＝背景トグル追加前と同一出力（無リグレッション）**。ユニットテスト7件（トグルの既定active・クリックでの active 遷移とメニュー非クローズ・`resolveMenuBackground` 全分岐＋null防御・黒選択でSVGの `fill="black"` と文字色白への反転／白選択で `fill="white"` と黒文字の伝播）。型/lint/compile/ユニット465件パス。B-1（透過背景＋黒文字はダーク環境貼付で視認性低＝既存・別課題）は ROADMAP へ。**PNGの透過/白/黒の見た目・黒背景での白文字・クリップボード結果・メニュー操作感は実機（拡張機能開発ホスト）確認が前提**。`media/` のみ＝統合テスト非対象。 |
| 2026-07-20 | `72f488e` fix: 透過背景でコピーした数式画像の文字色（自動／黒／白）を選べるように（実機確認前提） | ROADMAP バグ修正セクションの最上位 todo（前サイクルの `/local-review` B-1 由来）。ブロック数式のPNGコピーは背景（透過／白／黒）を選べるが、文字色は白・透過背景では黒固定（`blockToPngBlob` の `wrapper.style.color`）で、**透過PNGをダークな資料/チャットへ貼ると黒文字が背景に埋もれて見えない**問題。**修正**: 右クリックメニューに**文字色トグル**（`.math-fg-btn` の `data-fg="auto|black|white"`・既定 `auto` が `active`）を背景トグルの下へ追加。背景・文字の2行は新ヘルパ `buildToggleRow(label, btnClass, dataAttr, options)` で生成し、クリックで**自行内の active のみ**を移す（行が独立＝背景選択と文字色選択は互いに影響しない）。文字色の解決は純粋関数を2段に分離: `resolveMenuTextColor(menu)`（`.math-fg-btn.active` の `data-fg`・`black`/`white` 以外は `auto`）でモードを読み、`resolveTextColor(background, textColor)`（`black`/`white` はその16進色、`auto` は背景追従＝黒背景のみ白・白/透過は黒）で実際の文字色を決めて `wrapper.style.color` へ与える。`copyBlockAsPng(block, background, textColor)`→`blockToPngBlob(block, undefined, background, textColor)` と第3引数を配線。**既定（背景=白・文字=auto）は従来と同一出力（無リグレッション）**＝前サイクルの黒背景白文字反転も `auto` の背景追従として維持。CSS は `.math-fg-btn` を既存 `.math-bg-btn`/`.mermaid-bg-btn` と同ルールへ追加。`/local-review` 指摘: A/B なし。C-1（`buildToggleRow` が両行に `math-menu-bg-row`/`-label` の「bg」命名を流用＝Mermaidと共有する既存CSSクラスの意図的な再利用のため据え置き）・C-2（`resolveTextColor` の未知値は既に auto 分岐へ落ちる＝誤検知）は対応せず報告のみ。ユニットテスト8件（文字色トグルの既定active・行独立・`resolveMenuTextColor`/`resolveTextColor` 全分岐・透過＋白指定でSVGに rect 無し＆白文字が伝播）。型/lint/compile/ユニット473件パス。**透過＋白文字のダーク環境での見え方・メニュー操作感は実機（拡張機能開発ホスト）確認が前提**。`media/` のみ＝統合テスト非対象。 |
| 2026-07-20 | `018eb37` feat: 表の挿入UI（空行の右クリック→行数・列数ダイアログ）の追加（実機確認前提） | 表挿入の中核（`table.buildEmptyTableMarkdown`／`insertTable`、`78b0295`）に対する**呼び出しUIの配線**。`table.js` に `setupContextMenu(editor)` を追加し `editor.js` init で数式メニューの後に配線。`contextmenu` を監視し、**空行・エディタ余白**でのみ自前メニュー「➕ 表を挿入…」を出す。表示可否は純粋関数 `shouldShowInsertMenu(target, editor)`＝`findExcludedAncestor`（数式 `.math-block`/`.math-inline`・Mermaid・既存 `table`/`.table-container` 上は対象外）＋`isBlockEmpty(block)`（テキスト無し＝空白/ゼロ幅のみ・`<br>` 以外の要素を含まない）で判定。メニュー項目→行数・列数ダイアログ（Webviewでは `prompt()` 不可のため `.link-dialog` 系スタイルで動的生成・`markdownEditor.ts` 非変更）→OK で `clampTableDimensions`（空/非数値/0以下→1・上限 行50/列20）を通し `insertTable(rows, cols)`。挿入位置は右クリック時の `document.caretRangeFromPoint` を `pendingInsertRange` に退避し確定直前に選択へ復元（ダイアログ入力へフォーカスが移ると選択が失われるため）。メニュー/ダイアログCSSは `.table-context-menu`/`.table-menu-item` を `.mermaid-context-menu`/`.math-context-menu` 系と同ルールへ追加。**`/local-review` 指摘 A-1（medium）を同サイクルで修正**: 初版は数式/Mermaid/表以外の**本文全域**で `preventDefault` していたため、右クリックの既定メニュー（貼り付け・切り取り・スペル修正等）が「表を挿入」1項目に置き換わる回帰があった。ROADMAP の「エディタ空欄で右クリック」の意図どおり**空行・余白限定**（`shouldShowInsertMenu`）へ絞り、本文テキスト上は既定メニューを尊重するよう修正。C-1（`pendingInsertRange` の挿入後クリア）・C-2（キャンセルボタン上のEnterはキャンセル扱い）も同サイクルで対応。ユニットテスト16件（`computeMenuPosition`/`clampTableDimensions`/`findExcludedAncestor`/`isBlockEmpty`/`shouldShowInsertMenu` の各分岐＋空行で表示・本文で非表示の回帰・テーブル上で非表示・メニュー→ダイアログ→挿入フロー）。型/lint/compile/ユニット491件パス。**メニュー/ダイアログの見た目・操作感・右クリック位置への挿入は実機（拡張機能開発ホスト）確認が前提**。`media/` のみ＝統合テスト非対象。 |
| 2026-07-20 | `3045b4f` docs: 「画像のクリップボード貼り付け」(サイズL)をS/Mの子項目2つに分割 | コード変更なしのROADMAP整理サイクル（`/evolve` の「Lはまずタスク分割」ルールに従う）。優先度:中の先頭にあった L 項目「画像のクリップボード貼り付け（ファイル保存＋相対パス挿入）」を、実装単位の M 子項目2つへ分割: **(1/2) 拡張機能側**＝Webviewからの `saveClipboardImage`（base64＋mime）を受けてドキュメント隣へ一意名で保存し `insertImagePath`（相対パス）を返す（`saveMermaidPng` の `atob`→`writeFile` を流用・純粋関数〈mime→拡張子／ファイル名生成／相対パス算出＋衝突回避〉をユニットテスト化・`markdownEditor.ts` を触るため統合テスト対象）、**(2/2) Webview側**＝`paste` の `clipboardData.items` から画像検出→base64化→保存要求→返信の相対パスで `![](path)` をキャレット挿入（純粋関数〈画像アイテム抽出／markdown組み立て〉をユニットテスト・画像記法 `![]()` の往復が保たれるか事前確認）。(2/2)は(1/2)のメッセージ契約に依存＝この順で着手。`docs/ROADMAP.md` のみ変更。 |
| 2026-07-20 | `3f52b0f` feat: 画像のクリップボード貼り付け(1/2) — 拡張機能側の保存＋相対パス返却（実機確認前提） | L項目を分割した前半（拡張機能側）。Webview からの新メッセージ `saveClipboardImage`（`{ base64, mime }`）を `markdownEditor.ts` で受け、編集中ドキュメントと同じフォルダ（`document.uri` 基準）へ一意名で画像を書き込み、`insertImagePath`（`{ path }`＝POSIX区切りの相対パス）を postMessage で返す。保存は既存 `saveMermaidPng` と同方式（`atob`→`Uint8Array`→`vscode.workspace.fs.writeFile`）だがダイアログ無しで自動命名（`image-<yyyyMMdd-HHmmss>.<ext>`・同フォルダの既存名と衝突したら `-1`,`-2`…）。未保存（untitled/非fileスキーム）は警告して中断、空base64は無視、`readDirectory` 失敗時は衝突チェック無しで続行。**純粋ロジックは vscode 非依存の新規 `src/imagePaste.ts` に分離**（`mimeToExtension`＝未知/パラメータ付き/大文字対応・`formatTimestamp`/`buildImageFilename`・`disambiguateFilename`＝拡張子手前に連番・`toMarkdownRelativePath`＝`path.relative`＋`/`正規化）しユニットテスト11件（`imagePaste.test.ts`）で網羅。統合テスト3件（`extension.test.ts`＝実書き込み・別名保存・untitled中断）を追加。型/lint/compile・ユニット502件・統合12件パス。**`/local-review` 指摘**: A/なし。B（medium）＝Webview結線が未実装でバックエンドは現状到達不能＝**意図した(1/2)分割**（(2/2) が ROADMAP に残存することを確認）。C-1（衝突テストが秒境界で非決定的）＝テスト名を実際の保証（2ファイルが別名で残り上書きしない）へ改め、`-1` 連番自体は `imagePaste.test.ts` で決定的に検証。C-2（同時pasteのTOCTOU＝直列化で実害なし）・C-3（一時ファイル後始末＝既存踏襲）は対応せず報告のみ。**(2/2)=Webview側の paste 検出→保存要求→`![](path)` 挿入・実機での貼り付け〜往復確認は残置**。`src/markdownEditor.ts` 変更＝統合テスト対象。 |
| 2026-07-20 | `7669d23` feat: 画像のクリップボード貼り付け(2/2) — Webview側の paste 検出→保存要求→`![](path)` 挿入（実機確認前提） | (1/2, `3f52b0f`)の拡張機能側に続く後半（Webview側）。スクショ等の画像を `Ctrl/Cmd+V` で貼ると、同フォルダへ保存され `![](相対パス)` がキャレットへ入る。`editor.js` の paste ハンドラで、**`text/plain` を持たない**クリップボード（＝純粋な画像）に限り `commands.findClipboardImageItem`（`type` が `image/` で始まる最初の item）で画像を検出→`FileReader`→base64 化→`saveClipboardImage` を postMessage。貼り付け時のキャレット範囲を `pendingImagePasteRange` に控え、応答 `insertImagePath` を受けて範囲を復元（今もエディタ内に生きている場合のみ）→`commands.insertImageMarkdown(path)` で挿入。`buildImageMarkdown` は alt 空・パスの空白/丸括弧を `%20`/`%28`/`%29` エンコード。**画像記法はレンダリング対象外**でリテラルテキストとして入り往復不変（`markdown.js` のリンク正規表現は alt 必須で `![]()` はリンク化されない・直列化は `$` のみエスケープ）。**`/local-review` C-1（medium）を同サイクルで修正**: 初版は `image/*` item があれば**テキストより先に**横取りしていたため、Excel等の「テキスト（TSV/表）＋画像」併載クリップボードで表貼り付けが画像保存に化ける回帰があった。**`text/plain` が無いときだけ**画像として扱うよう変更し、テキスト併載は従来の表/テキスト貼り付けを優先。C-2（応答が来ない場合の `pendingImagePasteRange` 残存）＝復元前に範囲がエディタ内に生きているか確認して古い範囲の誤復元を防止。ユニットテスト9件（`buildImageMarkdown` の空alt/エンコード/空・`findClipboardImageItem`・`insertImageMarkdown` のキャレット挿入/末尾フォールバック/空パス・いずれも往復検証）。型/lint/compile・ユニット510件パス（`media/` のみ＝統合テスト非対象）。B-1（`buildImageMarkdown` が `_`/`*` 等のMarkdownアクティブ文字を未エンコード＝将来のサブディレクトリ/alt拡張時の潜在リスク）と、**ローカル画像記法のインライン表示（WYSIWYGで画像を描画）**は ROADMAP へ。**実機で スクショ貼り付け→ファイル保存→`![](path)` 挿入→保存Markdownの往復を確認**。 |
| 2026-07-20 | `c1007b2` feat: 画像記法 `![alt](url)` の Markdown⇔HTML 対応（`<img>`レンダリング＋往復）(1/2)（実機確認前提） | 「ローカル画像のインライン表示」M を分割した前半（`media/modules/` のみ・純粋変換）。これまで `markdown.js` は画像記法を扱わず `![](path)` はリテラルテキスト表示だった。**変更**: `convertInline`（読込パス）と `convertInlineText`（ライブ変換パス）に `![alt](url)`→`<img src alt>` を**リンクより先に**追加（`![` を `!`＋リンクに割らない）、`serializeInline` に `<img>`→`![alt](url)`（`data-original-src` を優先＝2/2で `src` を webview URI へ差し替えても元パスで往復）。**`/local-review` A-1（medium）を同サイクルで修正**: 生成した `<img>` を素のHTMLで挿すと URL/alt 中の `_`/`*`/`~`/`+` が後続の強調変換で `src="a<em>b</em>c"` のように壊れ往復が崩れるため、インラインコード・数式と同じく**プレースホルダ（`U+0003`）へ退避→強調変換後に復元**する保護を両パスへ追加（回帰テスト付き）。ユニットテスト（往復・属性`"`エスケープ・`data-original-src`優先・`_`等を含むパスの非破壊・ライブ変換でimg化＆リンク非該当・通常リンク回帰）を追加。型/lint/compile・ユニット518件パス。B-1（タイトル記法 `![](url "title")` 未対応＝画像/リンク共通の既存制約）は ROADMAP へ。C-1（IMGシリアライズが `]`/`)` 含む属性を非防御）・C-2（空srcのimgは無音欠落）は生成名が安全なため報告のみ。**残り(2/2)＝ローカル相対パス画像の実表示（CSP/localResourceRoots/baseUri。現状は壊れた画像アイコン）は ROADMAP に残置**。`media/modules/` のみ＝統合テスト非対象。 |
| 2026-07-20 | `52bef01` feat: ローカル相対パス画像 `![](path)` をエディタ内で実表示(2/2)（実機確認前提） | (1/2, `c1007b2`)の画像記法⇔`<img>`往復に続く後半。ローカル相対パスの画像を webview で実際に表示する。**拡張機能側**（`markdownEditor.ts` の `resolveCustomTextEditor`）: ドキュメントのフォルダを `webview.options.localResourceRoots` に追加し、そのフォルダの webview URI（`webview.asWebviewUri(dirUri).toString()`・file スキームのみ）を `update` メッセージの `imageBaseUri` で Webview へ渡す。**Webview側**（`editor.js` の `resolveLocalImages`）: 各描画後（input・handleUpdateMessage・Raw復帰）に `<img>` を走査し、`markdown.isResolvableRelativeImageSrc(src)`（スキーム付き/プロトコル相対/ルート絶対は false）が true の相対 src を、元パスを `data-original-src` に退避してから `markdown.resolveImageSrc(imageBaseUri, src)`（`base + '/' + rel`・先頭 `./` 除去・`#`/`?` を `%23`/`%3F` エンコード・`%` は二重エンコードしない）で解決した webview URI へ差し替える。`data-original-src` があれば再解決しない／`serializeInline` は `data-original-src` を優先＝**往復は元の相対パスで不変**／`incoming===current` 比較が成立し再レンダリングループしない。CSP `img-src ${webview.cspSource}` は変更不要。**`/local-review` A-1（low）を同サイクルで修正**: `resolveImageSrc` が `#`/`?` を素通しし `img#1.png` 等でフラグメント/クエリ扱いになり表示が壊れる問題→両文字をエンコード（既存 `%20` を壊さないよう `%` は非エンコード）。B-1（`insertImageMarkdown` の陳腐化コメント＝画像はもうレンダリングされる）も更新。C-1（`../` 相対の URL 正規化）・C-2（Windows `\` 区切り）は実害薄く報告のみ。純粋関数（`isResolvableRelativeImageSrc`/`resolveImageSrc`）をユニットテスト、統合テスト12件パス（openEditor が新経路を通過）・ユニット523件・型/lint/compile パス。**実機で 相対パス画像（同フォルダ・`assets/…`・`../`）・`data:`・貼り付け画像の表示と往復を確認**。`src/markdownEditor.ts` 変更＝統合テスト対象。 |
| 2026-07-21 | `d74c1ba` fix: 触っていない行の箇条書き `- ` が `* ` に勝手に書き換わる問題の修正（ユーザー報告） | ファイルを開いて別の行を編集しただけで、触っていない `- ` 箇条書きが `* ` に書き換わっていた（`631060f` 起票の箇条書き側）。**原因**: 書き戻しは表示中の全ブロックを `htmlToMarkdown` で再直列化するが、`serializeList` が箇条書きマーカーを常に `* ` で出力し、`- ` の既存行が編集と無関係に正規化されていた。**修正**: `markdownToHtml` のリストパースが元マーカー（`-`/`*`）を各 `<ul>` の `data-marker` に保持（`buildListHtml`）し、`serializeList` がそれを尊重して復元（無い/不正値は従来どおり `*`＝新規入力・execCommand 生成 ul の既定を維持）。`getCleanHtmlFromEditor` は `data-marker` を除去しないため書き戻し経路で有効。回帰テスト（`- `/`* `/ネスト/タスクの各バリアントで往復バイト一致）を追加、既存の markdownToHtml 出力アサーション2件を `data-marker` 付きへ更新。`/local-review`: A なし。B（表区切り `\|---\|`→`\| --- \|` は未対応＝ROADMAP へ）。C-1（空行なしのマーカー混在 `- a\n* b` は先頭マーカーへ統一＝非退行の既知制約・現状挙動固定テスト追加）・C-2（`+` は非リスト扱いで往復不変）は報告のみ。型/lint/compile・ユニット527件パス。`media/modules/` のみ＝統合テスト非対象（書き戻しは実機で目視確認推奨）。 |
| 2026-07-21 | `5d5c574` fix: 触っていない行の表区切り `\|---\|`→`\| --- \|` が勝手に書き換わる問題の修正（ユーザー報告・箇条書き側`d74c1ba`に続き表側も解消） | `serializeList`の`data-marker`と同じ発想で、`serializeTable`が区切り行を常に`\| --- \|`形式（アライメントコロンも`---`へ潰す）で出力していたため、表を一切触っていなくても書き戻しで区切り行の表記（スペース有無・`:---`/`---:`/`:---:`）が失われていた。**修正**: `markdownToHtml`の表パース時に区切り行をtrimせず列ごとの生テキストへ分解する`splitSeparatorRow`を追加し、列数がヘッダーと一致する場合のみ元表記を`<table data-sep="...">`（カンマ区切り）へ保持。`serializeTable`はこれがあり列数が一致していればそのまま復元し、無い／列数不一致（列追加・削除後）はデフォルト`---`書式へフォールバック。回帰テスト（コンパクト・左/右/中央アライメントの各バリアントで往復バイト一致・列数不一致時のフォールバック）を追加。`/local-review`: Aなし。C-1（列追加・削除で他列の書式も丸ごとデフォルトへ落ちる＝低優先）はROADMAPへ。C-2（`data-sep`属性値の未サニタイズ＝許可文字が空白/-/:のみで実害なし）は対応不要。型/lint/compile・ユニット534件パス。`media/modules/`のみ＝統合テスト非対象。 |
| 2026-07-21 | `ca1da0f` feat: スクロール時に現在位置の見出しをパンくず状に上部固定表示 | ROADMAP優先度:中の先頭項目。ツールバー直下・`#editor`の直前へ動的生成した固定バー（`editor.js`の`initHeadingBreadcrumb`・`markdownEditor.ts`非変更）が、スクロール（rAF間引き）・入力・外部編集・Raw⇔プレビュー切替のたびに更新される。ロジックは3つの純粋関数へ分離: `commands.collectHeadings(editor)`（見出しを出現順に`{level,id,text,el}`へ）、`commands.findCurrentHeadingIndex(tops,scrollTop)`（スクロール位置以前の最後の見出し＝いま見ている節を特定・文書先頭なら`-1`）、`commands.buildBreadcrumbChain(headings,currentIndex)`（そこから手前へ辿りレベルが単調に小さくなる見出しだけを祖先として積み上げる。レベル飛びは存在するものだけ）。各パンくず項目をクリックすると`commands.scrollToAnchor`で該当見出しへ遷移。**`/local-review`指摘A-1（medium）を同サイクルで修正**: `execCommand`による見出し化やライブ変換直後の見出しは`markdownToHtml`の再パースを経るまで`id`が無く、パンくず項目がクリック可能に見えて実際は無反応だった問題→`id`があるものだけ`heading-breadcrumb-item-clickable`クラスでcursor/hoverを付与するよう修正。B（デバウンス未対応・差分描画無し・キーボード操作不可の3件、いずれもseverity low）はROADMAPへ。ユニットテスト12件（純粋関数3つの全分岐＋jsdomレルムのArray.from正規化）。型/lint/compile・ユニット544件パス。`media/editor.js`（DOM依存部）は統合テスト非対象、`media/modules/commands.js`の純粋関数はユニットテスト対象。**スクロール時の実際の表示位置・パフォーマンス・パンくずクリックの遷移感は実機（拡張機能開発ホスト）確認前提**（`getBoundingClientRect`ベースの位置計算はjsdomでは検証できないレイアウト依存処理のため）。 |
| 2026-07-21 | `31003ef` feat: 「/」入力によるNotion風コマンドメニューの表示（初期スコープ: 目次挿入・表の挿入） | ROADMAP優先度:中の先頭項目。空の段落で「/」だけを入力すると直後にポップアップメニューを表示し、「📑 目次を挿入」「➕ 表を挿入…」を選べる（`mermaid`/`math`/`table`の各コンテキストメニューと見た目を共有）。判定は純粋関数`commands.isSlashCommandTrigger(block)`（可視テキストが「/」1文字だけ・`<br>`以外の子要素を含まない）、表示/非表示は`editor.js`の`setupSlashCommandMenuEvents`が`selectionchange`を監視して駆動（`setupRawMarkdownCaretEvent`と同じ設計）。項目選択（`runSlashCommand`）は既存の`commands.insertToc()`/`table.showInsertDialog()`（「キャレットのあるブロックの直後へ挿入」処理）に委譲。**`/local-review`指摘A-1（high）・A-2（medium）を同サイクルで修正**: 当初は挿入処理を呼ぶ前に無条件で「/」を消していたため、表挿入ダイアログのキャンセル（Escape/キャンセルボタン）や見出し0件での目次挿入失敗時に、入力していた「/」が復元されず無言で消えるだけだった→「/」の除去を**挿入が実際に成功した後にのみ**行うよう変更。これを担保するため`commands.insertToc()`を実際に挿入できたかを`boolean`で返すよう変更し、`table.showInsertDialog(onInserted)`にOK確定（挿入成功）時にのみ呼ばれるコールバック引数を追加（キャンセルでは呼ばない。既存呼び出し元は引数省略で後方互換）。表挿入はダイアログがフォーカスを奪うため、右クリックの挿入メニューと同じ「キャレット位置退避→確定時に復元」の仕組みを外部から使える`table.setPendingInsertRange(range)`を追加。A-3（low・テスト網羅性）も同サイクルで追加。C（見出し・引用・テーブルセル内では`utils.findBlockAncestor`が対象タグを認識せずトリガーしない・severity low）はROADMAPへ。ユニットテスト19件（`isSlashCommandTrigger`全分岐・`insertToc`の返り値・`setPendingInsertRange`によるライブ選択上書きからの復元・`showInsertDialog`コールバックの確定/キャンセルでの呼び分け）。型/lint/compile・ユニット555件パス。`media/editor.js`（DOM依存部）は統合テスト非対象、`media/modules/commands.js`・`table.js`の純粋関数/契約変更はユニットテスト対象。**メニューの表示位置・キャレット直後への出現・クリック操作感は実機（拡張機能開発ホスト）確認前提**。 |
| 2026-07-21 | `626c6f0` docs: 「表の矩形選択とコピー＆ペースト」(サイズL)をS/Mの子項目3つに分割 | コード変更なしのROADMAP整理サイクル（`/evolve` の「Lはまずタスク分割」ルールに従う）。優先度:中の先頭にあった L 項目「表の矩形選択とコピー＆ペースト（Excel等外部アプリとの相互貼り付け対応）」を、実装単位の子項目3つへ分割: **(1/3) マウスドラッグでの矩形範囲選択とハイライト表示**〈M・依存なし＝(2/3)(3/3)が依存する基盤〉、**(2/3) 選択範囲をTSV＋text/htmlでクリップボードへコピー**〈S・(1/3)依存〉、**(3/3) 選択範囲への矩形貼り付け対応**〈S・(1/3)依存、(2/3)とは独立に実装可能〉。この順で着手予定。`docs/ROADMAP.md` のみ変更・コードレビューはコード変更が無いためスキップ。 |
| 2026-07-21 | `38ca924` feat: 表のマウスドラッグによる矩形範囲選択とハイライト表示(1/3) | ROADMAP優先度:中の先頭項目。既存の単一セル選択（`selectCell`）に加え、セルをクリックしたままドラッグして複数セルにまたがる矩形範囲を選択・ハイライト（`table-cell-range-selected`）できるようにした（現時点ではハイライトのみ・コピー＆ペーストは(2/3)(3/3)で対応予定）。`table.js`の`setupCellRangeDrag`が各セルへ`mousedown`（主ボタンのみ）/`mouseenter`（ドラッグ中に別セルへ入ったら`computeCellRange`で矩形を正規化し`applyRangeHighlight`）を配線、`document`の`mouseup`（`editor.js`のinitEditorで一度だけ配線）でドラッグ追跡のみ終了し確定した範囲は次の操作まで保持。単なるクリック（同一セルでmousedown/mouseup）は別セルへの`mouseenter`が起きないためこの配線は無反応＝既存の単一セル選択と競合しない。**`/local-review`指摘A-1（medium-high）・A-2（medium）・A-3（low-medium）を同サイクルで修正**: (A-1)複数テーブルを持つ文書で片方のハイライトが永続残留し、さらに別テーブルのセルクリックで誤って正しい方の選択を消してしまう不具合→`mousedown`時に別テーブルの残留ハイライトを解除するよう修正。(A-2)`.table-cell-selected`/`.table-cell-range-selected`が単体だと詳細度で`.table-cell:hover`に負けドラッグ中のセルがホバー色に上書きされる→`.table-cell`と複合させ詳細度を揃えて修正。(A-3)`cleanup()`が新設クラス・range状態を後始末していなかった→追加。B（`makeEditable`のリスナー多重登録が今回さらに悪化／大きな表での`applyRangeHighlight`のO(セル数×行数)、いずれもseverity low）はROADMAPへ。C（セル間ドラッグが文字列選択よりセル矩形選択を優先する挙動＝Excel的UXとして意図的な仕様と判断・対応不要）。ユニットテスト18件（純粋関数の全分岐＋DOM配線のドラッグ・mouseup後の追跡終了・単純クリックとの非競合・複数テーブル間の非干渉・cleanupでのクラス除去）。型/lint/compile・ユニット572件パス。`media/`のみ＝統合テスト非対象。**実際のドラッグ操作感・ハイライト色の見た目（CSS優先順位を含む）は実機（拡張機能開発ホスト）確認前提**。 |
| 2026-07-21 | `f3d5800` feat: 表の矩形範囲選択をTSV＋text/htmlでクリップボードへコピー(2/3) | ROADMAP優先度:中の先頭項目。(1/3, `38ca924`)で導入した`currentCellRange`に対応する範囲だけをコピーできるようにした（範囲が無ければ従来どおりテーブル全体・出力/トーストとも無退行）。純粋関数へ分離: `extractCellTextMatrix(table, range)`（`isCellInRange`を再利用してテキスト行列を抽出）、`buildTsvFromMatrix(matrix)`、`buildHtmlTableFragment(table, range)`（`cell.innerHTML`をそのまま使い装飾・リンクを保った`<table>`断片を組み立てる）。書き込みは`writeToClipboard`が`ClipboardItem`/`navigator.clipboard.write`の可用性をfeature-detectし、使える環境ではTSV＋text/htmlの両方を、無ければ従来の`writeText`（TSVのみ）へフォールバック。**`/local-review`指摘A-1（medium）を同サイクルで修正**: `selectCell`は単一クリックで`currentCellRange`を解除するが`addRow`/`addColumn`/`deleteRow`/`deleteColumn`はいずれも解除しないままだったため、「範囲確定→単一セル選択を経ずに行・列を増減→コピー」の順で操作すると行・列インデックスがずれたstaleな範囲のまま誤った内容を黙ってコピーする不具合があった→新設の`invalidateRangeSelectionFor(table)`をこの4関数の冒頭で呼び対象テーブルの範囲選択を無効化するよう修正。B（`buildTsvFromMatrix`のタブ・改行未エスケープ＝旧実装から存在する既存制約／失敗トーストのテスト欠如、いずれもseverity low）はROADMAPへ（失敗トーストのテストは同サイクルで追加済み）。C（`new ClipboardItem`の同期例外が`.catch`で捕捉できない可能性・確信度低）は対応不要と判断。ユニットテスト11件（純粋関数3つ＋range/別テーブル/stale範囲でのcopy分岐＋ClipboardItem利用可否での書き込み先切り替え＋失敗トースト）。型/lint/compile・ユニット584件パス。`media/`のみ＝統合テスト非対象。 |
| 2026-07-22 | `ecb38fd` feat: 表の矩形範囲選択への矩形貼り付け対応(3/3・完結) | ROADMAP優先度:中の先頭項目。「表の矩形選択とコピー＆ペースト」を(1/3, `38ca924`)(2/3, `f3d5800`)に続き完結させた。(1/3)の`currentCellRange`が確定している場合、貼り付け先セルに関わらずその範囲の左上を起点に貼り付けデータを展開するよう`pasteData`を拡張（範囲が無ければ従来どおり貼り付け先セル自身が起点＝無退行）。純粋関数`computePasteTargets(anchorRow,anchorCol,data,rowCount,colCount)`（アンカーからのオフセットで書き込み先を列挙・`rowCount`/`colCount`を超える位置＝はみ出し分は含めない＝行・列の自動追加はしない）と、それをDOMへ適用する`writeMatrixIntoTable`（見出しセル`<th>`は個別にスキップ）へ分離。データが範囲より小さい場合は残りのセルを変更せず、大きい場合はデータ側の行列数がそのまま使われる（範囲はアンカー位置を決めるだけ）。**旧実装は貼り付け起点が見出しセルだと貼り付け全体を無条件でno-opにしていたが、新実装は見出しセル自体だけをスキップし本文行側へは書き込む（意図した挙動変更・回帰テストで固定）**。`/local-review`: Aなし。B（`writeMatrixIntoTable`の列数算出が全行同一セル数という暗黙の前提に依存／行ごとの`Array.from`再生成、いずれもseverity low）はROADMAPへ。C（コメント表現の不正確さは同サイクルで修正／範囲より小さいデータ貼り付け時に残りセルをクリアしない仕様はdocstringで明記）。ユニットテスト14件（`computePasteTargets`全分岐＋`writeMatrixIntoTable`のth除外/はみ出し無視＋`pasteData`の範囲優先/見出し起点/別テーブル非干渉/ドキュメント反映）。既存の`pasteData`にはテストが一件も無かったため、単一セル起点の従来動作についても新規にテストを追加。型/lint/compile・ユニット598件パス。`media/`のみ＝統合テスト非対象。**Excel等からの実際の貼り付け・大きい/小さいデータでの見え方は実機（拡張機能開発ホスト）確認前提**。 |
| 2026-07-22 | `8a07c28` feat: 脚注（`[^label]` / `[^label]: 本文`）のサポート | ROADMAP優先度:低の項目（優先度:中の脚注ホバー表示の前提として着手）。`本文[^1]`と`[^1]: 脚注の本文`の記法に対応。`markdownToHtml`が読込・Raw⇔プレビュー切替時に(1)定義抽出`extractFootnoteDefinitions`（コードフェンス・ブロック数式の中は除外）→(2)本文段落の`convertInline(text, footnoteLabels)`が参照`[^label]`を`<sup class="footnote-ref"><a href="#fn-label">label</a></sup>`へ変換（未定義ラベルはリテラルのまま）→(3)定義を文書末尾の`<section class="footnotes"><ol>`一覧へ集約（脚注表示の一般的慣習・既知の仕様＝中程にあった定義は保存で末尾へ移動）。直列化（`htmlToMarkdown`）は`SUP`/`SECTION.footnotes`ケースを追加し`[^label]`/`[^label]: 本文`へ復元。既存の`commands.handleLinkClick`（任意の`a[href^="#"]`をCtrl/Cmd+クリックで遷移）にそのまま乗るため参照⇔定義間の追加配線は不要。**`/local-review`指摘（severity high）を同サイクルで修正**: 初版は「参照の有無を問わずどんな行でも`[^label]: 本文`の見た目なら定義として剥がす」実装だったため、正規表現の説明文（`[^0-9]: 数字以外にマッチ`のような地の文）まで誤って脚注定義として扱ってしまう不具合があった→**同じラベルが文書内のどこかで実際に`[^label]`として参照されている場合のみ**定義として確定するよう`extractFootnoteDefinitions`を2パス化して修正（あわせて`convertInline`側の参照変換にも`(?!:)`ガードを追加し、無効化された重複定義の`[^label]:`部分が誤って参照変換される副作用も解消）。低優先度の残課題（コロン直後の空白2つ以上での往復ロス／入力中のライブ変換未対応・意図的スコープ）はROADMAPへ。ユニットテスト19件（`extractFootnoteDefinitions`の参照ガード・フェンス/数式除外・重複ラベル、`convertInline`の参照変換・未定義ラベル、`SUP`/`SECTION`の直列化、往復6パターン、地の文の誤検知防止）。型/lint/compile・ユニット617件パス。`media/modules/`のみ＝統合テスト非対象。 |
| 2026-07-22 | `bd0a94f` feat: 脚注のホバーツールチップ表示 | ROADMAP優先度:中の項目（前サイクルの脚注サポート`8a07c28`により前提解消済み）。脚注参照（`sup.footnote-ref`）にマウスを乗せると、少し待ってから対応する脚注定義の本文をポップアップ表示する。`commands.getFootnoteDefinitionText(editor, label)`（`li[id="fn-label"]`から戻りリンクを除いたテキストを取り出す。ユニットテスト対象）＋`editor.js`の`showFootnoteTooltip`（`table.computeMenuPosition`を流用して参照要素の直下へ配置）。表示/非表示は`mouseover`/`mouseout`のイベント委譲＋300msデバウンス（`mouseenter`/`mouseleave`はバブリングしないため）。**`/local-review`指摘（severity medium）を同サイクルで修正**: VS Code側からの外部更新（`handleUpdateMessage`が`state.editor.innerHTML`を丸ごと差し替える経路）でツールチップを閉じておらず、表示中に外部更新が来ると古い内容のまま残ったり、保留中の`mouseover`タイマーがdetachedになった旧`sup`要素に対して発火して左上に誤表示される不具合があった→Raw⇔プレビュー切替の同種の全書き換え経路と同様に`hideFootnoteTooltip()`を追加。B（`<sup>`と内側の`<a>`の出入りで`mouseover`/`mouseout`が再発火しデバウンスタイマーがリセットされる軽微な非効率・severity low）はROADMAPへ。ユニットテスト5件（`getFootnoteDefinitionText`の本文取得・装飾除去・未定義ラベル・null安全・複数脚注での該当ラベル特定）。型/lint/compile・ユニット622件パス。`editor.js`側のDOM/位置計算・イベント配線は`media/modules/`外＝ユニットテスト対象外（`markdownEditor.ts`非変更のため統合テストも対象外）。**実際のホバー表示位置・遅延の体感・外部更新時の挙動は実機（拡張機能開発ホスト）確認前提**。 |
| 2026-07-22 | `986346c` feat: 脚注参照（`[^label]`）の入力時ライブ変換 | ROADMAP優先度:中の項目。これまで`markdownToHtml`（読込・Raw⇔プレビュー切替・再読込時）でのみ変換していた脚注参照を、`commands.js`の`convertInlineText`（太字等が即時反映される入力中ライブ変換の経路）でも変換するようにした。`walkInline`が`section.footnotes li[data-footnote-label]`から既存の脚注ラベル集合を収集し、対応する定義があるラベルの`[^label]`だけをその場で`<sup>`へ変換する（定義がまだ無い参照はリテラルのまま。同一セッションで参照・定義を両方新規入力した直後は保存/再読込まで変換されない意図的なMVPスコープ）。**`/local-review`指摘A-1（high）を同サイクルで修正**: 初版は脚注変換をコード/数式/画像/エスケープ`$`の復元より後段に置いていたため、数式・画像の中身に既存の脚注ラベルと一致する`[^label]`相当の文字列がたまたま含まれていると、復元済みの`data-math`/`src`属性値の途中に`<sup>`が誤挿入されデータが破壊される不具合があった→脚注参照専用のデリミタ付きプレースホルダへ一旦退避し、リンク変換の直後（他の記法の復元より前、`markdown.js`の`convertInline`と同じ位置関係）で退避、全復元の最後に実HTMLへ差し戻すよう修正。**`/local-review`指摘A-2（high、再レビューで検出）を同サイクルで修正**: `markdown.js`は脚注ラベルを段落（`<p>`）変換にしか渡さず見出し・リスト・表・引用・アラート本文では脚注を変換しない仕様だが、ライブ変換側はブロック種別を問わず変換していたため、見出し等で入力すると編集中はリンクに見えるのに保存/再読込で消える非対称な回帰があった→直近のブロック祖先が`<p>`の場合のみラベル集合を渡すよう修正。B（同一段落内で同じラベルを複数回参照すると`id`が重複／ラベル収集クエリが毎キー入力で走る、いずれもseverity low）はROADMAPへ。ユニットテスト7件追加。型/lint/compile・ユニット629件パス。`media/modules/`のみ＝統合テスト非対象。**実際のタイピング体感・見出し等での意図的な無変換の見た目は実機（拡張機能開発ホスト）確認前提**。 |
| 2026-07-22 | `80d42cb` fix: 脚注定義行のコロン直後の空白数が変換往復で1つに正規化される問題の修正 | ROADMAP優先度:中の項目（`/local-review`指摘由来）。`FOOTNOTE_DEF_PATTERN`（`markdown.js`）は従来`:`の後の空白を`\s?`（0または1文字のみ）で吸収しており、`[^1]:  本文`のように空白が2つ以上あると余分な空白が本文側の先頭空白として残り、直列化時の`trim()`で失われていた（往復後は常に空白1つへ正規化）。パターンを`:(\s*)(.*)$`へ変更し、`:`直後の空白（0個・1個・2個以上いずれも）を独立した`sep`として捕捉、`buildFootnotesSectionHtml`が`<li data-footnote-sep="...">`属性へ保持、`htmlToMarkdown`のSECTIONケースがその属性値を`[^label]:${sep}${text}`の形でそのまま差し戻すことで元の空白数を保つようにした（属性が無い＝この対応より前に生成されたHTML等は半角スペース1つへフォールバックし後方互換）。`/local-review`: Aなし（correctnessバグ・回帰は見つからず）。C（`data-footnote-sep`属性値が正規表現の文字クラス制限のみに安全性を依存した非エスケープ埋め込みである点・severity low, 確信度中）は既存の`data-sep`（テーブル区切り）と同じ設計方針であり対応不要と判断。ユニットテスト7件追加（`extractFootnoteDefinitions`のsep捕捉0/1/3個・`buildFootnotesSectionHtml`の属性出力・往復3パターン）。既存の脚注関連テスト3件は新属性を含む期待値へ更新。型/lint/compile・ユニット632件パス。`media/modules/`のみ＝統合テスト非対象。 |
| 2026-07-22 | `c7cca7f` fix: 脚注ホバーツールチップの`mouseover`/`mouseout`が`<sup>`内側の`<a>`との出入りで再発火する問題の修正 | ROADMAP優先度:中の項目（`/local-review`指摘由来）。`editor.js`の`setupFootnoteTooltipEvents`は`sup.footnote-ref`への`mouseover`をイベント委譲で監視しているが、`<sup>`とその内側の`<a>`の間でマウスが出入りするたびにバブリングで再発火し、300msデバウンス用の`clearTimeout`/`setTimeout`が毎回リセットされていた（機能停止には至らない軽微な非効率）。新設のモジュール変数`activeFootnoteRef`で「現在処理中のref要素」を追跡し、`mouseover`ハンドラは`ref === activeFootnoteRef`（同一refへの再入場）ならタイマー再設定をスキップするようにした。`hideFootnoteTooltip()`（実際にref外へ出た場合・スクロール時・外部更新時など、ツールチップを閉じる全経路の集約点）で`activeFootnoteRef`を必ずリセットするため、次の正当なホバーを取りこぼす状態異常は起きない。`/local-review`: Aなし。C（無関係な`.claude/settings.json`が同梱・コメント表現の精度に関する軽微な指摘、いずれもseverity low、確信度低）は対応不要と判断。`editor.js`はDOM/イベント配線のみで`media/modules/`外＝既存の慣習どおりユニットテスト対象外（`markdownEditor.ts`非変更のため統合テストも対象外）。型/lint/compile・ユニット632件パス（既存回帰なし）。**実際のホバー挙動の体感は実機（拡張機能開発ホスト）確認前提** |
| 2026-07-23 | `957aa9d` feat: 定義リスト（`用語`\n`: 定義本文`）のサポート | ROADMAP優先度:低の項目。`markdown.js`の`markdownToHtml`本文パースループに、リスト・引用と同様の連続行スキャンとして`scanDefListTerms`を追加。用語候補（見出し/hr/リスト/引用/テーブル開始行でない通常行）を連続する限り集め、直後に`: `定義行が続く場合のみ`<dl>`として確定（複数用語での定義共有・1用語への複数定義行に対応、Pandoc等と同じ規則）。`:`直後の空白は`data-def-sep`属性で保持し変換往復で元の個数を保つ（脚注定義行の`data-footnote-sep`と同型の設計）。**`/local-review`指摘（severity high、複数ラウンドで検出・同サイクルで修正）**: 脚注定義行（`[^1]: ...`）は本文パース前に`contentLines`から除去されるため、除去後に前後の行が直接隣接し本来無関係な行同士が定義リストへ誤結合される回帰があった。最初に試みた「除去箇所へプレースホルダ文字を残す」方式は、リスト・引用・テーブル（各行が`-`/`>`/`|`の記法で自己申告するため元々縫い目の影響を受けないはずのブロック）まで誤って分裂させる副作用があり撤回（さらにこのプレースホルダに生のNUL制御バイトを直接埋め込んでしまいファイルがバイナリ判定される問題もあり、この点も含めて全面的に設計をやり直した）。最終的に`extractFootnoteDefinitions`が返す`seamIndices`（隣接性が失われている位置の集合）を導入し、これを参照するのは用語収集・複数`: `定義行の収集・グループ連鎖・段落継続の4箇所のみに限定（リスト・引用・テーブルは自身の記法判定のみで縫い目の影響を受けず正しく結合される）。`/local-review`: 最終ラウンドはAなし。B（`utils.findBlockAncestor`が`DT`/`DD`を認識せず定義リスト内で自動整形が無効／`buildDefListHtml`が`footnoteLabels`を渡さず脚注参照が変換されない、いずれもseverity low）はROADMAPへ。ユニットテスト多数追加（用語/定義グループの各パターン・`data-def-sep`往復・縫い目をまたぐ誤結合防止の複数パターン・リスト/引用が縫い目をまたいでも正しく結合されることの確認）。型/lint/compile・ユニット660件パス。`media/modules/`のみ＝統合テスト非対象。 |
| 2026-07-23 | `956b7ca` feat: YAML front matter の折りたたみ表示 | ROADMAP優先度:低の項目。`markdownToHtml`本文パースループの先頭（`i===0`のときのみ）に`parseFrontMatter`（文書先頭が`---`で後続に`---`があれば`{raw, endIndex}`を返す純粋関数）を追加し、`buildFrontMatterHtml`で折りたたみ可能な`<div class="frontmatter">`（既定折りたたみ、ヘッダクリックで`frontmatter-expanded`をトグル）を構築。直列化は`.frontmatter-body`（`<pre>`）のtextContentから`---\n...\n---`を復元。`<pre>`開始タグ直後の最初のLFがパース時に無視されるHTML仕様への対処として、常にダミー改行を1つ先頭へ足す。**`/local-review`指摘（severity high、複数ラウンドで検出・同サイクルで修正）**: (1) `extractFootnoteDefinitions`が本文パースとは別に脚注除去後の行配列に対して`parseFrontMatter`を再度呼んでいたため、文書1行目が参照済み脚注定義行で除去後にたまたま`---`が現れると誤ってfront matterと認識される回帰があった→front matter判定を元の行配列に対して一度だけ行い、両方の処理で結果を共有する設計へ変更。(2) front matter内が偶然`[^label]: ...`形式に一致すると脚注定義として剥がされる問題→`extractFootnoteDefinitions`がfront matter範囲をコードフェンス・ブロック数式と同様に保護。(3) `insertToc`のフォールバック挿入位置がfront matterの「文書絶対先頭」制約を壊しうる問題→先頭が`.frontmatter`ならその直後へ挿入するよう修正。**設計判断（実装せず受け入れた既知の仕様）**: 「本文0行」と「空行1行だけ」の区別を`data-frontmatter-lines`属性で保持する案を一度実装したが、`.frontmatter-body`はcontenteditableでユーザーが自由に編集できるため、この属性は編集直後に陳腐化しユーザーの入力内容を誤って捨てる重大なデータ消失リスクがあると再レビューで判明→撤回し、保存のたびに前者へ正規化される仕様として受け入れた。**B（severity low、いずれもROADMAPへ）**: ヘッダのトグルがマウス操作限定でキーボード操作不可（既存の`.code-lang-selector`等と同じ制約）／ラベルが英語表記で他のUI文言と不統一／`parseFrontMatter`が内容を検証せず位置関係のみで判定するため水平線から始まる通常文書を誤認識しうる（Jekyll/Hugo等も同様の設計）。ユニットテスト多数追加（`parseFrontMatter`単体・`markdownToHtml`/`htmlToMarkdown`の変換往復・脚注/定義リストとの相互作用・`toggleFrontMatter`のクリック挙動・`insertToc`の先頭割り込み防止）。型/lint/compile・ユニット683件パス。`media/modules/`のみ＝統合テスト非対象。 |
| 2026-07-23 | `5a8fea2` docs: ROADMAP整理（「テーブルのセル結合」をS/M子タスク4件へ分割） | ROADMAP優先度:低の先頭項目がサイズL（要分割）だったため、`/evolve`のルールに従い実装には着手せず子タスクへ分割した。標準Markdownテーブルにはセル結合の記法が無いため、MultiMarkdown方式（結合先セルを空にする＝colspan、`^^`＝rowspan）を軸に、(1/4)colspanの記法設計とパース/シリアライズ対応、(2/4)rowspanの記法設計とパース/シリアライズ対応、(3/4)既存の矩形範囲選択（`table.js`の`currentCellRange`）を流用したセル結合UI、(4/4)結合解除UI、の4項目（M/M/M/S）に分けた。コード変更は無く、`docs/ROADMAP.md`のみ更新。 |

## 作業中（未コミット）の変更

ドキュメント作成時点（2026-07-14）でワーキングツリーに存在する、コミット前の変更内容です。

- **`media/modules/` の新規追加（大規模リファクタリング）**: これまで単一ファイル（`media/editor.js`）に実装されていたロジックを、`state.js` / `utils.js` / `markdown.js` / `mermaid.js` / `table.js` / `search.js` / `commands.js` の7モジュールに分割。`editor.js` はオーケストレーション用のエントリーポイントとして再構成された（3148行規模の削減に相当する構造変更）。
- **`src/markdownEditor.ts` の変更**: 新しいモジュールファイル群を `<script>` として読み込むようにWebview HTML生成部分を更新。
- **`package.json` の変更**:
  - `engines.vscode` / `@types/vscode` のバージョン要件を `^1.108.x` から `^1.95.0` に緩和。
  - `openAsText` / `toggleEditor` コマンドをコントリビュートに追加。
  - `toggleEditor` 用のキーバインドを追加。
- **`sample.md` の軽微な修正**。
- **`sample_original.md`**: リファクタリング前の比較用ファイルとして追加（未追跡）。

### 2026-07-14 のバグ修正・設計改善（未コミット）

**明確なバグの修正:**
- 下線コマンド（`Ctrl+U`）が太字を実行していた問題を修正。`++text++` ⇔ `<u>` の往復変換も全パーサで統一。
- Markdownテーブルの空セルが変換時に削除され列がずれる問題を修正。
- テーブルセルからフォーカスが抜けられなくなるフォーカストラップを解消。
- 検索ハイライトのspanが保存用HTMLに混入する問題を修正。
- テーブルセル内容がHTMLエスケープされずに埋め込まれていた問題を修正。

**設計改善:**
- Mermaid図がキーストロークごとに全破棄・全再描画されていた処理を、新規ブロックのみ描画する方式に変更。
- MermaidテーマをVS Codeのカラーテーマ（light/dark）に追従させ、テーマ切替時も再描画するように変更。
- メイン入力からの文書書き戻しを150msデバウンス化し、無変更時は送信しないように変更。

**根本対策（アーキテクチャ改善）:**
- `updateTextDocument` を全文置換から**差分適用**（共通の先頭・末尾を除いた最小範囲のみ置換）へ変更。Undo履歴の肥大化とカーソル飛びを解消。ドキュメントのEOL（CRLF/LF）も保持。
- `markdownToHtml` を正規表現の一括置換から**行ベースのブロックパーサー**へ刷新。不正なHTML（対応しない閉じタグ）の生成を解消し、ネストしたリストに対応。
- `htmlToMarkdown` を正規表現ベースから**DOMウォーカー**へ刷新。ネスト構造・テーブルセル内の装飾を往復で保持。
- 往復変換の冪等性（1往復で収束）をユニットテスト（40ケース）で検証済み。

> 注: 本ドキュメントはワーキングツリーの実ファイルを調査して作成しているため、上記の未コミット変更も「実装済み機能」として [features.md](./features.md) と [architecture.md](./architecture.md) に反映済みです。
